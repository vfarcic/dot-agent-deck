name: PR Labeler

on:
  pull_request_target:
permissions:
  contents: read
  pull-requests: write

jobs:
  label:
    runs-on: tenki-standard-medium-4c-8g
    steps:
      - uses: actions/labeler@v5
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}