[alias]
# `--workspace` (issue #489, duplicate #470): the workspace root is itself a
# package and there is no `default-members`, so cargo's default target
# selection is the root package ALONE. Without this flag every test in
# `xtask/linkage-check` and `xtask/docs` executed in NO gate anywhere —
# neither alias below, nor CI, which runs the same commands. Measured:
# `cargo nextest run` = 1657 tests, `--workspace` = 1705.
#
# The sibling of #436, which added `--workspace` to clippy for the same
# reason. Clippy's half kept the COMPILE-time guarantees alive (those tests
# are built, so a type error or lint still fails), which is precisely why the
# gap was easy to miss: only the runtime assertions were inert.
#
# Free warm: the 48 added tests run in ~0.07s wall and every one is under
# 40ms, so they vanish into the existing parallelism. Measured back to back,
# the full tier is 26.4-26.9s root-only and 25.3-26.7s with `--workspace` —
# indistinguishable, because the tier's wall clock is set by its slowest test
# (~20s), not by its count.
test-fast = "nextest run --workspace"
# Issue #494: `--features e2e` names the CARGO FEATURE `e2e`, NOT a nextest
# profile. There is no `-P` / `--profile` here, so this — like `test-fast`
# above, and like every `cargo nextest run` in `.github/workflows/` — runs
# under nextest's `default` profile, the only profile `.config/nextest.toml`
# declares. Timeout carve-outs therefore belong under
# `[[profile.default.overrides]]`.
test-e2e = "nextest run --workspace --features e2e"
# Issue #502 lane 2. NOTE THE SHAPE: enabling BOTH features is a SUPERSET run
# — every `tests/e2e_*.rs` file, i.e. the deterministic ones `test-e2e`
# already runs PLUS the credentialed ones. It is NOT a live-only run, and
# there is deliberately no alias that selects the live subset alone: those
# files open with `#![cfg(all(feature = "e2e", feature = "e2e-live"))]`, so
# `e2e-live` without `e2e` compiles every e2e file to an empty crate.
#
# So the three commands are: `test-fast` (fast tier, the per-task gate),
# `test-e2e` (lane 1 — needs no agent credentials, which is why CI runs it on
# every PR) and `test-e2e-live` (lane 1 + lane 2 — needs a working agent
# credential, and runs ONLY on a developer's machine; credential handling is
# issue #785). Lane 2 is deliberately not in CI: a credential in a public
# repository's CI is a vulnerability whose existence running it less often does
# not reduce, and the line the `e2e-live` feature draws is "reaches a real
# agent", not "reads a key" — on a developer box the agent CLIs are already
# authenticated, so a test that spawns `claude` spends a credential without
# ever touching an environment variable. CLAUDE.md rule 5 is the policy;
# `docs/develop/e2e-lanes.md` is the operational page.
#
# `--workspace` for the same reason as the two aliases above (issue #489):
# without it cargo selects the root package alone and every test in
# `xtask/linkage-check` and `xtask/docs` drops out of the gate.
test-e2e-live = "nextest run --workspace --features e2e,e2e-live"
# PRD #77 Decision 7: `cargo xtask linkage-check` enforces catalog↔test
# linkage. Args after the alias pass through to the binary.
xtask = "run --quiet --package xtask-linkage-check --"

# CLAUDE.md rule 12's cross-version contract check, scripted
# (`xtask/cross-version`, operational page `docs/develop/cross-version-harness.md`).
# `cargo xver --branch <branch>` builds that branch in a standalone disk-backed
# clone, stands a previous-release daemon up with an orchestration under it
# inside a private bubblewrap namespace (both endpoint roots masked), drives the
# branch TUI against that same daemon over a PTY, and writes a markdown evidence
# file. `cargo xver -- --branch <branch>` works too: this alias already ends in
# `--`, and the binary drops the second one.
#
# Its own binary rather than another arm of the `xtask` multiplexer: it needs
# `portable-pty` and `vt100`, and putting those in `xtask-linkage-check` would
# make every `cargo xtask linkage-check` build a terminal emulator it never
# uses.
xver = "run --quiet --package xtask-cross-version --"

# Issue #863 — the machine-wide link gate. Concurrent workspace builds from
# separate dispatch worktrees saturated the shared dev box: `io full avg300=65.95`
# (for two-thirds of a five-minute window EVERY runnable task was blocked on
# disk), `dm-0` at 100%, and `ld invoked oom-killer`, which took out
# `systemd-resolve`, `apparmor_parser`, `upowerd` and `wpa_supplicant`. The CPU
# was idle throughout — `cpu some avg300=0.08` — so this bounds I/O and memory,
# not CPU.
#
# `scripts/link-gate.sh` takes one slot from a pool shared by every checkout on
# the machine and then execs the real linker driver with rustc's argv unchanged.
# Cargo resolves this path relative to the directory holding `.cargo`, so every
# worktree points at its own copy of the script and all of them contend for the
# SAME pool — which is the point, since the thing to bound is concurrency
# ACROSS worktrees. `build.jobs` and `-j` cannot express that: they bound one
# invocation, and nothing coordinates invocations.
#
# Measured on the 27 GiB / 16-core box in the report, one relink of every test
# binary: ungated, that ONE build peaked at 60 concurrent linker processes
# holding 8.3 GB resident. The gate's computed default there is 6 link slots.
#
# LINUX ONLY, deliberately. `build-windows` and `build-macos` select different
# target triples and never read this key, so a POSIX shell script can never
# become the difference between a green and a red required check on a platform
# that has no shell to run it. The Linux `build` job DOES go through it. On a
# 4-core / 16 GiB runner the clamp resolves to 4 slots against cargo's own
# default `-j 4`, so the gate should be inert there — but that is arithmetic on
# a runner spec, not a measurement, and a larger runner or a changed default
# moves it. `DAD_LINK_JOBS=0` in the job is the lever if it ever bites.
#
# THE SCRIPT IS A BUILD INPUT, not an optional wrapper. rustc resolves this
# path before it links anything, so if the file is absent the build stops with
# `error: linker '...' not found` and none of the scripts' degradation paths get
# a chance to run. Anything that builds this repo from a filtered copy of the
# tree has to carry it — `flake.nix`'s fileset names both scripts for that
# reason, after CI's `nix` job failed on exactly this.
#
# Kill switch: `DAD_LINK_JOBS=0`. Slot count: `DAD_LINK_JOBS=N`. Different
# linker driver: `DAD_LINKER=clang` — needed because this key overrides a
# `linker` set in a personal `~/.cargo/config.toml`. Every failure path inside
# the two scripts runs the link ungated rather than failing it; the rationale
# and the degradation ladder are in `docs/develop/build-gate.md`.
[target.x86_64-unknown-linux-gnu]
linker = "./scripts/link-gate.sh"

# Issue #451 — keep DEPENDENCY RESOLUTION blind to the declared `rust-version`.
# Declaring a floor in `Cargo.toml` has two effects, and only the first one is
# wanted. It makes cargo refuse an older rustc up front and name the number,
# which is the whole point of the declaration. But under resolver v3 (selected
# by `edition = "2024"`) it ALSO switches `cargo update` to preferring
# versions compatible with the floor rather than with the installed toolchain
# (`incompatible-rust-versions = "fallback"`, the default).
#
# That second effect makes Renovate fight itself. Its per-dependency PRs run
# `cargo update --package X@old --precise new`, and `--precise` keeps a version
# whose own `rust-version` is above the floor. Its weekly lock file maintenance
# runs plain `cargo update`, which DOWNGRADES that same version back to the
# newest one compatible with the floor. Measured with a 1.88 floor standing in
# for a future dependency above ours: `--precise 0.9.3` kept `aes@0.9.3`, and
# plain `cargo update` proposed `Downgrading aes v0.9.3 -> v0.9.2`. So a
# dependency patch that needs a newer rustc would be automerged (CI builds on
# the pinned toolchain, well above the floor), downgraded the next Monday, and
# re-proposed.
#
# `allow` picks the newest version that satisfies the requirement and ignores
# `rust-version` entirely. With it, the same 1.88 probe proposed no downgrade,
# and on today's lockfile `cargo update --dry-run --verbose` proposes the same
# versions it did before the floor was declared. It is not quite the old
# behaviour: undeclared, cargo preferred versions compatible with the rustc
# running `cargo update`, and `allow` does not. The two differ only for a
# version that needs a newer rustc than the one resolving, and CI refuses such
# a version on the pinned toolchain, so it turns that PR red rather than `main`.
# It does NOT weaken the up-front
# refusal: that check compares each package's `rust-version` against the
# running rustc at build time and is independent of this key. The cost is the
# one `docs/develop/versioning.md` already accepts — a dependency bump can raise
# the real floor while the declared number stands still, and nothing notices
# until someone re-measures.
#
# Renovate's cargo commands pass no working directory, so they run from the
# repository root, where cargo's config discovery starts, and should therefore
# pick this key up. That is read off Renovate's source (`lib/util/exec/index.ts`,
# `getCwd`) and has not been observed in a Renovate run: the first lock file
# maintenance PR after this lands is the confirmation. If it proposes a
# downgrade, this key is not reaching it.
[resolver]
incompatible-rust-versions = "allow"
