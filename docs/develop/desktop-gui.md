# Desktop GUI developer preview

The desktop GUI under `desktop/` is an opt-in Tauri preview for PRD #176. It is a second local client of the existing daemon, not a replacement for the TUI, and it is not included in the default release artifacts. The current M0/M1 spike deliberately depends on the root `dot-agent-deck` library before the protocol crate is extracted so that terminal transport and the control-deck interaction model can be tested first.

## Prerequisites

- Enter `devbox shell` for the repository's pinned toolchain: Rust 1.97.1, `cargo-nextest`, Clippy, rustfmt, Node.js 24.12.0, and pnpm 11.22.0. Provide equivalent versions yourself if you do not use Devbox — the frontend needs Node.js 20.19 or newer, and a pnpm that reads `desktop/pnpm-lock.yaml`'s `lockfileVersion: '9.0'` without rewriting it. Note that **nothing tests that Node floor**: CI's `desktop-web` job pins Node 24 and pnpm 11, matching the Devbox pins rather than the stated minimum. (Corrected 2026-09-04: this line claimed pnpm 10.34.5 and a `desktop-web` job deliberately running Node 20 so the floor "stays tested rather than merely claimed" — both were false, and the second inverted what the job actually establishes.) Devbox supplies Node with its **plugin disabled** (`disable_plugin: true` on the `nodejs` entry); that removes a corepack shim this repository never used and nothing else — see [devbox and concurrent starts](devbox-concurrency.md).
- Install the [Tauri 2 system prerequisites](https://v2.tauri.app/start/prerequisites/) for your platform. On macOS this includes the Xcode command-line tools. On Linux a `devbox shell` already carries them — see [Linux system libraries](#linux-system-libraries) below — and only a non-Devbox Linux setup installs WebKitGTK and the related `-dev` packages by hand.
- Install the desktop JavaScript dependencies once:

```sh
cd desktop
pnpm install
```

Agent CLIs and their credentials are needed only for agents you deliberately start through the daemon. The fixture preview does not call an LLM, execute an agent command, or modify project files.

### Linux system libraries

This is not optional reading on Linux, because `desktop/src-tauri` is a **workspace member**: both gates CLAUDE.md mandates carry `--workspace`, so `cargo clippy --workspace --all-targets --features e2e -- -D warnings` and `cargo test-fast` build this crate whether or not you are working on the GUI. Without GTK 3, WebKitGTK and glib they fail — the first at build time (`The system library 'glib-2.0' required by crate 'glib-sys' was not found`), the second at run time (`libgdk-3.so.0: cannot open shared object file`). Issue #771.

**In a `devbox shell` there is nothing to install.** `devbox.json` carries a `path:tauri-deps#tauri-deps` entry, and `tauri-deps/flake.nix` builds the transitive pkg-config closure of the same libraries `ci.yml`'s `build` job installs with apt. `pkg-config@0.29.2` is pinned there too, so resolution does not depend on the host having one. Nothing else is exported — in particular `LD_LIBRARY_PATH` stays unset.

**What actually makes pkg-config find the store, corrected by issue #815.** It is the *package entry*, not `devbox.json`'s `env` block. `pkg-config@0.29.2` puts Nix's pkg-config **wrapper** on PATH, and that wrapper discards the caller's `PKG_CONFIG_PATH` outright — it re-execs the real binary with `PKG_CONFIG_PATH` replaced by a role-mangled variable its own setup hook builds from the profile (`PKG_CONFIG_PATH_FOR_TARGET` here). Measured: with `PKG_CONFIG_PATH` pointed at a directory holding a hand-written `.pc`, `pkg-config --modversion` for that module fails, while the same value in `PKG_CONFIG_PATH_FOR_TARGET` resolves. So the `env` block is a fallback for a *non-wrapper* pkg-config and a no-op the rest of the time; do not reach for it when diagnosing a resolution problem, and do not read its value as evidence of anything.

**Outside Devbox, on a distribution toolchain, plain apt is enough.** The Debian/Ubuntu set is the one CI installs:

```sh
sudo apt-get install -y libwebkit2gtk-4.1-dev libgtk-3-dev \
  libayatana-appindicator3-dev librsvg2-dev libxdo-dev libasound2-dev
```

The last one is not Tauri's. PRD #802 M7 declares `cpal` in this crate for microphone capture, and on Linux its `alsa-sys` build script resolves `alsa` through pkg-config — so without `libasound2-dev` the crate does not build at all, which is issue #771's failure arriving through a different dependency. macOS needs nothing for CoreAudio and Windows nothing for WASAPI, so the edit is Linux-shaped. Four places declare this set and they are meant to stay in step: `tauri-deps/flake.nix` (the devbox shell), both `apt-get` blocks in `.github/workflows/ci.yml` (`build` and `e2e-deterministic`), and the bundle job's block in `.github/workflows/release.yml`.

**Do not mix the two.** `apt-get install` inside a `devbox shell` looks like it should work and does not, which is what made issue #771 expensive rather than merely annoying:

- A devbox shell runs under **Nix glibc**, whose loader cache does not exist — `ldconfig -p` returns *zero* entries. Libraries under `/usr/lib` are therefore invisible to the dynamic linker no matter what apt put on disk, so `pkg-config` can report `glib-2.0` present, the build can succeed, and the test binary still dies at `libgdk-3.so.0: cannot open shared object file`.
- The obvious second workaround breaks something else. `export LD_LIBRARY_PATH=/usr/lib/x86_64-linux-gnu` does make both Rust gates pass, and then Nix's own `node` fails with `undefined symbol: uv_tcp_keepalive_ex` because it picks up the system `libuv` ahead of its own. `pnpm install`, `pnpm build` and `pnpm test` are all unusable while that variable is set.

The fix avoids `LD_LIBRARY_PATH` entirely rather than pointing it somewhere safer, and the reason is worth knowing if you touch `tauri-deps/flake.nix`: each `.pc` file names absolute `/nix/store` paths, so the linker is invoked with `-L/nix/store/…-gtk+3-3.24.52/lib`, and Nix's `ld` wrapper turns every in-store `-L` into a matching `-rpath`. The test binary finds its libraries through its own RUNPATH, which is per-binary and cannot leak into `node`. `LD_LIBRARY_PATH` takes precedence over `DT_RUNPATH` for *every* process in the shell, which is the same class of problem in the other direction.

### The stale-environment trap, and the check that catches it

Issue #815 is worth reading before you debug a GTK failure here, because its symptom is indistinguishable from a `devbox.json` defect and its cause is neither.

`devbox run` **reuses an already-computed devbox environment whenever one is inherited** — `DEVBOX_PATH_STACK` is the trigger, measured by stripping the `DEVBOX_*` variables one at a time — and it then neither re-installs the profile nor re-applies the `env` block. A shell entered before `devbox.json` last changed therefore keeps a profile with no `tauri-deps` and no Nix `pkg-config`, and *every nested `devbox run` inside it inherits that staleness silently*. That matters more here than it looks: this repository's orchestration runs each dispatched agent inside a devbox shell, so nested `devbox run` is the normal case rather than an exotic one.

What makes it expensive is that the degraded state reports success everywhere. The host's `pkgconf` answers instead, `pkg-config --exists gtk+-3.0` passes, `--modversion` prints a real version, the compile links clean and `cargo clippy --workspace --all-targets` goes green. Only the loader notices, tens of minutes later, because Nix's `ld` wrapper mints an `-rpath` for `-L` paths under `/nix/store` and for no others — so a `/usr/lib/x86_64-linux-gnu` answer is the exact moment the binary loses its RUNPATH, and `cargo test-fast` then dies before its first test with `libgdk-3.so.0: cannot open shared object file`.

The remedy is to leave every nested devbox shell until `DEVBOX_PATH_STACK` is unset, then `devbox install` and re-enter.

`scripts/devbox-check-gtk.sh` catches it at the point the wrong answer is given: for each of the 14 modules it asserts the resolved **`libdir` is under `/nix/store`**, not merely that the module exists. `libdir` rather than `prefix` because every one of the 14 defines it, `libxdo` reports an empty `prefix`, and `libdir` is the value that becomes the linker's `-L` flag. `scripts/devbox-smoke.sh` invokes it and CI's `devbox` job runs that — the only job that can see this regress, since every other one installs the compile set with apt and would stay green with `devbox.json` empty of GTK. `xtask/linkage-check/src/devbox_gtk_origin.rs` drives the script under a stubbed `pkg-config` in the fast tier, so weakening the assertion back to a presence check goes red on the per-task gate; that matters because a presence check is precisely what was there when #815 shipped.

Bundling a `.deb` locally needs more than the compile set — `patchelf`, `fakeroot`, `file` and `desktop-file-utils` — which nothing in this repository's gates exercises, so they are deliberately not in `devbox.json`. Install them yourself before `pnpm tauri build`.

## Fixture preview

Run the web frontend without Tauri for a deterministic, safe UI fixture:

```sh
cd desktop
pnpm dev
```

Open `http://localhost:1420/`. A normal browser defaults to fixture transport; `http://localhost:1420/?fixture=1` selects it explicitly. `?state=` picks the scenario, and the six it accepts are `connected` (the default four-agent deck), `crowded`, `empty`, `disconnected`, `error` and `fleet` — for example `http://localhost:1420/?fixture=1&state=disconnected`. An unrecognised value falls back to `connected` rather than failing. See [The connection states](#the-connection-states) for what each one is for. (It said *five* until PRD #742 M4 added `fleet`, which is the only one of the six that renders more than one deck; every other scenario is one deck, byte for byte what it was before that milestone. `fleet` began as three decks — two answering and one not — and M14 added a fourth that has not reported yet, so it now carries every degraded state the overview can draw at once.)

`?older=` makes the fixture play decks from before PRD #1223 — no directory listing and no new-agent options — so the New agent dialog's handling of one can be driven without an old daemon: such a deck's connection carries the live crate's `newAgentReason`, its group header offers no **New agent**, and the dialog's deck field lists it disabled with that reason, because browsing is the only way the flow chooses a directory. `older=1` (or `older=all`) plays every deck that way; otherwise it takes a comma-separated list of fixture deck ids, for example `http://localhost:1420/?fixture=1&state=fleet&older=dev%40build-box` for the fleet's remote deck. Unset, every fixture deck answers both queries. A deck played as older also answers no options, and the fixture refuses an authoring start aimed at it in the live crate's words. `?nonunix=` takes the same kind of list and plays those decks as built for a non-Unix platform: they list directories and answer the options query, so the form is reachable, but they withhold the `Orch:` chips with the live crate's configured-role-command reason and refuse the launch. Of the decks that do answer, the fleet's remote deck (`dev@build-box`) reports its own experimental flag on and the local deck reports it off, so `schedule: issues` shows on the one and is withheld on the other — the flag is the deck's, not this app's. Orchestrations (PRD #1223 M6) follow it too: on a deck that answers, each fixture deck's `demo-project` defines one orchestration, `demo-loop` (a `planner` start role running `claude` and a `builder` running `codex`), `scratch/twin-project` defines `twin-loop` twice and `solo-loop` once so the namesake case below can be seen, every other fixture directory is an ordinary one, and launching it adds both roles to that deck under the run's title; a deck played as older or as non-Unix withholds the `Orch:` chips with the live crate's reason and refuses the launch.

`?experimental=1` (or `true`) shows the surfaces issue #1198 puts behind the `experimental` flag — the deck, Projects, Prompts, Workflows and Agent Profiles. Without it the fixture plays the shipped default: the overview, its agent pane and Settings, with a two-entry rail. The browser tier's deck cases add it (`desktop/e2e/support/overview.ts`), and so do the vitest cases that drive the deck. The live app reads the flag from its own environment instead; see [the experimental flag's desktop section](experimental-flag.md#the-desktop-app).

The fixture's **Advance fixture** control walks a fixed review → test → human-approval sequence. Terminal input, pause/resume, retry, approval, workflow ordering, and agent-profile editing affect fixture or browser-local state only.

## Live Tauri preview

The live preview requires a daemon built from the same checkout. The exact build match is intentional: the desktop refuses to attach across a protocol or build mismatch and never recycles a daemon that may own valuable running agents.

Build the matching CLI once so the desktop can resolve a sibling executable:

```sh
devbox shell
cargo build --locked --bin dot-agent-deck
```

Then start the desktop window:

```sh
devbox shell
cd desktop
pnpm tauri dev
```

Tauri selects live transport automatically and initially performs a connect-only probe. If no daemon owns the socket, use the visible **Start deck** action; daemon creation never happens merely because the window opened. The Rust core resolves only a pre-launch `DOT_AGENT_DECK_BINARY`, a sibling `dot-agent-deck` binary, or one already on `PATH`—the webview cannot supply an executable path. You may instead run `cargo run --locked --bin dot-agent-deck -- daemon serve` in another terminal and leave it running.

If another build already owns the default socket, do not stop it until you have confirmed that terminating its agents is safe; use `dot-agent-deck daemon stop` only as a deliberate lifecycle action.

To keep the preview away from your everyday deck altogether, `task run-desktop` runs it against an isolated sandbox daemon with its own `desktop.toml`, and `task run-all` runs the TUI beside it against that same sandbox daemon. [local-run.md](local-run.md) has what they isolate, what they do not, and how to stop them.

### Attaching across a contract difference (development only)

**Since [#801](https://github.com/vfarcic/dot-agent-deck/issues/801) the everyday reason to reach for this is gone**, and that is the first thing to know about it. This section used to open by saying the **build stamp** (`git describe`) had to match — which made the daemon you actually use the one daemon the preview refused to open, since every commit restamps the desktop on its next rebuild and a released daemon never matches a branch build at all. The stamp no longer classifies anything. Two builds that agree about the contract connect however far apart their stamps are, with no switch and no confirmation.

What is left refusing, above the `PROTOCOL_VERSION` floor, is a **declared contract break** between the two builds — which means the deck genuinely may read a field with a meaning this app does not intend. Set `DOT_AGENT_DECK_DESKTOP_ALLOW_BUILD_MISMATCH=1` before launching to downgrade *that* from a refusal to a warning:

```sh
DOT_AGENT_DECK_DESKTOP_ALLOW_BUILD_MISMATCH=1 pnpm tauri dev
```

The variable keeps its name for continuity with the shells and scripts that already set it; what it bypasses is the contract comparison, not a stamp comparison, and it no longer has a stamp comparison to bypass. The protocol check is unaffected and still refuses an incompatible daemon, and the mismatch is not swallowed — the connection banner keeps naming the break and both builds for the whole session. Only `1` and `true` arm it.

This is a **development** switch, not a compatibility guarantee, and it is now a sharper thing to arm than it used to be: it no longer waves through a cosmetic stamp difference — it waves through a break somebody wrote a `changelog.d/*.breaking.md` fragment for, which is the semantic break `CLAUDE.md` rule 12 describes. The contract check is what protects a daemon that owns live agents from a client that reads its state differently. Use it to inspect a daemon, not to run work you care about, and never in a packaged build.

Users get the same escape hatch without a shell, through **Connect anyway** (issue #801). It appears in the connection banner — and on the agent overview's incompatible note — only when the desktop crate reports the refusal as overridable, meaning the protocol check already passed. (The DTO field is still spelled `buildStampMismatchOnly`; it is named for what used to set it, and since #801 it is set by a declared contract break.) It is confirmation-gated, it is **session-scoped** (a process-global flag, never written to disk, so quitting the app restores the refusal), and it is offered whatever the live-agent count, which is the case a packaged `.app` could not otherwise recover from: **Replace deck** is correctly refused while agents are live, and an env var is not something a `.app` launched from Finder ever receives. Accepting it does not clear the caveat — the connection banner stays up while connected, naming both builds, for the rest of the session.

A **protocol** mismatch offers nothing. The version check runs first in `classify_handshake`, so an allowance cannot reach it, and the crate never sets the overridable flag on that path — so no screen puts a button on it either. `protocol_mismatch_never_advertises_an_override` in `desktop/src-tauri/src/daemon_bridge.rs` pins both halves.

### Compatibility is classified from the CONTRACT, not from a git tag ([#801](https://github.com/vfarcic/dot-agent-deck/issues/801))

The handshake asks three separate questions and they used to collapse into one. They are now kept apart, and the least meaningful of them no longer decides anything:

| question | mechanism | verdict |
|---|---|---|
| can we decode each other's frames at all? | `PROTOCOL_VERSION` | exact equality, checked first, the hard floor for **every** deck kind — not bypassable by an allowance or an endpoint kind |
| do we agree what the fields *mean*? | the two builds' declared contract breaks | refuses, overridably |
| may I use *this* verb? | the `Hello` reply's advertised capability set | degrades the surface, never refuses |
| are we the same build? | the `git describe` stamp | **reported, never classified** |

**Why the stamp had to stop deciding.** `git describe` names the nearest tag *reachable from HEAD*, and the release workflow applies that tag *after* the content it names has landed — so a branch cut in between describes as the previous release while being functionally the new one. Measured on #801: a branch whose `git diff v0.39.0 HEAD -- src/` was **empty** was refused with `build mismatch: desktop is 0.38.0-gfa02054-dirty, daemon is 0.39.0-g1ea0fe7`. Two identical trees, classified incompatible. Reading the release digits off that same stamp — which is what this crate did until #801, via a `0.MINOR` compatibility key — narrows the false positive to a minor boundary rather than removing it, and a minor boundary is exactly where the interesting case lives.

**What replaces it.** `daemon_protocol::CONTRACT_BREAKS` is an append-only list of the TUI↔daemon contract breaks a build declares — one entry per `changelog.d/<issue>.breaking.md` fragment, appended in the same commit. It rides every `Hello` reply as `contract_breaks`, and `classify_handshake` compares the two lists as sets: equal is compatible, and any difference is a refusal naming the breaks and which side is behind. It cannot suffer the tag-timing error, because it is content in the contract's own source rather than a tag in the commit graph, and it sees one thing a digit cannot: a break declared on an **unreleased** branch, which has no minor to move yet.

Nothing is lost by retiring the digit comparison. While `0.x`, only a *declared* break bumps the minor ([versioning.md](versioning.md)), so the digits could only ever refuse across a break somebody had already written a fragment for — which is exactly what the list holds.

**A deck that declares no list at all connects, and that is an assumption about a population rather than a deduction.** The list does not ship empty, so "a build that omits the field is at our contract by construction" would be false. What holds is narrower: `PROTOCOL_VERSION` must match for the comparison to be reached, so a deck landing here is a protocol-9 build older than this change — and every *released* build in that set carries #617 (`v0.40.0` onward), so it is at this build's contract and merely cannot say so. Refusing the set would refuse every released daemon up to `v0.40.2` for staying silent, which relocates #801's false positive rather than removing it. The residual is the unreleased case, stated in `ContractComparison::Undeclared`: a dev build from the 73-minute window between PRD #882's bump and #617 genuinely predates a declared break and is connected anyway. The population that can land there is fixed and ageing, since every build from this change onward declares.

**The deck's KIND no longer changes the answer, which supersedes PRD #741 M8.** M8 demoted the stamp for a **remote** deck only, and its reasoning was about the *remedy*: the refusal's own sentence named **Replace deck**, which against a deck on another host means terminating a daemon somebody else may be using, and M2 had made that structurally impossible anyway — so the sentence named an action whose button is disabled on the same screen that printed it. That argument was right about the stamp, and the stamp now refuses nobody of either kind. What is left refusing is a *declared* break, which is not noise for a deck of either kind, so it refuses both — and **Connect anyway** is the escape hatch it leaves, for both.

What a screen acts on when the deck is older but compatible is still the **capability set**. `connection.projectActionsReason` is derived from what the daemon advertised in its `Hello` reply through `DaemonCapabilities::supports`, so it answers *"does this deck do what I am about to ask it to"* rather than *"is this deck the same build as me"*. A daemon that advertises none of `list-projects`, `resolve-project`, `prepare-workflow`, `start-prepared-agent` is an older daemon; the workflow panel says which are missing and disables **Launch live loop**, and the agents already running on that deck stay visible and usable.

**What this still does not catch, stated rather than implied.** An **undeclared** semantic break. If the author writes neither the `.breaking.md` fragment nor the `CONTRACT_BREAKS` entry, no mechanism here sees it: the verb is still advertised and still answers, the wire shape is unchanged, and the stamp did not see it either (an undeclared break bumps only the patch, which the digit comparison passed). So this is a residual carried over, not one introduced. What stands behind it is `CLAUDE.md` rule 12's cross-version manual test, run against the previous release before such a change merges, and the fragment its outcome demands — which `xtask/linkage-check`'s `contract_breaks` rule then pairs with the entry the handshake reads. The same argument is on `CONTRACT_BREAKS` in `src/daemon_protocol.rs`, which is where a reader changing this code will meet it.

In live mode the preview lists daemon-owned agents, attaches xterm.js to each PTY stream, forwards terminal input and resize requests, refreshes status, and exposes a confirmed stop action. Open **Projects**, choose the project this launch runs in, then **Configure workflow** to pick one of that project's orchestrations and choose **Launch live loop**, which starts its configured role set with the enabled profile commands — [The launch flow](#the-launch-flow-and-why-its-order-does-not-commute) below is why that order does not commute. **Start deck** and **Launch live loop** both require a confirmation. The project configuration must contain exactly the submitted roles and one start role; the current bundled `dot-agent-deck` profile uses `orchestrator` as that start role and requires every listed profile to remain enabled for a live launch. Before spawning, the **daemon** materializes the same canonical coordinator context the TUI uses, on its own filesystem: since #819 M6 the desktop asks for it through the `prepare-workflow` verb, which loads one validated config snapshot, resolves, composes and publishes before answering, and starts no roles when any of that fails. Non-Pi coordinators use a readiness-gated, identity-bound, idempotent submission with bounded retry. Pi cannot be the desktop workflow coordinator in this preview: its native seed path has no delivery acknowledgement, so the bridge rejects that launch before spawning any role. Use a non-Pi coordinator or launch that orchestration from the TUI until acknowledged native seed delivery is available. A partially-created workflow—or one whose coordinator context cannot be delivered—is rolled back in reverse role order. Those rules belong to the Runs screen. The overview's **New agent** dialog also launches orchestrations (PRD #1223 M6), the way the TUI's `Ctrl+n` does, and shares the daemon verbs and the bridge's rollback and delivery machinery with this launch but none of its form rules: it prepares with no task, and starts each role through `start-prepared-agent` with `use_configured_command`, so the deck runs the command and agent type the role's own config gives it (the deck must advertise `prepared-role-command`; the dialog withholds its `Orch:` chips with the reason otherwise). A Pi start role is seeded natively by the deck, as the TUI's is, so that dialog does not refuse a Pi coordinator; a non-Pi start role gets the same acknowledged delivery and rollback described above.

Desktop bundles include `dot-agent-deck` as a Tauri sidecar built from the same checkout. Run `pnpm bundle:app` to prepare the matching sidecar and produce the native app; the separate bundle config keeps ordinary workspace `cargo test` runs independent of generated binaries. A contract/protocol mismatch exposes **Replace deck** only when the deck now running reports zero live agents; replacement uses the bundled binary and never force-stops live agents. A contract mismatch — one the protocol check already passed — additionally exposes **Connect anyway**, described above, which starts and stops nothing.

The **Projects** rail opens the project picker, and since #819 M6 everything in it comes from the daemon of the **selected** deck — `desktop_list_projects` resolves its daemon through `trusted_daemon()`, so a fleet on the overview changes nothing here and no non-selected deck is ever asked: it lists the projects that one daemon can see on its own machine, and a project it has nothing live in is named by pasting an absolute path, which the daemon resolves on that same machine. Nothing is saved — choosing a project is a step in assembling one launch rather than an entry in a library — and the picker neither moves nor deletes a repository. Projects still need a matching `.dot-agent-deck.toml`, and the launch verb remains the authoritative validation boundary.

Live workflow launch from the desktop preview is currently supported on macOS and Linux only. On Windows, the workflow sheet detects the platform, explains the limitation, and disables **Launch live loop**; the Rust bridge independently rejects a crafted `StartWorkflow` IPC request before validating or spawning roles. This guard is intentional because generated profile commands use POSIX shell quoting and must not be passed to `cmd.exe`. The fixture, daemon connection, and existing-agent terminal views remain available on Windows; use the TUI or launch commands manually until native Windows command construction is implemented.

Whole-run pause, fixture advancement, approval, and retry are not sent to the daemon. Workflow ordering remains a local preview and the live launch follows the role order in `.dot-agent-deck.toml`; command overrides apply to that launch only and do not rewrite project configuration.

### The launch flow, and why its order does not commute

The order is **daemon → project → workflow**. It is not a preference: the workflow list comes out of the chosen project's own `.dot-agent-deck.toml`, which the daemon reads on its own machine, so there is nothing to offer until a project resolves. The **Workflows** sheet reflects that — its workflow field is a `<select>` over the resolved project's orchestrations rather than a name you type, and its project directory is read-only, holding the daemon's own canonical spelling of the project you picked. The way to change it is to pick a different project. That spelling is load-bearing rather than cosmetic: canonicalising a symlinked path changes its basename, and an orchestration the config leaves unnamed is named after that basename, so a listing built from one spelling and a spawn built from another disagree about the name — PRD [#220](https://github.com/vfarcic/dot-agent-deck/issues/220)'s bug verbatim.

**Nothing persists a project client-side, and nothing on the daemon does either.** `desktop_project_cwd()` — the desktop crate's own guess at a project directory — is deleted, and so is the `dot-agent-deck.desktop.projects.v1` `localStorage` list that was the source of truth for the launch cwd. The resolved selection lives in React state and dies with the launch. Enumeration comes from what the daemon already holds and is derived from **live** state: its own startup cwd when that directory is a project, the cwd of every agent it owns, every orchestration's cwd, and every scheduled task's working directory — and the scheduled one is the seed that outlives a restart, since a daemon's agents are its own children and go with it. Every candidate is revalidated through the same bounded reader before it enters the list, because a seed is a candidate directory rather than a proven project. The design self-seeds through the action it exists to support: launching in a project puts an agent there, so the daemon enumerates it for as long as anything runs there. Any future convenience must be prefill for a field the daemon still resolves, never an authority (PRD #819, *Nothing remembers a project*).

The desktop may send back only a path the **daemon** supplied or the **user** typed. It may never derive one from its own environment — that is the whole invariant, and [the tripwire](#the-tripwire-on-that-invariant-and-what-it-cannot-do) is what notices a regression against it.

**Three states, and each is an ordinary outcome rather than an error:**

1. **The daemon knows nothing live.** Its startup cwd is not a project and it owns no agents, so it has none to offer. The picker says so and offers the paste-a-path field. This is the first-run behaviour, and it is the only one — there is no remembered list sitting behind it.
2. **A project left the set between listing and launch.** Enumeration is live-derived, so a project stops being known once the last thing running there exits, possibly seconds after it was drawn. The picker presents that like the empty state — pick another, or paste the path again — and re-lists, which is what turns it back into an honest picture of what is live. It is expected rather than a fault, and the refusal it is recognised by is the daemon's stable `unresolved` code and not its prose.
3. **An older daemon.** It never reaches a project verb at all. The desktop's handshake check is exact equality on `PROTOCOL_VERSION`, which #819 moved 8 → 9, so a v9 desktop refuses a v8 daemon at the handshake with `protocol mismatch: desktop expects 9, deck reports 8`, naming both versions; the build-stamp allowance runs after that check and does not bypass it. There is deliberately no local fallback — resolving client-side "when the daemon is local" would reinstate the silently-wrong-filesystem behaviour on the least-tested path.

**Refusals disclose by trust rather than uniformly.** A path the daemon **already knows** — meaning it has something live there — gets the detailed parse diagnostic, which is where "your config is broken, not empty" earns its keep. An arbitrary pasted path gets the same stable code and one fixed sentence, so the wire response does not directly distinguish "no such directory" from "no config there", and the detail is logged daemon-locally instead. The reason is concrete rather than precautionary: `ProjectConfigError`'s `Display` renders the offending TOML source line verbatim, so returning it for a path the caller merely typed would disclose file *content* and not merely existence. No timing property is claimed alongside it — canonicalisation, traversal, `open(2)` and TOML parsing do observably different amounts of work, and `src/project_resolve.rs`'s module doc says so rather than leaving it to be assumed.

## Decks: which ones the app talks to, and how it reaches another machine

PRD [#741](https://github.com/vfarcic/dot-agent-deck/issues/741) turns the daemon the app connects to from a constant into a **choice**, and PRD [#742](https://github.com/vfarcic/dot-agent-deck/issues/742) turns that choice into a **set**. Before #741, a daemon *was* a `PathBuf` — this machine's attach socket, resolved from the platform paths — and every operation the app performs against one took that path. This section is what replaced that: the type, where the configured decks are stored, what the app trusts about each kind, how a remote one is reached, how to set one up by hand, and — since #742 — what it means to watch several at once.

**The kind is in the type, and that is the whole mechanism.** `Endpoint` in [`src/daemon_client.rs`](../../src/daemon_client.rs) has two variants — `Local(LocalEndpoint)` and `Remote(RemoteEndpoint)` — and `Endpoint::as_local()` returns `None` for the remote arm. Three operations act on a **local process or a local inode** and would act on the wrong thing while reporting success if handed a remote deck's address: `peer_pid`-based termination (`run_daemon_stop`), the stale-inode `remove_file`, and lazy-spawn. All three take a `LocalEndpoint`, and `as_local()` is the only way to obtain one, so a remote deck cannot *reach* them — a compile error rather than a runtime `if`. `Endpoint::require_local(operation)` is the same thing with a refusal that names the operation and the deck, which is what the desktop's disabled **Stop deck** and **Replace deck** buttons render.

**Three *operations*, but four `as_local()` gates.** The fourth is a check rather than an operation, it does not refuse, and it is described in [Trust is a property of the deck's kind](#trust-is-a-property-of-the-decks-kind) — read that before assuming the count of three is the whole list.

`Endpoint::connect_address()` is deliberately a bare `&Path` and errors for the remote arm. Under [the transport decision](#why-ssh--l-and-what-was-rejected) a remote deck is reached through a forwarded socket that exists on *this* filesystem, and spelling that value `LocalEndpoint` would hand it straight back to `run_daemon_stop`, whose `SO_PEERCRED` lookup would then name the local `ssh` client. The address comes from a **lease on a live tunnel** instead, which is why `establish()` in `daemon_bridge.rs` asks `EndpointTunnels` for one rather than asking the endpoint.

### The `[endpoints]` section

The decks live in the same per-installation `desktop.toml` as everything else in [Application settings](#application-settings), under `[endpoints]`, and the section holds exactly two things: `remote`, a list of rows, and `selection`, a token naming the deck the app is talking to.

```toml
[endpoints]
selection = "k3n9wq2mf4ph7x1a"

[[endpoints.remote]]
host = "build-box.example.com"
id = "k3n9wq2mf4ph7x1a"
port = 22
socket = "/run/user/1000/dot-agent-deck-attach.sock"
user = "vfarcic"
```

**The local deck is not a row, and that is the point.** It needs no configuration — `Endpoint::local()` resolves it from the platform paths the way every caller did before endpoints existed — so a fresh install has no `[endpoints]` section at all and still works, and a user who deletes the section gets the local deck back rather than nothing. `selection = "local"` is the reserved token that names it, which is why `local` is the one word an endpoint id may not be.

**Every field is a validating newtype, and the type is doing two jobs at once.** `String` is not available here: `ALLOWED_FIELD_TYPES` in [`xtask/linkage-check/src/desktop_settings_secrets.rs`](../../xtask/linkage-check/src/desktop_settings_secrets.rs) is a closed list, `String` is deliberately absent from it, and the check runs in the **required** `build` job — so `host: String` reddens a required check on the first push. Satisfying that guard with newtypes buys the credential bound the guard exists for *and* the ssh-argument validation nothing in this tree performed before: `SshTarget::parse` (`src/remote.rs`) splits on the first `@` and stores both halves verbatim, with no length bound, no charset check, and no rejection of a leading `-`, whitespace or NUL.

| field | type | required | bound and charset, and what the shape rule adds |
| --- | --- | --- | --- |
| `host` | `Hostname` | yes | 253 bytes (RFC 1035's presentation limit, which also clears every IP literal form); ASCII alphanumerics plus `. - _ [ ] : %`. Outside brackets `:` and `%` are refused, because `:` would be read as a port separator in a `-L` forward spec and `%` as an IPv6 zone id; a bracketed value must be closed and non-empty. |
| `id` | `EndpointId` | yes | 64 bytes; ASCII alphanumerics, `-` and `_`; `local` and `all` refused case-insensitively. An **opaque minted token** rather than the address — editing a host must not silently re-point a selection — and rather than a list index, so removing a row does not re-point every selection after it. `mint()` produces 16 bytes; the charset is wider so a token a *newer* build wrote still parses. |
| `port` | `SshPort` | defaults to 22 | the type *is* the bound: a newtype over `NonZeroU16`, so `1..=65535` and nothing else is representable. It was a bare `u16` until PRD #741's Greptile review of [#1035](https://github.com/vfarcic/dot-agent-deck/pull/1035), which admits `0` — the one value the webview's own predicate refuses, and not an inert disagreement: a hand-edited `port = 0` loaded and was handed to OpenSSH as `-p 0`. The default comes from `SshPort::DEFAULT` rather than a literal `22`, so a row stored without a port and a row built by `RemoteEndpoint::new` cannot describe different decks. |
| `user` | `Option<SshUser>` | no | 64 bytes (past POSIX's 32-character `LOGIN_NAME_MAX`, which UPN logins routinely exceed); ASCII alphanumerics plus `. - _ @`. Every `@`-separated part must be non-empty, so `user@realm` works while `@realm`, `user@` and `user@@realm` are refused. |
| `identity` | `Option<KeyPath>` | no | 4096 bytes (`PATH_MAX`); ASCII alphanumerics plus `. - _ / ~`; must start `/` or `~/`. A **path**, never key material and never a passphrase. |
| `jump` | `Option<HostAlias>` | no | 253 bytes; ASCII alphanumerics plus `. - _`. A `Host` block *name* from the user's own `~/.ssh/config`, so the bastion's address, port, user and key stay there. No `*` or `?`: a jump target is one host. |
| `socket` | `Option<RemoteSocketPath>` | no | 104 bytes — the smaller of Linux's 108-byte and macOS/BSD's 104-byte `sun_path`, applied to both ends because the far host's OS is not knowable from here; ASCII alphanumerics plus `. - _ /`; must start `/`. No `:`, because `-L` is parsed by splitting on `:`. |

Each newtype's `Deserialize` runs the same check its constructor does, so a hand-edited `desktop.toml` cannot smuggle past what the settings form applies. They live in [`src/remote_tunnel.rs`](../../src/remote_tunnel.rs) rather than in the desktop crate because the CLI's `remotes.toml` has the same problem.

**There is no display name, deliberately.** A user-chosen label is exactly the arbitrary `String` the field-type guard refuses, and it would need its own bidi and control-character handling before anything rendered it. `RemoteEndpoint::describe()` derives the label from the address instead, and every byte of that came through a validated ASCII charset.

**`identity` is named that rather than `key`, and the cost is written down.** `key` is what `remotes.toml` calls it and it trips the naming tripwire in `settings.rs`, whose path allowlist is empty. The two available answers were an exemption pinned to `KeyPath` or a name that is not credential-shaped; this is the second, because `IdentityFile` is OpenSSH's own name for the option and because an empty allowlist is worth keeping — the first entry is the one that makes the second easy. What it costs is that the tripwire is now silent about the one field in this schema that sits next to credential material, which is why that is said out loud in the field's doc comment. The control that matters is unaffected: `KeyPath` is on `ALLOWED_FIELD_TYPES` with a written reason, bounded at `PATH_MAX`, restricted to a charset that cannot represent a multi-line PEM block, and required to start `/` or `~/`.

**`socket` is optional, and a row without one is *storable* and *not connectable*.** It cannot be derived: OpenSSH expands neither `~` nor an environment variable on the remote side of `-L`, and the far host's `XDG_RUNTIME_DIR` and uid are not knowable from here without a second ssh round trip. Requiring it would mean typing `/run/user/1000/dot-agent-deck-attach.sock` correctly *before* anything could test whether it was right, so instead `EndpointSettings::resolve()` returns the local deck plus a named `SelectionFallback::NoRemoteSocket`, the panel turns that into "not configured yet — press Test connection", and **Test connection** discovers the path over the round trip it is already making and writes it back into the row. The write-back is the webview's, deliberately: `useDesktopSettings` already serialises the document's read-modify-write, and a second writer in Rust would be [#828](https://github.com/vfarcic/dot-agent-deck/issues/828) created on purpose.

#### `Option<EndpointSettings>` is merge protection, and it puts an obligation on every future panel

`DesktopSettings::endpoints` is an `Option`, and `None` means **unspecified** rather than **empty**. That distinction is load-bearing and it is not a style choice.

`desktop_set_settings` takes the **whole document** from the webview, and the webview builds that document with `normalizeDesktopSettings`, which constructs a fresh object with a fixed key set — that fixed set is itself a credential guard, so it is not going away. A section the frontend does not declare is therefore already gone before any panel spreads it. If the field were a plain `EndpointSettings`, `#[serde(default)]` would turn "the webview said nothing" into "the webview said empty", and `merged_document` would then write that empty list over a hand-edited `[endpoints]` table: every remote deck deleted by somebody changing the theme. With an `Option`, TOML omits a `None` field entirely and the merge preserves what is on disk — the same protection the merge gives *across builds*, now available *across clients*. `a_client_that_cannot_render_endpoints_cannot_delete_them` pins it.

**The obligation that comes with it:** the moment a panel can render a section, the frontend must **round-trip** it — read it in `normalizeDesktopSettings`, keep it, and send it back — and must never fabricate a default for it. A default `{ remote: [], selection: "local" }` on the frontend side is exactly the deletion the `Option` exists to prevent, arriving from a client that *does* claim to know about endpoints. This generalises: any future section whose frontend half has not landed yet is declared `Option<…>` on `DesktopSettings`, and `endpoints` is the worked example. [Adding a setting](#adding-a-setting) step 2 carries the same rule at the point where it is needed.

**Since PRD #742 M6 that obligation is a mechanism rather than an assertion.** `endpointSectionToSave()` in [`desktop/src/lib/endpoints.ts`](../../desktop/src/lib/endpoints.ts) takes the section the panel was handed and the section it wants to write, and returns `undefined` — *write nothing at all* — when the two say the same thing. Both `EndpointsPanel` and `DeckSelector` route every write through it: the chooser, the row editor, the remove button, the **Test connection** write-back and the selector's own menu. The write-back was the exception until PRD #742 M8 — it called `onSave` directly because it writes against `latest.current` rather than the document its handler closed over, and `saveSection` closed over that document. It was inert (its `row && !row.socket` find-guard fires only on a genuine change, and on an unreadable document the fabricated `remote: []` makes the `find` miss, so nothing is written), but inert by a property of that one write-back rather than by the shared rule these three sentences claimed for it. M8 gave `saveSection` a document parameter so both kinds of write use one gate. So a click that selects the deck already in force, or that abandons a draft without moving the selection, cannot reach the merge and cannot delete a row it never saw. It is shared rather than a guard inside one handler because every one of those sites carried the same hazard; `EndpointsPanel::chooseDeck` had written a `{ remote: [], selection }` section on every click since #741 with no same-selection guard at all.

**What it does not close, deliberately, because this is the kind of thing that gets rediscovered expensively.** An *intentional* selection change against an **unreadable** document still writes `remote: []`. The webview cannot tell "there is no `[endpoints]` section" from "`load_from` could not parse the file and handed you defaults" — both arrive as `endpoints: undefined` — and refusing to create a section when none was declared would make All Decks unstorable on a fresh install, which is the majority case. Closing it needs either `remote` optional on the wire, so the webview can say "I am changing the selection and asserting nothing about the rows", or `load_from` to stop handing the webview a document that is not the file. Both are schema-or-load-path changes, and both are recorded rather than attempted.

**And the wider fact M6 established is the more useful half, because it reframes the whole protection: after a parse failure every field this build's schema owns is already lost on the next save, not just endpoints.** `load_from` logs and returns `DesktopSettings::default()`, the webview is handed those defaults, and `merge_tables` overwrites every key the struct owns — so appearance and zoom are reset by the next save whatever the endpoints section does. The `Option` was never protecting the user's data against *this* client; it protects a section against a client that does **not** render it, which is exactly the claim `a_client_that_cannot_render_endpoints_cannot_delete_them` makes and no more. Worth knowing alongside it: the preservation depends on the document still parsing as a **TOML table**. `merged_document` treats a document it cannot parse as a table as empty and replaces it outright, so a file that is not TOML at all loses its rows whether or not the client declared a section — the `Option` only reaches the case where the bytes are valid TOML and the *schema* is what rejected them.

#### The selection is a growable value, not an id

`Selection` in `settings.rs` is an enum with room for a variant, not an `Option<EndpointId>`, and `DeckSelection` in [`desktop/src/lib/endpoints.ts`](../../desktop/src/lib/endpoints.ts) mirrors it as a union. The stored form is one token — `local`, `all`, or a row's id — and `Selection` is the authority on it. That shape is what made [#742](https://github.com/vfarcic/dot-agent-deck/issues/742)'s **All Decks** additive, and the prediction held when M1 spent it: one variant plus one arm in each of `parseSelection`, `selectionToken` and `deckChoices`, against a change to every prop that carries a selection and a `""`-means-all convention in each of them. `sameSelection` needed none, because it compares stored tokens rather than variants. **No component below `endpoints.ts` handles a raw endpoint id.**

`all` is reserved on both sides, like `local`: `EndpointId::parse` refuses it case-insensitively, so a hand-written `id = "all"` row fails to load rather than making the one stored string ambiguous between the fleet and that row. PRD #742 accepted that refusal as a clean trade and it was not one at the time — "fails to load" then meant the whole document read as defaults and the next save replaced it; that is [#1072](https://github.com/vfarcic/dot-agent-deck/issues/1072), and under the refusal described in [When the document cannot be read](#when-the-document-cannot-be-read) the trade is now what it was assumed to be. `EndpointId::mint` cannot produce it — sixteen hex characters — so only a hand-edited document can reach that refusal. `EndpointSettings::resolve` deliberately has no `All` arm: it answers which *single* deck the deck screen and its terminals talk to, a fleet has no single answer, and `Selection::All` therefore travels through the same `let … else` as `Selection::Local` and gets the local deck with **no** fallback — nothing failed to be honoured. `EndpointSettings::observed_endpoints` is where the fleet's set lives instead, and [All Decks](#all-decks-the-fleet-and-the-one-screen-that-merges-it) is what that set does once it exists.

An unknown token — including one a newer build wrote, as `all` itself was before #742 — parses as `One`, finds no row, and resolves to the local deck with `SelectionFallback::UnknownDeck` reported alongside, which the app renders rather than swallowing. Nothing throws: the document is hand-editable and may have been written by a build with more variants than this one. The token is also **written back unchanged**, so an older build degrades rather than destroying a newer build's selection. That is what let `All` ship without damaging any older build's document, and it is pinned by `a_selection_token_this_build_does_not_know_degrades_without_being_lost` against a word this build still does not know.

### All Decks: the fleet, and the one screen that merges it

PRD [#742](https://github.com/vfarcic/dot-agent-deck/issues/742) adds **All Decks** to the Deck selector — the `all` token, the second reserved word beside `local`. Choosing it puts the app on a *set* of decks rather than one: `EndpointSettings::observed_endpoints` returns the local deck plus every configured row that has an address to connect to, the crate keeps a tunnel and a watcher alive for each, and the **agent overview** renders one group per deck with that deck's agents and that deck's connection state. **A row with no `socket` path is omitted from that set** — it has no address to connect to and a watcher for it would have nothing to watch. That is the CONNECTABLE set; the set the screen draws is wider. PRD #742 M12 separated the two after a half-configured deck turned out to be absent from the fleet entirely — not in the numerator, not in the denominator, no group anywhere, so three configured decks read as `2/2`. A row with no address now gets a group of its own carrying the settings panel's own "press Test connection" words, and M14 did the same for the other way a configured deck can be missing: one whose watcher has not reported yet. Both are in the denominator and neither is ever in the numerator. The **deck screen stays single-deck** — a tile's terminal is always the selected deck's, and All Decks resolves that single answer to the local deck.

**That split is a decision, not an oversight, and the reason is the one cost in this app that is already linear.** Attach streams are deliberately **not** multiplexed: the frame header is five bytes, one kind byte plus a four-byte big-endian length, with **no stream id** (`read_frame` in `src/daemon_protocol.rs`), so muxing would be a wire change and a `PROTOCOL_VERSION` bump rather than a refactor. A live terminal therefore costs one connection and one daemon-side task **per visible tile**. Putting N decks of terminals on one screen multiplies that; putting N decks of *rows* on one screen does not — the overview has no terminal anywhere on it. Cross-deck attach is the follow-up PRD's subject, not a gap in this one.

Two consequences of the split are worth knowing before touching either screen. The attach model below is keyed by **bare agent id** (`setShownTerminals(agentIds)`, `desktop_terminal_attach(agent_id)`), and agent ids are per-daemon monotonic integers, so two decks both mint `"1"` — that holds only because `terminal::attach` resolves its daemon through `trusted_daemon()`, which is `selected_endpoint()`, so every terminal the app opens belongs to whichever deck is selected at the time. And `groupAgents` was untouched by the fleet: it buckets *within* a deck by tab membership, which is a different question from which deck an agent is on, and calling it over a merged list would put two machines' roles in one orchestration card.

#### Per-deck connection state, and the one field that is still the selection's

`ConnectionStatus` and the four fields #741 added beside it stopped describing "the selected deck" and describe **a** deck, one per observed endpoint. `deckKind` and `localOnlyReason` are properties of a deck — whether *this* deck runs on this machine, and therefore whether Stop and Replace can act on it — and reading them off the process-global selection would have disabled the lifecycle controls for the whole view whenever the *selected* deck happened to be remote, and enabled them for a remote group whenever it happened to be local. `projectActionsReason` is likewise the capability set *that* daemon advertised in its own `Hello` reply, so a fleet spanning builds carries N capability sets rather than one.

**`selectionFallback` is the deliberate exception: it is still the selection's, not a deck's.** It describes the selection rather than any deck — "the deck you chose is gone, so you are on local" — so attaching it to every group would be a claim about each of them. The narrow fact that makes reading it off the process-global correct is worth stating rather than assuming, because it is the whole justification: `EndpointSettings::observed_endpoints` returns `[resolve().endpoint]` for every selection but `All`, so every other selection observes exactly the deck it resolved to and the fallback is that deck's by construction; and `All` — the only selection that can observe more than one — resolves to the local deck with **no** fallback at all, so a fleet never has one to misattribute.

#### The aggregate is honest about what it could not see

The overview's header prints `DECKS 2/3` beside the agent and group counts, and those counts are computed over the decks that **answered**. Summing over every deck is the failure the instrument exists to prevent: a deck that is not answering has an unknown number of agents, and adding zero for it under-counts the fleet by however many that deck is running — a wrong number that looks exactly like a right one. `DECKS` renders at one deck too, where `1/1` is the honest reading, because a caveat that appears only once something is already wrong is a caveat nobody learns to read; its hover carries the sentence a ratio cannot, naming that every count beside it is over the decks that answered.

A deck that is not answering renders as a **degraded group** — its own header, its own lamp, its own note, and an em dash where its neighbours' status pips are — and never as a group with zero agents. "This deck runs nothing" and "we cannot see what this deck runs" are different statements and only the first is a number. That is the same rule the header instruments have always followed for a single deck, restated one level out: the instruments read `—` rather than `0` while no deck is answering.

#### `socketPath` is a label; `deckId` is the key

`connection.deckId` is `EndpointIdentity::wire_id()` — FNV-1a over the newtype's *derived* `Hash`, rendered `deck-<16 hex>` — and it is what anything keying on a deck must use. `connection.socketPath` is `Endpoint::describe()`, which is what a reader sees, and the distinction is enforced rather than advisory because the collision it prevents is the most instructive defect in this PRD.

**The defect.** The webview derived its per-deck `daemonId` from `socketPath`, and `describe()` renders a remote deck as `user@host[:port]` — omitting the remote socket path, the identity file and the jump host. So two `[[endpoints.remote]]` rows pointing at one host with different `socket` values — **two daemons on one machine, which is exactly what that field exists for** — described identically, folded into one group, and their agents shared a `daemonId`. The composite `(daemonId, agentId)` key the overview has used since day one does not save it, because the key *component* is the collision. It fails silently and plausibly: the two decks alternate inside one group, last writer wins per emit, and the screen looks like one healthy deck. `EndpointIdentity` was introduced on PR [#1035](https://github.com/vfarcic/dot-agent-deck/pull/1035) specifically for the N-deck case and then stopped at the Rust boundary; `wire_id` is it crossing.

**Derived through `Hash`, hashed rather than rendered, and FNV rather than `DefaultHasher`** — each for its own reason. Hashing the newtype inherits the key: a field added to `RemoteEndpoint` tomorrow joins the token with nothing here to update, where a hand-picked tuple of fields would have been a second copy to keep in step. A hash keeps the value **opaque**, which is a misuse control rather than a disclosure one — the desktop's IPC is same-process and `socketPath` carries the host beside it — so the `deck-…` string cannot become a second hand-assembled identity the way `describe()` did. And the algorithm is written out in `src/daemon_client.rs` because `std` documents `DefaultHasher`'s output as unspecified and free to move between Rust releases, so the token names the same deck across a restart. **That last clause used to end "while the webview keys `localStorage`-backed state on this token", and nothing does** — M8 enumerated the five keys in [The five `localStorage` keys](#the-five-localstorage-keys-and-the-one-that-is-gone) and every one is scoped by runtime mode rather than by a deck. The stable hash is still the right choice, for the forward-looking reason rather than the stated one: this is the identity a per-deck preference would be keyed on the first time anyone stores one, and a token that moved under a toolchain upgrade would break that store in a way nobody would trace back to the upgrade.

**Two things `wire_id` does not promise**, stated because the honest scope is narrower than "stable": identity across a future change to `std`'s own `Hash` impl for a component type, and identity across architectures, since the default `Hasher::write_*` methods feed integers in native-endian order. Either would reset a per-deck UI preference on one machine. Neither can confuse two decks, because both sides of every comparison are minted by one process from one build. The same token is also what stamps `desktop://daemon-event`, and it had to move with the snapshot key rather than after it: the webview's "is this event from the deck the screen is on" filter compares the two, so a stamp left on `describe()` would have gone on answering *yes* for a second daemon on the same host after the snapshot key had stopped doing so.

#### Fleet membership rides on every snapshot

`DesktopSnapshot.fleet` is the observed deck ids in observed order, **selected first** and **never empty**, and every deck's snapshot carries the same list because it is a property of the applied document rather than of the deck that happened to emit. So a webview may prune on any arrival.

**It is on the wire because nothing else says a deck has left.** A deck *joining* announces itself by emitting; a deck leaving produces no event at all — `apply_selection` ends its watcher, and under `All` → `local` the resolved deck does not move, so nothing is emitted on that path either. A webview that only upserts what arrives would keep that deck's agents on screen, frozen and looking live. Selected-first is the second half: `fleet[0]` answers "which deck are the single-deck surfaces bound to", which nothing on the stream could answer before, since under `All` every observed deck emits the same shape. Never-empty is the third: a deck the app cannot reach is still an entry and still emits its own `disconnected` snapshot, so an empty list is a malformed payload rather than an empty fleet — and the webview's prune treats it as such rather than clearing the screen.

One ordering subtlety, because it is cheap to get backwards: **prune after the upsert, not before.** A watcher being torn down can land one last snapshot whose `fleet` no longer names its own deck, and pruning first deletes the entry only for the upsert to reinstate it.

#### What the fleet costs, and what nothing bounds

Per **live remote** deck, in the observe-only scope: one `ssh -N -L` child, one detached OS thread draining its stderr (spawned and never joined), one forwarded Unix socket plus client fds, roughly two held daemon connections, one client-side watcher task and one daemon-side task per held connection. A **local** deck costs the same minus the ssh child, the stderr thread and the forwarded socket.

**Nothing bounds the number of configured or live endpoints.** `MAX_ENDPOINT_ID_BYTES` bounds an id and `MAX_SETTINGS_BYTES` (256 KiB) bounds the file, so the only ceiling is accidental and sits around a thousand rows. And the first thing to hurt is not the one that list suggests: `SNAPSHOT_COALESCE_INTERVAL` is applied **per watcher loop**, so with per-deck watchers the aggregate emit rate into the webview is N × the single-deck ceiling, and the frontend merges every one of them. That is IPC and CPU pressure scaling with **deck count regardless of how few agents each deck has**, which is the opposite of the intuition that a small fleet is cheap. The first hard OS ceiling is separately the ssh child and fd count, reaped only by `retain`/`close_all`.

Coalescing per watcher is the deliberate choice rather than an accident of where the sleep sits. The alternative — one global coalescer over N inbound streams — shares the window across decks and reintroduces exactly the cross-deck stall the per-deck design exists to remove.

#### One unreachable deck must not degrade the others, and that needed two locks split

`DaemonLinks.trusted` and `EndpointTunnels::acquire` each held **one async mutex over the entire map, across establishment**. With one selected deck that serialisation is a feature: concurrent first-uses of the same deck collapse into one handshake. With N decks it inverts — an unreachable deck's connect attempt queues every *other* deck's handshake behind its connect timeout — and it is invisible at every call site, which is why it is written down here. Both are now a per-deck gate keyed like the map and held across establishment, with the map's own lock held only for the lookup and the insert around it, so concurrent first-uses of one deck still collapse into one handshake while two decks never wait for each other.

**Both had to be split, for asymmetric reasons.** `establish()` holds `DaemonLinks`' gate *across* `acquire()`, so fixing only `DaemonLinks` would have left every `acquire` serialised behind one deck's ssh authentication — the property unmet for exactly the remote decks it is about. Fixing only `EndpointTunnels` would have left the `DaemonLinks` test red. And the coverage is asymmetric too: **`EndpointTunnels::acquire` has no test** and was fixed from the code deliberately. The risk test injects its stall at `hello()`, which sits *after* `acquire` inside `establish()`, and for a local endpoint `acquire` returns promptly — so that test passes whether or not this lock was ever split. Stalling `acquire` deterministically means stalling `EndpointConnection::open`, whose slow path is spawning `ssh`, which needs either a real network timeout or a production test seam; neither was judged worth it.

#### Splitting that lock gave up a second property, and putting it back is one counter

The whole-map lock was doing **two** jobs. The first — collapsing concurrent first-uses of one deck — is what the per-deck gate replaced. The second was that, being held *across establishment*, it also excluded **teardown**: `EndpointTunnels::retain` and `DaemonLinks::invalidate_all` blocked until the establishment finished and then removed the entry. The gate does not do that. `retain` and `invalidate_all` take only the map lock, and a deck being established has **no entry in that map yet**, so a teardown landing mid-establishment removes nothing for it and the establishment publishes it afterwards.

The user-visible shape: a refresh is inside `acquire(D)` for up to `FORWARD_READY_TIMEOUT` (30 s) while the user picks a different deck; `retarget_selection` runs `invalidate_all()` and `retain({the new set})`, neither of which can see `D`; and `D`'s lease and trusted link land in maps nothing will consult again until the next settings save. One `ssh -N -L` child, its stderr thread, a forwarded socket and a held daemon connection, to a host the user believes they have disconnected from. Bounded and eventually reaped — but *"I removed that deck" stops meaning "that ssh session is gone"*, which is the part worth a mechanism.

**The mechanism is `desktop/src-tauri/src/generation.rs`: a monotonic counter read before the await and compared after.** A value that moved means somebody removed something, so the publish is refused. **Not publishing is the correct fallback rather than an error** — the lease or the link is still returned, the caller's own `Arc` is then the last one, and what it holds dies with the request instead of outliving it. The compare and the insert happen under **one** hold of the map lock, and each teardown bumps under that same lock, so the two are serialised rather than merely likely to interleave correctly.

**One counter serves both maps**, because they describe one live set and are torn down together — `retarget_selection` calls `invalidate_all` and `retain` in consecutive statements. It lives on `EndpointTunnels` and `DaemonLinks` reads it through `tunnels().generation()`. What that buys precisely: `DaemonLinks::trusted`'s compare is *exact* against `invalidate_all` (same lock), and conservative against the tunnel map's own three teardowns; it does not need to be exact against those, because a link outliving a tunnel `release` is the designed behaviour — `TrustedDaemon` holds its own lease so a classified link cannot describe a deck whose transport has gone.

**What it costs, stated because the cheaper-looking alternative is worse.** The counter does not say *which* deck departed, so a settings save that keeps the deck being established still costs that establishment its cache entry and the next call establishes again. Testing `dto::deck_is_observed` instead would be precise — and would depend on the applied selection having been written before the teardown ran, which is an ordering invariant *between two modules* rather than a property of the one doing the check. One re-establishment on a rare overlap is the cheaper side of that trade.

**The same counter is what names a watcher claim.** `DesktopState::start_watcher_once_for` returns a token rather than a `bool`, and `register_watcher` stores a handle only into the claim that asked for it. Before that the slot was a bare `Option<JoinHandle>` and could answer only "is there a claim here" — so if a deck left and rejoined inside the claim → spawn → register window, watcher A's handle went into watcher B's slot and B's own registration then *replaced* it. Replacing drops a `JoinHandle` rather than aborting it, so A ran untracked and unstoppable for the life of the process, double-folding every broadcast for that deck. A handle arriving for somebody else's claim is now aborted exactly like one arriving for no claim.

#### The applied selection is one value, and the evidence ring follows the selection

Two smaller things the fleet made worth stating.

**`SELECTED_DECK` and `OBSERVED_DECKS` were two `RwLock`s written in sequence**, and the comment on the second said the pair "can never describe different saves" because there is one writer. True of *saves*; false of *reads* — a reader scheduled between the two `write()` calls sees one new value and one old one. They are now one `AppliedSelection` under one lock, with `selected_deck`, `observed_decks`, `deck_is_observed` and `observed_fleet` all deriving from a single read. The direction that cost something was `release_if_not_observed` reading a pre-save observed set while a just-added deck's watcher already held a lease: the probe drops the map's handle, the watcher's lease keeps the child alive, and the next `establish()` opens a **second** `ssh` child. Reported as unreachable-in-practice by the two people who found it; a reconstruction of the two-lock shape failed the regression test 6 times out of 6, so the window is real even though a human-paced settings save makes it rare.

**The evidence drawer and the handoff rail follow the selected deck.** `TauriDeckBridge` keeps one working ring rather than one per deck — the per-deck storage is each `DeckSnapshot`'s own `evidence`/`handoffs`, which the fleet already carries — and `adoptEvidenceDeck` swaps that working copy for the arriving deck's own history whenever `selectedDeckId` moves, in `foldSnapshot` and in `connect()`. It was process-global, handed to whichever deck was currently selected and never cleared on a switch, so a switch to `build-box` mapped its first snapshot carrying every hook event the local deck had produced — one machine's agent ids, roles and pane ids under another machine's name. `isSelectedDeckEvent` stops *live* events crossing and does nothing about a ring that survives the switch, so the filter reads as a stronger guarantee than it was. **Pre-existing rather than introduced by the fleet** (`4a7ae532` passed the same global ring), fixed here because a partial guarantee that reads as a total one is worse than neither. One consequence worth knowing: the sequence counter behind each item's `hook-<n>` id is **never** reset, because those are React keys and two decks restarting the count would collide two distinct items under one key.

### Trust is a property of the deck's kind

This is the conceptual heart of the PRD, and the shortest statement of it is: **`Local` keeps uid + mode `0o600`; `Remote` rests on ssh host-key and user authentication; neither pretends to be the other.**

**#741 left the local check unchanged, and for the first time pinned its refusals.** `verify_endpoint_trusted` (`src/platform/fsperm/unix.rs`) stats the path and requires that it is a socket, that its owner uid is this process's, and that its mode is **exactly** `0o600` — a comparison, not a mask, so `0o644` and `0o666` are both refused and so is `0o700`. It runs **out of band and before the first connect**, which is its position as much as its content: moving it inside `connect` would change the Unix semantics it exists for. What #741 M1 added is the test coverage that did not exist before it — the wrong-mode, foreign-uid, not-a-socket and nothing-at-the-path refusals plus the healthy accept — because nothing else would catch a later refactor silently loosening the predicate. The foreign-uid arm is not testable without a second account, so the comparison is factored out as pure data and *that* is tested, the way the Windows owner-SID check is pinned.

**One clause has moved since, and it is a fourth requirement rather than a change to those three** ([#1020](https://github.com/vfarcic/dot-agent-deck/issues/1020)). The stat is now an `lstat`, and a **symlink** at the endpoint path is refused on its own account. It used to be `std::fs::metadata`, which reads *through* a link — so the uid and mode tested were the target's and the link's own were never consulted, and a link owned by anybody at all was accepted as long as it resolved to a socket of ours at `0o600`. That is reachable only where a foreign uid can create the entry, which means the `/tmp` fallback `attach_socket_path()` takes when `XDG_RUNTIME_DIR` is unset — an ssh session or a container, which is exactly this PRD's case. It is redirection rather than disclosure: the attacker is not the listener, and the payoff is steering our own client at another of our *own* endpoints. #741 M1 had pinned the old behaviour as characterization precisely so this change would go red rather than flip silently. Only the **final** component is `lstat`ed, so a symlinked ancestor resolves as before — which matters on macOS, where `/tmp` is a symlink to `/private/tmp`. The residual is availability and is [#1121](https://github.com/vfarcic/dot-agent-deck/issues/1121): `/tmp`'s sticky bit stops the deck unlinking an entry another uid owns, so a squatter still wedges that path until they or root clear it, and the refusal now names `DOT_AGENT_DECK_ATTACH_SOCKET` as the manual way around it.

**The local check is deliberately NOT run against a forwarded socket, and that is a claim about what it can prove.** A remote deck's connect address is a Unix socket the local `ssh` client created, under a `0700` directory this app owns. A uid + mode test on that inode says something true and useless: it proves the local `ssh` process is ours. It says **nothing** about who is on the far end, which is the only question that matters for a remote deck. Running it anyway would be the disguise PRD #741 exists to remove — a client treating a remote endpoint as local because the address happens to look like one. `establish()` gates it on `endpoint.as_local()` for exactly that reason, and the comment above it says so.

What stands in its place for a remote deck is ssh's own authentication, and #741 M5 made sure it is actually in force rather than merely available:

- **The host-key CHECK is forced on; the trust anchor it consults is not.** `StrictHostKeyChecking=yes`, through the generated config, on both hops. Leaving it unset meant the *user's* config decided, and a `Host * / StrictHostKeyChecking no` line left a long-lived channel the GUI presents as trusted with no host-key check at all — an on-path attacker who answered instead of the real host got a healthy-looking remote deck, and nothing in the app could notice, by construction, because the local inode check is correctly not run against a forwarded socket. Forcing `yes` costs nothing against the default `ask`, since `BatchMode=yes` already makes `ask` fail on an unknown key; against `accept-new` it costs silent trust-on-first-use, which is the point. **What it does not buy** (PRD #741 final audit F2, and this bullet used to imply otherwise): `KnownHostsCommand`, `UserKnownHostsFile` and `GlobalKnownHostsFile` are all inherited from the user's config, measured on both hops, so a `KnownHostsCommand` emitting whatever key the server presents satisfies the forced `yes` against any host. Forcing those three works — `KnownHostsCommand none` from the leading block wins, measured — and is deliberately not done, because it breaks the CA- and inventory-driven fleets `KnownHostsCommand` exists for. This sits inside DECISION 1A's trust boundary, so the answer is the one the inherited forwards got: **Test connection** discloses the resolved host-key source beside them.
- **`StrictHostKeyChecking=no` and `accept-new` are never set by us.** A first-contact decision belongs to a user who can see a fingerprint, and this app cannot show one. The failure surfaces as its own named state with the remedy — run `ssh <target>` once in a terminal — built from the endpoint the tunnel actually uses, bastion and port included.
- **`UserKnownHostsFile` is deliberately not forced**, nor are `GlobalKnownHostsFile` and `KnownHostsCommand`. Pointing any of them anywhere would decide *which* keys count as trusted on the user's behalf. We force that the check happens; the user's config decides what passes it — and **Test connection** shows which source it chose, so the decision is visible rather than merely inherited.
- **The forwarded socket's mode is forced too.** `StreamLocalBindMask=0177` gives a `0600` socket. OpenSSH's default is already `0177`, but it is a config option, and a user config `StreamLocalBindMask 0000` made the socket world-readable and world-writable — with nothing in the app checking, since the trust check is not run there and `await_forward` treats any socket inode as readiness. The only thing keeping a foreign uid off the channel was the `0700` directory the tunnel creates, which does hold but held alone. See [the generated config](#the-generated-ssh-config-and-why-its-layout-is-load-bearing) for why this one option has to be emitted *after* the includes.

**Nothing terminates a process by peer credential on a remote deck.** That is the `as_local()` property above, restated where it matters: `run_daemon_stop` performs an `SO_PEERCRED` lookup and SIGTERMs the pid it finds. Over a tunnel that pid is the local `ssh` client, so the call would kill the tunnel, report success, and leave the remote daemon running. It is unreachable by type, and **Stop deck** / **Replace deck** render as disabled with the reason `require_local` produced rather than silently doing something else.

#### The trust check is the FOURTH `as_local()` gate, and the only one that substitutes rather than refuses

[Decks](#decks-which-ones-the-app-talks-to-and-how-it-reaches-another-machine) above names **three** operations that are local-only because `Endpoint::as_local()` returns `None` for the remote arm: `peer_pid` termination, the stale-inode `remove_file`, and lazy-spawn. #741's own Risks section warned that `peer_pid` was "not the only locality assumption, only the one that was found", and PRD #742's audit enumerated every production call site of `as_local()` and `require_local()` and discharged that warning. **The fourth is the trust check itself**, in `establish()` (`desktop/src-tauri/src/daemon_bridge.rs`), gated by `if let Some(local) = endpoint.as_local()`. For a remote deck `verify_endpoint_trusted` never runs at all.

**The difference in *shape* is the dangerous part, and it is why this belongs beside the three rather than inside their list.** The three that were named **refuse**: a disabled button carrying the sentence `require_local` produced, so the user is told. This one **silently substitutes a different trust basis** — ssh host-key and user authentication in place of the uid-and-mode inode predicate. Nothing in the flow says "this deck was trusted differently"; it simply was. The substitution is correct — a uid and mode test on a socket the local `ssh` client created would prove only that the local `ssh` process is ours — but it is invisible, and an invisible correct answer and an invisible wrong one look identical from the screen.

**With a fleet in one window that matters more than it did with one deck at a time.** Under **All Decks** the local groups and the remote groups sit side by side with the same chrome, the same lamp and the same status pips, so it is newly easy for a reader to assume every group in the view cleared the same gate. The local ones did and the remote ones did not, by design. For the observe-only scope this is not a leak — each remote deck's ssh trust is per-connection and keyed by `EndpointIdentity`, so no deck inherits another's — but a reader reasoning about what the screen proves should reason per group and not per screen.

### Why `ssh -L`, and what was rejected

The app shells out to the system `ssh` and owns a long-lived `ssh -N -L` child per remote deck. Two alternatives were costed first, and both are worth recording because the next person to ask will otherwise re-run the measurement.

**An in-process SSH client (`russh`) was measured out, and the deciding half is `cargo audit`.** `security` is a **required** check on this repository and it is currently clean.

| variant | packages | new to this repo | `cargo audit` | `cargo check` |
| --- | --- | --- | --- | --- |
| baseline (this tree) | 615 | — | exit 0, zero vulnerabilities, 8 informational | — |
| `russh`, default features | 181 | **81** | **exit 1** — RUSTSEC-2023-0071 | not attempted |
| `default-features = false, ["aws-lc-rs","flate2"]` | 178 | 78 | exit 0 | not attempted |
| `default-features = false, ["ring","flate2"]` | 183 | 78 | exit 0 | exit 0 |
| pure Rust, no crypto backend | 170 | — | — | **exit 101** |

`rsa` is a **default** feature of russh 0.63.3, pinned `=0.10.0-rc.18`. RUSTSEC-2023-0071 (the Marvin timing attack) has `patched = []` — there is no version to bump to — and the advisory's own workaround is "avoid using the `rsa` crate where attackers can observe timing", which describes a network SSH client exactly. Getting past it means either dropping RSA, which silently loses `ssh-rsa`/`rsa-sha2-*` host keys and RSA user keys for precisely the users most likely to have a remote daemon, or writing this repo's **first** `.cargo/audit.toml` ignore — a durable weakening of a clean required gate in exchange for a feature. Of the 81 new crates the overwhelming majority are `0.x` cryptographic primitives, and russh pins two release candidates as hard `=` requirements (`ssh-key = "=0.7.0-rc.11"`, `rsa = "=0.10.0-rc.18"`), so the tree could not take a patch of either without russh cutting a release; this repo's entire crypto exposure today is zero. And **a pure-Rust russh is impossible**, measured rather than assumed: a `compile_error!` demands `ring` or `aws-lc-rs`, both native C, so it would also add a C toolchain requirement to `build`, `build-windows`, `build-macos` and to `flake.nix`.

Against all that, auth is the hard part and the system `ssh` already solves it: `Host` blocks, `Include`, `Match`, `ProxyJump`, `IdentityAgent`, FIDO `sk-` keys, PKCS#11, GSSAPI, certificate auth, `known_hosts` with hashed names and `@cert-authority`. Reimplementing the *config* half of that is a project, not a dependency. Delegating it is `src/remote.rs`'s existing bet and it has held.

**The honest cost of shelling out is that OpenSSH has no stdio-to-remote-*Unix-socket* forwarding** — `-W` is `host:port` only — which leaves two shapes.

- **1A — `ssh -N -L`, app-managed.** What shipped. It forwards the remote attach socket to a local one the app owns, under a `0700` directory it creates.
- **1B — `ssh <host> dot-agent-deck daemon proxy-stdio`, a new deck subcommand.** The remote binary connects to its own attach socket and pipes it to stdio, so the ssh child's stdin/stdout *is* the transport. It is genuinely attractive: no local socket anywhere on the client, so no `StreamLocalBindUnlink`, no chosen-path unlink, and no orphaned-tunnel false-healthy state; a plain pipe pair, which the five generic framing helpers already take; and it is the Windows answer at no extra cost, since a Windows client never needs a named pipe to reach a *remote* daemon and Windows 10+ ships OpenSSH.

**1B was not chosen now, and the reason is arithmetic about concurrency.** The desktop holds *concurrent* connections, not one: roughly one attach socket per shown terminal (a nine-tile deck is nine, plus a warm set bounded at 3), plus one long-lived event subscription, plus transient request connections — call it ~11 at once. Under 1B each of those is a separate `ssh` child with its own TCP connection and its own authentication, and each is an `exec` session, so they count against sshd's `MaxSessions`, whose **default is 10** — squarely inside our range. Under 1A a single `ssh` child carries all of them as **forwarding channels** over one authenticated connection, and forwarding channels are not sessions. `UNVERIFIED:` that last clause is recalled OpenSSH semantics rather than a measurement taken here. It is not load-bearing for 1A, which avoids the question entirely, but it **is** the thing that has to hold before 1B ships, so whoever picks up the follow-up measures it first.

1B is filed as the deferred follow-up that removes the local socket and unlocks the Windows desktop. Its own costs are known: a remote daemon whose binary predates the verb fails with `unknown subcommand` — loud, not silent, and the same class as a protocol floor — and it needs the remote binary's path resolved, for which `src/connect.rs`'s `install_path` machinery is the precedent.

**And the local socket stops being the defect it is today, for a reason worth stating precisely.** The disguise was harmful because the client *did not know* it was remote. Under `Endpoint::Remote` it knows: the transport owns that socket, the local trust check is not run against it, its presence is never read as health, and the app owns its lifetime. It is an implementation detail of the remote transport rather than a false claim about the endpoint's nature.

**Four things about the child are not optional**, each a measured cost of shelling out rather than an argument against it. `ssh` is resolved by **absolute path** at startup and never as a bare `"ssh"` from `PATH`, because a GUI launched from Finder or a `.desktop` entry inherits launchd's or the session manager's environment and a user-writable directory earlier in `PATH` can substitute a binary (`src/login_shell.rs` exists because this repo already has this problem). `BatchMode=yes` is forced, because a GUI has no tty: without it ssh tries to read the host-key prompt from a closed stdin and refuses anyway, but the app gets an unclassified failure instead of a classified one — and on a Linux desktop with `SSH_ASKPASS` and `DISPLAY` set, ssh may pop **an unrelated third-party dialog the app did not draw**. Nine of `apply_observation_options`' ten options are inherited, all but `ClearAllForwardings`. And the child's lifetime is owned — process group, reap, and kill on quit including the paths where `Drop` does not run — which was genuinely new work: both pre-existing `Command::new("ssh")` sites block on the child.

**Where a tunnel lives is a design decision, not an implementation detail.** A `TrustedDaemon` is re-established every `HANDSHAKE_REVALIDATE_INTERVAL` (5 s), so a tunnel owned by one would re-authenticate ssh every five seconds: tunnel lifetime is not handshake lifetime. [`desktop/src-tauri/src/endpoint_tunnels.rs`](../../desktop/src-tauri/src/endpoint_tunnels.rs) is the owner — keyed by `EndpointIdentity`, a field of `DesktopState`, shared by `Arc` with `DaemonLinks` because establishment is merely where a transport is *needed*, and **never** a field of `TrustedDaemon`. Its module docs carry the five ownership rules and the four teardown triggers. A live terminal session holds its own `TunnelLease`, because a session's write half outlives the request that opened it by the whole life of the tile.

### The generated ssh config, and why its layout is load-bearing

This is the subtlest part of the transport, and a reader who does not know why will undo it. The tunnel is invoked with `-F <generated file>`, and that file is:

```text
Host *
    <forced_options()>                  first-value-wins: must precede the Includes
Include ~/.ssh/config                   the user's config still decides
Include /etc/ssh/ssh_config             -F suppresses this file, so put it back
Host *
    <forced_last_wins_options()>        last-value-wins: must follow the Includes
```

**Two `Host *` blocks, because OpenSSH resolves different options in different directions.** Almost every option is first-value-wins — `readconf.c` assigns only when the field is still at its sentinel — which is why a leading `Host *` block beats whatever the user's config says later. `StreamLocalBindMask` is parsed with an **unconditional** assignment, so for it the *last* value wins and the usual reasoning inverts. Measured on OpenSSH 10.2p1, all three cases: ours first and theirs second resolves to theirs; theirs first and ours second resolves to ours; and `-o StreamLocalBindMask=…` with theirs in the config resolves to theirs. So the obvious fix — "add `StreamLocalBindMask=0177` to `forced_options()`" — does **not** hold, and a trailing block after the includes is the only channel that forces it.

**Why a generated file at all, when `-o` exists.** `-o` reaches the `ssh` process we spawn and nothing else. When a jump host is set, OpenSSH implements `ProxyJump` by synthesising an implicit `ProxyCommand` that runs a **fresh `ssh`** carrying only `-l`, `-p`, `-J`, `-F` and `-v` — not one `-o` survives. Measured on OpenSSH 10.2p1 with `Host * / ForwardAgent yes` in the user's config and every option on the command line, the *jump* hop resolved `forwardagent yes`, `batchmode no` and `stricthostkeychecking false`: the user's ssh-agent was forwarded to the bastion for the whole life of the tunnel, with the GUI showing nothing. `-F` **is** propagated into that implicit `ProxyCommand`, so the generated config is the one channel that reaches both hops. Re-measured with the file in place, the jump hop resolves `forwardagent no`, `batchmode yes`, `stricthostkeychecking true`, `updatehostkeys false` and `streamlocalbindmask 0177`. The `-o` flags stay as well, because they are what holds if the file is ever read differently than we expect and because the values are identical, so first-value-wins makes the duplication inert.

**`-F` is passed unconditionally, not only when a jump host is configured.** The interpolation is conditional on `-F` being on the command line, not on `-J` being there — measured with `Host * / ProxyJump bastion` in the included file and no `-J` of our own: the implicit `ProxyCommand` still carried `-F <path>`. A jump-only `-F` would therefore leave open the case where the *user's* config adds a jump hop the app never asked for, which has the whole of the problem above.

Four more facts the layout rests on, each measured rather than reasoned from the manual: an `Include` of a **missing** file is not an error, so a user with no `~/.ssh/config` resolves cleanly; a second `Host *` block after the includes still applies, and still applies when the included file ends inside a `Match` block; `-F` **suppresses `/etc/ssh/ssh_config`**, so without the second `Include` a site config would silently stop being read the moment this file existed, and including it after the user's preserves ssh's normal user-over-system precedence; and a *relative* `Include` inside the user's own config still resolves against `~/.ssh` even when reached through ours, so the common `Include conf.d/*` pattern survives being nested one level deeper.

**Writing the file fails the tunnel closed, and that is the design rather than caution.** Without it, the forced options reach only the direct hop — so a tunnel that came up anyway would be exactly the silent agent-forward to the bastion the file exists to prevent. The same rule applies to `Test connection`'s own short-lived `ProbeConfig`: a probe that ran without the file could resolve and *report* a configuration with the user's `ForwardAgent yes` still in force on the jump hop.

**The path the file lives at is itself an ssh input.** `-F`'s value is interpolated **unquoted** into the `ProxyCommand` string OpenSSH hands to `/bin/sh`, and that string is percent-expanded first. Measured: a `%` in the path fails the connection with `vdollar_percent_expand: unknown key`, which no classifier here names; a space makes the jump hop read `-F <first-word>` and treat the rest as a destination. That is why the path check refuses `%`, whitespace and the shell metacharacters. The metacharacter half is worth stating carefully: with an existing config file named `a;touch marker;b.conf`, OpenSSH 10.2 **did** build the string — the implicit `ProxyCommand` carried it verbatim — and the run then ended without creating the marker, for a reason that investigation did not isolate. So the honest claim is that the string *reaches* the `ProxyCommand`, not that 10.2 executes it or that 10.2 refuses it; the app runs against whatever OpenSSH the user has, so refusing the byte here is the defence either way.

### What your own ssh config still decides

DECISION 1A inherits the user's ssh config on purpose — that is most of what shelling out to `ssh` buys — so some of what the tunnel does is not ours to force. These are residuals, not oversights, and **Test connection** is where a user can see the two that matter.

- **Your forwards are inherited for the whole life of the tunnel.** `ClearAllForwardings` is not forced, because it clears command-line forwards too and would therefore clear our own `-L`, and OpenSSH has no per-direction alternative. So a `Host *` block carrying `RemoteForward`, `DynamicForward` or `LocalForward` applies to the direct hop — measured with `Host * { RemoteForward 9999 localhost:22; DynamicForward 1080 }`: both resolve. `remote doctor` refuses to create such a forward at all and calls it a criterion violation; the tunnel makes the same exposure for orders of magnitude longer. `ExitOnForwardFailure=yes` sharpens it in a second, non-obvious way: a config `RemoteForward` that fails to bind now kills the tunnel, because that option applies to **every** requested forward and not only ours. The *jump* hop is exempt, and that is measured rather than assumed — `-J` invokes it with `-W`, which sets `clearallforwardings yes`. **`Test connection` discloses this**: it reuses `remote_doctor::parse_ssh_g` against the tunnel's own `-F` config and forced options, so what it reports is what the tunnel will actually inherit. The panel renders **all three answers in words**, which is PRD #741's final audit F1: *"This deck's tunnel will also carry, from your ssh config: …"* with the list, *"Your ssh config adds no forwards to this deck's tunnel."* when it read the config and found none, and *"This test could not read your resolved ssh config…"* when the resolution did not run or came back truncated. Rendering only the non-empty list made the second and third answers both come out as a blank screen, which is the one claim that block must not make, arrived at from the render rather than from the parse. The parser side of the same finding: a forward line whose value `ssh -G` printed unquoted — `LocalForward "/Users/First Last/db.sock" …`, or any Unix-socket forward, which is the shape this app itself uses — is reported as unreadable rather than dropped, so the list is never quietly shorter than the config.
- **Your config decides where host keys are checked against.** `StrictHostKeyChecking=yes` is forced on both hops, so the check happens; `KnownHostsCommand`, `UserKnownHostsFile` and `GlobalKnownHostsFile` are inherited, so what the check is anchored to is the config's. Measured: all three survive a `Host *` block onto both hops, and a `KnownHostsCommand` that emits whatever key the server presents satisfies the forced `yes` against any host. All three are forceable and deliberately are not forced — `KnownHostsCommand` is how CA- and inventory-driven fleets distribute host keys, and a per-project `UserKnownHostsFile` is ordinary — so **`Test connection` discloses this too**, beside the forwards and from the same `ssh -G` run: *"Host keys for this deck are checked against: …"*. The block is additive, so an empty one asserts nothing (a local deck runs no `ssh` and has no source to name); "could not look" is covered by the forwards sentence above, which is gated on the same fact.
- **Your config can still run code.** `ProxyCommand`, `Match exec` (which runs at config-parse time on *every* invocation, and is not closed by `PermitLocalCommand=no` — measured), `KnownHostsCommand`, and `PKCS11Provider` / `SecurityKeyProvider` `dlopen`s. None is closable without breaking `ProxyJump`, which DECISION 1A exists to inherit.
- **Your config can retarget the connection.** A `Hostname` inside a matching `Host` block silently sends ssh somewhere else, so `describe()` can name a different host than ssh reaches.
- **On macOS, nothing bounds an orphan `ssh` after a force-quit.** `process_cmdline_mentions` is Linux-only, so the orphan reaper unlinks the socket and leaves the process running. It holds an authenticated session until its keepalives notice a dead link, or indefinitely if the far end stays up. `proc_pidpath` / `sysctl KERN_PROCARGS2` would close it.

### Configuring a remote deck by hand

The remote leg needs a **second machine** for the part that is genuinely about two machines — what the client *renders*, which is the half still unticked. It does not need one for the **transport**, and it does not need one for **who resolved a path**; both of those are now covered on one box, and the sentence that used to sit here ("a loopback `ssh` to the same box is not a remote test: locally the client's filesystem *is* the daemon's, so every path assertion passes whichever side resolved it") was one claim too wide in both directions.

- **The transport is covered by `fleet/observe/002`** (`tests/e2e_remote_deck_transport.rs`, lane 1). A loopback tunnel is a *complete* test of the transport: a real `ssh -N -L` child, a real forwarded Unix socket, the real `EndpointConnection::open` arm, the forced host-key check really running, and a real teardown. Sharing a filesystem takes nothing away from any of that. It runs against an `sshd` the test starts itself, so it touches no operator account; see that file's header.
- **"Whichever side resolved it" is covered by `project/resolve/002`** (`tests/e2e_project_verbs.rs`, lane 1). What has to differ to make that falsifiable is not the *filesystem* but the **resolution inputs**, and those can diverge on one machine: the daemon starts in one project directory and the client stands in another, equally valid one, and `ListProjects` must name the daemon's. Measured by inversion — start the daemon where the client stands and the listing carries the client's directory and the test goes red. Note `HOME` is *not* the lever: no project-resolution path reads it (`ResolveProject` refuses a non-absolute path, `read_project_config` does not walk upwards), and the harness has given every daemon its own `HOME` all along.

What a second machine is still needed for is the **display** half — that the project header and agent panes name the daemon's paths and never the client's — which is what [#819](https://github.com/vfarcic/dot-agent-deck/issues/819) left its M10 unticked for.

On the machine that will host the deck:

1. Run `dot-agent-deck daemon serve` (or start the TUI, which lazy-spawns one) and leave it running with at least one agent under it, so there is something to see.
2. Note its attach socket path. The daemon logs it at startup as `Attach protocol listening on <path>`, and it is typically `/run/user/<uid>/dot-agent-deck-attach.sock` on Linux. You do not have to get this now — step 6 discovers it.

On the machine running the desktop app:

3. Make sure plain `ssh <host>` works from a terminal first, non-interactively. The tunnel forces `BatchMode=yes`, so anything that would prompt — an unknown host key, a locked key with no agent — fails instead of asking. If the host key is not yet in your `known_hosts`, this is the step that puts it there.
4. Open **Settings → Decks**, press **Add a deck**, and fill in the host. Leave `User` empty if your `~/.ssh/config` already decides it; leave `Key file` empty if your key is in an agent; set `Jump host` to a `Host` block *name* if you reach the machine through a bastion.
5. Press **Test connection**. Read the state it reports — the twelve of them are below — and fix what it names.
6. On a successful test the **Deck socket** field fills itself in from discovery. Leave it; it is now stored, so the next connection needs no probe.
7. Select the deck in the same panel, or from the **Deck selector** in the top bar of the deck and the agent overview. Selecting a deck detaches every live terminal session, ends the event subscription, and emits a fresh snapshot for the chosen deck — a tile left attached to a deck that is no longer selected is a tile whose keystrokes go to another machine's agent.
8. Or select **All Decks**, which watches this row *and* every other configured deck at once and merges them on the agent overview. The deck screen stays on one deck — All Decks resolves it to the local one — so the terminals do not move; [All Decks](#all-decks-the-fleet-and-the-one-screen-that-merges-it) is what that selection does and why the split is there. **Test connection** tests one deck, so it is disabled while `all` is selected and says why.

**What differs once you are on a remote deck**, none of it silent: **Stop deck** and **Replace deck** are disabled with a reason, and project and workflow actions gate on the daemon's advertised capability set and name what is missing. A build-stamp difference is not one of the differences any more — since [#801](https://github.com/vfarcic/dot-agent-deck/issues/801) it refuses no deck of [either kind](#compatibility-is-classified-from-the-contract-not-from-a-git-tag-801), where PRD #741 M8 had demoted it for remote decks alone.

### The twelve `Test connection` states

**Test connection** exercises the whole transport stack without any screen having to work first, which is what makes it the thing to reach for when a remote deck misbehaves. Every outcome is a **distinct named state** rather than one "failed" — `EndpointTestState` in [`desktop/src-tauri/src/endpoint_test.rs`](../../desktop/src-tauri/src/endpoint_test.rs) — and they are declared in the order the probe reaches them: what stops it before it leaves this machine, then the ssh hop, then the deck at the other end.

| state | what it means, and what to do |
| --- | --- |
| `unknown_deck` | The selection names a row this document no longer holds — removed by hand, or a token a newer build wrote. `SelectionFallback`'s own case, reported as itself. **It carries a second, opposite case since PRD #742 M6**: the selection is `all`, which is every deck at once rather than a deck to test, and the report says so and names no deck. The state is shared rather than split because a thirteenth variant would mean a thirteenth arm in the TypeScript union and a thirteenth row in every table that enumerates them, to distinguish two reports that differ only in one sentence — but note what that costs the reader: `unknown_deck` no longer means "your selection is not in force", since under All Decks it plainly is. |
| `ssh_unavailable` | No `ssh` program at any of the standard absolute locations. Nothing about the deck is wrong; this machine has no client. Install an OpenSSH client. |
| `no_remote_socket` | The row has no socket path and discovery could not produce one. Usually means the deck is not running over there, since discovery asks it. |
| `host_unreachable` | ssh could not reach the host at all: refused, unresolvable, no route, or timed out. |
| `host_key_unverified` | The host's key is not in this machine's `known_hosts`, or it changed. Carries the run-`ssh`-once remedy, built from the endpoint the tunnel actually uses so the bastion and the port survive — an earlier version built it from an unvalidated `SshTarget` and named a different endpoint than the one that failed. |
| `auth_failed` | ssh reached the host and the login was refused: no usable key, wrong user. |
| `transport_failed` | ssh authenticated and something else about the transport failed — a forward that would not bind, a config that would not write, a child that died saying something this build cannot name. |
| `deck_not_answering` | The ssh connection is up and nothing is listening on the remote socket path. Usually "Agent Deck is not running over there". |
| `handshake_refused` | A daemon answered and refused the `Hello` itself. |
| `protocol_refused` | A daemon answered and the wire versions disagree. Both numbers are in the report. **The one state no override can reach**: `PROTOCOL_VERSION` is exact equality, checked first, and is the hard floor for every deck kind. |
| `contract_differs` | The wire agreed and the two builds' **declared contract breaks** did not, so a compatibility break somebody wrote down sits between them ([#801](https://github.com/vfarcic/dot-agent-deck/issues/801)). Not ok, for either deck kind — the protocol agreed, so it is overridable on the connection screen, but an override is a judgement the user makes there and not something a green tick here should pre-empt. The build stamps are reported beside it and are not what was measured. |
| `reachable` | The deck answered and this build can talk to it. |

The report also carries the handshake's own numbers when it got that far — the server's protocol version, the daemon's build stamp, the live agent count — plus the remedy and the forwards disclosure above. In the browser fixture tier the panel renders the states without reaching anything, and says so: *"Open the packaged app to reach a deck."*

## The agent overview

PRD #745 adds the app's second screen: a fleet overview of every agent the app can see, with **no terminal anywhere on it**. It lives in `desktop/src/components/AgentOverview.tsx`, is reached from the **Overview** button in the left rail, and works in both fixture and live mode. It was *"every agent the daemon owns"* until PRD #742 made it every agent of **every observed deck** — one group per deck, merged into one screen; [All Decks](#all-decks-the-fleet-and-the-one-screen-that-merges-it) is what that means and why this is the screen that merges while the deck screen does not. It renders *instead of* the deck rather than as an overlay sheet, so the view state lives in `App` above `ControlDeck` as a discriminated union (`DeckView`) — a union from the start, even at the two values it carried for its first two PRDs, so later destinations arrive as added variants rather than as a refactor of a boolean. That is what happened: PRD #1105 added a third variant for [one agent's pane](#the-agent-pane-overlay), which is the only variant that does *not* replace the mounted screen. Since issue #1196 the overview **is** the landing screen: `DeckShell`'s `initialView` defaults to it and its entry leads the rail. No view is persisted, so every launch opens there rather than on the last screen used. Since issue #1198 the deck and its four panels (Projects, Prompts, Workflows, Agent Profiles) are behind the `experimental` flag, off by default: with it off the overview and Settings are the whole rail, [the agent pane](#the-agent-pane-overlay) is how a terminal is seen, and a view that names the deck — an `initialView`, a pane opened over it — is shown as the overview instead. The flag's desktop mechanics — where it is read from, and why not from a project — are in [the experimental flag's desktop section](experimental-flag.md#the-desktop-app). It now has one destination rather than none, and #745's other two iteration-3 parts (the group view, and the promotion itself) are untaken (see [Current milestone limits](#current-milestone-limits)).

### What it shows, and why not more

**Four columns by default, nine available, and the operator chooses.** Out of the box: the agent's **name**, its **status**, its **working directory** and its **uptime** — who it is, whether it is healthy, where it is working, and how long it has been at it. The other five — last activity, CLI, active tool, tool count, last prompt — are one click away in the **Columns** picker in the top bar, and the choice is remembered. See [Columns, and who chooses them](#columns-and-who-chooses-them). Every one of the nine is a value the daemon genuinely reports, and there is no `Unavailable` anywhere on the screen. Model, tokens, cost, context window, git branch and attempt count are **absent rather than shown as unavailable**, because none of them exists in daemon state at all — not on the wire, not in `RunningAgent` (`src/agent_pty.rs`), not in `SessionState` (`src/state.rs`). Inventing a source of truth for them is [PRD #633](https://github.com/vfarcic/dot-agent-deck/issues/633)'s discovery work, not this screen's. Session *duration* was on that list too and is no longer: the uptime column ships one, from a source the daemon genuinely holds — see [Uptime](#uptime).

That decision is enforced by the compiler rather than by review. The screen renders from `OverviewAgent`, a `Pick<>` projection of `AgentSession`, and never from `AgentSession` itself — so reaching for `model`, `cost`, `tokens`, `contextPercent`, `worktree`, `attempt` or `duration` on this screen is a **compile error** rather than something to remember. Two fields are widened past the raw `Pick<>` for the same reason a `Pick<>` exists: it closes dishonest field *names* but cannot close a dishonest *sentinel* inside an allowed one. `cwd` is optional here so a directory the daemon did not report travels as absence the whole way, and `writeLease`'s `"unknown"` sentinel is reversed to absent at this boundary. A cell with nothing to say renders blank — not a dash, and not a placeholder.

Sanitisation is a property of the screen rather than of individual cells: every daemon-supplied string passes through `desktop/src/lib/displayText.ts` before React sees it — rendered text, `title` attributes, and the copies behind `data-*`, DOM ids, IDREFs and React keys — while grouping, sorting and the `(daemonId, agentId)` identity keep the raw values. An agent whose reported display name consists entirely of invisible characters renders as `unnamed agent <id>` rather than as a blank cell, on the one screen whose whole job is telling agents apart.

Two fabricated values on the **deck** were withdrawn by the same principle, since a fabricated value is worse than an absent one — it looks like data. Every tile printed `ATT 01` and the top bar printed `ATTEMPT 01`, both read from a hardcoded `1`, and no daemon tracks a retry count anywhere; the branch line printed a literal `Unavailable` where a branch name belongs, and no daemon tracks a per-agent branch either. In live mode the attempt readouts now show the deck's established em dash and the branch chip is simply absent until something reports a branch. The deterministic fixture keeps its own attempt counts and branch, which are legitimately fixture data.

### Columns, and who chooses them

The picker lives in the overview's top bar, so choosing columns never leaves the screen. It offers exactly nine options, and **it cannot offer a tenth by mistake**: the option list is `COLUMN_FIELDS` in `desktop/src/components/AgentOverview.tsx`, whose ids are declared `satisfies readonly (keyof OverviewAgent)[]`, so a column named `model`, `cost`, `tokens`, `contextPercent`, `worktree`, `attempt` or `duration` is a **compile error** rather than something review has to catch. The picker inherits the screen's honesty guarantee from the same `Pick<>` that enforces it everywhere else, instead of restating it.

**There is deliberately no deck column, and it is still nine under a fleet.** Deck is a **grouping** on this screen rather than a cell — one `daemon-group` section per observed deck, which was already the outer unit — so a column repeating the name in every row of a card that is titled with it would say the same thing twice. `daemonId` *is* an `OverviewAgent` field and a tenth entry naming it would be type-legal, so this is a choice rather than a limit; if it is ever wanted it goes through the existing picker and needs no new mechanism.

**The CLI column names a binary, not an identifier, and the daemon names it.** It shows the *command* that launches the agent (`claude`, `opencode`, `pi`, `codex`, `devin`) — not the serialised `AgentType`, which is the wire identity and reads `claude_code` and `open_code`, neither of them a name anybody types. Since issue [#856](https://github.com/vfarcic/dot-agent-deck/issues/856) the resolution happens **daemon-side**: `attach_cli_names` stamps `AgentRecord.cli_name` at the `ListAgents` wire boundary from `AgentSpec::default_command` in `src/agent_registry.rs`, reading the identity the reply itself reports, and the desktop copies it through to `DesktopAgent.cli_name` without consulting its own copy of that table. Which binary a running agent is, is a fact about the daemon's world — it forked the process. Where the **daemon** names no binary (an agent type whose spec has no default command, `AgentType::None` — which carries `#[serde(other)]` and so absorbs a type from a *newer* peer — or a daemon predating the field) the field is absent and **the cell is empty**. No generic word and no local lookup: a word reads as a fact about the agent, an empty cell reads as "the deck did not say", and a fallback to the client's own registry would reinstate exactly the divergence #856 closed. Same disposition as the uptime and activity columns.

**The name column is permanent.** Its checkbox is checked and disabled, and `orderedColumns` puts it back whatever a stored value asks for — a disabled checkbox governs the UI, not a `localStorage` key an older build wrote. A row with no name is not a shorter row, it is an anonymous one, on the screen whose whole job is telling agents apart.

**There is a way back.** A **Restore defaults** entry at the foot of the menu puts the four back; without it the only route out of a set somebody unticked their way into was remembering which four the screen opened on. It persists through exactly the path every other change does — the selection goes up to the screen, whose effect writes it — so there is no second way for a choice to be saved.

**The menu dismisses on an outside click, and three details are the whole thing.** It listens on `pointerdown` rather than `click`: a click fires *after* focus has already moved, so a menu closing on it closes after whatever was clicked took focus, which reads as the menu lagging behind the user. Anything inside the picker's root element is ignored, and the trigger button lives inside that root — were it outside, its own pointer-down would close the menu and its click would toggle it straight back open, so the button would appear not to work at all. And the listener is bound only while the menu is open and removed when it closes, not merely on unmount, because `open` is in the effect's dependency list. `Escape` still works and was previously the only way out.

**Status is ONE column, and it used to be two.** There was a coloured mark in the first track and a textual State column in the third, both rendered from `agent.status` and neither saying anything the other did not. Two picker entries for one field would have been a question with no right answer, so the mark and the label now share one cell: the colour is still what you scan, the word is still what you read, and removing the column removes both. Row-level status signalling is unaffected either way — `data-status` on the `<tr>` is what tints a failed row, and it is not a column.

**The choice is remembered, per mode.** It is written to `dot-agent-deck.desktop.overview-columns.v1.<mode>` through `modeScopedKey`, for the reason every persisted key in this app is scoped that way: a fixture visit must not hand live mode a layout, and vice versa. `readStoredColumns` is where every way a stored value can be unusable turns back into something renderable, and each case is about a value an **older build** wrote rather than about today:

- **Absent, unparseable, or the wrong shape** — a bare string, a number, an object with no `columns` array — takes the defaults. A `SyntaxError` on mount would take the whole screen down.
- **A column that no longer exists is dropped**, not carried through. A retired or renamed id would otherwise render as a `<th>` with no cell under it and one dead grid track down every card — a fault that looks like a layout bug and is really a migration one.
- **Nothing recognisable left** — every stored id unknown, or an empty array — takes the defaults rather than collapsing the screen to the single permanent column. A user cannot reach that state by unticking, because the permanent column has no checkbox to untick.

Whatever survives is written straight back, so the next visit reads a value this build understands.

**No column is hidden by viewport any more.** Two media queries used to drop uptime, CLI and the working directory below 1180px and the active tool, tool count and prompt below 680px, by `nth-child` index. Once the operator picks the columns, hiding one by window width fights them silently — and the index-sensitivity is what made adding a column a five-rule renumbering across two queries plus three hardcoded templates. The narrow case is a **horizontal scroll of the whole table region** instead: the legend and every group card sit inside one `.overview-table-region`, which is the load-bearing part. All the cards share one `grid-template-columns` — generated from the selection and published as the `--overview-grid` custom property on the region's track — and that shared template is what makes the fleet read as a single table across card boundaries. Scrolling cards individually would let two of them sit at different horizontal offsets and the columns would stop lining up, precisely when the chosen set is wide enough to overflow. `min-width: min-content` on the track is what produces a scrollbar at all: every flexible column's track carries a fixed px minimum (`minmax(150px, 1.3fr)`, not `minmax(0, 1.3fr)`), so the grid's min-content width is the sum of those minimums — a fixed number — rather than zero (tracks crushed to nothing, no overflow) or the width of the longest prompt on screen.

### Grouping

The daemon's own `TabMembership` is the grouping key, so the overview groups agents exactly as the deck's tabs already do: one group per orchestration, one per mode name, and one standalone bucket for dashboard panes. **Standalone leads, then orchestrations, then modes.** That order is TUI parity rather than aesthetics — the TUI's dashboard tab is always first, so an overview that buried the same agents at the bottom would describe a different deck than the one running beside it. Orchestrations and modes follow in first-appearance order; roles inside an orchestration are sorted by `roleIndex`, and the start role carries a `COORDINATOR` badge.

The outermost unit is a **daemon group**, not an agent group. With one deck it renders as minimal chrome: a connection lamp, the words `Local deck`, and the per-status pips; under **All Decks** the overview maps over the observed decks and renders one of these per deck, a remote one named by its address rather than by `Local deck`. **No socket filename is on screen.** A shortened label used to sit under the name, and its stated purpose was keeping a uid or a username out of screenshots — but on the default socket of the day that label read `dot-agent-deck-attach-501.sock`, so it printed the very uid it existed to hide and told the reader nothing they could act on either way. (Issue #1121 moved the uid up into the directory — `dot-agent-deck-501/attach.sock` — so the same label would now read a bare `attach.sock`: no leak, and no information at all. The conclusion is unchanged either way.) The full path is genuinely diagnostic, so it lives on the name's own `title`, where it costs no layout; `data-daemon-id` on the section carries the sanitised, bounded identity for tests and a future drill-in — since #742 M5 that attribute is `connection.deckId` rather than the label, so a test asserting which section it found cannot be fooled by two decks that describe identically. The two build stamps disclose themselves through that same hover when they differ (issue #801) — they used to hang off the connection message, which for a healthy connection no longer renders, and on the daemon's own name they are more discoverable than they were, because a reader hovers a thing they can see. Agents are keyed by the composite `(daemonId, agentId)` from day one, because agent ids are per-daemon monotonic integers starting at 1 and two daemons both mint `"1"`.

**Both decisions were made to stop [#742](https://github.com/vfarcic/dot-agent-deck/issues/742) being a rewrite, and the prediction held — with one correction that is worth the paragraph.** A second deck did become a sibling group and changed no inner component: M4 lifted `daemon-group` from one hardcoded section to a `.map` over the observed decks and `groupAgents` was untouched. What the composite key did **not** buy is protection against the `daemonId` half itself colliding, which it did, because that half was derived from `socketPath` — see [`socketPath` is a label; `deckId` is the key](#socketpath-is-a-label-deckid-is-the-key). The lesson is narrow and general: a composite key is only as distinguishing as its least distinguishing component.

**The connection message renders only when it says something the lamp does not.** The lamp is `connection-<status>`; the message beside it, for a healthy connection, was literally `Deck responding` — two renderings of one bit. It is suppressed for that case and for no other: the disconnected and incompatible explanations stay, and so does the build-mismatch caveat that must remain visible for the whole session after **Connect anyway**. That last one is why the test is `!connected || buildStampMismatchOnly`, the same flag Connect anyway is gated on, and not a naive "hide when connected" — the bypassed-mismatch state *is* `connected`, and hiding its message would silently undo #801's guarantee.

### Last activity

The **Last activity** column is the one thing on the screen that needed a daemon-side change. `last_activity_ms` was added to `SessionSnapshot` as an additive optional field tagged `#[serde(default, skip_serializing_if = "Option::is_none")]`, which `src/daemon_protocol.rs`'s own policy names as an explicit do-not-bump case, so it moved `PROTOCOL_VERSION` by nothing and older and newer peers interoperate in both directions — a new app against an older daemon simply shows an empty column. (The constant has moved since — to 9 for the unrelated request variants PRD #819 added, and to 10 for issue #1049's `StopDaemon`. It was 8 when this column landed and the field is additive either way.) It crosses the wire as epoch milliseconds rather than a formatted string, so the relative wording stays the webview's decision, and it carries its unit in its name because a bare integer invites the seconds-versus-milliseconds mistake that turns every reading into fifty-seven years.

It is on the screen where a duration built from `SessionState.started_at` is not, and the line between them is honesty rather than taste. `started_at` is invented as `Utc::now()` on hydration, so a duration built on it resets under a restarted daemon and silently lies about long-running work. (The screen *does* carry a duration now — see [Uptime](#uptime) — from a source the daemon genuinely holds. The narrow claim here, about `started_at`, still stands; it is only ever that source this section is rejecting.) `last_activity` is a high-water mark of observed event timestamps, advanced only when a newer frame arrives, so an agent quiet for an hour snapshots as quiet for an hour. And the daemon-restart case resolves to **absence** rather than to a lie, for free: the daemon persists no `AppState`, so a restarted daemon has no sessions at all, `AgentRecord.live` is `None`, the field never reaches the wire, and every cell renders empty. A duration had no absent state to fall back to, because `started_at` is always populated with *something*.

`displayActivity` in `desktop/src/lib/displayText.ts` renders one unit, largest that fits, floored — `just now`, `34m ago`, `2h ago`, `3d ago` — with the exact UTC instant on hover. It renders **nothing at all**, with no hover, for every value it cannot express honestly: absent, non-finite, outside `Date`'s ±100,000,000-day range, or more than a minute in the future. That last one is the clock-skew rule. The instant is stamped by whichever hook process emitted the event rather than by the daemon, so a small positive skew is the ordinary case and one minute of it reads `just now`; beyond that the value is deliberately *not* rewritten into `just now`, because a cheerful `just now` for a stamp ten years out is the same fabrication as a `started_at`-derived duration would be, and a negative `-60m ago` is merely the more obvious bug of the two. Note the desktop is **stricter than the TUI** here, whose `format_elapsed` clamps unconditionally to `0s`; the divergence is intentional and one-directional, since the TUI never renders a negative either.

The watcher's cached path had to be taught to keep the value, and until issue #804 it did not. `AgentView::install` re-seeds its fold with `AppState::seed_hydrated_session`, and that function used to stamp every seeded session with the moment it ran. `records()` then reported each paned agent's live state from the fold, so the snapshots the watcher pushed showed when the last full reply landed, which was at most five seconds earlier (`RECONCILE_INTERVAL`), and read `just now` however long the agent had been quiet. The same function is the TUI's reconnect hydration, where the symptom was a card's `Last:` readout resetting to `0s`. Since #804 the seed overlays the snapshot's instant, **newer-only**: it is taken only when it is strictly newer than every session the state already holds for that pane or agent, and no later than the moment of seeding. The condition is there because `last_activity` is also the ordering evidence `supersedes_generation` weighs and the key every newest-wins pick selects on. The second half costs the desktop something, and it is stated here rather than hidden. A stamp in the future keeps the seed's own moment, so on the watcher's snapshots it still reads `just now`, where the direct-fetch path renders nothing for one more than a minute ahead (the clock-skew rule above). That was the watcher path's behaviour for every agent before #804. The reason is ordering: a client state has no registry to retire a disowned generation with, so an imported future stamp would pin the card against every successor. The fold is built fresh from each reply and seeds only paned records, one per registry id. So for the desktop the condition can fail only where two records in one reply name the same pane, and everywhere else the column carries the daemon's value. The rule, and what each clause buys, is on `seed_hydrated_session`'s doc comment.

### Uptime

The **Uptime** column is the second daemon-side change, and it is the duration this screen used to refuse. The refusal was aimed at `SessionState.started_at`, and that aim was right but the reason given for it — that `started_at` is invented as `now` on hydration — understated the problem. `started_at` is **event-derived**: a session exists only once a hook event has arrived, so an agent that has never emitted one has no start instant at all, and that is exactly the agent whose uptime a reader most wants.

The source that works is **when the daemon forked the process**. `AgentPtyRegistry::spawn_agent` stamps `RunningAgent.spawned_at` immediately after `spawn()` returns and nothing ever rewrites it; it reaches the wire as `AgentRecord.spawned_at_ms`, an additive optional field on the same do-not-bump basis as `last_activity_ms`, so it moved `PROTOCOL_VERSION` by nothing. It is an **observation rather than an inference** — signal-independent, present for an agent that has never reported anything, and something the daemon definitionally knows. It is also never invented: `spawn_agent` is the only site that writes a value, so a record that arrives any other way (an older daemon's id-only `ListAgents` reply, the synthetic test seam) carries `None` and every consumer renders nothing. There is no `Utc::now()` fallback anywhere on the path.

**What the number means follows from where it comes from, with no flag needed.** A `clear = true` delegate respawns its worker by removing the old registry record outright and spawning a fresh one, so a restarted worker reports the age of its **current iteration**; a role nobody has restarted — an orchestrator, typically — keeps its original record and reports its **whole lifetime**. And because `agent_records()` filters entries whose child has exited, a spawn instant never outlives the process it describes and ticks up as a phantom uptime.

`displayUptime` in `desktop/src/lib/displayText.ts` renders it: the same buckets `displayActivity` uses, worded as a span rather than as a moment — `<1m`, `34m`, `2h`, `3d`, with the exact UTC spawn instant on hover. No `ago`, because the interval named is still running. The two functions share **one** clock-skew and usability guard (`relativeTo`) and fork only the vocabulary, so the rule a reader learns for one cell holds for the other: absent, non-finite, outside `Date`'s range or more than a minute in the future all render as nothing at all. Sharing the guard is deliberate — the three refusals *are* the policy, and a second copy of them is a second thing to keep true.

### The attach model

This is the least obvious part of the desktop client and the easiest to break, so it is written out in full.

**"Shows no output" and "opens no PTY" are separate properties, and only the first comes free.** Attaching a PTY was never tied to rendering one. `attachAgents` used to fire from `connect()`, from every `desktop://snapshot` event, and from the `start_daemon` action, attaching every agent in the snapshot regardless of what was on screen — filtered on an `attached` set rather than on anything mounted, and `TerminalViewport` triggered nothing at all. Each attach opens a real daemon `AttachStream` socket, and an `AttachStream` replays the full scrollback before it starts streaming, so a nine-agent fleet cost nine sockets and nine replays behind a screen displaying none of them.

Attach is now driven by one declaration, `DeckBridge.setShownTerminals(targets)` — real on `TauriDeckBridge`, a genuine no-op on `FixtureDeckBridge`, which owns no PTYs. A target is the composite `{ deckId, agentId }`; see [the attach model has a deck dimension](#the-attach-model-has-a-deck-dimension) below for why it is not a bare id. It is the only attach trigger left. Its semantics, in the order they matter:

- **Shown terminals are always attached, and are never capped.** This is the constraint that shaped everything else: the deck mounts a terminal on every tile, so a bound applied over everything *attached* would kill six of nine visible panes the moment a nine-agent fleet is displayed.
- **Leaving a terminal does not detach it.** It moves into a bounded **warm** set, still attached, so bouncing between the handful of agents you are actually working with costs no scrollback replay.
- **The warm set alone is bounded**, by `MAX_WARM_TERMINALS` (`3`, exported from `desktop/src/lib/bridge.ts`), least-recently-left evicted first. Eviction is a full client-side teardown before the daemon is told: every map keyed by agent id — sessions, terminal channels, pending attachments, pending resizes, resize frames — is cleared, and only then does `desktop_terminal_detach` go out. A surviving channel entry would let a dead attach keep delivering output, and a surviving resize entry would push a size computed for the old pane at the next session.
- **An empty shown set flushes warm to zero**, not down to the bound. That is what makes the overview's "no terminals attached" true when you arrive from a nine-tile deck, rather than true only on a cold start.

**`setShownTerminals` is declarative, and must be called once per render commit with the whole set — never once per tile.** Nine single-id calls would leave eight of the nine warm and evict five of them, which is the same broken deck the bound exists to avoid. The deck does this in `desktop/src/App.tsx`, deriving the shown ids from the tiles whose tab is `"terminal"` (repeating `AgentTile`'s `?? "terminal"` default exactly) and keying one effect on those ids joined with a newline. That joined string is a dependency key and nothing else — never split it back apart, since a raw agent id containing a newline would come back out as two shown agents. The overview declares the empty set the same way — from `DeckShell` since PRD #1105 M4, not from `AgentOverview`, which declares nothing for itself any more. The move is why: an agent pane sits *over* a base screen rather than instead of one, so an overview that declared `[]` for itself while a pane above it needed `[agentId]` would be two declarations in one commit, which is the thing this rule forbids. [The attach model with the pane open](#the-attach-model-with-the-pane-open) has the rest.

What the overview does **not** claim is that every socket a previous screen opened is already gone by the time it renders. The declaration is fire-and-forget, and an attach still in flight is cancelled by *marking* rather than by awaiting — one slow attach must not freeze every later terminal switch — so its daemon-side tear-down lands shortly afterwards. The screen used to say exactly that in a footnote rather than claim the stronger thing; the footnote is gone (the app does not annotate its own design), so the precise claim lives here and in `AgentOverview`'s docstring.

The property is pinned at the bridge level rather than by inspection. `desktop/src/lib/bridge.test.ts` drives `TauriDeckBridge` directly — the live bridge, the only one that owns a PTY at all — and asserts that a nine-agent snapshot attaches zero terminals while nothing is shown, and that `connect()` alone attaches nothing. Note the asymmetry when you touch this code: deleting the deck's effect fails no bridge test, because the bridge is behaving correctly; it silently leaves the deck with no attached terminals at all.

#### The attach model has a deck dimension

**It did not, until the agent pane learnt to attach across decks.** `setShownTerminals` took bare agent ids, every map in `TauriDeckBridge` was keyed by bare agent id, and `desktop_terminal_attach` took an `agent_id` and nothing else — while agent ids are per-daemon monotonic integers, so two decks both mint `"1"`. That held for one reason and one only: `terminal::attach` resolved its daemon through `trusted_daemon()`, the process-global `selected_endpoint()`, so every attach this app issued went to whichever deck was selected at the time. A bare key was adequate because of the **single-deck screen**, not because of anything in the maps — they had no way to tell two decks' `"1"` apart and nothing asked them to.

**PRD #1105 is what asks.** The overview merges every observed deck, and the product decision is that any agent it lists opens as a working pane with a live terminal — the desktop app's whole reason to exist over the TUI (PRD [#802](https://github.com/vfarcic/dot-agent-deck/issues/802)). So a deck-screen tile on the selected deck and an overview pane on another deck are attached at the same time, both possibly called `planner`. Four things changed together, and none of them is optional on its own:

- **`setShownTerminals` takes `{ deckId, agentId }` targets**, and every per-agent map in `TauriDeckBridge` is keyed by `agentKey(deckId, agentId)` (`desktop/src/lib/agentKey.ts`) — `shown`, `warm`, `attached`, `sessions`, channels, pending attachments, pending resizes, applied geometry, the attach guard. The key is NUL-joined and is **never split back apart**; where the pair is needed again it travels beside the key as a value.
- **`desktop_terminal_attach` takes a `deckId`**, and `terminal::attach` resolves that deck's link through `DaemonLinks::trusted` instead of `trusted_daemon()`. The id is matched against `observed_decks()` and refused if it names nothing there (`terminal::endpoint_for_deck`), so a value from the webview can never become an address.
- **A `TerminalSession` stores the `Endpoint` it was attached over**, which is what makes the other three verbs deck-correct without a parameter of their own. Write and detach already resolved by session id; `resize` did not, and now takes its daemon from the session's endpoint rather than from the applied selection.
- **The session registry evicts by `(deck, agent)`.** `insert_unique_session` compared agent ids alone, so attaching build-box's `planner` closed the live local `planner` a user was watching.

**Read "adequate" as having been scoped to the maps torn down with the selection**, which is the correction the first security audit made and which the above generalises. "Every attach goes to the selected deck" protects a map whose entries die when the selection moves; it protects nothing about a map that **survives** one — and the runtime's retained PTY buffers, recorded send verdicts and applied geometry all did. Those three are keyed by `(deckId, agentId)` too (see [The composite identity, all the way down](#the-composite-identity-all-the-way-down)).

#### What a selection move now tears down, which is not "everything"

`retarget_selection` called `terminal::detach_all` whenever the selected deck moved. That was right while attach was process-global: every session belonged to the selected deck by construction.

**It is now wrong in both directions, so the rule changed rather than being relaxed.** A session names its own deck and resolves its own link, so a selection move says nothing about it — and tearing one down would close a pane the user deliberately opened on another machine, which is the feature. Meanwhile a deck can *leave the observed set* without the selected deck moving at all (removing a row while `All` is selected), and the old `if moved` gate could not see that case: its transport was retained away and its watcher ended under a session still holding a stream.

So the rule is **`terminal::detach_decks_outside(state, &observed)`, run unconditionally** — the exact sibling of `EndpointTunnels::retain` and `DesktopState::retain_watchers`, one line earlier so a DETACH frame still has a transport to travel over. What ends a session is now one of three things, and a selection change is none of them:

| trigger | what it detaches |
|---|---|
| a deck leaves the observed set | that deck's sessions (`detach_decks_outside`, from `retarget_selection`) |
| Stop deck / Replace deck | that deck's sessions (`detach_deck`) — these refuse anything but the local deck, so tearing down a remote pane would be collateral |
| app exit | everything (`detach_all`, now its only caller) |

Both directions are pinned at the **call site** rather than only on the helper, which matters: `a_selection_move_keeps_a_still_observed_decks_terminal` and `a_deck_leaving_the_fleet_loses_its_terminal_without_the_selection_moving` (`desktop/src-tauri/src/terminal.rs`) drive `retarget_selection` itself, and restoring `if moved { detach_all }` turns both red. A test that drove only the helper stayed green against that restoration, which is the "passes either way" trap.

### The connection states

The overview owns six states, and every one of them is reachable in the fixture without a daemon. **Since PRD #742 each is a state of *a deck* rather than of the screen** — one per group, so a fleet can hold several at once, and the table below reads as what a single deck's group says. `?fixture=1&state=fleet` is the scenario where they are on screen together: four decks, two connected, one that stopped answering and one that has not reported yet.

It was four until PRD #742, and the two it added are both about a deck that is in the fleet and has nothing to show, which the four could only express as a failure. Both keep the deck in the header's **denominator** and out of its numerator; neither offers a button, because in neither is there anything for the reader to press.

| State | What the screen says | Fixture URL |
|---|---|---|
| Connected | The fleet, grouped. `state=crowded` is fifteen agents across two orchestrations, a mode bucket and standalone panes — a four-agent fixture cannot answer a question about many agents | `?fixture=1&state=crowded`, or `?fixture=1` for the default four |
| Connected, zero agents | "No agents are running yet" — the first-run screen, stated explicitly as what a fresh install looks like rather than as a failure, with what to do next | `?fixture=1&state=empty` |
| Disconnected | Nothing can be said about the fleet until a daemon answers, so the list is blank rather than stale | `?fixture=1&state=disconnected` |
| Incompatible | A daemon answered the handshake but this build cannot read its agent list, with its reported running-agent count when it gave one | `?fixture=1&state=error` |
| Not configured (M12) | A configured deck whose socket path `Test connection` has not filled in — nothing was asked of it and nothing failed, so it gets the settings panel's own words rather than the disconnected note's | the third deck of `?fixture=1&state=fleet` is disconnected, not this; reachable live by adding a row and leaving `socket` blank |
| Waiting (M14) | A deck the app has been told exists and has not heard from yet — its watcher is acquiring a tunnel, handshaking and reading an agent list. Stated as what it is rather than as a failure | the fourth deck of `?fixture=1&state=fleet` |

A seventh, `loading`, is the initial seed and is not selectable from the URL. It and **Waiting** share a `status` and are told apart by `connection.pending`: the seed is the app having no deck, and Waiting is a deck having no snapshot.

**Waiting is bounded rather than hopeful, and that is the half worth knowing.** The three replies that stand between a deck joining the fleet and its first snapshot — the handshake, the event subscription and the `ListAgents` — each waited indefinitely until M14, so a deck that accepted a connection and then went silent produced no failure any caller could observe. Each is now bounded at `DECK_REPLY_TIMEOUT` (15 s), the `ssh` forward before them already was at `FORWARD_READY_TIMEOUT` (30 s), and a watcher whose *task* has ended no longer holds its deck's claim — so Reconnect starts a replacement instead of being silently refused.

The header instruments read `—` rather than `0` while **no deck is answering**. This is not decoration: a reconnect failure replaces the connection but *keeps* the previous snapshot's agents, and both the `disconnected` and `error` fixtures ship the default four, so deriving the counts unconditionally printed `AGENTS 4 · GROUPS 1` above a body correctly saying the fleet could not be read. "No agents" and "we cannot see the agents" are different statements and only one of them is true there. With one deck that test is exactly the old `connection.status === "connected"`; with N it is "at least one deck answered", because one reachable deck makes the instruments a true statement about a knowable part of the fleet — and `DECKS n/N` beside them is what says how much of it, per [the aggregate](#the-aggregate-is-honest-about-what-it-could-not-see).

The overview offers no deck lifecycle action — Start deck, Replace deck and Stop all stay on the deck screen, and the incompatible note says so. The one exception is **Connect anyway**, which is not a lifecycle action: it changes this app's own classification of a handshake it already performed, so it belongs on the screen that is refusing to show you the fleet. It appears on the overview's incompatible note under exactly the conditions described in [Attaching across a contract difference](#attaching-across-a-contract-difference-development-only) — live mode, and a refusal the desktop crate reports as overridable.

## The agent pane overlay

PRD #1105 gives the app a third rendering of an agent while deliberately refusing it a third *component*. A maximise control on a deck tile, and another on an overview row, open **that agent's pane at window size over the screen it was opened from**; `Esc`, or the close control in the pane's own header, returns you there. It lives in `desktop/src/App.tsx` — `AgentPaneFrame` positions it and `OverviewAgentPane` supplies the overview's half — and works in both fixture and live mode, so `?fixture=1&state=connected` reaches it with no daemon.

**Back is a field on the view value, not a history stack.** `DeckView` (`desktop/src/types.ts`) grew a third variant, `{ kind: "agent"; deckId; agentId; from: "deck" | "overview" }`, and `from` is the only record of where closing goes — `closeAgent` is `setView({ kind: current.from })` and consults nothing else. The first two variants replace the mounted screen and `DeckShell`'s own doc comment says so; this one does not, and that is the structural constraint the rest of the feature hangs off. An overlay that unmounted the screen beneath it would re-declare that screen's shown terminal set, which is expensive in both directions — see [the attach model below](#the-attach-model-with-the-pane-open). So `from` names the base screen to keep mounted underneath, and `DeckShell` renders the base screen *with* the pane on top rather than instead of it.

Carrying the origin in the value rather than popping it off a stack also makes an agent view that was never navigated *to* — the app's `initialView`, a future deep link — close to a real screen instead of to nothing. This is the mechanism #745's own open question 2 asked for (two paths to an agent view, different back behaviour each); when its group view arrives it adds a third `from` and nothing else here changes.

**`deckId` travels with it because an agent id does not identify an agent.** Agent ids are per-daemon monotonic integers, the overview merges every observed deck, and two decks both mint `"1"`. `OverviewAgentPane` therefore resolves its agent *and* its evidence out of the fleet entry `view.deckId` names, rather than out of the selected deck's snapshot where a bare-id lookup finds a different agent wearing the same id.

**And that had to reach further than the lookup**, which is [the security audit's finding](#the-composite-identity-all-the-way-down) below. The runtime's three remaining per-agent maps — the terminal feed's retained buffers, the recorded send verdicts and the applied geometry — were keyed by bare id and were deliberately left that way, on the reasoning that nothing could put a second deck's agent in front of them. That reasoning was wrong for a reason older than this feature: the ordinary `DeckSelector` moves the selection, and the next deck's same-id agent then read the previous deck's state. They are now keyed by `agentKey(deckId, agentId)` (`desktop/src/lib/agentKey.ts`) end to end, and the four lifetime paths a second audit round found above that keying are closed too — [the same section](#the-four-findings-it-had-to-reach) enumerates them.

### One component at two presentations

The grid tile and the pane are the **same** `AgentTile`, rendered at `presentation="tile"` or `presentation="overlay"` (`AgentPanePresentation`, `desktop/src/types.ts`). Every difference between the two is derived from that one prop inside the component, and the prop is emitted as `data-presentation` on the tile's root element, which is the seam the stylesheet keys off.

The outcome being prevented is an `AgentTileLarge`. An agent pane in this app is not a small widget — five panel tabs, evidence, rename, status, an embedded `TerminalViewport`, the Reader launcher — and `OutputReader` is already a second rendering of the same agent's output. A third would drift from the other two one bug fix at a time. The rule that keeps that from happening: **a difference that cannot be expressed as a prop means the difference is wrong**, not that a second component is needed. `OutputReader` is the counter-example that earns its separateness by being a different *projection* — resolved text, with no cursor, no colour, no alternate screen and no keyboard. "Bigger" is not a different projection.

**A test enforces it rather than a convention.** `renders the same AgentTile component from the deck and overview origins` (`desktop/src/AgentPaneOverlay.test.tsx`) pulls the real component through `vi.importActual`, wraps it in a mount-effect tracker, then starts directly in a pane from each origin and asserts the tracked component mounted for that agent both times. A parallel large-pane component that copied the markup would satisfy every DOM query in the file and never reach the tracker, so a fork fails the suite instead of merely being frowned upon. The tracker counts mounts rather than render-body calls, so ordinary rerenders cannot satisfy it either. Read the guarantee at its actual width: it covers the two paths the test drives, a deck-origin and an overview-origin pane. A fork introduced on some third path the test does not open is not caught by this, and the enumeration that keeps that honest is that `AgentPaneFrame` is the only thing in the tree that renders `AgentTile` at either presentation — `grep -rnE '<AgentTile\b' desktop/src` is one hit. Use the word boundary — without it the `Omit<AgentTileProps, …>` on the wrapper's own signature comes back as a second, which is a type and not a render.

What the prop covers, recorded on the prop's own doc comment in `AgentTile.tsx` as well as here:

- **All five panel tabs at both presentations.** The tab set is a property of the agent, not of the box. `tab` is controlled state owned by `App` and keyed by agent id, so the pane inherits whatever tab the tile was on and hands it back on close — which *is* the "panel state intact" commitment. A presentation that dropped tabs would have to coerce that shared state behind the user's back. What the presentation does change is the strip's *labels*: they are shown today by a `min-width: 1450px` media query, which asks how wide the **window** is when the question is how wide this pane is.
- **The same header, with the clamps relaxed.** Nothing is removed. Every header element identifies the agent, and identity matters *more* in the pane, where the grid position that used to tell you which tile you were looking at is gone. `.agent-assignment`'s two-line clamp exists for a half-width tile and is relaxed at overlay presentation.
- **The terminal's box, in CSS only.** `TerminalViewport` measures its host element with `FitAddon` under a `ResizeObserver`, so a bigger box reports a bigger geometry through the path that already exists — "enlarging really enlarges" needed no new mechanism and no measurement code. Overlay presentation drops `.agent-panel`'s fixed `height: 42vh` band for `flex: 1 1 auto`.
- **No Reader inside the pane.** `OutputReader` binds `Escape` on `window` and so does the pane. Two listeners on the same target both fire for one event — `stopPropagation` does nothing between them — so one `Escape` inside a Reader opened in the pane would dismiss both and land the user back on the deck. Hiding the launcher is not sufficient on its own: `readerOpen` is `AgentTile`-local state reset only when `agent.id` changes, so a user who has the Reader open on a tile and then opens that agent's pane carries it across. The **render** of `<OutputReader>` is gated on the presentation too.

  **That gate is necessary and was not sufficient, which the PR review caught.** It governs the tile being *promoted* and says nothing about the other tiles the pane is drawn over: those stay at `presentation="tile"` and keep any Reader they already had, mounted behind the scrim with its own `window` listener. Open the Reader on tile A, open the pane on tile B, press `Escape` once, and both closed. So there is a second gate — `panePresent`, a fact about the *screen* rather than about this box — and the two are deliberately separate props for that reason. It **dismisses** rather than hides: a Reader restored when the pane closed would answer the next `Escape` instead of the deck, which is the same surprise one key later. `dismisses a Reader open on another tile when a pane opens over it` is the guard.

Two things are deliberately **not** derived from `presentation`, and the line between them and the list above is worth keeping. `onOpen` and `onClose` are *capabilities* supplied by a parent that owns the view — closing acts on the view and `AgentTile` holds no view state — following the pattern `onRename` already used. And the prop cannot express that the tile and the pane share one xterm instance, which is the next section.

### One xterm per agent, and why the wrapper is always rendered

The required property is **exactly one live `TerminalViewport` per agent at any moment**. Two viewports for one agent means two WebGL contexts and two `ResizeObserver`s both writing a single per-agent coalesced resize entry, so the one viewer geometry the daemon holds would ping-pong between tile size and pane size every frame — an intermittent reflow rather than anything that reads as a wiring mistake. There is a second, independent reason in this codebase: `terminalRegistry` is a module-level `Map` keyed by bare agent id, so a second live viewport overwrites the first's registration and the first viewport's unmount then cleans up nothing.

That property is not something a prop can deliver, because it is a property of *where the element sits in the React tree*. React reconciles children by position, key and type, so a component rendered at a different place in the tree is an unmount and a remount — which tears down and rebuilds the xterm instance. So the tile's element is **promoted in place**: `AgentPaneFrame` wraps every tile in the grid, open or not, and flipping `open` changes only that wrapper's attributes and the tile's `presentation`. React keeps the same `AgentTile` fiber, the same `TerminalViewport` beneath it and therefore the same instance with its scrollback, selection and cursor. Closed, the wrapper is `display: contents`, so `.agent-tile` remains the grid item it has always been and the deck's layout is unchanged.

**Rendering the wrapper only when open would have been the bug.** That swaps a `div` in where an `AgentTile` was, which React treats as an unmount — a rebuilt xterm, a second WebGL context and a client-side transcript re-write on every open and every close. `promotes the deck tile in place, so the agent keeps one terminal across open and close` is what pins it, and it asserts against a mount effect rather than against the DOM: a tile that unmounted its viewport and mounted a fresh one in an overlay renders an identical-looking element and reads identically to every DOM query. The test asserts one build and zero disposes across the round trip, and that `getByTestId("terminal-planner")` is the same DOM node at all three points.

**The overview path is asymmetric on purpose.** The overview mounts no terminal — #745's commitment is untouched — so there is no tile there to promote, and the pane is a sibling of the screen rather than a promotion inside it. The one-viewport property still holds, because the screen underneath contributes none. An overview pane for an agent on a deck this app cannot reach mounts none either, by design rather than as a saving ([why](#an-agent-on-any-reachable-deck-opens-with-a-live-terminal)), so the required property is satisfied there with zero rather than one.

**The pane must override the tile's own responsive rules, and one of them is a trap.** `@media (max-width: 680px)` sets `.agent-panel, .agent-tabs { display: none }` — a reasonable thing to do to a half-width tile on a phone and the wrong thing entirely to do to a pane that *is* the window. A pane that inherited it would open full-screen with a header, an assignment, a footer and nothing where the terminal belongs — not the explicit no-terminal state [below](#an-agent-on-any-reachable-deck-opens-with-a-live-terminal), which is a different thing and says so: just the whole feature missing, with no tab strip either, rather than a layout blemish. jsdom evaluates no media query, so `desktop/e2e/agent-pane-narrow.spec.ts` asserts it at 400x780 in both browser engines instead. It drives the **overview** origin because that is the one reachable at that width — the deck's own `.agent-tile:not(.is-selected) { display: none }` hides every tile until one is selected, so there is no Open control on screen to press.

One specificity correction is written into the stylesheet at the rule it applies to, because it is easy to get wrong in the other direction: an attribute selector is `(0,1,0)`, exactly like a class, so `[data-presentation="overlay"] .agent-panel` is `(0,2,0)` and beats `.agent-panel` inside any media query — media queries add no specificity — but a bare `[data-presentation="overlay"]` does **not** beat `.agent-tile:not(.is-selected)`, since `:not()` carries its argument's specificity. The overlay's own root rule is therefore written `.agent-tile[data-presentation="overlay"]`.

### The attach model with the pane open

This builds directly on [the overview's attach model](#the-attach-model) above; `setShownTerminals` and `MAX_WARM_TERMINALS` are that section's.

**Over the deck, the pane changes no shown set and therefore costs nothing.** The deck's shown ids are derived from the tiles whose tab is `"terminal"`, and opening a pane changes neither `snapshot.agents` nor `tabs` — so the dependency key is unchanged and the open/close transition makes **no `setShownTerminals` call at all**. No attach, no detach, no eviction, no scrollback replay. (Read that as a claim about the transition, not about the interval: a snapshot that adds or retires an agent while the pane is up moves the key and declares, exactly as it would with no pane open. What the pane itself costs is nothing.) The steady-state cost of a pane over the deck is exactly today's deck cost, and the feature adds no sockets on this path. That is asserted rather than arranged: `does not redeclare the unchanged deck shown set while the pane opens or closes` records the initial four-agent declaration, clears it, and counts zero calls through an open and a close.

**Read that paragraph as the deck path's alone.** From the overview the pane costs **one** attach, whichever deck the agent is on, which is the minimum the feature can cost there: the overview's shown set is empty, so opening an agent goes from zero attached to one — exactly #745's "whatever is showing output is what attaches". The declaration names the agent's own deck, so the one attach goes to that deck's link and not to the selected one. Closing declares `[]` again, which flushes the warm set to zero, so the pane's terminal is detached on the way out. Where the agent's deck has **no live link** — disconnected, still reporting nothing, no address — it costs **none**: the declaration stays `[]` and the pane renders its no-terminal state instead ([below](#an-agent-on-any-reachable-deck-opens-with-a-live-terminal)). The no-flicker commitment belongs to the deck path and to nothing else.

**The arithmetic that makes unmounting the deck the wrong instinct**, since "the grid is hidden anyway, unmount it to save resources" is the intuitive optimisation and it is backwards. Unmounting `DeckSurface` invokes no cleanup declaration of its own — `useShownTerminals` has no cleanup — so unmount by itself leaves the bridge holding its previous shown set, stale; what costs something is whatever declares next, and there are two ways to get it wrong:

- **Declare `[agentId]` directly from nine shown ids.** The other eight move to the warm set, `8 - MAX_WARM_TERMINALS` = **five** are evicted immediately, and one shown plus three warm survive. Closing then re-attaches those five, and an attach is not cheap: the daemon replies OK and immediately follows with a single frame carrying the consistent scrollback snapshot before it starts streaming, over a socket opened per session. Five scrollback replays is precisely the flicker this design exists to avoid.
- **Declare `[]`,** which is what a base screen that declares nothing for itself produces. All nine move to warm and the overflow is `warm.size` rather than `warm.size - MAX_WARM_TERMINALS`, so it detaches **nine**, not five. A one-id pane after that costs one replay to open and eight more to restore the deck.

Both ways of unmounting the grid are expensive, which is what makes the keep-it-mounted decision stronger rather than weaker. There is no cheap way to do it.

**The shown set has one owner per mounted screen tree, and that is what M4 bought.** `setShownTerminals` must be called once per render commit with the whole set; before the pane existed that rule was kept by two independent effects agreeing not to be mounted at the same time — the deck's, and the overview's `[]` on mount. The pane broke the agreement, because it sits *over* a base screen rather than instead of one: the overview's `[]` and the pane's `[agentId]` are two declarations in one commit. Nothing about that fails loudly — depending on effect order the result is either a pane with a dead terminal or an overview that has quietly attached one PTY while its own doc comment says it attaches none.

So the declaration moved into `useShownTerminals` (`desktop/src/hooks/useShownTerminals.ts`), and there is one owner per tree: `DeckSurface` owns the deck's set, `DeckShell` owns the overview's. `AgentOverview` declares nothing for itself any more, and a comment stands where its effect was saying nothing there may declare a shown set again. The two trees are mutually exclusive, so exactly one owner is ever mounted.

**`undefined` is not the empty set, and collapsing them would be expensive.** A component that is mounted but is not the owner in its current state passes `undefined`, which makes **no call at all** — including on the commit that hands ownership over, where firing would be a second declaration racing the new owner's first. `[]` is a real declaration and a very expensive one, since it flushes the warm set to zero. The obvious `agentIds ?? []` would therefore detach every terminal on the very commit the new owner is about to declare them. The dependency key is `agentIds?.join("\n")`: `undefined` for "no declaration", `""` for "declare nothing shown".

#### The reachability condition, which is the one a future editor would remove

**An agent is declared shown only once its own deck is ANSWERING.** In `DeckShell` that reads as `paneDeck?.connection.status === "connected"`, where `paneDeck` is the fleet entry the pane's `deckId` names. It looks like defensive tidiness. It is a correctness condition.

**It used to ask whether the deck was SELECTED, and the change is the feature.** `terminal::attach` resolved its daemon through `trusted_daemon()` — whichever deck was linked at the instant the attach ran — and agent ids are per-daemon monotonic integers, so declaring `[agentId]` while another deck was in force did not attach early: it attached **that** deck's agent of the same id, on another machine, under the right name, with nothing on screen saying so. The fleet fixture has a `planner` on two decks, which is the ordinary case rather than a contrived one. The declaration now names the deck and the crate resolves that deck's link, so being selected has stopped being a precondition for having a terminal.

**What remains a precondition is being reachable**, and that is not a hypothetical branch. A deck can be disconnected, still waiting to report, configured with no address, or not observed at all — and there is no link to attach over in any of those. A pane there must say so rather than mount a viewport that will receive nothing. An agent on a deck this app is not observing falls here too, through `paneDeck` being `undefined`: a pane cannot promise an attach against a deck it cannot name.

**One expression, read twice, and that is deliberate.** `DeckShell`'s `paneDeckAttachable` decides both whether an attach may be declared and whether the pane shows a terminal or says why it has none, because those two must agree: a pane rendering a terminal that nothing attached to is a black rectangle the user reads as an agent gone quiet, and a pane explaining itself while an attach is live would be explaining a state it is not in. Two comparisons of the same values is how they would come apart. Note what is *not* folded in: `deckPaneRetargeted`, which closes a deck-origin pane, keeps its own comparison of the pane's deck against the selected one — the deck surface renders the selected deck and nothing else, so a deck-origin pane's claim stops being true of anything on screen. An overview-origin pane makes no such claim and is kept.

**It is necessary and it is not sufficient, and reading it as sufficient is the mistake the first security audit caught.** What it governs is the *attach* — when this app asks for a PTY. It says nothing about what a viewport reads out of the retained terminal state at mount, which happens before any attach at all; that is [the second blocker](#terminal-stream-state-is-invalidated-by-being-keyed-not-by-being-cleared) below, and it needed the state fixed rather than the declaration delayed.

### An agent on any reachable deck opens with a live terminal

The overview offers every observed deck's agents. **Every one of those rows carries the same open control, and the pane it opens has a live terminal whichever deck the agent is on.** `OverviewRow` renders the control whenever it can read `OpenAgentContext` — which `AgentOverview` is the only provider of, and always provides — and asks no question about which deck the agent is on. The rule is the one a reader can hold: *if an agent is on the screen, there is a way into its pane, and that pane works.* The guard is `offers an open control on every row, whichever deck the agent is on` (`desktop/src/components/AgentOverview.test.tsx`), which also presses the control on a non-selected deck's row and checks the navigation carries **that agent's own deck** into the view.

**This is the product decision the feature exists for, and it was reached the long way round.** An intermediate commit on this branch gated the *control* on the deck, leaving a listed agent with no way to open it — a dead row, made worse by the fact that nothing on a row tells a reader which deck they are pointing at. A later one restored the control and opened a pane with no terminal in it. The owner's decision closed both:

> The point of the overview page is to be able to see all agents of the selected deck or from all decks. It's like a control room… **It's a control plane from which you can access any agent and interact with it.** That makes the desktop app "special" when compared to the TUI. […] Now, if that results in us not being able to open a full agent from the overview page when all decks are selected, I consider it a big failure to meet the idea.

So the pane carries the composite `(deckId, agentId)` through attach, output, input, resize and refit, and the crate resolves that deck's own link — [the attach model has a deck dimension](#the-attach-model-has-a-deck-dimension) has the mechanism. **The selected deck does not move**, and nothing writes `desktop.toml`.

#### The deck with no link, which is what the no-terminal state narrowed to

`overviewShown` declares a terminal only once the pane's deck is **answering** ([the reachability condition](#the-reachability-condition-which-is-the-one-a-future-editor-would-remove) above). Where it is not, a `TerminalViewport` mounted inside the pane would sit there receiving no bytes — a black rectangle reading *"this agent is producing no output"* about an agent that may be working normally on a machine this app has merely lost contact with. So the pane renders **no viewport at all** and an explicit state in its place:

- `.agent-terminal-stack` carries `data-terminal-state`, `"attached"` or `"unreachable-deck"`. It is the sibling of the viewport's own `data-input-state` and answers the outer question — is there a terminal here at all. `"attached"` is written positively rather than left absent so both directions are assertable from the DOM.
- Where it reads `unreachable-deck`, an element with `data-testid="terminal-absent-<agentId>"` and `role="status"` stands where the terminal would be, carrying one sentence: which deck the agent is on, named by `deckName` so it matches the overview group header and the label in the Deck selector, and that deck's **own** account of why it is not answering, repeated rather than guessed at.

The derivation is `unreachableDeckTerminalState` in `desktop/src/lib/terminalInput.ts`, beside #1042's input gate and deliberately **not** inside it: `terminalInputState` answers "will what is typed here be delivered" in the `SendResult` vocabulary, and that vocabulary is the wire's — it mirrors the Rust enum the daemon sends, so a desktop-only condition cannot be spelled in it without inventing a value no daemon will ever produce. The *shape* is copied rather than re-invented (one state attribute plus one `role="status"` sentence), because a second vocabulary for the same kind of fact is how two seams come to disagree. There is no input gate rendered beside it either: where this state is set no terminal is mounted, so there is nothing to type into and nothing a lease could say about it.

**The trigger was narrowed, not invented.** It read *"this agent is on `<deck>`, which is not the selected deck"* and fired for every non-selected deck, because attach was process-global. Being non-selected is no longer a reason to have no terminal, so that sentence would now be false about the ordinary case; what survives is the case it was always also covering. `desktop/src/AgentPaneNoTerminal.test.tsx` holds the pair plus its control: `replaces an unreachable deck's terminal with an explicit state naming that deck` drives a healthy cross-deck pane and then takes its deck away, `attaches the terminal and clears the notice when the pane's own deck answers again` runs it back the other way and asserts the terminal arrives on the **same DOM node** — no close, no reopen, no identity change — and `gives a non-selected but reachable deck's agent a live terminal instead` is the control that stops the other two passing against a production that reverted to the old trigger.

**A disconnected deck is entered through a pane that was already open, and that is not a shortcut.** The overview lists no agents for a deck that stopped answering, because such a deck cannot vouch for what it was running — so the only way a user meets this state is by having opened the pane while the deck was healthy, which is what those tests reproduce.

#### The deck switch stays withdrawn, and the history matters

PRD #1105 originally opened a non-selected deck's agent by writing the selected deck to `desktop.toml`, letting the crate apply it, and writing the selection back on close — `useCrossDeckSelection`, plus `selectionForDeck` in `lib/endpoints.ts` to map a deck back to a stored row by its `Endpoint::describe()` label. It worked on the happy path. Two security audits then found the same shape twice: **state created under deck A being attributed to deck B, because identity is read from the current selection at use time rather than captured at creation.**

Attaching on the pane's own deck is what made the switch unnecessary rather than merely unwise, and the audits' four findings had to be closed to get there — capturing an origin deck with each channel, attempt and session and checking it through installation, delivery and write authority is exactly what cross-deck attach needs anyway ([#1116](https://github.com/vfarcic/dot-agent-deck/issues/1116), and [the section below](#the-four-findings-it-had-to-reach)). The switch itself is not coming back: it moves the user's selection for them, which is a decision about the whole app taken on a navigation. The withdrawn implementation is in `git log` on this branch.

**Nothing about opening or closing a pane writes the settings document**, which is the withdrawal's user-visible half. Three things in this app call `settings.save` — the settings sheet, the `DeckSelector` and `useZoom` — and none of them is reachable from opening or closing a pane. This page previously said a cross-deck open rewrote `desktop.toml` through the crate's `toml::Table` → `toml::to_string_pretty` round trip, losing comments and hand formatting, and that a kill while such a pane was open left the selection moved. Both were true of the withdrawn behaviour and neither is reachable now. `writes no settings document when a pane opens or closes` (`desktop/src/AgentPaneDeckIdentity.test.tsx`) is the guard, so re-introducing a write there goes red rather than quietly restoring the side effect.

**And no multiplexing.** One overlay pane is one stream to a deck this app already holds a tunnel to, over the existing attach verbs at the existing `PROTOCOL_VERSION`. [#1073](https://github.com/vfarcic/dot-agent-deck/issues/1073)'s objection — that the five-byte frame header carries no stream id — is about N decks × M *visible tiles* on the deck screen, which this does not attempt.

### The composite identity, all the way down

A security audit of PRD #1105 found two blocker-class defects, both of the same shape: **a pane showing or writing another deck's agent under the identity the user believes they opened.** Neither was exotic. Agent ids are per-daemon monotonic, the fleet fixture has a `planner` on two decks, and both defects run through that ordinary case.

**The heading used to read "part of the way down", and the qualifier was earned.** A second audit round found four lifetime and ordering paths the storage-key fix below does not reach, all of the *same* shape one layer up: identity read from mutable current selection at use time rather than captured at creation. [The four findings it had to reach](#the-four-findings-it-had-to-reach) says what each one was and what closed it. They were closed because cross-deck attach could not be built over them, not as a tidying pass.

**The pane is a real modal now, because it was claiming to be one.** `AgentPaneFrame` renders `role="dialog"` with `aria-modal="true"` — a claim that the rest of the interface is unavailable — over a base screen that is deliberately still mounted, which is the whole no-flicker design. So every control behind it kept its place in the tab order: the rail, the other tiles, and the `DeckSelector`, which retargets the entire app. Reaching the selector under an open deck-origin pane and choosing a deck that runs a same-id agent promoted **that** agent into the open pane, attached its session, and resolved `sendTerminalInput(agentId, …)` to it. Same role, same display text, no deck identity anywhere in the dialog.

`useInertBackground` (`desktop/src/hooks/useInertBackground.ts`) is the mechanism. It walks from the pane's own element up to `document.body` and marks every element that is **not** an ancestor of it `inert`, then moves focus inside. Marking one subtree was not available: `inert` is inherited and a descendant cannot escape it, and on the deck path the pane *is* a tile inside the base screen, promoted in place so its xterm survives. Walking siblings is also what stops it rotting — a region added to the deck later is covered with no edit to that hook, where a list of a dozen `inert` props in the JSX would silently omit it. The effect deliberately has no dependency array, so background content that appears while the pane is open (a toast, a connection banner, a newly reported agent) is marked on the commit that adds it. It has exactly one exemption — the voice surface, which is a peer rather than a region; [the voice surface](#the-voice-surface-and-the-four-decisions-in-it-worth-knowing-before-you-edit-it) says why and what the fence still holds.

#### A deck-origin pane is fenced on identity, and closes rather than retargets

Inertness makes the attack unreachable through the UI; it does not make the pane's claim true. A deck-origin pane means *"this agent, on the deck you are looking at"*, and the selection can still move for reasons that are nobody's gesture — a `selectionFallback` the crate reports, another window writing the settings document. So `DeckShell` compares `agentView.deckId` against the selected deck and **closes** the pane when they part. Closing was chosen over suspending because it leaves nothing to be wrong about: a suspended pane still has to say whose agent it is showing, and the honest answer is nobody's.

The render does not wait for that effect, and that is the half worth reading twice. Effects run after the commit is painted, so a fence that was only an effect would still have one frame in which the arriving deck's namesake wears the pane. `DeckSurface` therefore promotes a tile only on the full `(deckId, agentId)` match — `openAgent` replaced a bare `openAgentId` prop — so the mismatched commit shows no pane at all and the effect closes the view behind it. `promotes no tile for an open-pane identity naming another deck` drives `DeckSurface` directly for exactly this reason; the `DeckShell`-level test can only observe the state after effects have run, and it passes with the bare-id match restored.

#### Terminal stream state is invalidated by being keyed, not by being cleared

The second blocker is the ordinary overview path and needs no unreachable gesture at all. `terminalFeed`'s buffers were keyed by bare agent id, and **leaving a deck detaches its sessions without clearing a single buffer**. So opening deck B's `planner` mounted a fresh `TerminalViewport` whose first act is `terminalFeed.get(agentId)` — which answered with deck A's retained bytes, and the `!previous` branch wrote them straight into the new xterm. Deck B's live `AgentSession.transcript` is empty, so what the user saw under B's correctly resolved heading was up to the feed's 1 MiB retention of another machine's output. On a successful switch B's own `replace` frame eventually resets it; where the switch is refused, the save fails or the attach fails, no replacement ever arrives.

**Delaying the shown-set declaration does not touch this**, which is the reason [the reachability condition](#the-reachability-condition-which-is-the-one-a-future-editor-would-remove) above — correct and kept — is not the fix. The viewport's backlog read happens at mount, before anything is attached at all.

So the three maps are keyed by the composite identity end to end. `TerminalFeed` takes `(deckId, agentId)`; `AgentTile` supplies `agent.daemonId`, which is the agent's **own** deck rather than the selected one; and `TerminalChunk` carries a `deckId` stamped by the bridge at `deliverTerminal`, the single funnel every chunk passes through. The stamp is applied at the producer because only the producer can name the deck the bytes came from — `selectedDeckId` there is read off `fleet[0]` on every snapshot arrival, while React's copy is a commit behind.

`terminalInputResults` and `appliedGeometry` were the same root cause and are keyed the same way. A verdict recorded for deck A's `planner` could disable deck B's pane and print A's rejection notice under B's heading; `appliedGeometry` is removed by no per-agent eviction, so B's pane applied A's grid, and an attach that beat the pane's first fit submitted A's cached dimensions to B — which the daemon's smallest-viewer policy turns into a real, transient reflow of B's PTY and of every other client watching it, from a viewport on another machine.

The bridge's own `appliedGeometry` cache — read at attach time to declare this client's viewport — is keyed by the composite identity too, and each entry carries the target its session was created for. **`adoptGeometryDeck` is gone with that change**, and its removal is the fix rather than a loss: it emptied the whole cache when the selection moved, and emptying is not an identity boundary. A deck-A geometry event arriving *after* the clear rewrote the bare-id cache and notified listeners with the deck selected now, so both caches recorded A's dimensions as B's. An entry keyed and stamped with its own session's deck cannot be read for any other deck at all, so there is nothing left for a clear to protect against. `adoptEvidenceDeck` stays: the evidence ring genuinely is the selected deck's, and PRD #742 DECISION 1 keeps it single-deck.

**This reverses a decision this PRD took, and the reversal is the point.** PRD #745 left nine maps keyed by bare agent id and #1105 explicitly declined to fix these three, on the reasoning that a tile's terminal is always the selected deck's and the overview mounts none — so nothing could put a second deck's agent in front of them. That was true, and a cross-deck pane is precisely a second deck's agent in front of them. **The keying is kept even though cross-deck opening was descoped**, because the defect it closes is not the pane's: the sentinel scenario below needs no overlay at all.

#### The four findings it had to reach

**What the storage keying alone does.** A **fresh mount** — a viewport built for `(deckId, agentId)` — reads the buffer, the verdict and the applied geometry belonging to the deck it names, rather than whatever the bare id last held. The reproduction that needs no overlay: with two decks running a `planner` each, stream a sentinel into deck A's, switch decks with the ordinary `DeckSelector`, and deck B's same-id `planner` used to come up carrying A's scrollback under B's correctly resolved heading. The test reproduces the disclosure rather than the fix's shape (`does not replay the previous deck's buffer under the next deck's namesake`, `desktop/src/CrossDeckTerminalState.test.tsx`).

**What it did not, and what closed each one.** A second audit round found four paths above that keying, all carried forward with `file:line` evidence in [#1116](https://github.com/vfarcic/dot-agent-deck/issues/1116). Cross-deck attach could not be built over any of them, so all four are closed here rather than deferred:

| finding | what closed it |
|---|---|
| **The terminal stamp identified the deck selected when `deliverTerminal` ran**, not the deck that created the channel. `foldSnapshot` moves `selectedDeckId` before React receives the fleet while existing channel callbacks stay valid, so a late frame from deck A was stamped B and stored under B's composite key — a confidently wrong producer defeating a correct storage key. | `deliverTerminal` takes the origin `AgentTarget` as a parameter and stamps from it. Every producer holds its session's target in scope from before the attach was invoked, captured once in the `attachAgents` closure, so no path has to guess. |
| **A pending attach from the previous deck installed into the bridge's bare-id `sessions` map and took `sendTerminalInput`.** | It installs under its **own** deck's composite key, which is issue #1116's own suggested shape and is also simply correct: the pane that asked for the stream is still showing it. There is nothing to discard and nothing to reattribute, and `sendTerminalInput({ deckId, agentId }, …)` resolves the deck it was asked for. |
| **`TerminalViewport` reused one xterm and its rendered scrollback across a deck change**, because its build effect depended only on `agentId`; the `deckId` prop changed the feed subscription and nothing reset the bytes already on screen. | `deckId` is in that effect's dependency list, so an identity change rebuilds the terminal. The module-level registry is keyed by `(deckId, agentId)` for the same reason — two decks' same-id xterms are now routinely live at once, which is exactly the premise the first audit cleared the bare-id registry on. |
| **A late geometry event from the previous deck repopulated the cache `adoptGeometryDeck` cleared**, under the new deck's identity, and a later attach submitted it. | The geometry listener resolves its session by **session id** — minted per attach by the desktop crate, so it names one stream on one deck — and notifies with that session's creating deck. `adoptGeometryDeck` is gone; see above for why clearing was never an identity boundary. |

**Every one of those is mutation-checked**, because a test written alongside a fix that passes either way is what this branch has already been caught by three times. Breaking each fix alone — stamping from `selectedDeckId`, resolving the write session by bare agent id, making the registry bare-id again, resolving `resize`'s daemon from the applied selection, putting the registry's eviction back on the bare agent id, restoring `if moved { detach_all }` — turns a named test red in each case.

**The value-not-identity rule is part of the fix, not a style note.** A target is re-allocated on every render (`{ deckId: agent.daemonId, agentId: agent.id }` is a fresh object each time), so anything keying a `Map` on the object passes a test that happens to reuse one reference and finds nothing in production. `agentKey` is the one way to turn a target into a key, and `routes input and geometry to the requested deck when two attached agents share an id` (`desktop/src/lib/bridge.test.ts`) deliberately hands in a newly allocated but value-equal target to prove it.

### What the two entry points do not share

The overview-origin pane is given no `onRename` and no `onEvidenceSelect`, so it renders no rename control and selecting a handoff's evidence does nothing. Both are **capabilities the screen genuinely lacks** rather than presentation differences, which is why neither is expressed through `presentation`: the evidence drawer is the deck's and no screen is mounted underneath that could open it, and a rename reports its outcome through the deck's **toast**, so wiring it there would fail silently — which is worse than a header without the pencil. It is stated at the call site in `App.tsx` as well as here so it is not rediscovered as a bug. Closing the rename half needs `DeckShell` to grow a notice surface of its own, which is a new user-visible surface and therefore `CLAUDE.md` rule 9's question rather than a loose end.

One more behaviour is an open question rather than a decision: an agent that leaves the snapshot while its **overview** pane is open renders nothing, leaving the base screen up with `Escape` and no visible close control, because `OverviewAgentPane` returns `null` when the lookup fails. From the deck the same event unmounts the tile and the frame goes with it. A second, smaller edge rides on the same case: the overview's shown set still declares that agent's target — `overviewShown` asks whether the pane's deck is answering, not whether the fleet still lists that agent — so there is one declaration with no viewport consuming it, reconciled by a later `End`. PRD #1105 records both under the same open question.

### What the automated tiers prove about the resize, and what they do not

The pane asks the daemon for a bigger PTY rather than scaling a small one up, which is a product decision with a consequence for anyone else watching the same agent — `prds/1105-desktop-agent-pane-overlay.md` has the decision and the policy it rests on. A PTY has one window size, and since PRD #1105 M11 the daemon decides it by **focus**. The last client to claim focus decides the size of every agent on which it has a viewer that has **reported** a geometry: the smallest each axis over that client's own reporting viewers, which is exactly the one viewer's geometry in the ordinary case. Where no client has claimed focus, or the focused client has no reporting viewer of the agent, the daemon falls back to PRD #882's rule, the smallest each axis over every registered viewer that has reported one. This app claims focus when its window gains focus, and when it attaches a terminal while its window is focused, on each deck where it holds a viewer (`terminal.rs`'s claim path). A TUI claims on a terminal focus-in and, throttled to once a second, on keyboard or mouse input. So with this window focused, opening the pane gives the agent the pane's size and closing gives it back the tile's, and a TUI user attached to the same agent sees their pane reflow twice per open/close cycle.

**Claims are bounded on the daemon and dropped on this side when they go out of date** (PRD #1105's security audit). The daemon applies focus claims at most once per `FOCUS_REAPPLY_INTERVAL` (250 ms): claims arriving inside one window are applied together when it ends and only the newest of them is recorded, so two clients alternating claims cannot cost one PTY resize — a SIGWINCH, a cleared scrollback ring and a geometry push to every viewer — per claim. A claim is answered once it or a newer one has been applied, so `ok` still means the size it decides is in force; a claim landing inside a window is answered up to an interval late. On this side, every window focus report drops the claims of the report before it that have not been written yet, and so does a pane's attach-time claim: a claim still waiting on a slow deck's link when the window loses focus is never sent, rather than landing after the person moved to a TUI, and taking the size back to a window they are no longer looking at. A claim whose request was already written cannot be recalled.

**Under focus, the other client is not protected, and that is the accepted cost rather than an oversight.** The fallback's minimum is what keeps a viewer from being handed a grid larger than the geometry it last reported. The focused branch gives that up for every viewer that does not belong to the focused client: while this window holds focus, a TUI whose pane is narrower or shorter than the desktop's is handed the desktop's grid and clips at its right and bottom edges, and the same happens to the desktop pane when a larger TUI takes focus. The client being clipped is the one nobody is looking at. A TUI from a build before M11 never claims focus, so it pays this for as long as this app holds focus on an agent they share (`changelog.d/1105.breaking.md`).

**Where the fallback applies, its protection carries a precondition, and it is a precondition rather than a given.** Because the fallback takes a minimum, raising the desktop's request can only raise the PTY up to the smallest of the other *reporting* viewers — so a viewer is not handed a grid larger than the geometry it last **reported**, which is the same as "larger than itself" exactly while that report is current. A viewer that has reported nothing is not protected at all, and that is not only the legacy case: this app's own tiles attach through `attach_pending_viewport` and contribute no constraint until their first measured fit, which is a frame or two on every attach for an agent the bridge has not yet measured. The same unmeasured window is why a focused client's freshly attached viewer does not decide the size until its first fit: until then the daemon falls back rather than pinning a guess. Neither class is created by the pane. What the pane does is exercise the first-attach window again on the overview path, where it is a *first* attach, and make the largest single jump in requested geometry the app has.

The automated coverage stops well short of that sentence, and the boundary is worth stating precisely because the tests are green either way.

`reports the pane's larger proposed grid and restores the applied grid` (`desktop/src/components/TerminalViewport.test.tsx`) drives the real `DeckShell` and the real `TerminalViewport` with fake xterm and WebGL dependencies and a controllable `ResizeObserver`, and observes two things: `runtime.resizeTerminal(agentId, cols, rows)` — the **client's proposed geometry handed to the runtime** — and the grid the fake xterm is left parsing at. It records the tile's proposal, opens the pane, asserts both reported dimensions grow, asserts the terminal itself is back at the daemon-**applied** 80×24 rather than the 160×48 just proposed, then closes the pane and asserts the tile's dimensions are reported again. Those two halves need separate observations, and this paragraph previously claimed the opposite: `fit()` reports the proposal *before* `applyAppliedGrid()` runs, so the reported numbers are identical whether the applied geometry is found, missing or ignored, and an earlier version of this test — which asserted on `resizeTerminal` alone and said here that inspecting `terminal.cols` would pin the wrong authority — stayed green when the production `appliedGeometry` handoff was severed at its `App.tsx` call site. Asserting that `cols` equals the *proposal* would pin the wrong authority; asserting it equals the applied grid is the `applyAppliedGrid` contract itself, and it is what makes the pane's deliberate small-then-grow round trip observable. Two negative controls were run: making the pane's fit equal the tile's turns the test red, and passing `appliedGeometry={undefined}` in `App.tsx` turns the restoration assertion red while every `resizeTerminal` assertion stays green.

What that leaves unproven, enumerated rather than summarised:

- **That a real PTY changed size.** The fixture runtime's resize method is a no-op. Nothing in the jsdom tier reaches IPC, let alone a daemon.
- **That a native window measured the same cells.** jsdom has no layout engine, and the browser tier runs Playwright's own Chromium and WebKit builds rather than the WebKitGTK your distribution packages or macOS's WKWebView.
- **That the daemon's focus-driven arbitration produced the size the client asked for.** The tests see the proposal and a fixture's stand-in applied geometry, never a real daemon's disposition. The daemon's rule is tested below this app — `resize/policy/001`–`011` in `tests/e2e_pty_size_policy.rs` read `stty size` inside a real agent PTY — but with client-library stand-ins for the desktop, so they say nothing about this app's own claims or proposals.
- **That a TUI client attached to the same agent visibly reflowed.** Nothing automated puts this app and a TUI on one agent.

All four are the manual smoke check's, below. There is no `tauri-driver` and no WebDriver session anywhere in this repository ([#953](https://github.com/vfarcic/dot-agent-deck/issues/953)), so the real Tauri window is driven by the walk and by nothing else — and manual checks decay.

The browser tier does answer the geometry questions it can: `desktop/e2e/agent-pane-overlay.spec.ts` measures that the pane's box really occupies the viewport in both engines, that the grid beneath is the **same DOM node** and still `isConnected` while the pane is up, that the overview beneath is likewise, and that `Escape` returns to the screen the pane was opened from. Those are engine-side measurements about this app's layout; they say nothing about a PTY.

## The New agent flow

PRD [#1223](https://github.com/vfarcic/dot-agent-deck/issues/1223) — the desktop's version of the TUI's `Ctrl+n`, with a deck field in front of it because the desktop drives several decks. It closes [#1041](https://github.com/vfarcic/dot-agent-deck/issues/1041), which was that `DesktopAction::StartAgent` existed and nothing in the frontend reached it. It mirrors `Ctrl+n` and **not** the Runs screen: the two share daemon verbs (`PrepareWorkflow`, `StartPreparedAgent`) and no form rules, and the Runs screen's divergences are [#1044](https://github.com/vfarcic/dot-agent-deck/issues/1044)'s. Workspace modes are out of scope ([#1199](https://github.com/vfarcic/dot-agent-deck/issues/1199)). The frontend is `desktop/src/components/NewAgentDialog.tsx` with its rules as testable functions in `desktop/src/lib/newAgent.ts`; the backend is the `desktop_list_directories`, `desktop_new_agent_options` and `desktop_new_agent_orchestrations` commands plus the `StartAgent` and `StartOrchestration` arms of `desktop_run_action`, all in `desktop/src-tauri/src/lib.rs`. It ships visible — the desktop has no experimental-flag mechanism.

### Where it opens

Four entry points, all on the overview: the top bar's **New agent** button; `Ctrl+N` / `Cmd+N` (either modifier on every platform, and the shortcut stands down in a text field, in a terminal, while an agent's pane is open over the overview and while the dialog is already open); the **New agent** button on a connected deck group's header, which preselects that deck; and the first-run note's button. All four go through `VOICE_ACTIONS.openNewAgent`, and so does a fifth: the voice row `open_new_agent` ("new agent", "start a new agent", "spawn an agent", "I want another agent", and "new agent on <deck>", which preselects that deck exactly as a group header does — and, with the dialog closed, any start-shaped request such as "start the new agent", since `start_new_agent` cannot run until the dialog is open). The row opens the dialog and starts nothing — the dialog's Start is still the only thing that does — so it is outside PRD #802 D5's confirmation set. Its `deck` param is the table's first `deck_ref` and its first **optional** param: the bare phrase dispatches with no deck; a deck that matches nothing or several is dropped and the report says so ("No deck matches …, so none is preselected."), and one that resolves — named by the user or filled in by the model — is preselected and named in the report ("Preselected deck: Local deck."). Voice preselects only a deck the dialog can: the webview declares the deck step with every utterance (each deck's id and, for a disabled one, the reason the step shows beside it), a disabled deck is never offered to the model, a named one is answered with that reason ("Deck ci@stale-box cannot take a new agent, so none is preselected: No deck is listening on the configured socket."), and when exactly one deck can take a new agent voice dispatches it, as the dialog would preselect it anyway — naming it after a dropped deck's note or when a deck was referred to, and saying nothing about it for a plain "new agent", where there was no choice to report. A `deck_ref` resolves against the snapshot's `observed` fleet (every observed deck, not only the selected one), by the name the overview shows — "Local deck" or `user@host[:port]` — plus the host on its own, the host's first dotted component, and the literal "local" for this machine's deck (`voice::outcome::resolve_deck_ref`). The resolved `deckId` reaches the entry as the dispatch target's `preselectDeckId`, deliberately not its `deckId`, which falls back to the selected deck and so cannot say "the user named none". The overview publishes `openNewAgent` on its voice channel only while the dialog is closed, the same condition `Ctrl+N` stands down on. Directories are still not sayable; that, and "switch deck", are PRD #1195's.

### One dialog, and what each field asks the deck

The dialog is **one surface**, not a wizard: the deck field, the directory browser and the form are all mounted at once, top to bottom, and each is its own control (PRD #1223, the voice-first redesign — a wizard resists voice, because each step's meaning depends on the one before it and there is nothing to address until the previous gate is passed). Changing a field re-derives only its dependents: choosing a deck asks that deck for its listing and its options, confirming a directory asks it for that directory's orchestrations and resets Mode, and choosing an agent overwrites Command. The form's fields are disabled until a directory is chosen. Choosing a different deck clears the chosen directory, its listing, options and orchestrations, keeping only a Name or Command the user typed. Focus goes to the first unsatisfied control — the deck field, or with a deck preselected the browser — then to the browser after a deck is chosen and to Name after a directory is confirmed; the tab order is deck → directory (filter, list, **Use this directory**) → Mode → Agent → Name → Command → **Start**, and there is no Next or Back. `q` closes the dialog only while focus is in the browser's list; in Name, Command or the filter it is a letter. The directory browser is reachable by voice: `open_dir` ("open dir billing") goes into a child **on screen** — the filtered rows of the level being shown, never a directory elsewhere on the deck, because that would need the directory-search verb this PRD declined — `go_to_parent` is the `..` move, and `use_this_directory` is Space's; each calls the function its key calls, so the manual paths are unchanged. The browser declares what it shows with each utterance (`VoiceDirectories`, beside the mounted screen), and the rows' `requires` column makes them `callable: false` whenever it shows nothing. The form's own fields are reachable the same way: `choose_mode` ("make it a dispatcher", "use the review orchestration") resolves a `mode_ref` against the Mode chips **as offered** — they vary by the deck's capabilities, its experimental flag (`schedule: issues`) and whether the directory is a project, so a chip that is not offered is refused as `param_unresolved`, and "schedule issues" on a deck without that chip does not quietly become `schedule` — twice over: a chip's name inside a longer reference counts only when the rest is filler (`voice::outcome::resolve_choice`), and because a model shown only the offered chips was measured answering `schedule` anyway, the dialog also declares the authoring chips it withholds (`withheld_modes`, never resolvable) and the app refuses by name when the model's answer or the transcript names one (`withheld_mode_named`); `choose_agent_type` ("use claude") resolves an `agent_type_ref` against the Agent picker's entries — the deck's registry, or the desktop's labelled fallback, plus `auto` — by label or registry id, and overwrites Command exactly as the picker does; and `name_new_agent` ("call it billing worker") reuses `spoken_prefix`, taking the Name from the transcript after the marked words, with the transcriber's closing punctuation trimmed. The dialog declares its form with each utterance (`VoiceNewAgent`, whose `form` is present only while the fields are live: a deck and a directory chosen and no start in flight), and `requires = ["new_agent_form"]` makes the three rows `callable: false` otherwise. Each re-checks the declared deck and directory against the live form and refuses in the dialog's words (`FORM_MOVED_ON`, `MODE_NOT_OFFERED`, …) when the form moved during the round trip. **Command has no voice row, by decision:** it is the one field that executes, dictating a shell command is where a transcriber is least reliable (flags, quotes, paths, punctuation), and choosing an agent already fills it with that agent's default — so it stays typed by hand, and "set the command to …" is the model's `none`.


| field | what it does | daemon verb | capability |
| --- | --- | --- | --- |
| **Deck** | lists every deck in the overview's fleet; a deck that cannot take a spawn — anything but connected, which already excludes a pending, unconfigured or incompatible deck, while one accepted through **Connect anyway** reads as connected — is shown disabled with the overview's reason, and so is a connected deck that does not advertise `list-directories`, with the crate's `newAgentReason` (see [Older decks](#older-decks)); with exactly one eligible deck, or when opened from a deck's header, that deck is preselected **and chosen** on open, so its first listing loads with no key pressed; `j`/`k` move the field's cursor, and Enter or a click chooses — Enter on the deck already chosen only moves focus on to the browser | none — it reads `runtime.fleet` | — |
| **Directory** | a panel below the deck field, empty until a deck is chosen, that opens in the deck's configured `default_dir` when `NewAgentOptions` carries one and in the daemon user's home otherwise (see [the deck's default directory](#the-decks-default-directory)), and browses that deck's filesystem one level at a time with the TUI picker's keys (`j`/`k` move, `l`/Enter/Right enter, `h`/Left/Backspace go up, Space uses the current directory, `/` filters). Browsing is the only way to choose one: the typed-path field was removed (PRD #1223 U1). The `..` row goes up; there is no **Up** button (U3), and no **Back** now that there is no step to return to. **Use this directory** sits in the panel | `ListDirectories` — bounds and threat model in [directory-listing-verb.md](directory-listing-verb.md) | `list-directories` |
| **Form** | below the browser, enabled once a directory is chosen: the TUI's `NewPaneFormState` minus workspace modes, and minus the Agent picker, which PRD #1223 removed from both clients: a Mode row, Name and Command. Voice's `choose_agent_type` ("use claude") sets Command to that agent's default command from the deck's registry, the only way to choose an agent without typing | `NewAgentOptions` for the deck's default command, default directory, agent registry, experimental flag and authoring kinds; `ResolveProject` for the `Orch:` chips, asked unless the listing marked the directory as not a project | `new-agent-options`; the project verbs and `prepared-role-command` for orchestrations |

#### The deck's default directory

`default_dir` is a key in the deck host's own `DashboardConfig` (`~/.config/dot-agent-deck/config.toml`, `DOT_AGENT_DECK_CONFIG` included), beside `default_command`, and it lives there rather than in `desktop.toml` on purpose: `desktop_settings_secrets`' `ALLOWED_FIELD_TYPES` refuses free text in the desktop's settings — the same gate that keeps the last command in memory — and a path is free text by that gate's definition. On the deck it has one source that a remote deck answers for itself, and that both clients read: the desktop through `NewAgentOptions` below, and the TUI — which always runs on the deck's host — straight from its own `DashboardConfig`, through the same `usable_default_dir` (its `Ctrl+n` picker and the Scheduled Tasks manager's Add start there, falling back to the TUI's cwd; an Edit starts at the row's own `working_dir`). Editing it means that config file (`dot-agent-deck config set default_dir <absolute path>`, which refuses a relative one); a settings UI for it would need a daemon write verb and is not built.

The daemon reports it as the optional `default_dir` field of the `NewAgentOptions` reply (`src/new_agent_options.rs`, `usable_default_dir`), vetted the way a listing path is — `is_valid_orchestration_cwd`, then `canonicalize_project_dir`, then the predicate again on the canonical form, then an open — and **absent rather than an error** when any of that fails: unset, relative, missing, a file, unreadable, or non-UTF-8. A bad setting must never fail the options query, which also carries the default command. The value goes out canonical, so it is a path `ListDirectories` accepts back as sent.

**The wire answer: no capability and no `PROTOCOL_VERSION` move.** It is an additive optional field (`#[serde(default, skip_serializing_if = "Option::is_none")]`) on a reply PRD #1223 itself introduced, already behind the `new-agent-options` capability — the protocol's written "Do NOT bump" case. An older desktop ignores the field; an older deck omits it, which a newer desktop reads as "open in home".

The dialog asks for the options first and lists **after** they answer, so the browser opens at `defaultDir` directly rather than listing home and jumping. That costs one round trip before the first listing; an options query that fails for anything but a lost deck still lists home, and a `defaultDir` that no longer lists by the time it is asked falls back to home rather than opening on an error. `..` walks above it — it is a starting point, not a root. Where the browser started is the listing's own current-path line; nothing else is added.

**Start** then sends one of three things, each to the chosen deck:

- **A plain agent** — `StartAgent`, with a `DOT_AGENT_DECK_PANE_ID` the desktop mints. A blank Command sends no command, which is the deck's default shell. The Name is sent trimmed and omitted when blank, as the TUI's `resolve_display_name` trims it.
- **An authoring agent** (`schedule`, `schedule: issues`, `dispatcher`) — the same `StartAgent` plus `authoring_kind`, through `DaemonClient::start_authoring_agent`, which decides from a fresh handshake whether the deck advertises `authoring-kind`. The deck composes the seed from `src/authoring_seeds.rs` — the one copy the TUI composes from too — and delivers it once the agent is ready, through its existing readiness-gated delivery. A blank Command is resolved in the dialog, where the TUI resolves it: the deck's `default_command`, then the default command of the deck registry's `claude` entry, then `claude`. `schedule: issues` is offered only when the **deck** reports its experimental flag on. The deck refuses an authoring start whose `cwd` fails `authoring_seeds::is_safe_authoring_path` (audits A2 and D1), because the seed names that directory verbatim.
- **An orchestration** — `PrepareWorkflow` with an **empty task**, which the deck composes with no `## Your task` section at all, then `StartPreparedAgent` per role with `use_configured_command`, through `DaemonClient::start_prepared_role` (capability `prepared-role-command`, advertised on Unix only). The deck starts each role with the command and agent its project config gives it, re-read under the preparation's staleness checks — the desktop neither sends a role's command nor receives one from the preparation, whose `ProjectRole` still carries none. The Name is the run's title, carried verbatim as each role's `display_title` — the TUI sends it verbatim too — and suggested as the next free `<basename>-orchestrator-N` until the user edits it; one equal to a live orchestration's title on that deck is refused, and a directory that already runs one gets the TUI's warning. A project may define two orchestrations with one name — config validation only warns — and `ResolveProject` lists both, but the launch names an orchestration by its name alone and `PrepareWorkflow` takes the first definition with it, so choosing the second would run the first's roles and commands. The dialog therefore shows every such namesake as a disabled chip with the reason ("rename one to launch it here") and never submits an ambiguous name; a uniquely named orchestration in the same project is offered as usual (audit F2). The action boundary refuses it as well (audit V4): `start_orchestration_action` resolves the project on the chosen deck and refuses a name that snapshot reports more than once, before it prepares anything, and the fixture bridge's action mirrors that (a name the project does not define at all stays the deck's to refuse, which its preparation does before publishing anything) — so the invariant does not rest on the dialog's presentation, which a direct action or a frontend regression goes around. `PrepareWorkflow`'s own first-match rule is untouched: changing it would change a verb older desktops and the TUI already call (#1233). A non-Pi start role gets its coordinator prompt through the desktop's acknowledged, readiness-gated delivery; a role that fails to start stops the ones already started, in reverse.

Every deck call a start makes is bounded except the preparation itself (audits F4 and W3), so a deck that takes the connection and never answers one of them cannot hold the dialog's **Starting…** open indefinitely. A plain or authoring start, and each role's configured start — `start_prepared_role`'s fresh handshake and its reply together — each get `DECK_REPLY_TIMEOUT` (15 s), and so do the two reads an orchestration launch makes before it prepares anything: the capability handshake behind `orchestration_launch_unavailable`, and the `ResolveProject` behind the namesake refusal (audit W3 — that one is a read, writes nothing and is idempotent, so dropping it is safe). `PrepareWorkflow` deliberately gets no client-side deadline (audit V1): the deck resolves, composes, issues the token and publishes `orchestrator-context.md` on its blocking pool, and dropping the client's future does not stop any of that, so a preparation the dialog had reported as timed out could still publish afterwards — over a retry's context, once the retry's last prepared-role check had passed. What the two bounded kinds have that a preparation does not is a remedy for the elapsed case, and it is a different one each (audit W6). A role start that elapses is reported as indeterminate and goes through the pane-and-orchestration reconciliation, so a start that landed anyway is found and stopped. A stop that elapses is reconciled against nothing: it is recorded as an unconfirmed stop and named in the launch's error, which reports the role to the user rather than stopping it — what the bound buys there is that the rollback reaches the roles behind a wedged stop instead of queueing on it. A preparation has neither, so it is waited out. A role whose start fails goes through a pane-and-orchestration reconciliation, so a start that landed is found and stopped. What the reconciliation finding nothing *means* depends on how the start failed (audit V5): a refusal the deck composed, or a request this client withheld, is definitive — the deck started nothing. A timeout, or a transport or decode failure that could have followed a request the deck already read, is not: the role is then reported as cleanup the launch could not confirm, because the deck may still spawn it late. A deck that reads a start and drops the connection is exactly that case, and it reaches the client as `daemon closed connection before sending RESP`. Each rollback stop is bounded on its own at the same 15 s, so the rollback reaches every started role past a wedged stop, and the error names every role whose stop it could not confirm. The Runs launch shares the rollback and got the same start bound.

Those bounds are what let the dialog refuse to close while a start is in flight (audit F5) — with the preparation the one wait they do not cover, as above: the header's close button, Esc, a backdrop click and the directory browser's `q` all wait until the deck has answered, so a refusal — above all a cleanup warning — is never unmounted with nobody left to read it. Once the deck has answered, including during the fleet wait below, closing works as before. The dialog has no Cancel button (PRD #1223 U2): it was the same close as the header's X, and no other dialog here carries both — sheets have an X, confirmations a Cancel. While every control is disabled, focus is held on the dialog itself and the `role="status"` sentence says why nothing answers. A reply that lands after the overview dropped the dialog for some other reason leaves the runtime's global error alone, because by then it is the only copy of the failure.

That list also travels as data (audit F6). `desktop_run_action` rejects with a bare string for every failure, as it always did, except a launch whose rollback could not confirm every role stopped: that one rejects with `{ message, unconfirmedStops }` (`DesktopActionError` in `desktop/src-tauri/src/dto.rs`), which the bridge rethrows as a `LaunchCleanupError` (`desktop/src/lib/actionError.ts`). The dialog shows those roles as their own alert above the error, because in the sentence they come last — after the failed role and every started role's name — and the 240-character display clamp cuts them first; more of a longer sentence is readable behind **Detail**, which is itself clamped at 2048 characters and so is not labelled as the full text (audit V7). The alert lists the roles rather than joining them into one clamped sentence: each name is clamped on its own, the first eight are named, and any beyond that are counted as "…and N more" rather than dropped silently. The same alert is shown on the global toast — the runtime keeps the roles beside the sentence, so a failure that lands after the dialog is gone still reports them — and that toast renders its message through `displayText`, as every other daemon-influenced string on the screen does, since a role name reaches it inside the failure sentence. **Both screens render it** (audit W2): the deck's copy also carries that screen's own notice, and the overview mounts one over `runtime.error` alone. The overview's had been missing, which is where it mattered most — the New agent flow lives there, the dialog leaves the runtime's error alone once it is unmounted because by then it is the only copy of the failure, and the overview's Refresh calls `reconnect()`, which clears it. The two screens replace one another, so there is at most one toast on screen. The runtime holds the sentence and the roles as ONE value (audit W1), so a later failure cannot replace the message and inherit an earlier launch's roles — reachable with two operations in flight, and with a Refresh interleaved. The Runs launch rejects the same way (audit V2), and the Runs screen checks for a `LaunchCleanupError` before it translates any refusal code: each of those translations says nothing was started, and a `stale-preparation` refusal of a later role after an earlier one had started, followed by a stop the deck did not confirm, carries both.

### Every request names its deck

The deck's wire id is captured when a deck is chosen in the deck field and carried by every later call — listing, options, orchestrations, start. Each backend entry resolves it with `DeckScope::resolve(Some(deck_id))`, PRD #1105's mechanism, once and before its first await, and none reads the applied selection. So under **All Decks**, which resolves to the local deck ([#1083](https://github.com/vfarcic/dot-agent-deck/issues/1083)), an agent still lands on the deck the user chose. An id the app no longer observes is refused with `DeckScope::resolve`'s "that deck is not one this app is observing", nothing is started, and the dialog clears the directory panel and the form, puts focus back on the deck field and shows that message there, rather than retargeting. None of these paths adds a raw `trusted_daemon` / `selected_endpoint` read, so `selection_capture.rs`'s per-module budget did not grow.

### After Start: the fleet wait

The dialog does not open the new agent's pane on the start's reply. `paneAgentRetired` in `App.tsx` closes a pane whose connected deck does not list its agent, so a pane opened early would close itself. Instead the start emits the **target** deck's snapshot directly and nudges that deck's watcher (`DesktopState::request_refetch`), and the dialog waits until the target deck's fleet entry lists `(deckId, agentId)` — the composite identity, because two decks mint ids from their own counters — and then opens the pane in the PRD #1105 overlay. For an orchestration that is the start role's pane. The wait is bounded by `NEW_AGENT_APPEAR_TIMEOUT_MS` (12 s), set against the crate's 5 s reconcile interval with room for a remote deck's tunnel; past it the dialog closes and the overview says the agent was started but has not appeared. A refused start keeps the dialog open with the error inline and the values the user entered — an improvement on the TUI, whose status-bar error arrives after its form has closed.

### Closing what it creates

A creation flow the user cannot close from is not one they can use (PRD #1223 U4), so the overview closes both things it starts:

- **One agent** — a stop control on the agent's **row**, beside the open control. The row rather than the PRD #1105 pane overlay: the row is where the overview already puts an agent's one action and it needs nothing opened first, while a stop from inside the pane would close the pane under the reader as soon as the deck stopped listing the agent (`paneAgentRetired`). It asks the deck screen's own `ConfirmDialog` — same title shape, same *Stop agent* / *Stopping…* labels — naming the agent and the deck it is on, then sends `stop_agent` with that agent's deck.
- **A whole orchestration** — a **Close** control on the orchestration's card. The TUI's Ctrl+W closes a tab and every pane in it (`resolve_close_plan` → `ClosePlan::Tab` → `close_panes_concurrently`), and there is no orchestration-wide daemon verb, so the desktop does what the TUI does: `stop_orchestration` fans `StopAgent` out over every role the card lists, **concurrently**, on the card's deck. The confirmation says plainly that it stops every role, and names them.

Both are deck-scoped. `DesktopAction::StopAgent` carries a required `deckId` and `stop_agent_action` resolves it with `DeckScope::resolve(Some(deck_id))`, exactly as the start actions do — it used to resolve `DeckScope::selected()`, so under **All Decks** an agent started on another deck from the overview could not be stopped without switching. An id the app no longer observes is refused with the resolve error and stops nothing. No wire change: it is the same `StopAgent` request, sent to a different deck. The deck screen's own Stop sends its deck's id too.

Each stop — a single one and every role of a close — is bounded by `DECK_REPLY_TIMEOUT` (15 s). A close runs its stops through `stop_roles_concurrently`, so one wedged stop costs one bound for the whole close rather than one per role behind it, and the roles that did stop have their terminals detached. **A role whose stop the deck refused, or did not answer within the bound, is named**: the close rejects with the same `{ message, unconfirmedStops }` a launch's rollback uses (`DesktopActionError::launch` → `LaunchCleanupError`), so the overview's toast shows the cleanup warning listing exactly those roles, whose summary now reads *"… may still be running on this deck: their stops could not be confirmed"* for both callers. The target deck is refreshed whatever the outcome, since a close that could not confirm every stop has still stopped the rest.

### Older decks

Every addition is capability-gated, and the check lives in the client library, so each missing capability degrades the flow instead of failing it:

- **No `list-directories`** → the deck is **not eligible**. The typed-path field that used to be this case's fallback was removed at the product owner's direction (PRD #1223 U1: the desktop is experimental, and by the time people use it they will not be on a deck that old), so such a deck cannot be chosen at all. The crate derives `newAgentReason` from the `Hello` reply's advertised set (`new_agent_reason` in `daemon_bridge.rs`, beside `project_actions_reason`), and the deck field shows the deck disabled with it; its group header offers no **New agent**. Should a listing still come back `unsupported` — a deck replaced by an older build after its handshake — the directory panel says it cannot be browsed and puts focus back on the deck field, since choosing another deck is what is left. Defence in depth is unchanged: `start_agent_action` still applies `validate_pasted_project_path` to any `cwd` it is given before a deck is asked (audit D2), and the deck makes its own checks, although the dialog now only ever sends a path a deck listed. An absent `cwd` is still accepted there — it is the deck's default-directory start — though the dialog always sends one.
- **What browsing alone cannot reach.** The listing omits hidden and symlinked directories and stops at 1000 entries (`truncated`), and the typed path was the only route to those. They are unreachable from the desktop now — a follow-up tracks it — and the truncation hint says so rather than telling the user to type a path.
- **No `new-agent-options`** → Command is prefilled from this app's own last command for that deck, and voice's `choose_agent_type` resolves against the registry compiled into this app instead of the deck's. The authoring chips are withheld with the reason, since the deck cannot say which seeds it composes.
- **Options listing no authoring kind, or no `authoring-kind`** → the authoring chips are withheld with the reason; if a start reaches `start_authoring_agent` anyway, it answers the unsupported sentence and sends nothing.
- **Missing a project verb, or `prepared-role-command`** → the `Orch:` chips are withheld, carrying the connection's `projectActionsReason` (the sentence the Runs screen shows) or the configured-role-command reason. A non-Unix deck advertises neither `prepare-workflow` nor `prepared-role-command`, so it always lands here.

A current deck keeps control, line-separator and bidi-formatting characters out of the paths it lists (D1). An older deck does not, so the directory browser renders the listed names and the current path through `desktop/src/lib/displayText.ts`, whose sanitizer drops C0/C1 controls, bidi formatting characters and — its one widening past the Rust policy — U+2028 and U+2029, which a webview can break a line on.

### The last command is kept in memory, not in `desktop.toml`

The TUI prefills Command from `default_command`, then its `last_command`, then blank, and so does the dialog — with the deck's `default_command` from `NewAgentOptions` and a last command per deck. That last command lives on `DesktopState`, keyed by the deck's endpoint identity, recorded once a deck has accepted a plain or authoring start with a non-blank command, and gone when the app quits. It is kept out of `desktop.toml` on purpose: linkage-check's `desktop_settings_secrets` rule holds that nothing in that schema is free text (`ALLOWED_FIELD_TYPES`), and a command line is exactly where people put credentials. Persisting it belongs on the deck, shared with the TUI's `session.toml`, which is [#1048](https://github.com/vfarcic/dot-agent-deck/issues/1048)'s Part 1. It is never global, for #1048's reason: one deck's command means nothing on another.

### A Pi coordinator: allowed here, refused on the Runs screen

The Runs screen refuses a Pi coordinator because its launch promises an acknowledged, all-or-nothing delivery of the coordinator prompt, and Pi's native seed gives no acknowledgement the desktop could wait on. This flow starts roles with their configured commands, as the TUI does, and a Pi start role is seeded by the deck at spawn — PRD #201's native delivery with the deck's PTY safety net — exactly as the TUI's is. So it follows the TUI rather than the Runs form, and that delivery has no acknowledgement here either; that is the TUI's position rather than a new one. The Runs screen keeps its Pi-coordinator, empty-task and Windows refusals.

### Known limitations

These were found by PRD #1223's final audit and deliberately left for their own work rather than fixed in that PR.

- **The coordinator context path is shared per project (audit F1).** The TUI's `Ctrl+n`, the Runs screen and this flow all publish the coordinator context to the one fixed `.dot-agent-deck/orchestrator-context.md` in the project directory, and the per-role start verification checks it only up to the last role's start. So a second launch prepared in the same project in the window before the first coordinator reads the file can replace it, and the first coordinator would then read the second launch's context. This is pre-existing (PRD #819 and the TUI), and this flow adds another ordinary producer of the same kind: a launch in the same project, made from the app instead of from a terminal. No attacker is needed — two ordinary launches close together in one project can interleave, which is what `src/prep_token.rs`'s module doc records the fixed path costing. The fix changes the context-path contract every orchestration reads, so it belongs in its own PR: it is tracked as **[#1233](https://github.com/vfarcic/dot-agent-deck/issues/1233)**. The dialog's same-directory warning is the existing mitigation.
- **The working directory is spawned by pathname after the identity check (audit F3).** A prepared start verifies the project directory's identity and then spawns the role by its pathname, so a rename-and-replace of that directory in between would start the role in the replacement. Doing that needs write access to the directory's parent. For a project under the daemon user's own home that is the daemon's user, who already has full authority over the deck — but the daemon accepts any absolute project path, so a project under a shared or group-writable parent gives the same rename ability to a local principal with no authority over the attach socket at all, and the precondition is then not "already full authority". This is pre-existing in prepared starts; handing the verified directory to the PTY spawner as a descriptor (`fchdir`-style) is the complete fix and was judged disproportionate for this PR, so it is tracked with the above as **[#1233](https://github.com/vfarcic/dot-agent-deck/issues/1233)**.
- **The in-flight wait is not bounded end to end.** Each deck call after the preparation has its own bound (above), so a launch against a deck that stops answering waits out each one in turn — the role start, then one stop per role already started — before the dialog can be closed again. The preparation itself has no client-side bound (audit V1), so a deck that takes it and then neither answers nor drops the connection holds **Starting…** open for as long as it does; what would bound it is a deadline the daemon owns and that also withholds the publication once it has expired, which this PR does not add.

### What tests it

`desktop/src/components/NewAgentDialog.test.tsx` and `desktop/src/lib/newAgent.test.ts` (vitest) cover the steps and form rules; `desktop/e2e/new-agent.spec.ts` (Playwright, both engines) drives the flow over the fixture bridge, older decks included through `?older=` (see [Fixture preview](#fixture-preview)); the `daemon_bridge.rs` tests drive the backend against real daemons with **All Decks** selected; and the daemon half is `newagent/*` and `project/launch/*` in [`tests/CATALOG.md`](../../tests/CATALOG.md), with `newagent/live/001` as the lane-2 real-agent scenario (`cargo test-e2e-live newagent_live_001`, your own credentials, no CI job runs it). Nothing drives the real Tauri window, so the manual walk under [The New agent flow, on a local deck and a remote one](#the-new-agent-flow-on-a-local-deck-and-a-remote-one) is the compensating control.

## Release bundles

Every tagged release publishes the GUI as an **alpha** bundle: a `.dmg` for macOS arm64 and a `.deb` for Linux x86_64. They arrive on the release as `dot-agent-deck-desktop-alpha-<platform>.<ext>` with their own `checksums-desktop-alpha.txt`. The `.deb` comes straight from `release.yml`'s `desktop-bundle` matrix. The `.dmg` does not: the matrix's macOS leg builds only the `.app` (`--bundles app --no-sign`) and hands it on as a `ditto` archive, and a separate `desktop-sign` job — the one job that holds the Apple credentials, with no checkout — builds the `.dmg` from it, signed and notarized when the credentials are registered and **unsigned** while they are not, which is the state until the maintainer registers them. [`desktop-signing.md`](desktop-signing.md) has the design, the credentials and the runbooks (PRD #757 M4/M5). The release notes carry a section composed from what was published: a paragraph per platform actually present, saying whether that asset is signed and, for an unsigned `.dmg`, how to get past the OS. There is no user-facing install page on purpose while the GUI is alpha; [#765](https://github.com/vfarcic/dot-agent-deck/issues/765) writes one when that changes.

**That note is the only installation instruction this project publishes, so four things about its wording are guarded rather than left to taste.** Its unsigned macOS paragraph must offer the **System Settings > Privacy & Security > Open Anyway** route alongside the `xattr -dr com.apple.quarantine` command — macOS Sequoia removed the Control-click bypass, so a note carrying only the terminal command leaves a user unwilling to run a recursive command against `/Applications` with nothing, and teaches everyone else a habit that transfers to the next thing that asks for it. And its Linux half must speak for itself: it used to be covered by "unsigned, so your OS will warn you", which is false for the `.deb`, because `dpkg` ships with `no-debsig` and `dpkg -i` verifies no package signature and issues no warning. `the_alpha_note_does_not_leave_a_user_with_only_a_terminal_command` in `release_workflow_wiring.rs` asserts both. The third is **order**: the `gh attestation verify` command must come before both macOS override routes. The bundles are unsigned, so provenance is what tells a user where a download came from, and the note used to list the overrides first — a reader following it top-down had bypassed Gatekeeper before reaching the check that would have justified it ([#1153](https://github.com/vfarcic/dot-agent-deck/issues/1153)). The wording is identical in either order, so no presence check can hold this; `the_alpha_note_verifies_provenance_before_it_overrides_gatekeeper` asserts the positions, and also that the command pins the signing workflow (next paragraph). The fourth is that the quarantine workaround appears **only** for an unsigned `.dmg`: the note reads `desktop-sign`'s `signed` output and puts the `xattr` command and the unsigned wording inside an `if [ "$SIGNED" = "false" ]` branch, because a stale workaround on a signed release teaches a habit that transfers (PRD #757 Decision 10); `desktop_note_offers_quarantine_bypass_only_when_unsigned` asserts it. Signing and notarization — the thing that removes the macOS dialog — were declined in [PRD #757](../../prds/757-desktop-artifact-trust.md) on 2026-09-04 and that decision was reversed on 2026-09-24; the PRD records both, and why the free path on Windows is still better than any paid one.

**Every asset the release workflow uploads carries build provenance, and it needs no key.** A separate `attest` job runs `actions/attest-build-provenance` over the `.dmg` (the bytes `desktop-sign` uploaded and `desktop-publish` published), the `.deb`, the four CLI binaries and both checksum manifests, so a user can run `gh attestation verify <file> --repo vfarcic/dot-agent-deck --signer-workflow vfarcic/dot-agent-deck/.github/workflows/release.yml` and learn that the exact bytes they hold came out of this repository's release workflow at a named commit — a stronger claim than `checksums.txt`, which only says a file matches a number printed on the same page that served it. **`--signer-workflow` is what makes that sentence true.** `--repo` pins the repository but not the workflow within it, so on its own it accepts an attestation minted by any workflow in this repository — `gh attestation verify --help` says so and points at `--signer-workflow` for the rest. Measured on v0.41.0's `.dmg` and `.deb`: naming `release.yml` exits 0, naming `ci.yml` exits 1. The release note and the landing page (`site/src/data/landing-content.js`) both hand out this form. **How the two manifests reach that job is the part worth knowing before editing it ([#1152](https://github.com/vfarcic/dot-agent-deck/issues/1152)).** `checksums.txt` is written inside `finalize` and `checksums-desktop-alpha.txt` inside `desktop-publish`, while `attest` has no checkout and can reach a file only as an artifact, so each manifest travels as an artifact of its own (`release-checksums` and `release-checksums-desktop`) uploaded by the job that published it. The obvious shortcut — re-running `shasum` inside `attest` — is the one thing that must not happen: it would mint provenance over bytes that are very probably identical to the published ones and provably nothing, and an attestation over a file that differs from the published one is worse than none. `both_checksum_manifests_are_attested_subjects` in `release_workflow_wiring.rs` pins the hand-off in both directions (it reads each artifact's name off the producer and asserts the consumer downloads *that* name, so a rename in one place fails) and asserts `shasum` never appears in `attest`. The one thing still unattested is GitHub's auto-generated **Source code** archive: GitHub synthesizes it from the tag rather than the workflow uploading it, so no job ever holds its bytes. The signing certificate is short-lived and comes from Sigstore through the job's OIDC token (`id-token: write`), so **there is no private key here to rotate or leak**; that property is what `the_attest_job_holds_no_credential_and_needs_none` pins, along with the job's exact permission set. It is the only trust mechanism that reaches the `.deb` at all, since `dpkg -i` verifies no package signature. It does **not** remove the macOS Gatekeeper dialog or Windows SmartScreen — those check different chains — and no OS verifies it on a user's behalf. Like the desktop jobs, `attest` hangs off `finalize` without `finalize` waiting on it, so an attestation-service blip reddens the run and leaves the release complete; PRD #757 Decision 12 has the reasoning.

Three things about that job are worth knowing before editing it, because each is a single line that reads as housekeeping and is not. It hangs off `prepare` rather than `build`, so it runs beside the CLI matrix instead of extending the critical path. `finalize` — which creates the release and uploads the five CLI assets — deliberately does **not** list it in `needs:`, so a bundler that fails, hangs, or is skipped cannot delay or break an ordinary tag; the run goes red while the release stays green and complete. And `finalize`'s artifact download is constrained by `pattern: dot-agent-deck-*` because `merge-multiple: true` would otherwise flatten the desktop bundles into the same `dist/` directory, where the release glob would sweep them up and `task checksums`' `shasum -a 256 dot-agent-deck-*` would choke on the space in Tauri's `Agent Deck_<version>_amd64.deb`. All three are asserted by `xtask/linkage-check/src/release_workflow_wiring.rs`, since nothing else can check a workflow that only fires on a tag.

`tauri.conf.json` hardcodes `version: "0.1.0"` — the same deliberate placeholder as `Cargo.toml`. The job merges the real version into the bundle config at build time with `jq` and then fails the build if no output filename carries it, so a bundle labelled `0.1.0` cannot be published. `bundle.active` is likewise `false` in the base config and only the overlay flips it, which means an invocation that loses its `--config` produces no bundle *and still exits 0*; the job asserts the output exists rather than trusting the exit code.

**Windows is not built.** A bundle carries the daemon as a sidecar and no Windows daemon binary is published; a Windows GUI cannot attach to a remote Linux daemon instead, because the IPC transport is a named pipe on Windows and a Unix socket everywhere else. **PRD [#741](https://github.com/vfarcic/dot-agent-deck/issues/741) did not change that, and it is worth saying so rather than leaving the earlier expectation standing**: its DECISION 1 landed on `ssh -N -L` (option 1A), which forwards a remote Unix socket to a *local Unix socket* and therefore still has nothing to bind on Windows. The option that would have removed the local socket entirely — `ssh <host> dot-agent-deck daemon proxy-stdio`, where the ssh child's stdin/stdout is the transport — is [filed as the deferred follow-up](#why-ssh--l-and-what-was-rejected) for exactly this reason, and it is that follow-up plus [#754](https://github.com/vfarcic/dot-agent-deck/issues/754) that makes a Windows GUI reachable. `prepare-sidecar.sh` nonetheless handles the `.exe` suffix on `*-pc-windows-*` triples — in both the cargo output path and the staged name Tauri resolves as `{path}-{triple}{ext}` — so that groundwork is done whenever it arrives. It is covered by `xtask/linkage-check/src/sidecar_staging.rs`.

Bundling locally needs more than the compile set `ci.yml` installs: on Linux the `deb` bundler additionally wants `patchelf`, `fakeroot`, `file` and `desktop-file-utils`.

AppImage is deliberately not built. Its bundler downloads five third-party artifacts at build time, two of them shell scripts from a `master` ref, which is not a supply-chain property a release pipeline should have; it also failed on the first real attempt while the `.deb` from the same run verified clean.

One trap if you bundle locally in a `devbox shell`: the shell runs under nix's glibc, whose `ld.so.cache` does not exist, so `ldconfig -p` returns nothing and system libraries are invisible to the loader. Exporting `LD_LIBRARY_PATH=/usr/lib/x86_64-linux-gnu` fixes the Rust side but then breaks nix's `node` (`undefined symbol: uv_tcp_keepalive_ex`, from picking up the system libuv). Bundle without it; the Rust link resolves through pkg-config's `-L` flags and does not need it.

## Security and ownership model

The daemon remains the single source of truth for agent lifecycle, PTYs, hooks, and orchestration. The desktop opens no HTTP API or TCP listener. For a **local** deck the Rust core connects to the same per-user IPC endpoint as the TUI, verifies that endpoint's ownership and permissions, performs the existing `Hello` protocol and build handshake, and then bridges typed snapshots and bounded terminal chunks through Tauri IPC. For a **remote** deck it does the same over an app-managed `ssh -N -L` child, and what it trusts is different in kind rather than weaker in degree — ssh host-key and user authentication instead of a uid-and-mode check on an inode that would only ever prove the local `ssh` client is ours. [Trust is a property of the deck's kind](#trust-is-a-property-of-the-decks-kind) is the whole of that split, including what is deliberately *not* checked and why. PRD #742 lets the app hold several such connections at once, and changes nothing about the split: trust is established **per deck, per connection**, keyed by `EndpointIdentity`, so no deck in a fleet inherits another's — but the two kinds still clear different gates while looking identical on screen, which is [the fourth `as_local()` gate](#the-trust-check-is-the-fourth-as_local-gate-and-the-only-one-that-substitutes-rather-than-refuses). PTY output travels daemon → Tauri channel → xterm.js; focused terminal input and resize requests travel in the opposite direction.

The window uses a restrictive content-security policy and a minimal Tauri capability set. Bridge commands are scoped to the main webview, connection errors are sanitized before reaching it, terminal input and launch commands are bounded to 64 KiB, dimensions are constrained to `1..=4096`, and terminal sessions use opaque IDs and are detached when the bridge is disposed. A build mismatch is shown as incompatible rather than triggering an automatic daemon restart, and the only way past it is an explicit, confirmed, session-scoped **Connect anyway** that relaxes the stamp comparison alone.

Treat every live terminal as equivalent to its underlying agent CLI: it has the daemon user's filesystem and process permissions, and terminal input can authorize consequential work. The stop control has a confirmation step, but this preview is not a sandbox or an access-control boundary.

## Ownership: where every value comes from

This is PRD [#819](https://github.com/vfarcic/dot-agent-deck/issues/819)'s M1 ownership sweep. It exists so the boundary can be stated as a **checked list** rather than as "everything except settings" — the false-absolute shape `CLAUDE.md` rule 17 exists to stop, and one this PRD's own history has already had to correct twice.

The governing principle it checks against, from #819 and [#803](https://github.com/vfarcic/dot-agent-deck/issues/803): **a client gets everything from the daemon, wherever that daemon runs; the only thing it owns is its own settings plus genuinely presentational state.** Where the daemon does not expose something a client legitimately needs, the daemon is extended — "the client can compute it locally" is not an acceptable answer even when the client is on the same machine.

Every value below is one of three things. **Daemon-sourced**: it comes over the wire. **Client-owned by policy**: it describes this machine, this display or this installation, so the client is the only party that knows it. **Computed locally**: the client derives it — each of those is either fixed by #819 or carries an issue.

### The sweep is mechanical, and here are its sources

The done-condition is that the enumeration is *derived*, not asserted, because the rule-17 trap this sweep exists to close could otherwise recur inside the sweep. Three mechanical sources, each with the command that reproduces it:

1. **Every field of the frontend snapshot DTO** — `DesktopSnapshot` and the types it nests in [`desktop/src-tauri/src/dto.rs`](../../desktop/src-tauri/src/dto.rs), mirrored field-for-field (camelCased) by `DesktopSnapshotDto` in [`desktop/src/lib/bridge.ts`](../../desktop/src/lib/bridge.ts). `grep -n 'pub struct\|pub enum' desktop/src-tauri/src/dto.rs` enumerates the Rust side; `DesktopSnapshotDto` and `DesktopAgentDto` are the TS side. The two agree on every field name and optionality but one, noted in the table.
2. **Every `localStorage` key** — `grep -rn 'localStorage\|sessionStorage' desktop/src/ | grep -v '\.test\.'`. There are **five**: #819 M6 removed `projects.v1` and PRD [#803](https://github.com/vfarcic/dot-agent-deck/issues/803) added the fixture bridge's settings key. **Four** of the five go through `modeScopedKey` so a fixture visit cannot write a key live mode reads back; the fifth deliberately does not, for a reason given in its row below.
3. **Every settings key in an on-disk desktop store** — **there is one store, and it now holds four top-level keys.** PRD #803 landed `desktop/src-tauri/src/settings.rs`, whose document is `platform::paths::config_dir()/desktop.toml` (`DOT_AGENT_DECK_DESKTOP_CONFIG` overrides the path). `awk '/pub struct DesktopSettings /,/^}/' desktop/src-tauri/src/settings.rs` enumerates it: `version` (the document's own schema number), `appearance.mode` (`light` / `dark` / `system`), `endpoints` (PRD #741's decks — an `Option`, and the one key whose absence means *unspecified* rather than *empty*) and `zoom.level` (PRD #744's scale factor). **This bullet said "exactly two keys" until #741 wrote it, and the count had already been wrong once**: zoom arrived with #744 and did not update it, which is the same unremarked drift the `localStorage` count below records against itself. All four are **client-owned by policy** and correctly so — a schema number describes the file, an appearance override and a zoom level describe this display, and an endpoint describes reachability *from here*, which is the criterion in [The ownership boundary](#the-ownership-boundary) rather than an exception to it. Still no `tauri-plugin-store` and no window-state plugin: the window's geometry remains a static literal in `tauri.conf.json`, not a persisted preference. [Application settings](#application-settings) below is that store's contract, including the criterion for what may join these four.

### The snapshot DTO

| value | class | where it comes from |
| --- | --- | --- |
| `connection.status` | computed locally | the client's verdict on its own connection (`classify_handshake`). Its own state, legitimately. |
| `connection.socketPath` | client-owned by policy | how the deck *this* client dials is named. For a local deck that is its socket path, which is what this reported before the endpoint type existed; for a remote deck it is `user@host` with the port when it is not 22, derived by `RemoteEndpoint::describe` from validated fields. Scrubbed through `safe_display_text` — bidi format characters stripped, not only category `Cc` — because this is the line that tells a user which machine they are talking to. **A label and never a key.** The TS layer used it as the deck identity until [#742](https://github.com/vfarcic/dot-agent-deck/issues/742) M5, and that was the collision described in [`socketPath` is a label; `deckId` is the key](#socketpath-is-a-label-deckid-is-the-key); `daemonId` is derived from `deckId` now. |
| `connection.deckId` | client-owned by policy | what the deck **is**, for anything keying on it: `EndpointIdentity::wire_id()`, rendered `deck-<16 hex>`, minted from the endpoint this client dials — so the client is the only party that knows it, the same way `socketPath` is. Always emitted. `daemonId`, the agent composite key's first half, every `data-daemon-id`, the `desktop://daemon-event` stamp and `DesktopSnapshot.fleet`'s entries are all this one value. |
| `connection.deckKind` | client-owned by policy | `"local"` or `"remote"`, from the selection in force (PRD #741 M7). Always emitted, because the webview branches on it to disable **Stop deck** and **Replace deck** and an absent field must not read as `"local"`. |
| `connection.localOnlyReason` | computed locally | `Endpoint::require_local("Stop deck")`'s own refusal, present exactly when `deckKind` is `"remote"`. M2 made the operation unreachable by type and wrote the sentence at the same time, so this renders an error that already exists rather than inventing one. |
| `connection.selectionFallback` | computed locally | `SelectionFallback`'s own sentence: why the app is on the local deck when the stored selection named another. Reported rather than folded into "connected", because "that deck is gone" and "that deck has no socket path yet" are different things to tell a user, and a silent substitution is how someone ends up acting on the wrong machine's agents. |
| `connection.projectActionsReason` | **daemon-sourced**, in the sense that matters | why the project-aware surfaces are unavailable, derived from the capability set the daemon **advertised in its `Hello` reply** through `DaemonCapabilities::supports` (PRD #741 M8). The client composes the sentence; the daemon owns the fact. It is what replaces the build stamp as the thing a screen acts on — "can this deck do what I am about to ask" rather than "is this deck the same build as me". `None` means available, and is omitted from the wire. |
| `connection.error` | mixed | the daemon's own `Hello` rejection when there is one, otherwise the client's transport error. Sanitised locally by `safe_message` (control characters stripped, 2048 chars). |
| `connection.clientProtocolVersion` | client-owned by policy | this binary's `PROTOCOL_VERSION`. |
| `connection.serverProtocolVersion` | daemon-sourced | `AttachResponse::server_version`. |
| `connection.clientBuildVersion` | client-owned by policy | this binary's `DAD_BUILD_ID`. |
| `connection.daemonBuildVersion` | daemon-sourced | `AttachResponse::build_version`. |
| `connection.daemonVersion` | daemon-sourced | `AttachResponse::daemon_version`. |
| `connection.runningAgentCount` | daemon-sourced | `AttachResponse::running_agents.count`. |
| `connection.buildStampMismatchOnly` | computed locally | the client's compatibility verdict, comparing a daemon-sourced stamp against its own. Always emitted from Rust including as `false`; the TS type marks it optional so a test DTO need not restate the common case. This is the one field where the two declarations differ. |
| `agents[].id` | daemon-sourced | `AgentRecord.id`. |
| `agents[].paneId` | daemon-sourced | `AgentRecord.pane_id_env`. |
| `agents[].displayName` | daemon-sourced | `AgentRecord.display_name`. |
| `agents[].cwd` | daemon-sourced | `AgentRecord.cwd` — the daemon's own view, which is the point: it diverges from any client's `current_dir()` the moment an agent has `cd`'d. |
| `agents[].rows` / `agents[].cols` | daemon-sourced | `AgentRecord.rows` / `.cols`. |
| `agents[].agentType` | daemon-sourced | the live snapshot's type, else `AgentRecord.agent_type`. Falls back to the literal `"none"`, which is honest: it is the value that also catches a type this build has never heard of. |
| `agents[].cliName` | daemon-sourced | `AgentRecord.cli_name`, stamped at the `ListAgents` wire boundary by `attach_cli_names` from the daemon's own `agent_registry`, for the identity the reply reports. It **used to be computed locally** — looked up in the client's own compiled-in copy of that table — which was the last entry in the third class no other issue covered, and [#856](https://github.com/vfarcic/dot-agent-deck/issues/856) closed it. It could not diverge then, because the handshake required an exact `PROTOCOL_VERSION` match *and* matching build stamps, so both sides compiled one table — but that was a property of the gate, not of the design, and [#801](https://github.com/vfarcic/dot-agent-deck/issues/801) has since removed the stamp half of it. Absent means the daemon named no binary; the cell is then empty, and **nothing falls back to the local table**. |
| `agents[].status` | daemon-sourced | the live snapshot's `SessionStatus`. Falls back to `"running"` when the daemon reported no live session, which is a *reading* of daemon-held state rather than an invention — the record exists because the daemon holds the process — and not a placeholder the user could mistake for a report. |
| `agents[].activeTool` | daemon-sourced | the live snapshot's `active_tool`. |
| `agents[].toolCount` | daemon-sourced | the live snapshot's `tool_count`, `0` when there is no snapshot. |
| `agents[].lastUserPrompt` | daemon-sourced | the live snapshot's `last_user_prompt`. Absent, never blank. |
| `agents[].writeLease` | daemon-sourced | projected from the live target's `Writable` into read/write/none. Absent when the daemon declared no live target, which is not the same as declaring a non-writable one. |
| `agents[].lastActivityMs` | daemon-sourced | the live snapshot's `last_activity_ms`, copied through unclamped. The daemon owns the instant; the webview owns the wording and the clock-skew rule. |
| `agents[].spawnedAtMs` | daemon-sourced | `AgentRecord.spawned_at_ms`, same split. |
| `agents[].tab` | daemon-sourced | `TabMembership`, every field bound explicitly in `map_tab` rather than through a rest pattern, so the next field added there is a compile error at this seam. |
| ~~`projectCwd`~~ | **gone** | It was `desktop_project_cwd()` — `option_env!("CARGO_MANIFEST_DIR")`'s grandparent if it held a `.dot-agent-deck.toml`, else `std::env::current_dir()` — and it was the defect #819 exists to remove: a compile-time constant cannot name a runtime machine's project, and against a remote daemon neither leg was even on the right host. **#819 M6 deleted the function and the field**, with nothing replacing either. The project a launch runs in now comes from `desktop_list_projects` / `desktop_resolve_project`. |
| `scheduleRevision` | daemon-sourced | `AttachResponse::schedule_revision`, the daemon's monotonic count of changes to its registered schedule set, copied through and rendered by nothing. It exists because the daemon seeds its project list partly from every registered schedule's working directory, and no schedule data reaches this app at all — so a schedule registered while the app is open added a project the picker had no way to notice ([#887](https://github.com/vfarcic/dot-agent-deck/issues/887)). `projectsRevision` in `App.tsx` keys on it. Absent from a daemon that reports none, in which case the manual **Refresh** button is the remedy it always was. Comparable only against earlier values on the same connection: it counts from 0 on every daemon start. |
| `fleet` | client-owned by policy | which decks this installation is observing, by `connection.deckId`, in observed order with the selected deck first and never empty (PRD #742 M5). It is a property of the **applied settings document** — which decks the user configured and chose — so it is client-owned by the same criterion every endpoint field is, and every deck's snapshot carries the identical list. No daemon is told it exists, let alone which others do. See [Fleet membership rides on every snapshot](#fleet-membership-rides-on-every-snapshot). |
| `protocolVersion` | client-owned by policy | this binary's `PROTOCOL_VERSION`, restated at the top level. |
| `source` | client-owned by policy | the literal `"daemon"`, distinguishing a live snapshot from the fixture bridge's. |

### Beyond the snapshot

The bridge emits **six** more DTOs plus one event payload. They are listed because leaving them out would make "every value the desktop presents" the kind of claim this sweep exists to replace. (It said *four* until this line was recounted at PRD #742 M7, and the drift is the same unremarked kind the `localStorage` count below records against itself: `DesktopSettingsSnapshotDto` arrived with PRD #803 and `EndpointTestReportDto` with PRD #741, and neither updated the number.)

| value | class | where it comes from |
| --- | --- | --- |
| `DesktopActionResult.ok` / `.message` | computed locally | the client's outcome for the action it just issued. |
| `DesktopActionResult.agentId` / `.agentIds` | daemon-sourced | ids the daemon minted for the agents it just started. |
| `DesktopActionResult.sendResult` | daemon-sourced | `AttachResponse::send_result`, the daemon's honest delivery outcome (PRD #20). |
| `DesktopActionResult.snapshot` | — | the table above. |
| `TerminalAttachResult.sessionId` / `.generation` / `.reused` | client-owned by policy | the desktop's own per-window terminal bookkeeping: an opaque id minted from a process-local counter, for a session that exists only inside this app. |
| `TerminalAttachResult.agentId`, `TerminalStateEvent.agentId` | daemon-sourced | the agent the session is attached to. |
| `TerminalStateEvent.state` / `.message` | computed locally | this client's view of its own stream. |
| `DesktopSettingsDto.version`, `.appearance.mode`, `.zoom.level` | client-owned by policy | PRD #803's settings document, source 3 above. The daemon is never told and never asks. |
| `DesktopSettingsDto.endpoints` | client-owned by policy | PRD #741's decks: the remote rows and the selection token. An endpoint describes reachability *from this installation* — a socket path, an ssh host, a forwarded socket — so it is client-owned by the criterion rather than by exception, and none of it syncs. **Optional, and `undefined` means *unspecified* rather than *empty*** — see [`Option<EndpointSettings>` is merge protection](#optionendpointsettings-is-merge-protection-and-it-puts-an-obligation-on-every-future-panel). The daemon is never told which decks exist, including the one it is. |
| `EndpointTestReportDto.endpointId` / `.deck` | client-owned by policy | the `Selection` token this report is about, and how that deck is named to a user — `RemoteEndpoint::describe`, or the local socket path. |
| `EndpointTestReportDto.state` / `.ok` / `.message` / `.remedy` | computed locally | `Test connection`'s verdict on a deck, as one of twelve named states. The classification is this client's and so is every sentence: the vocabulary is protocol versions, build stamps and forwarded sockets, and a state whose sentence lives beside its classification cannot drift from it. `ok` is emitted rather than derived in the webview so the twelve-way judgement stays in one place, and it is stamped once on the way out by `sealed()` rather than maintained beside each assignment to `state`. |
| `EndpointTestReportDto.detail` | mixed | ssh's own stderr, or this client's own transport error, absent when there were none. Scrubbed through `safe_display_text` — control **and** bidi — because these bytes were written by a remote host or by a `~/.ssh/config` someone else may have planted. (A daemon's *handshake* rejection reaches `message` instead, where it is the daemon's own sentence.) |
| `EndpointTestReportDto.clientProtocolVersion` / `.clientBuildVersion` | client-owned by policy | this binary's `PROTOCOL_VERSION` and `DAD_BUILD_ID`, the same two values the connection DTO restates. |
| `EndpointTestReportDto.serverProtocolVersion` / `.daemonBuildVersion` / `.runningAgentCount` | daemon-sourced | the `Hello` reply's own fields, when the probe got that far. |
| `EndpointTestReportDto.forwards` / `.forwardsKnown` | computed locally | what this deck's tunnel will inherit from the user's `~/.ssh/config`, resolved by `ssh -G` against the tunnel's own generated config and forced options. It describes *this machine's* ssh configuration, which no daemon can know. `forwardsKnown` is the difference between an answer and an absence, and is separate precisely so an unknown is never rendered as "none": empty **and** known is the only combination that means there are none. |
| `EndpointTestReportDto.discoveredSocket` | daemon-sourced | the remote attach socket path, learned over the probe's own round trip. Discovered rather than derived, because OpenSSH expands neither `~` nor an environment variable on the remote side of `-L` and the far host's `XDG_RUNTIME_DIR` and uid are not knowable from here. The **panel** writes it back into the row, not this reply — `useDesktopSettings` already serialises the document's read-modify-write, and a second writer in Rust would be [#828](https://github.com/vfarcic/dot-agent-deck/issues/828) created on purpose. |
| `DesktopSettingsSnapshotDto.path` | client-owned by policy | where *this installation* keeps that document. Absent from the fixture bridge's reply, which has no file — the surface renders the browser-preview wording rather than naming a path that does not exist. |
| the `deck` field on a `desktop://daemon-event` payload | client-owned by policy | which deck a forwarded daemon broadcast came from (PRD #742 M3) — `connection.deckId`'s own value, minted by this client from the endpoint its watcher is pinned to. The **event** underneath it is daemon-sourced and unchanged: `deck` is flattened beside a `BroadcastMsg` rather than wrapping it, so every snake_case field `daemonEvents.ts` reads stays exactly where it was. It exists because `BroadcastMsg` carries no deck id, so "which deck is this event from" used to be answerable only by "there is exactly one". |

### The four values the deck still invents, and who owns them

`mapDesktopSnapshot` builds the deck's run model on top of the DTO, and four of its fields have no daemon counterpart at all. PRD #745 deleted the branch chip and the attempt readouts outright for exactly this reason — a fabricated `ATT 01` reads as data in a way an explicit "unavailable" does not — and these are what remain.

| value | class | verdict |
| --- | --- | --- |
| `repo` | computed locally | the basename of `cwd`, split in the webview. Still split here for the *agent-derived* `cwd`, which is a working directory rather than a project identity. The **project** header no longer goes through it: since #819 M6 the topbar prefers the resolved selection's `name`, which the desktop crate derives once from the daemon's canonical path, so the picker and the header cannot disagree about a symlinked project's basename (PRD #220's bug). |
| `worktree` / the `cwd` fallback chain | computed locally | **Three** legs since #819 M6, not four: a daemon-reported agent `cwd`, then the *previous snapshot's* `worktree`, then the literal `"No active project"`. The removed leg was `projectCwd`, the client's own guess, and it is what made the header and the panes able to name different hosts. The surviving middle leg carries a value that WAS daemon-reported, so the header does not blink to "No active project" when the last agent exits. |
| `spend`, `elapsed` | computed locally | hardcoded `0` and `"—"`. Model, cost, tokens and context window are [#633](https://github.com/vfarcic/dot-agent-deck/issues/633)'s subject — out of #819's scope but not out of the principle: #633's discovery work is choosing how the *daemon* acquires them. |
| `profiles` | computed locally | `DEFAULT_PROFILES`, overlaid from the `agent-profiles.v1` key below. [#824](https://github.com/vfarcic/dot-agent-deck/issues/824). |

`runId`, `stages`, `health`, `currentNode`, `totalNodes` and `paused` are presentation derived from daemon-sourced agent state, or inert constants in live mode (`paused` is only ever written by the fixture bridge). They are client-owned by policy.

### The five `localStorage` keys, and the one that is gone

**Four** of the five are written through `modeScopedKey`, which suffixes the runtime mode — a fixture visit cannot write a key live mode reads back, which it once could, and which handed a real workflow launch a working directory that never existed. The fifth, #803's fixture settings key, is deliberately outside that scheme; its row says why, and it is the one row here where an unscoped key is not a defect.

| key | class | verdict |
| --- | --- | --- |
| ~~`dot-agent-deck.desktop.projects.v1`~~ | **gone** | It was a list the user typed into, with nothing validating it against the daemon's world, and it was the source of truth for the launch cwd. **#819 M6 removed it, with nothing persisted in its place** — the picker holds a resolved selection in React state and it dies with the launch. Any future convenience must be prefill for a field the daemon still resolves, never an authority. Record the decision on #824, which stays open for the other three. |
| `dot-agent-deck.desktop.agent-profiles.v1` | computed locally | per-project draft content. [#824](https://github.com/vfarcic/dot-agent-deck/issues/824), which stays open for this one. |
| `dot-agent-deck.desktop.prompts.v1` | computed locally | the prompt library. #824. |
| `dot-agent-deck.desktop.workflow-preview.v1` | computed locally | workflow role order. #824. |
| `dot-agent-deck.desktop.overview-columns.v1` | client-owned by policy | which columns *this display* shows. Genuinely presentational and correctly client-side. |
| `dot-agent-deck.desktop-settings` | client-owned by policy | PRD #803's settings document as the **fixture** bridge stores it, so a `pnpm dev` + `?fixture=1` preview survives a reload. Note the spelling: no `.desktop.` segment, no `.v1`, and **no `modeScopedKey`** — which is safe here structurally rather than by care, because `FixtureDeckBridge` never imports `@tauri-apps/api/core` and `TauriDeckBridge` reads `desktop.toml` and never reads `localStorage`, so the two cannot collide over it. Correctly client-side either way. |

**#824 says "the four `localStorage` keys" and there are now five — and none of the three moves since it was filed cancel out.** `overview-columns.v1` arrived with PRD #745, taking the count to five; PRD #803 added the fixture settings key, taking it to six; and #819 M6 removed `projects.v1`, bringing it to five. That is the order they actually landed in — #745 and #803 are both on `main`, and M6 is this PRD — so the count never passes through four on the way. The total is one above where #824 found it and the substance has moved further than that: the two keys that arrived are both on the right side of the boundary, and the key that left was the one furthest from it, so **#824 is now open over exactly three keys** — `agent-profiles.v1`, `prompts.v1` and `workflow-preview.v1` — rather than the four it names. All three moves are recorded here because a count that drifts unremarked is how the next key lands unclassified.

### The whole client-local filesystem surface

The criterion is **"the client resolves no project against a filesystem"**, not "the client touches no filesystem" — the wide version fails on day one and is quotable, so the narrow one is stated with its exceptions enumerated rather than waved at.

The production filesystem surface of `desktop/src-tauri/src/` falls in **two** places, enumerated below. Neither grep alone finds all of it: `grep -n 'std::fs' desktop/src-tauri/src/*.rs` misses every probe in group 1, because those go through `Path::is_file` and `Path::metadata`, while a probe-shaped grep misses the file handles in group 2. Both together are the enumeration that works:

```
grep -n 'is_file()\|is_dir()\|\.exists()\|metadata(\|read_dir\|canonicalize' desktop/src-tauri/src/*.rs
grep -n 'std::fs\|OpenOptions\|File::open' desktop/src-tauri/src/*.rs
```

1. **Locating the bundled daemon sidecar** for **Replace deck** — two call sites in one function: `is_executable_file` (`daemon_bridge.rs`: the `is_file` and the `metadata` inside it), the `current_exe` sibling search that calls it, plus the `DOT_AGENT_DECK_BINARY` and `PATH` env lookups beside them. **Legitimate and permanent:** this is the client finding *its own installation*, which no daemon can tell it.
2. **The desktop's own settings** — `settings.rs`, allowed by the same rule and **no longer an empty set**: PRD #803's store arrived after this sweep was first written. Its production call sites are the path vetting (`symlink_metadata`), the bounded read (`File::open` plus a capped read), and the atomic publish (`OpenOptions` create-new for the temp file, `write_all`, `sync_all`, `rename`, and a `remove_file` on the failure path), against a path from `config_dir()` or `DOT_AGENT_DECK_DESKTOP_CONFIG`. Every one of them touches **this installation's own preferences file and nothing else**, which is the client-owned side of the boundary by the criterion in [The ownership boundary](#the-ownership-boundary). It resolves no project and reads no project state; [linkage-check rule 12](#the-tripwire-on-that-invariant-and-what-it-cannot-do) passes over it unchanged.
3. ~~**`desktop_project_cwd()`**~~ — two `is_file` probes for a `.dot-agent-deck.toml`. **This was the defect, and #819 M6 deleted the function.** Nothing replaced it: the two groups above are the whole surface.

One more read used to be invisible to every grep above because it happened inside the shared root crate: `prepare_orchestrator_prompt`'s `create_dir_all` and `write`, reached from `lib.rs`'s workflow-launch path. It was a **write** rather than a read and it was the other half of the defect; #819 M4 moved it daemon-side and M6 moved the desktop's launch onto the `prepare-workflow` verb that performs it there. That it cannot be found by searching this crate is precisely why #819 M7 adds the linkage-check tripwire described [below](#the-tripwire-on-that-invariant-and-what-it-cannot-do) — and why the tripwire is labelled a tripwire and not a boundary, since the desktop crate path-depends on the root crate and every `pub` item there stays reachable.

### The tripwire on that invariant, and what it cannot do

PRD #819 M7 adds **rule 12** to `cargo xtask linkage-check` ([`xtask/linkage-check/src/desktop_project_boundary.rs`](../../xtask/linkage-check/src/desktop_project_boundary.rs)), over the production sources of `desktop/src-tauri/src/`.

**It is a regression tripwire. It is not enforcement, and it is not a security boundary.** The PRD's first draft called it enforcement and the security audit was right to object, so the framing is stated here rather than left to be inferred. `desktop/src-tauri/Cargo.toml` carries `dot-agent-deck = { path = "../.." }`, so every `pub` item in the root crate stays callable from the desktop crate whatever this rule says — which is exactly how `load_project_config` was reachable from `lib.rs` in the first place. The rule can notice the shapes that read like a client-side project resolution; it cannot make one unreachable.

What it catches, by parsing Rust rather than matching substrings — a substring scan drowns in comments and string literals, and a sweep that produces noise gets switched off:

| finding | what trips it |
| --- | --- |
| `root-module` | a `use` or qualified path naming a root-crate module that is not on the rule's positive allowlist. The allowlist is the **boundary as a positive statement** — the modules the production desktop may reach across — so a fifth forbidden name does not need adding to a blacklist to be caught. |
| `forbidden-symbol` | `project_config`, `orchestrator_context`, `load_project_config` or `prepare_orchestrator_prompt` as a path segment anywhere, including through an alias (`use … as pc` still spells `project_config` in its path), a grouped or multi-line import, or a fully qualified call with no import at all. |
| `project-state-literal` | a string literal carrying `.dot-agent-deck.toml` or `orchestrator-context.md`, including inside a `format!` — the desktop's project prose lives almost entirely in macro arguments, which syn leaves as opaque tokens, so the rule walks those token streams too. |
| `cwd-fallback` | `std::env::current_dir`, the guess `desktop_project_cwd()` fell back to. On a remote daemon that guess resolves against the wrong filesystem and is silently wrong, which is the defect #819 exists to remove. |

`#[cfg(test)]` items are out of scope — the desktop crate's test modules legitimately build project fixtures, and a rule that failed on them would be worked around rather than obeyed. `#[cfg(not(test))]` is production and *is* scanned.

**The residual, named rather than implied.** A source rule is bypassable, unintentionally as easily as deliberately:

- **a root-crate wrapper with an innocuous name** — a new `pub fn` in an already-allowlisted module whose body calls `load_project_config`. Nothing in the desktop's source says so, so nothing in this rule can see it. This is the likeliest accidental bypass and it is pinned by a test (`a_root_crate_wrapper_with_an_innocuous_name_is_not_caught`) that asserts the rule does **not** catch it, so the gap stays visible instead of being rediscovered;
- **a macro that generates a forbidden path** out of tokens that are not one — syn does not expand macros;
- **an already-imported module gaining a new method** that reads project state;
- **a trait method** brought in by an allowlisted `use`;
- anything reached through `std::fs` by a path the rule has no literal for.

Only removing the desktop's dependency on the full root crate gives compiler-enforced reachability, and that is [#176](https://github.com/vfarcic/dot-agent-deck/issues/176) M1.1 — out of scope for #819 and one more reason to do it.

**The rule shipped before the milestone that satisfies it, and the milestone has now landed.** M6 (the desktop launch flow) deleted `desktop_project_cwd()` and moved the launch onto the daemon's `prepare-workflow` verb. While the tree still violated linkage-check rule 12, those violations were enumerated one by one in the rule's `PENDING_M6` allowlist rather than the rule being weakened to let them through — a weakened rule catches nothing new either. An allowlist entry that matches **no** finding is itself a failure, and that forcing function fired exactly as designed: M6 removed the last client-side project read, the check went red on ten stale entries, and they were deleted along with what they excused. **`PENDING_M6` is now empty, and it should stay that way** — a new entry excuses a new client-side project read rather than fixing it.

## Application settings

The desktop app owns exactly one thing and gets everything else from the daemon: **its own settings**. They live in a per-installation TOML document at `platform::paths::config_dir()/desktop.toml` — a **sibling** of the TUI's `config.toml` and `keybindings.toml`, never a section inside them, because `DashboardConfig::save()` serialises its struct and would silently delete a `[desktop]` table it did not know about. `DOT_AGENT_DECK_DESKTOP_CONFIG` overrides the whole path and is the seam the tests use. Whatever resolves it, the path is **vetted before it is read or written**: absolute, with a file name, and either absent or a plain regular file — a symlink, directory, FIFO, socket, device or Windows reparse point is refused — and at most 256 KiB is read. That is not a privilege boundary (anyone who can set your environment can already run code as you); it is there because an app that hangs forever on a FIFO or dies on `/dev/zero` is a miserable thing to debug. A refused path is logged with the path and reported to the webview without it, the same split every write error uses. The store is `desktop/src-tauri/src/settings.rs`; the surface is the **Settings** rail button, *Open settings* in the command palette (⌘K), and the same Escape/backdrop/close-button trio as every other sheet. Saves are optimistic and **serialised**: `useDesktopSettings` applies the choice immediately, writes behind it one request at a time, and drops a superseded response, so two rapid choices land on disk in the order they were made. Two *processes* racing on the same field — two app windows, or the app and a hand edit — is still unhandled and tracked as [#828](https://github.com/vfarcic/dot-agent-deck/issues/828); the merge already stops either of them losing an unknown *section*. PRD #803 has the full design; this section is the contract.

Four behaviours are worth knowing before building on it. **Loading never fails** — a missing, unparseable or unreadable file, and an unknown enum value, all yield defaults, because a settings file is not worth failing an app launch over. **A document this build cannot read at save time is never written over** — see [When the document cannot be read](#when-the-document-cannot-be-read), which is the one behaviour here that changed rather than accumulated. **Writing is atomic and owner-only** — a temp file in the same directory, then a rename, with mode `0o600` on Unix. And **writing preserves what this build does not understand**: the save merges the serialised struct into the document already on disk rather than replacing it, so a section written by a newer build survives an older build's save. That last one is not something `#[serde(default)]` gives you — without `deny_unknown_fields` it means *ignore*, not *retain*, so the unknown table is dropped at load and the next save would write over it.

**It preserves layout as well as data, and [#825](https://github.com/vfarcic/dot-agent-deck/issues/825) is where the second half arrived.** The merge used to round-trip through `toml::Table`, which models data, so every save re-rendered the whole document canonically and a user's own comments went with it — which matters because this file is one PRD #803 explicitly invites people to hand-edit. It runs on a `toml_edit::DocumentMut` now and writes only the keys whose data actually changed, so comments (including a trailing one on a line whose value *did* change), inline tables, inline arrays, key order and blank-line grouping all survive, and a save with no new value to write leaves the file byte for byte as it found it. Two limits, because the wide claim would be false: a **changed** list is rewritten whole, so a comment between `[[endpoints.remote]]` rows goes with it the first time a deck is added, removed or edited — an unchanged list is left alone; and a document missing a section this build owns gains that section, appended after what is already there. `merged_document`'s doc comment in `settings.rs` carries both, and the properties are pinned by `a_hand_written_comment_survives_an_app_driven_save`, `a_save_that_changes_nothing_rewrites_nothing` and `a_comment_inside_a_list_survives_until_that_list_changes`.

**This is why the desktop crate names `toml_edit` and not `toml`.** It is a replacement rather than an addition: two TOML libraries in one crate means two parsers that can disagree about the same file, and `toml_edit::de::from_str` / `toml_edit::ser::to_string_pretty` cover every call site `settings.rs` had, byte-identically (pinned by `default_document_shape_is_pinned`). It costs nothing in dependency surface either — the root package has declared `toml_edit = "0.25"` since PRD #20 for exactly this reason (a format-preserving edit of the user's own `~/.codex/config.toml`, where a serde round trip would strip every comment), the desktop crate already links the root package, and the switch adds **zero** packages to `Cargo.lock`: the only change is two feature edges (`serde_core`, `serde_spanned`) on a crate that was already being built, both of which the graph already had through `toml`.

### When the document cannot be read

A `desktop.toml` this build cannot load leaves the app on defaults **for the sections it could not read**, and blocks every save until the file is fixed or removed ([#1072](https://github.com/vfarcic/dot-agent-deck/issues/1072)). Four things follow from that and all four matter:

- **Only the refused section goes to defaults.** `toml_edit::de::from_str` is all-or-nothing, so until PR #1163 one value this build rejected reset *everything* in memory — the user's `[endpoints]`, their appearance, their zoom, their deck selection — and a stale `[voice]` token from an older build was enough to do it. `sections_this_build_can_read` now judges each top-level key on its own and, for a section that fails as a whole, each of its children the same way; what survives is kept and only what does not is defaulted. The recursion stops at the section rather than the field, **deliberately**: pruning one refused key would leave its siblings, and for a voice stage a surviving `backend = "remote"` beside a dropped `endpoint` fills the endpoint from the *hosted* preset, which is a user's audio going somewhere they did not write. Nothing about the bullet below changes — the file itself was never modified, and still is not.
- **The app still starts, and starts normally.** Refusing to launch over a preferences file would be disproportionate and would hand anyone who can write that file a denial of service on the whole app; the existing "loading never fails" property is deliberately kept.
- **The reason is on screen, not only in the log.** `load_document` returns a `SettingsDocumentProblem`, `load_snapshot` puts its public half on the snapshot as `problem`, and `useDesktopSettings` folds it into the same slot a failed save uses, so every settings panel renders it the moment the sheet is opened — before the user has changed anything. It is a **locator** (`line 3, column 9`) plus fixed prose, never a byte of the document; the footer beside it names the file.
- **`save_to` refuses rather than merging.** It re-reads the document, parses it as `DesktopSettings` — not as a bare TOML document, because a document that is valid TOML which this build's *schema* rejects is equally unreadable and equally destructive to merge into — and returns an error naming the line. The file is left byte-identical. The check is at **save** time rather than inherited from the launch read, which is what makes the remaining window a microsecond-wide race between two writers rather than the whole lifetime of the app: a document that becomes unreadable between that read and the rename is still replaced, which is [#828](https://github.com/vfarcic/dot-agent-deck/issues/828)'s subject and the same residual the path vet already accepts.

**What this replaces is worth stating, because it shipped and it was silent.** The load answered every failure with defaults and an `eprintln!`, so the app came up looking like a fresh install. The next save then merged the struct over the file, and the merge makes the struct authoritative over every key it owns — appearance, zoom, endpoints and the deck selection all replaced with defaults. The symptom a user saw was not an error; it was "my settings reset themselves", one click later, with the original already gone.

**A schema that is still moving is the ordinary route into all of this, and PRD #802's `[voice]` section is the worked example.** Its three keys were scalars — `transcription = "off"` — and two of them became tables. A document written by that build therefore hands the parser a **string where a table is expected**, which is a *type* error rather than an unknown token, so the folding deserializers every `[voice]` enum has never get a look in and `#[serde(default)]` does not fire for a value that is present and wrong. It is not a corrupt file, nobody hand-edited it, and it cost the whole document. `StageSpec` in `settings.rs` is what stops it failing at all: a stage accepts either shape, and a bare token means *this backend, and say nothing else* — so the value lands on that backend's current coordinates through `for_backend`, and a token naming a backend this build deleted folds to the default the same way any unknown token does. The first save after that writes the section back in the new shape, so the migration completes rather than being re-folded on every load. **If you change a section's shape, write the fold at the same time** — a partial recovery is the safety net, not the migration.

**The failure is reachable without anyone hand-editing anything.** An interrupted write, a dotfile-sync conflict marker or disk damage all produce it. So does this codebase's own habit of tightening field validators, which converts previously-valid documents into unloadable ones: `port` moved to a newtype over `NonZeroU16` after a hand-edited `port = 0` reached OpenSSH as `-p 0`, and PRD #742 made `EndpointId::parse` reserve `all`. That is why the fix is here rather than in a rule about who may edit the file.

**`desktop.toml` stays hand-editable**, which #1072 raised as an open question. The argument for dropping it is that someone who edits config by hand is not reaching for a desktop GUI. It does not survive contact with the list above: dropping it removes neither the failure nor the need for this fix, while `DOT_AGENT_DECK_DESKTOP_CONFIG`, [Configuring a remote deck by hand](#configuring-a-remote-deck-by-hand) and PRD #803's "a file a user can read, edit and delete without the app running" all depend on it. The validating newtypes are what make hand-editing safe rather than what makes it costly.

**Two things this does not do.** There is no in-app "replace it anyway" button: the destructive action is exactly the one being prevented, the message names the line to open, and a user who wants defaults can delete the file. And a *save* failure is still not a *load* failure — a read-only directory or a full disk reports "saving failed, your change will not survive a restart" and leaves the change applied, because there the document on disk is fine.

### The ownership boundary

The rule the whole app is arranged around, and the thing to read before deciding where a new setting goes:

> The desktop app gets everything from the daemon, wherever that daemon runs. The only thing it owns is its own settings.

| | |
| --- | --- |
| **Daemon-owned** | the project config, the coordinator context, cwd and project paths, available orchestrations/modes/roles, dispatch targets, the agent list and PTY streams, telemetry |
| **Client-owned** | settings — endpoints, appearance, model backends — plus genuinely presentational state: window size and position, focused tab, zoom |

The criterion for anything not on those lists, stated so it can be applied without re-deriving the reasoning: **a setting is client-owned when it describes the client itself — this machine, this display, this installation. If it describes the work, the project, or the machine the agents run on, it is daemon-owned and does not belong in this store, however convenient it would be to put it here.**

The failure this prevents has already shipped once. Reading `.dot-agent-deck.toml` client-side gives the right answer whenever the daemon is local, so nothing looked wrong until a real remote session put a local `/Users/…` path in the header beside the remote's `/home/…` panes. A rule that applies only to remote leaves the same trap for the next feature; a rule that applies always cannot. The converse matters too: **if a settings item ever needs the daemon to know about it, that item is by definition daemon-owned** — and at that point CLAUDE.md rule 12's cross-version contract check applies to it, on the other side of the line.

Everything in the store is deliberately about *this installation on this machine*, and none of it syncs. An endpoint describes reachability from here — a socket path, an ssh host, a forwarded socket. An appearance override is about this display. Downloaded models are on this disk. The cautionary precedent is the TUI's: `keybindings_path()` resolves through `platform::paths::config_dir()` on the machine the *process* runs on, and `dot-agent-deck connect` runs the TUI on the remote, so a `keybindings.toml` edited on the laptop is never read. Never store desktop settings daemon-side, and never read daemon-owned state from the client's disk.

### Adding a setting

Three steps, and the contract is deliberately this small. Adding a **setting** is step 1 plus an edit to your own panel; adding a **section** is one registry row and one component. Neither requires touching the store, the sheet, or anything belonging to another feature.

1. **Storage.** Add a field to your feature's section struct in `desktop/src-tauri/src/settings.rs`, or add a new section struct plus one line on `DesktopSettings`. `#[serde(default)]` throughout gives it a default; there is deliberately no `deny_unknown_fields`, so a field a newer build writes survives an older build reading the file. Field names stay `snake_case` and single-word where that is natural: the same struct is serialised to TOML for the user *and* to JSON for the webview, and every name today is one word, so the two agree byte for byte. The first genuinely multi-word field is the point at which a separate webview DTO has to be introduced — not a `rename_all` on this struct, which would make the TOML read badly.
2. **UI.** Add one row — `id`, `label`, `icon`, `component` — to `SETTINGS_SECTIONS` in `desktop/src/lib/settingsRegistry.ts`, and own your panel component. The registry is a module of its own rather than a constant inside the sheet, so registering never means editing a #803-owned rendering component and two dependents cannot collide on adjacent lines of one array; `SettingsSheet.tsx` reads it and is otherwise none of your business. Implement `SettingsPanelProps` from `desktop/src/lib/settingsContract.ts`: that file *is* the type to build against, and it is short on purpose. A panel is an ordinary React component and owns its own layout — there is no generic key/value renderer, because #741's endpoint list and #802's model manager are not key/value widgets and a renderer built to fit both would fit neither. Send the **whole document** to `onSave` (spread `settings`, replace your own section) so a save can never drop a section this build's UI has not loaded.

   **That is necessary and not sufficient, which PRD #741 M6 found out the hard way.** The document a panel spreads is the one `normalizeDesktopSettings` produced, and that function constructs a fresh object with a **fixed key set** — which is itself a credential guard (check 3 below), so it is not going away. A section the frontend does not declare is therefore already gone before any panel spreads it, and `desktop_set_settings` then merges the struct it decoded over the file. If the field is a plain section struct, `#[serde(default)]` turns "the webview said nothing" into "the webview said empty", and the merge writes that empty section over the user's data. So a section whose frontend half has not landed yet is declared `Option<…>` on `DesktopSettings`: `None` means *unspecified*, TOML omits it, and `merged_document` preserves what is on disk — the same protection it gives across builds, now across clients. `#741`'s `endpoints` is the worked example, and the rule that comes with it is that the *frontend* half has to round-trip the section the moment it can render it, never fabricate a default for it.
3. **Secrets never go in the document, and never in `localStorage`.** The document may hold a non-secret *reference* — which backend holds the key, or a boolean saying one is stored — and nothing more. A real credential belongs behind the `SecretStore` seam in `desktop/src-tauri/src/secrets.rs`: store/load/delete keyed by a `SecretId`, over the OS keychain. PRD #803 M5 named it and PRD #802 M4 built it, and #802's `[voice]` section is the worked example of what the rule costs in practice — the *choices* became closed enums and the *key* went to the keychain, so the document holds no field for it at all. Note it holds no boolean either, although the rule would allow one: the panel asks the store, so the answer cannot go stale against the keychain. #802's later provider work is the **second** half of that example and the more instructive one: making each stage's endpoint and model user-selectable needed two values that genuinely are text, and the answer was two validating newtypes in `desktop/src-tauri/src/model_service.rs` — `ServiceUrl`, which refuses a `user:password@` authority and allows `http` only to loopback, and `ModelId`, bounded at 128 bytes over a charset with no whitespace. Neither is a way around the guard; each is a written claim about what that field can hold, which is what an `ALLOWED_FIELD_TYPES` row is. **A field-level claim is not always the whole rule, though, and #802's speech stage is where that bit.** `TranscriptionBackend::Local` means exactly one thing — *no `Authorization` header is sent* — and `ServiceUrl` on its own will accept any `https://host`, so the two fields are each valid and the *pair* is a captured utterance uploaded to a third party with no credential on it, reachable by picking the keyless backend in the panel and then editing the Endpoint field. Where a rule spans two fields it has to be enforced where the struct is built: `TranscriptionSettings`'s hand-written `Deserialize` refuses the pairing — covering the document and the IPC seam, which is every route an untrusted value arrives by — and `HttpTranscriber::keyless` refuses it again at construction, which is the route Rust arrives by. The panel still offers the Endpoint field for that backend, because a user who publishes the container on another port has to be able to say so; it narrows the hint instead, and is not the boundary. The next section is what enforces that, and the practical consequence for step 1 is short: **your field's type has to be on `ALLOWED_FIELD_TYPES`** in `xtask/linkage-check/src/desktop_settings_secrets.rs`, and `String` is deliberately not on it. If you need free text, that is the conversation to have before writing the field, not after.

**A setting is one row, and the row is two columns** — a **132px label column**, a 16px gutter, then the control left-aligned in the second column. Not "label left, control right": the row is 743px wide in the desktop sheet, so a flush-right control leaves ~500px of nothing between a label and the thing it labels, and the two stop reading as one setting. The label column is a fixed width rather than `auto` because that is the entire reason to use a grid here — every row's control starts at the same x, so a panel of ten settings reads as two columns instead of a ragged stack, and a dependent panel gets that alignment without measuring anything. 132px is chosen against labels that do not exist yet: it fits any two-word label at the size below (`Daemon endpoint` is 130px, the longest realistic one), and it is about the widest column that still leaves the control looking attached to its label. A longer label wraps inside its column and grows the row by one line rather than breaking anything — `Downloaded models` is 148px, and it takes the row from 27px to 30px. Below a 420px viewport the row stacks, label over control, because the columns plus a segmented control need 322px and the sheet goes full-bleed under 680px; that fallback is the one place the row is not two columns.

**A row's label is never a `<legend>`, and that is a WebKit finding rather than a preference** ([#1032](https://github.com/vfarcic/dot-agent-deck/issues/1032), found by PRD #741 M7, measured on the browser tier). `AppearancePanel` used to put the row's visible label in a `<legend>` and float it, because a floated legend stops being the fieldset's *rendered* legend and can then be an ordinary box among the fieldset's contents — the grid item in column one. Chromium honours that. **WebKit forces the rendered legend's `float` to `none`**: measured at the same `?fixture=1` page in both projects, `getComputedStyle(legend).float` is `"left"` under `chromium` and `"none"` under `webkit`, so the legend never became a grid item there, the segmented control landed in column **one** beside its label 148px to the left of every other control in the sheet, and the 132px column was wasted. The app ships on WebKit — WebKitGTK under Tauri on Linux, WKWebView on macOS — so the fieldset form was the one that did not hold where it matters, and the form that worked was the one only Chromium saw. Nothing caught it because no browser spec asserted settings-row geometry until `e2e/decks.spec.ts` did.

**So: use a `<span class="settings-row-label" id>` plus `role="radiogroup" aria-labelledby` on the control for a row that needs a group label**, which lays out identically in both engines and keeps the accessible group name — `EndpointsPanel`'s deck chooser and the converted `AppearancePanel` are the worked examples, and `DeckSelector`'s menu uses the same form outside the sheet. A row whose label is a plain `<label for>` — the select rows, `ZoomPanel` among them — was **never** affected: a single control needs no group, and a `<label>` is an ordinary grid item in both engines. There is no `<fieldset>` left in the app, and no `.settings-row > legend` rule in the stylesheet.

**The convention is pinned by measurement rather than by this paragraph.** `desktop/e2e/settings-rows.spec.ts` walks every section the registry offers, in **both** browser-tier projects, and holds each row to the same numbers `e2e/decks.spec.ts` holds the Decks panel to: every label and every control starts at the same x across the whole sheet, the gutter is 148px, the first grid track is `132px`, and every label is 13px/650. A panel registered later inherits the guard without anyone remembering to write one — which is the half that was missing when two panels were free to diverge. It also asserts directly that no row labels itself with a legend, so a failure names the cause rather than only reporting that a control moved.

**The label is 13px at weight 650, and that is part of the convention** — the row label is a sub-header, not body text, so it holds its own against the control beside it and gives a list of rows something to scan down. It sits well below the sheet's own 20px title, one notch lighter than the 13px/680 the app's in-page section headings use, and larger than the 11px of the sheet's section list — a sub-header inside a panel, not a heading over one. Copy it; a panel whose labels are 11px next to another panel's 13px is the same setting rendered at two different weights.

Pick the control by cardinality: a switch for a boolean, a **segmented control** for up to about four exclusive options with short labels (`.settings-row` + `.segmented` in `desktop/src/styles.css`; `AppearancePanel.tsx` is the worked example), a select beyond that. Per-option prose is the **exception, not the default**: write it only where an option's consequence genuinely cannot be read off its label, because a hint that restates its label costs a row and teaches nothing. Panel-level notes are a narrow exception with **no current instance**: the only one that ever existed — the terminals-stay-dark note in the Appearance panel — was deleted for failing the text rule below, and the `.settings-note` class went with it. If you think you have the first genuine one, that rule is the bar it has to clear, and you are adding the class back rather than reusing one.

**UI text earns its place by being actionable, not by being true.** The test is applied a sentence at a time: does it change what the reader does next? Explanation of *why* the software is built the way it is belongs in `docs/` and in the PRDs, and **never on a panel**. It is the easiest kind of text to justify keeping, precisely because it is accurate — which is exactly why it needs a rule rather than taste. Three worked examples, all of them deleted, because a deleted thing teaches this better than an abstraction: the Appearance panel carried a paragraph explaining why the agent terminals stay dark in both appearances — an engineering constraint, in vocabulary (truecolor SGR) nobody outside this repo has, answering a question nobody asked, while the *observable* fact needs no note at all; the settings sheet's header carried the client/daemon ownership boundary, an architectural principle for whoever adds the next setting, put in front of a user who only wanted dark mode; and the footer said the file was "readable, editable and deletable without Agent Deck running", which is reassurance about a design decision, leaving `Stored in <path>` as the actionable half. The first two are still written down — the terminals in this document and in `prds/743-desktop-light-dark-appearance.md`, the boundary in *The ownership boundary* above — which is where they belong. **An error, or a consequence the user has to act on, is a different thing and stays**: the Appearance panel's failed-save alert (`.settings-error`) tells the user their choice will not survive a restart, and that changes what they do next.

Why this is a rule rather than a preference: the sheet is 780px wide and full height, so **vertical space is the scarce resource**, and it is shared with every other feature's section. Appearance was first built as three full-width radio cards with a sentence of hint each — 195px for a single setting, and the hints ("Always light, whatever this machine is set to.") only restated their labels; as one row it is 27px. One row per setting is what lets a panel hold ten settings instead of three, and whatever density the first panel establishes is the one the next one copies.

**Do not put a heading above a control that already labels itself.** A section heading is chrome you add when there are sections to tell apart, not by default. With one section whose single setting shares its name, the heading was the word "Appearance" twice: the panel opened with a `.form-heading` carrying an `APPEARANCE` eyebrow over a `Light and dark` title, directly above a row whose own label then read *Appearance* from a `<legend>` — and `Light and dark` restated the option labels the three buttons were already showing. That cost 70px above a setting that occupies 61px, and it bought nothing a reader could not see. The row's own label is the one that survives, because it is doing two jobs the heading was not: it is the row's visible label *and* the group's accessible name, so `getByRole("radiogroup", { name: "Appearance" })` rides on it. (It was `getByRole("group", …)` on a `<legend>` until [#1032](https://github.com/vfarcic/dot-agent-deck/issues/1032) made it a `<span>` named through `aria-labelledby`; the accessible name is the property, and it survived the change.) The same round collapsed the sheet's own header to one line for the same reason — an `APPLICATION SETTINGS` eyebrow over `Settings` is one word twice — via a `.sheet-header.is-title-only` modifier, because the shared `.sheet-header` reserves 92px for the eyebrow/title/description stack that the four `ConfigurationPanels` sheets all still carry. Together: 223px of sheet chrome plus setting for one 27px control, now 119px — the setting itself did not move (61px both ways), all 104px came off the chrome. `App.test.tsx` pins it — the sheet holds exactly one heading, the panel holds none, and no `.eyebrow` or `.form-heading` survives on the surface.

**When [#741](https://github.com/vfarcic/dot-agent-deck/issues/741) or [#802](https://github.com/vfarcic/dot-agent-deck/issues/802) adds a second section, that is the moment to decide whether section headings earn their row** — and it is an open question, not a settled one, so do not pre-build for it. The argument against them survives the second section: the section column returns the moment the registry holds two rows, and it will already name the active section in the list beside the panel, so a heading repeating that name inside the panel is the same redundancy one level up. The argument for them is that two sections make a panel's identity worth confirming where the user is looking. Decide it with both panels in front of you; whichever way it goes, it is one rule for every panel rather than a per-panel choice.

Two things to expect while doing it. `default_document_shape_is_pinned` asserts the exact serialised default document, so **a new field fails that test by design** — updating it is the deliberate act that puts the ownership question in front of a reviewer. (An `Option` section whose default is `None` fails only the JSON half of that pin, because TOML omits a `None` field and JSON renders it `null`. The question still gets asked; the answer is just that the TOML default is unchanged, which is the point of the `Option`.) And the settings sheet **drops its section column while there is only one section**, rendering that panel full width; your registry row is what brings the column back, with no layout work on your side. Both directions are pinned with stub sections in `desktop/src/components/SettingsSheet.test.tsx`.

### The credential boundary

Two different kinds of check watch PRD #803's credential rule, and **confusing them is the mistake [#827](https://github.com/vfarcic/dot-agent-deck/issues/827) was opened about.** Read this before storing anything a user would mind losing.

`no_settings_key_name_trips_the_credential_tripwire` in `settings.rs` is a **naming tripwire, not a security boundary**. It fails the build on a key *name* containing `key`, `token`, `secret`, `password`, `credential`, `authorization`, `bearer` or `passphrase`, with an allowlist of full key **paths** (`voice.secret_backend`, not `secret_backend`, so an exception cannot travel to a same-named field in another section) — and since #827 each exemption also pins a **concrete type**, because an exemption granted for a boolean "is one stored" would otherwise keep covering that path after somebody changed the field to a `String` under the same name. What it still cannot see: a field called `endpoint` or `value` whose *value* is a token, and any value at all — it never looks at one. Read a pass as "nobody named a field like a credential", never as "a credential cannot get in here".

The checks that do look at values are these, and what they establish is narrower than an absolute — the narrow version is the one to quote:

| what it proves | where |
| --- | --- |
| **Every field in the Rust schema has a type from a pinned allowlist, and no field is free text.** Two strengths, and the difference matters: `u32`, `u16`, `AppearanceMode`, `ZoomLevel` and `Selection` cannot carry arbitrary text at all; the ssh-argument newtypes PRD #741 stores (`Hostname`, `SshUser`, `KeyPath`, `HostAlias`, `RemoteSocketPath`) and `EndpointId` **bound and charset-restrict** it, which is weaker — measured, a 45-byte `sk-…`-shaped token parses as a `Hostname`. What rules a credential out of *those* is where the value goes (each is handed to `ssh` as an argument and reaches no authentication surface), that key material and a passphrase are excluded by shape (`KeyPath` must start `/` or `~/`), and the policy that a credential goes behind `SecretStore`. A serde `skip`/`flatten` is refused too, because it hides a field from the document the sweep below walks | `desktop_settings_secrets::every_settings_field_has_a_type_that_cannot_carry_a_credential`, `settings::tests::an_ssh_argument_field_bounds_and_restricts_rather_than_forbidding_text` |
| **A uniquely-named sentinel reaches none of the sinks**: the `desktop_set_settings` echo, the `desktop_get_settings` snapshot, the parse diagnostic, both halves of `SettingsWriteError` including `safe_message` output, and both halves of `SettingsDocumentProblem` — which is the newest of them and the only one that reaches a **webview** as well as a log ([#1072](https://github.com/vfarcic/dot-agent-deck/issues/1072)). The **disk** sink is narrower than it was, and the old wording — *the document on disk across a load–modify–save round trip* — was true only because of the data loss #1072 fixed: the scrub was a side effect of publishing defaults over a document nobody had understood. A value that still lets the document load is overwritten by the round trip as before; one that makes it unreadable now stays in the user's own `0o600` file, which is the same answer already given for a key the schema does not own, and destroying a user's configuration is not a proportionate way to scrub a value they typed themselves. Both arms are exercised and the test fails if either stops being reached. Derived from the **default** document rather than a list, so a field #802 adds is covered the moment it appears *there* — which is the scope, not a formality: an optional field, or a field on a row in a list that defaults to empty, is outside this sweep because the default document does not contain it. PRD #741's endpoint fields are exactly that shape; the naming tripwire grew a populated `representative_document()` to reach them, and the value-side claim about them is made by `an_ssh_argument_field_bounds_and_restricts_rather_than_forbidding_text` instead. **What it reaches is the command *bodies*, not the commands** — a `#[tauri::command]` takes a `Webview`, which needs a running app, so `ensure_main_webview` and the framework's own argument decoding are outside every test in this crate ([#823](https://github.com/vfarcic/dot-agent-deck/issues/823) is the missing tier). `desktop_get_settings` is a one-line wrapper over `load_snapshot()`; `desktop_set_settings` wraps `save()` **plus** a `map_err` closure that logs `error.detail()` and returns `safe_message(error.public())`. `load_snapshot()` and `save()` are driven through the real path seam against a real file; the closure is reached by neither, and is covered instead by `no_settings_write_error_…`, which asserts both of the values it can emit are sentinel-free | `settings.rs`'s `a_credential_from_the_webview_…`, `a_credential_at_a_known_schema_leaf_…`, `the_settings_commands_own_bodies_…`, `a_parse_diagnostic_…`, `no_settings_write_error_…` |
| **The TypeScript DTO's shape is pinned field by field.** A frontend-only `apiKey` on `DesktopSettingsDto` used to be declared where nothing read it, because the Rust tripwire cannot see `bridge.ts` | `desktop_settings_secrets::the_typescript_settings_dtos_carry_exactly_the_pinned_fields` |
| **`normalizeDesktopSettings` constructs its result and never spreads its input**, so an undeclared field — a credential under any name — is dropped rather than carried into `localStorage` or into app state | `desktop_settings_secrets::the_settings_normaliser_…` (structural) plus `bridge.test.ts`'s three runtime tests |
| **The `localStorage` key set is pinned**, constant and literal, and `localStorage` may only be spelled `localStorage.<op>(...)` so an alias cannot name a key the scan is unable to read. Scope is `.ts`/`.tsx` under `desktop/src`: `sessionStorage`, IndexedDB, `document.cookie` and a `.js` file are outside it, and none of the four appears there today. Indifferent to [#824](https://github.com/vfarcic/dot-agent-deck/issues/824)'s question of which keys *belong* there | `desktop_settings_secrets::the_localstorage_key_set_is_pinned_and_holds_no_credential` |

**Three things none of that proves, stated because a boundary described too widely is worse than one described honestly.**

First, **a key this build's schema does not own keeps its value on disk.** The save merges rather than replaces, which is what stops an older build eating a newer one's section — so a credential a *user* or a *newer build* wrote into `desktop.toml` stays there. What it cannot do is reach anything else: the struct drops it at load, so it is absent from the IPC echo, from the settings snapshot and from every rendering of the document. `a_key_this_schema_does_not_own_keeps_its_value_and_reaches_nothing_else` measures exactly that rather than glossing it.

Second, **a refused IPC payload's serde message can quote the value back to the webview that sent it.** Nothing is disclosed to a party that did not already hold it, so this is a scope statement rather than a hole: the asserted claim is "no sink outside the sender".

Third, **the `SecretStore` now exists and "a credential can only enter through it" is still not assertable.** PRD #802 M4 built the seam #803 M5 named — `desktop/src-tauri/src/secrets.rs`, store/load/delete keyed by a [`SecretId`], over the OS keychain through `keyring` — and the field-type check is what forced it: adding the `[voice]` section turned that test red at four fields, and the resolution was closed enums for the choices and the keychain for the key. **Going red is how it said so, which is the mechanism working.** What is *not* assertable is the completeness claim: nothing stops a later feature storing a credential somewhere else entirely, and no check here would see it. What survives is the narrower claim PRD #741 M6 left behind — nothing in this schema is free text, every field is bounded and charset-restricted by a deserializer, and adding one that could hold a PEM block or a padded base64 blob means editing `ALLOWED_FIELD_TYPES` in a required gate.

Two properties of that store are worth knowing before reaching for it, because both are deliberate and both are easy to undo by accident. **There is no IPC command that reads a credential back** — the webview may ask whether one is stored, replace one and forget one, and `SecretStore::load` exists for the Rust side alone, where the backends make their network call anyway because the CSP forces every network hop there. A command returning a value to the webview would put it one `JSON.stringify` from the `localStorage` half of the rule. And **a failed store is an `Err`, never a status saying "nothing stored"**: the outcome the design is against is a user who thinks their key is saved and finds the feature broken tomorrow, so the three situations that produce a failure — no credential store on this machine, a locked one, and anything else the platform reports — each carry their own sentence naming what did not happen.

[`SecretId`]: ../../desktop/src-tauri/src/secrets.rs

One behaviour changed to close a sink rather than to name it. The load diagnostic used to print `toml::de::Error`'s own `Display`, which echoes the offending value **twice** — in a rendered source line and again in serde's `invalid type: string "…"` message — so a hand-edited document's bytes went straight into this process's stderr and the deck log, which is a file people paste into bug reports. `invalid_document_log` now prints a **locator** instead: the file and `line N, column M`, and nothing from the document. The trade is deliberate and it loses the `expected u32` half; the line is the line to open, where the offending text is in front of you anyway. That rule now governs two sinks rather than one — [#1072](https://github.com/vfarcic/dot-agent-deck/issues/1072) put the same locator in front of the **user**, so `parse_locator` is shared by the log line, the `SettingsDocumentProblem` the settings surface renders and the refusal `save_to` returns, and all three are a locator plus fixed prose.

Both scanners are in a **required** gate, and that placement is the point: they run under `cargo test-fast` (via `--workspace`) and in the CI `build` job, while `desktop-web` — where a vitest guard would run — is advisory and can be merged past. Same reasoning as the palette guards, and the same fail-closed discipline: every filesystem error fails the test rather than removing a file from the scan, and a symlink is refused rather than followed.

### Two persistence mechanisms, for now

The settings document is not the app's only storage. **Five keys** live in `window.localStorage`. Four are namespaced by `modeScopedKey` (`desktop/src/lib/bridge.ts`) with a `.live`/`.fixture` suffix — agent profiles, the prompt library, workflow role order, and the agent overview's column choice — and the fifth, `FIXTURE_SETTINGS_KEY`, is deliberately **unscoped**, because it holds the fixture preview's settings document and a theme choice is global. ([#824](https://github.com/vfarcic/dot-agent-deck/issues/824) and this document both said "four" until #827 pinned the set; the overview columns arrived with PRD #745 and the fixture settings key with #803, neither of which updated the count, and #819 M6 then deleted the project library key, taking the set from six to five. The whole set is pinned by `desktop_settings_secrets::the_localstorage_key_set_is_pinned_and_holds_no_credential`, so a drift now reddens a required check instead of leaving a stale sentence — that pin caught this very deletion on the merge, and its scope is `localStorage` in `.ts`/`.tsx` under `desktop/src`.) **Three of the five are per-project draft content** — agent profiles, the prompt library and workflow role order — which the boundary rule above puts daemon-side, so they were deliberately **not** migrated into `desktop.toml`: that store is per-installation too, and moving them there would entrench them on the same wrong side while looking resolved. [#824](https://github.com/vfarcic/dot-agent-deck/issues/824) tracks those three and [#819](https://github.com/vfarcic/dot-agent-deck/issues/819) is the plausible home; #819 M6 has already taken the worst of them, the project library, out of `localStorage` altogether. The other two are not in that argument: the overview column choice describes this display and the fixture settings key exists only so a browser preview survives a reload, so neither is waiting on #819. Whichever way #824 goes: **do not add a sixth `localStorage` key by pattern-matching.** Apply the criterion first — app preference goes in `desktop.toml`, project content waits for #819 — and note that since #827 the answer is no longer only a convention: the key set is pinned in a required gate, so a new key is a deliberate edit to `PINNED_STORAGE_KEYS` and a review conversation rather than a line nobody sees.

## Zoom and text size

`Cmd +` / `Cmd -` / `Cmd 0` on macOS, `Ctrl` equivalents elsewhere, scale the whole window — chrome and agent terminals alike — and the level persists in `[zoom]` in `desktop.toml`. The same level is visible and settable in the settings sheet's **Zoom** section, because a webview zoom has no indicator of its own and 110% against 100% is not a difference you can read off the screen. The ladder is ten levels from 75% to 300%, and 100% is what `Cmd 0` returns to. PRD #744 has the design.

### Webview zoom, not a CSS font scale

The mechanism is `webview.set_zoom()` — `webkit_web_view_set_zoom_level` on Linux, `WKWebView.setPageZoom` on macOS 11+, WebView2's zoom factor on Windows. **The alternative was not a real alternative**, and the measurement is worth keeping because it will come up again: `desktop/src/styles.css` contains **zero `rem` units** and 164 `font-size` declarations, every one of them in `px`, among 960 `px` literals. A root font-size is therefore not a scaling seam in this app — proposing one is proposing to rewrite those literals first — and it would still leave the terminals alone, because xterm renders to a canvas from its own `fontSize` option. Page zoom needs no stylesheet change and scales the panes with everything else.

Page zoom shrinks the **CSS-pixel** viewport, so the app's existing breakpoints (1450, 1260, 960, 680, 420) do the reflow: at 1440 × 1.5 it lays out at 960 CSS px and degrades exactly as it does when you narrow the window. That is also where the 300% ceiling comes from — `body` is `overflow-x: hidden`, so horizontal overflow is *clipped* rather than scrollable while vertical overflow merely scrolls, which makes width the binding constraint. `tauri.conf.json` declares `minWidth: 1024` and `styles.css` declares `min-width: 320px`; 1024 / 3.0 = 341 is clear of the floor and 1024 / 3.2 = 320 reaches it exactly. The 75% floor is the other end of the same argument: the agent footer is 6.8px, which renders at 5.1px there.

### Why `zoomHotkeysEnabled` is not used

Tauri ships a window option that looks like this feature in one boolean, and on macOS and Linux it injects a hotkey polyfill (`tauri/src/webview/scripts/zoom-hotkey.js`). Read it before reaching for it: it persists nothing (`let zoomLevel = 1`, so every launch is 100%), fires one un-coalesced IPC per keystroke, never re-fits the terminals, ranges 0.2–10.0 in additive 0.2 steps, calls no `preventDefault` in its keydown handler, and on **Windows** is not the polyfill at all — there the flag sets WebView2's `IsZoomControlEnabled` and `IsPinchZoomEnabled`, so the steps and range are the runtime's. It also needs `core:webview:allow-set-webview-zoom`, which we would otherwise not need at all.

### The call goes through Rust, and so does the launch apply

Two things follow from that permission. **`core:webview:default` does not include `allow-set-webview-zoom`** — its webview default set is `allow-get-all-webviews`, `allow-webview-position`, `allow-webview-size` and `allow-internal-toggle-devtools`, and this app's only capability is `["core:default"]`. So the frontend route (`getCurrentWebview().setZoom()`) would be denied, and widening the webview's own core surface to reach a call Rust can make itself is more privilege for no gain. The zoom is a `desktop_set_zoom` app command instead, behind `ensure_main_webview` like every other one, reached through the `DeckBridge` seam so fixture mode's no-op is structural.

**The stored level is applied Rust-side at launch**, in `run()`'s `setup` hook, before the webview has run any of our JavaScript — otherwise a user stored at 150% watches the app paint at 100% and jump. The frontend applies only on *change*. Note this is a deliberate **departure** from the appearance arrangement above, whose one effect applies on load *and* on change precisely so there is no second path to disagree with: zoom takes the split because it has a Rust-side option that removes a visible flash, and pays for it with the failure that split invites — if the Rust leg regresses, the window comes up at 100% while the Zoom row reads the stored level. `useZoom`'s applier effect deliberately records its first value without applying it, which is what stops the frontend fighting the launch apply. If the flash turns out to be imperceptible, collapsing to the appearance shape is the better design.

### The zoom keys are claimed in the capture phase, and that is load-bearing

**Do not move this listener to the bubble phase, and do not fold it into `App.tsx`'s handler.** xterm binds its own `keydown` on `.xterm-helper-textarea` — focused whenever an agent pane has focus, which is most of the time — and its key evaluation maps `Ctrl` plus a single-character key with `keyCode >= 48` to that literal character, and `Ctrl _` to 0x1F (undo in readline and emacs). `-` is keyCode 189, `=` is 187 and `0` is 48. In the bubble phase a window-level listener runs *after* the target's own handlers, so every zoom keystroke would have zoomed **and** written a byte into the focused agent's PTY, and `preventDefault()` cannot retract a sent byte.

`useZoom` therefore registers on `window` with `capture: true` and calls both `preventDefault()` and `stopPropagation()`, so xterm never sees the event.

**Where it is called from is load-bearing too, for a different reason.** `useZoom` is called in `DeckShell`, above the view switch, because that is the only component mounted for every screen. It was first called inside the deck — which `DeckShell` renders *instead of* the overview, not around it — so the zoom keys were dead on the Overview screen while working everywhere else. Note the symptom this produced, because it is easy to misread: the *level already applied* stayed applied there, since a webview zoom is a property of the webview rather than of anything React renders. Only the keys stopped responding. A screen added later inherits zoom by sitting under `DeckShell`; one that renders its own settings state instead of taking `DeckShell`'s would re-create the bug. Two things make this worth a paragraph rather than a comment. First, the failure is **platform-split**: the macOS binding uses `metaKey`, which xterm's branch excludes, so a Mac reviewer sees nothing wrong and only Linux and Windows type into the terminal. Second, `App.tsx`'s handler returns early on `editing`, which is true for `.xterm-helper-textarea` — so folding zoom into it would *also* break zoom in the app's normal state. The guard is `claims the keys before a descendant listener can see them` in `desktop/src/hooks/useZoom.test.ts`; if that test goes red, the zoom keys are typing into somebody's agent.

### Three rates, and the one that was already right

A zoom change does three things and each is rate-limited differently, because each costs something different.

| What | Rate | Why |
| --- | --- | --- |
| `set_zoom` on the webview | every change, unthrottled | it is what the user is waiting to see |
| `fit()` on every mounted pane | one pass per animation frame | `fit()` reads layout, so a held key would force one reflow per pane per key repeat |
| the write to `desktop.toml` | trailing flush every 400ms | each write is a temp-file-plus-rename |

The **daemon** resize is not in that table, and that is the point: each `fit()` reports through `TauriDeckBridge.resizeTerminal`, which already coalesces per agent on its own frame with a single-in-flight gate. Nothing about zoom touches it, and nothing should — a second layer there would reinvent something that works.

The persist is a *trailing flush* rather than a debounce, so a key held for ten seconds writes once every 400ms instead of writing nothing until it is released; a debounce loses the level entirely if the app dies mid-burst. `useZoom` also flushes a pending level on unmount, so the last change of a session survives.

### The re-fit is explicit, because relying on the observer is a trap

Each pane already re-fits from a `ResizeObserver`, and under page zoom that observer does fire — but **only because of the pane's width**, which descends from the viewport. Its height does not follow: `.agent-panel` is `min-height: 320px; height: 42vh; max-height: 560px`, and at the declared default window height of 920 the `42vh` term is 386px at 100% but 309px at 125%, so from 125% upward it is pinned at its floor and stops moving — six of the ten ladder steps. So "the box shrinks with the viewport" is half true, nothing in the code says the terminals depend on it, and jsdom has no layout engine to observe it either way. `TerminalViewport` therefore registers its `fit` with `terminalRegistry`, and a zoom change calls `refitAllTerminals()`; the observer stays as the backstop that catches the real post-layout geometry.

### What none of this proves

jsdom runs no layout engine, paints nothing and computes no geometry, and there is no driver-level or browser-level tier ([#823](https://github.com/vfarcic/dot-agent-deck/issues/823), [#836](https://github.com/vfarcic/dot-agent-deck/issues/836)). The tests cover the ladder, the key matching, the capture-phase interception, all three coalescers, the persistence round-trip and that a re-fit was requested. **They cannot show that anything got bigger.** Five claims rest on the manual check below and nothing else: that the webview actually scales, that `fit()` yields the right rows/cols afterwards, that the layout holds at 300% and 75%, that the terminal canvas re-renders crisply at the new device pixel ratio, and that the Rust launch apply happens at all.

## Appearance and theming

The app follows the OS light/dark appearance by default, with an in-app override — Light, Dark or System — in the settings sheet's Appearance section. `System` is the default and it **removes** the `data-theme` attribute rather than writing a value; the two are not interchangeable, because there is no `[data-theme="system"]` block and an absent attribute is what lets the media query decide, live and in both directions. The override is `desktop/src/lib/appearance.ts`, the panel is `desktop/src/components/AppearancePanel.tsx`, and the value persists in `[appearance]` in `desktop.toml`. PRD #743 has the design.

### The choice is on the first painted frame

An explicit choice is applied **before the webview parses the document**, not by the React effect that keeps it applied afterwards. `desktop/src-tauri/src/appearance.rs` reads the stored mode with `settings::load_snapshot()` — which is synchronous, so the answer exists before the window does — and registers it as a Tauri initialization script through a plugin's `js_init_script`. The script sets `data-theme` on the document root, and Tauri runs it after the global object is created and before the document is parsed, so the palette is already right when the page first paints.

**This is an ordering fix, and the two halves only work together** ([#845](https://github.com/vfarcic/dot-agent-deck/issues/845)). Without the injector the frontend cannot possibly be early enough: the document arrives over `invoke`, which is async, and until it does the root carries no attribute — which `styles.css` reads as "follow the OS", so a user who chose Dark on a light machine watched a light window turn dark. Without the frontend half the injector is silently undone: `App.tsx`'s appearance effect used to run on mount with the hook's unread placeholder, mode `system`, and applying System is a `removeAttribute` — indistinguishable from doing nothing on a fresh document, but a clobber on one the injector had already seeded.

The gate on that effect is `chosen`, not `loaded`, and the difference is load-bearing. `loaded` means the disk has replied; `chosen` means the mode is one somebody actually chose, which is **also** true of a click made while the initial read is still in flight — and nothing gates the appearance radios on `loaded`, so that click is reachable. A bare `if (!loaded) return` passes the clobber tests and breaks PRD #743's no-restart requirement; `desktop/src/App.test.tsx` pins both directions, and reverting the gate to `loaded` turns the control red.

A plugin rather than `WebviewWindowBuilder::initialization_script` is deliberate: the builder route needs `"create": false` in `tauri.conf.json` and window creation moved into `setup()`, and a window that fails to build is an app with **no window** — which nothing in CI would catch. `js_init_script` reaches every webview the app creates without changing how any of them are declared.

**What is verified and what is not.** The Rust tests assert the seam where the ordering is decided: that the plugin hands the webview the script for the stored mode, on the main frame, and that System yields no script at all. The vitest tests assert that mounting leaves a pre-seeded root alone until it has read a document that could replace it. Neither observes a paint — jsdom has no layout ([#836](https://github.com/vfarcic/dot-agent-deck/issues/836)) and no driver-level tier exists ([#823](https://github.com/vfarcic/dot-agent-deck/issues/823)) — so **that the first frame is actually correct is checked by the manual walk below and nowhere else.** Deleting the `.plugin(appearance::init())` line is caught, though not by a test: the module goes dead and `-D warnings` fails the build.

### Every colour is a token

`desktop/src/styles.css` opens with the palette on `:root` — 66 colour tokens plus three layout ones — and **no colour is written anywhere else**. That is not a style preference: a hex literal in a rule cannot follow a theme, so it stays light in dark mode, and the decay is invisible because nothing renders dark mode in CI. The families:

- **Light chrome** — surfaces (`--canvas`, `--paper`, `--paper-strong`, `--paper-sunken`, `--paper-sunken-strong`, `--skeleton`), text (`--ink`, `--ink-soft`, `--muted`, `--faint`, `--on-accent`), lines (`--line`, `--line-strong`, `--dialog-edge`).
- **Accents**, each a family at several depths: `--teal-*` (primary), `--green-*` (passed/done), `--amber-*` (waiting/caution), `--red-*` (failed/destructive), `--violet-*` (needs a human).
- **Live status** — `--status-ok`, `--status-warn`, `--status-error`. One family rather than two, because they are read against both the light chrome and the dark instrument chrome.
- **Instrument chrome** — `--terminal`, `--shell*`: the dark surfaces wrapped around the terminals (the rail, the tab strip, the agent footer, the output panels, the reader overlay). These stay dark in both appearances, so most of them keep their light value.
- **Channel forms** — `--scrim-rgb`, `--paper-rgb`, `--teal-rgb`, for colours also used at partial alpha. `rgb(var(--scrim-rgb) / .12)` keeps the alpha at the call site so one token serves every depth. `--paper-rgb` and `--teal-rgb` track the token they are named after; **`--scrim-rgb` tracks nothing** — its role is the nine scrims and drop shadows, and it goes to black in dark rather than following `--ink`, because a shadow cast in the near-white dark `--ink` would light the app up instead of darkening it. It was called `--ink-rgb` until that name invited exactly the wrong edit.

When you need a colour, use an existing token. When you genuinely need a new one, add it to `:root` **and** to both dark blocks; the guard below fails the build if you add it to one and not the others.

### The two dark blocks, and why both are needed

Light values stay on bare `:root`, so a document with no media-query support and no override renders the app exactly as it always did. The dark values are then declared **twice**, and neither copy is redundant:

- `@media (prefers-color-scheme: dark)` **under a `:root:not([data-theme="light"])` guard** — the OS default. The guard is what lets an explicit *Light* choice win on a dark machine; without it the media query would beat the attribute.
- `:root[data-theme="dark"]` — an explicit *Dark* choice, which has to win on a **light** machine, where the media query never fires at all.

Every colour token on `:root` is re-declared in both blocks, even where the dark value is deliberately identical to the light one — the terminals and the live-status colours are the worked examples. Re-stating them is what makes the dark block a complete list of decisions rather than a diff, and it is why the guard treats a missing token as an omission instead of an intentional carry-over.

`desktop/index.html` carries the other half: `<meta name="color-scheme" content="light dark">` is what makes the webview's **own** chrome — scrollbars, form controls, the default background behind the page — follow the appearance instead of staying light under a dark document, and `theme-color` needs one element per appearance, selected by a `media` attribute.

### The `theme-invariant:` opt-out

Any line carrying `theme-invariant: <reason>` in a comment is exempt from the colour guard, for that line only. Both qualifiers are enforced: the marker is read from the line's **comment text** and nowhere else, so a string literal quoting it (this documentation pasted into a component, say) exempts nothing, and a bare `theme-invariant:` with nothing after it is not an opt-out at all. It is not a way out of doing the work — it is for colour that is genuinely outside the theme. What qualifies today: the **xterm palette** in `.ts`/`.tsx`, because the terminals stay dark in both appearances (below), and colour arriving from another branch that has not been tokenised yet — PR #779 has light hexes in flight, so whichever of the two lands second needs a note rather than a wall. What does not qualify: "it's only a shadow", "it's only a border", "it's nearly the same in both themes". A shadow tuned for a light surface is invisible on a dark one, which makes it a theme colour in the way that matters. Always write the reason; the marker without one is a `TODO` wearing a disguise.

### The terminals stay dark in both appearances

`--terminal` and the `--shell*` family are not themed, and this is a decision rather than an omission. The panes are xterm.js fed raw PTY bytes: we control the background, the foreground and the 16 ANSI slots, but not what an agent emits into them. Agent CLIs pick colours for *their* user's terminal, and two of those choices break on a light background — dim greys tuned to read on black become near-invisible, and anything emitting truecolor SGR (`\e[38;2;r;g;b m`) bypasses the 16-slot palette entirely, so remapping cannot rescue it. The failure would land on the one surface people actually work in, and it is not one we can prevent from here. So the chrome around the panes themes normally and the pane stays a dark inset — which is what it already looks like in light mode. A "terminal follows the app appearance" setting was considered and deferred: it is cheap to add to a section that now exists, and shipping it means shipping a control whose other position nobody has measured against real agent output.

### The two guards

Both live in `xtask/linkage-check/src/desktop_palette.rs` and both run in `cargo test-fast` (via `--workspace`) and in the CI `build` job, which is one of the four **required** checks. That placement is deliberate: a vitest guard would run in `desktop-web`, which is advisory and can be merged past. Run them alone with `cargo test -p xtask-linkage-check desktop_palette`.

**The colour guard** scans every source file under `desktop/src` and fails on a hard-coded colour outside the palette: hex literals (`#rgb`, `#rgba`, `#rrggbb`, `#rrggbbaa`), the colour functions `rgb() rgba() hsl() hsla() hwb() lab() lch() oklab() oklch() color()` **unless** their arguments name a token, and the bare keywords `white` and `black`. `transparent`, `currentColor` and `inherit` are never flagged. Two things may hold a literal: a custom-property declaration inside a `:root` block of `desktop/src/styles.css` — that exact path, not any file merely *named* `styles.css`, because the palette is supposed to live in one place; the exemption is custom properties specifically, so `:root { color: #1d2522; }` still fails — and any line carrying the `theme-invariant:` marker in a comment, with a reason. **When it fires:** move the value onto a token (add one if no existing token has that role), or mark the line with a reason. Do not widen the guard.

**`check_dark_palette`** compares the three `:root` blocks and catches the ways the palette can go quietly wrong: the two dark blocks drifting apart (a token declared in one with a different value, or missing from the other), a light colour token with **no** dark counterpart, a **dark-only** token, which resolves to nothing in light mode, and either dark block sitting in the wrong place — the guarded one has to be **inside** the `@media (prefers-color-scheme: dark)` rule and the attribute one **outside** it. That last check is on byte ranges rather than on selector text, because a block dedented out of the `@media { … }` wrapper keeps its selector spelling and its values while applying them in *light* mode, and nothing textual can tell the difference. **When it fires:** it names each problem individually; the fix is always to make the three blocks agree, never to delete an assertion. Nothing else can see this — no test renders dark mode — so a token added to `:root` alone would otherwise ship as a colour that silently keeps its light value.

**Both guards fail closed**, which matters more than it sounds for a required check: every filesystem error — a directory that cannot be listed, an entry that cannot be typed, a file that cannot be read — fails the test rather than dropping those files from the scan, and a symlink under `desktop/src` is refused rather than followed (a directory link can leave the tree or loop back into it; a file link would be reported under a path it does not have). There is no symlink there today, so if you add one deliberately, `sources` has to be taught what it means.

**One false positive is worth knowing about in advance**, because the failure reads as a colour problem when it is a naming one: `#` followed by three hex digits inside a **string literal** is flagged, so `"…#803…"` and `"…PR #416…"` both trip the colour guard. Comments are masked before scanning; string contents deliberately are **not**, because `"#141817"` is exactly what the guard hunts. That is correct behaviour rather than a bug, and it will recur as more strings are written under `desktop/src`. The fix is to rewrite the string — `PRD 803`, no `#` — not to touch the guard and not to reach for the opt-out.

## Models and agent profiles

The fixture seeds an orchestrator and release profile using Claude and coder, reviewer, auditor, and tester profiles using `gpt-5.6-sol` with role-appropriate reasoning effort. Editing a profile stores a draft in the webview's local storage and **Confirm draft** does not execute anything or write `.dot-agent-deck.toml`; **Launch live loop** does execute each enabled profile's launch command through the daemon after the workflow validation described above. Treat launch commands as executable configuration, never store tokens in them, and review them before starting a live loop. **Reset defaults** clears the local draft.

For OpenAI, Anthropic, and OpenCode profiles, the launch command is generated from the current provider, CLI, model, reasoning-effort, and permission fields. The generated command is a read-only preview; on macOS and Linux, values are POSIX-quoted as individual shell words and invalid, NUL-containing, blank, or oversized commands block launch. An **advanced custom command override** is available for unusual CLIs, but it is explicitly labeled as an exact shell command that bypasses the structured fields. Its permissions are unmanaged by the profile UI, may be arbitrary, and must be encoded and reviewed in the command itself. Custom roles are excluded from structured full-access counts, and a custom override does not bypass the Windows platform guard. The launch confirmation distinguishes custom-command risk from permission claims made only for generated roles. The submitted command overrides the matching project role for that launch only; format-preserving profile write-back is not implemented.

Live sessions continue to use the commands and models that created them through the daemon. The current daemon snapshot exposes agent type but not a reliable model identifier, so the deck's live tiles still label model, cost, token and lease as unavailable instead of guessing. Two of that set have since moved: the branch chip and the attempt readouts are **gone** rather than labelled, because a fabricated `ATT 01` reads as data in a way an explicit "unavailable" does not (see [What it shows, and why not more](#what-it-shows-and-why-not-more)), and the write lease is genuinely reported on the [agent overview](#the-agent-overview), which reads it from `SessionSnapshot` rather than from the deck's fixture-shaped model.

## Voice control: the pipeline, the table, and what a row costs

PRD [#802](https://github.com/vfarcic/dot-agent-deck/issues/802) built voice control for this app, and its parts sit in four places on purpose: the **pipeline** and the **command table** are Rust-side under `desktop/src-tauri/src/voice/`, **dispatch** is frontend in `desktop/src/lib/voiceActions.ts` ([the next section](#the-voice-action-registry-and-what-its-guard-does-and-does-not-see)), the **surface** is `desktop/src/components/VoiceControlPanel.tsx` ([the one after that](#the-voice-surface-and-the-four-decisions-in-it-worth-knowing-before-you-edit-it)), and the **settings** are a `[voice]` section plus, for the backends that need one, a key in the OS keychain.

This section is what you need in order to **add a voice command without reading the PRD**. The short version: implement the feature as normal, add a row to `commands.toml`, flip its registry entry to `voice: true`, and update the tests that pin the shipped row set by value.

**Why voice is in the desktop app and not the TUI.** It needs a microphone, OS integration and a persistent local process — awkward through `ssh -t`, natural in a native app. Together with the fleet view it is what gives this app a job the TUI structurally cannot do.

### The pipeline, end to end

Six stages. The fifth is the one that keeps voice from becoming a second control surface.

| # | stage | where | what happens |
| --- | --- | --- | --- |
| 1 | capture | `voice/capture.rs` | the microphone, Rust-side, segmented into utterances by `Vad` |
| 2 | transcribe | `voice/transcribe.rs` | PCM in, a `Transcript` out, over the `Transcriber` seam |
| 3 | resolve | `voice/remote.rs`, `voice/openai.rs` | **one** model call carrying the whole command table, the live agent list and the transcript, over the `IntentResolver` seam |
| 4 | validate | `voice/outcome.rs`, `voice/table.rs` | the action exists, is callable on the declared screen, and every param resolves against live state |
| 5 | execute | `desktop/src/lib/voiceActions.ts` | the resolved action is handed to the handler a **click** uses |
| 6 | report | `voice/outcome.rs` | the app renders the sentence, from the table |

**Capture is Rust-side, and that decision propagates.** Of the three webviews this app ships on, `wry 0.55.1` grants microphone capture on exactly one: it answers WKWebView's permission callback with an unconditional `Grant` on macOS, leaves the kind entirely to WebView2's defaults on Windows, and on Linux **never sets `enable-media-stream` and connects no `permission-request` signal at all** — so webview-side `getUserMedia` there has no path to being granted without an upstream change. A capture path in the webview would therefore have worked on one platform and been dead on another, which is why the device is opened in Rust instead; `desktop_voice_stop` transcribes there and returns text. No audio buffer ever crosses into the webview. The CSP pushes the same way for every network hop: `connect-src` in `desktop/src-tauri/tauri.conf.json` names `ipc:` and `http://ipc.localhost` and nothing else, so **transcription, intent resolution and any future model download are Rust-side by construction**, not by preference.

**A device whose sample format this build cannot read is refused at OPEN, by name.** `cpal::SampleFormat` declares twelve PCM encodings plus a DSD trio, and PR #1163 found four of the twelve — `I24`, `U24`, `I64`, `U64` — falling through an empty match arm in the callback: the device opened, recorded, and delivered an empty buffer, so the user was told *the microphone heard nothing* with nothing to act on. 24-bit is ordinary on real interfaces. All twelve now convert (the two 24-bit ones through `DeviceSample` impls on `cpal`'s own newtypes, since there is no Rust primitive for a 24-bit sample and converting through a temporary `Vec` would allocate per callback). The DSD trio is **refused**: it is a 1-bit sigma-delta stream rather than PCM, and turning it into samples needs a decimation filter. `conversion_for` is one match with two consumers — `open_device` asks it before touching a device, `feed` asks it per callback — because two lists would drift, and the drift is silent in the one direction that matters. `SampleFormat` is `#[non_exhaustive]`, so a variant a later `cpal` adds is refused at open rather than dropped per callback: if you add a conversion, add it there and both halves move together.

**One open microphone becomes a sequence of utterances in `voice/capture.rs`, and that is what makes continuous voice control possible.** `Vad` is deliberately the simplest thing that can *end* an utterance: `SpeechDetector`'s verdict on each 20 ms frame — is this frame `SPEECH_MARGIN` (3x) louder than the room it arrived in? — plus a quiet that has run for `SILENCE_HOLD` (800 ms). It is fed every output sample as `PcmSink` produces it, carries a partial frame between device callbacks, and latches once it fires so a late callback cannot un-end a finished utterance. `CaptureSession::status` then reports `CaptureState::Done` while the recording is still open and the audio still there, which is the boundary the webview polls for.

Three things it is not, each stated because the unqualified version would be wrong. It is **not** `Pcm16::is_silent`, which looks at a finished buffer and decides only whether a device delivered anything at all; this one runs on the data path and answers a question with a time in it. It is **not** a speech/noise classifier — a steady loud noise never ends an utterance here, and what bounds *that* is `MAX_UTTERANCE` and the surface's discard. (Since the rule became relative that happens for the opposite reason to the one this sentence used to give: stationary noise is its own room, so no frame of it counts as speech, `heard_speech` never arms and the hold never starts. Before, every frame counted and the hold could never accumulate. Same outcome, and the new mechanism is the one that also keeps the noise out of `Pcm16::has_speech`.) And it does **not** end an utterance nobody started: quiet before the first speech is ignored, so a microphone open in a quiet room ends at the cap rather than here. The margin errs **high** on purpose, because the two directions do not cost the same — set too low it admits a modulated noise and never accumulates a hold in a room with any of it, so every command runs into the cap and is lost, while set too high it can only end an utterance after 800 ms of below-threshold audio, which is a pause somebody took rather than a gap between words. Nine unit tests drive it over stubbed audio, as the rest of that module is driven; none opens a device.

**A segment is only transcribed if somebody actually spoke in it, and `Pcm16::is_silent` was never that test.** `MIN_SPEECH` (120 ms) inside a `SPEECH_WINDOW` (200 ms) is: the most speech-level audio `Pcm16::measure_speech` finds in any one sliding window of the finished buffer, measured by the same `SpeechDetector` `Vad` feeds on the data path, and `handle_audio` refuses anything under it before calling any backend. What made this necessary is that `Vad::heard_speech` flips on **one** frame that counts as speech and then ends the segment 800 ms later, so a keyboard tap in a quiet room produces a segment — and `is_silent`'s `all()` needs *every* sample under `SILENCE_FLOOR`, so that one tap made thirty seconds of room tone eligible to send. Whisper-family models are trained on captioned video and emit their training artefacts on near-silence, so PRD #802's product owner was shown `Heard: "Don't forget to subscribe"` about words nobody had said. **Density over a window** is the discriminating half: a 20 ms RMS window dilutes an impulse to one frame, so a typist at 100 ms between keystrokes gets two frames — 40 ms — into any window, while a word reaches 120 ms even when a stop closure splits it. The threshold errs **low** on purpose, the opposite direction from `SPEECH_MARGIN`'s, because these two fail differently — refusing a command somebody said is indistinguishable to the user from the feature being broken, while a junk sentence is recoverable in one glance. It is a threshold on the **audio**: nothing reads what a model returned, and no list of known hallucinated phrases exists anywhere in the module. `HttpTranscriber::run` asks the same question as a backstop for a caller that goes straight to `Transcriber::transcribe`.

**The window rung was 0 ms until [#802's real-microphone report](https://github.com/vfarcic/dot-agent-deck/issues/802), and the gate is what refused speech.** The rule used to be an *unbroken* run of `MIN_SPEECH`, which reads as reasonable and is the wrong axis: what separates typing from speech is **density**, not run length, and **speech contains real silence inside single words**. A voiceless stop is a closure — *"next"* is an 80 ms vowel, a 40 ms silence and a 60 ms /st/ cluster — so it holds 140 ms of speech-level audio and never 120 ms of it consecutively. Measured against this module's own detector before the fix, three ordinary shapes were refused: that *"next"*, a quiet speaker's vowel with a 20 ms dip through it, and a *"back"* with both its closures. So an utterance armed by one frame and closed by `SILENCE_HOLD` was then classified as silence by a rule five times stricter, and the user was told *"Nothing was said"* about a word they had said. The two numbers no longer differ by 6× for no stated reason: there is **one** question — *is this 20 ms frame at speech level?*, which is `SPEECH_MARGIN` against the room — and a ladder of how much quiet each larger thing tolerates. Inside a **word**, 80 ms (`SPEECH_WINDOW − MIN_SPEECH`), which is a stop closure. Inside an **utterance**, 800 ms (`SILENCE_HOLD`), which is a pause somebody took. Beyond that, `MAX_UTTERANCE`, which is a cap and not a judgement. `Vad::heard_speech`'s single frame is deliberately on no rung of that ladder and is **not** a claim that anybody spoke: it exists to start the silence clock, and it is the most permissive test in the module because arming on a stricter rule would hold the microphone open to `MAX_UTTERANCE` through a segment of nothing but typing instead of closing it in a second. `Vad::speaking` and `Pcm16::has_speech` are the **same** predicate — they are now two callers of one `SpeechDetector` rather than two implementations of one rule, so `voice_capture_the_live_and_finished_rules_are_one_predicate` holds something weaker than it used to and something that cannot drift.

**The FLOOR was absolute until #802's second real-microphone report, and an absolute floor cannot work across devices.** `SPEECH_FLOOR` was an RMS of 600 out of `i16::MAX`, about -35 dBFS, picked from where near-field speech and room tone sit on a device whose gain nobody had measured. The owner supplied the falsification with a number: *"60 ms of speech inside the loudest 200 ms, where 120 ms is needed"* — the **density** branch, not the level one, so his peak did clear 600 while only 30% of his densest window did. In the loudest part of a word a correctly-set floor sees near-continuous energy; his saw a third. **Lowering the constant was the obvious repair and it is the wrong one**, because a microphone is a gain stage: 600 is too high for that device and too low for a hotter one, where room tone alone clears it, every frame reads as speech and the hallucinations this gate exists to stop come straight back. Any number fitted to one report is fitted to one microphone. So the rule is now **relative to the utterance's own noise floor** — `SpeechDetector` takes the room as the quietest 20 ms in the last `NOISE_WINDOW` (1 s, the standard minimum-statistics estimator, which works because every real signal visits its own floor while stationary noise simply sits there), and a frame counts as speech at `SPEECH_MARGIN` (3x, a little over 9 dB SNR) above it. The level cancels: the same voice on a quiet input and a loud one gets the same verdict, with nothing stored per device and no setup step, because a microphone always delivers its own room. **One absolute survives and it is deliberately not a speech threshold**: `SILENCE_RMS` (64, about -54 dBFS) floors the *room estimate*, not the frame, so the smallest bar a frame can face is `64 x 3` = 192. Flooring the frame instead would be a worse rule — a buffer holding a stretch of digital zero would have its room estimated at zero for a whole second, and ordinary room tone around 180 would read as speech for that second, which is precisely the near-silence a Whisper-family model answers with a training artefact. **A uniform buffer is refused, and that is the design rather than a gap**: a signal at one level for its whole length has a ratio of one between its loudest and quietest moments, so no relative rule can tell a held tone at speech level from a fan at speech level — the information is not in the buffer. Refusing is the safe direction and the physical one, since speech is modulated at the syllable rate and is never uniform. The visible consequence is that **every fixture in that module's tests carries room tone**, because a bare square wave is a signal no microphone produces; `voice_capture_a_low_gain_voice_passes_where_the_absolute_floor_refused_it` and `voice_capture_a_loud_room_is_refused_where_the_absolute_floor_admitted_it` are the pair that states the argument, each asserting first that the old absolute rule got it wrong and then that this one gets it right.

**The refusal is its own outcome and it now carries the whole measurement.** `TranscriptionOutcome::Silent` is a fourth kind beside `heard`, `not_configured` and `failed`, and it used to carry no `detail` "because there is nothing to diagnose" — which was true of the quiet-room case it was written for and false of the one that mattered. A user who *did* speak reads that row, so it has to separate the three things it can mean, and it branches on whether **any** frame counted as speech. If none did, nothing in the buffer would have passed at any length — *Too quiet to transcribe — the loudest moment reached 410 against a room at 180, where speech has to reach 540. Move closer or turn the input up; still listening.* If some did and `voiced` is still short, the audio was speech and there was not enough of it — *I did not hear enough to transcribe — 60 ms of speech inside the loudest 200 ms, where 120 ms is needed; the loudest moment reached 820 against a room at 95, where speech has to reach 285. Say that again; still listening.* Neither reads as a failure and neither blames the device, which is the rule that has not changed. **Both branches now carry all three numbers, and the density one did not**: PRD #802's owner hit exactly that branch, got a duration and no level, and the fact that mattered most — that his peak cleared the old absolute floor while two thirds of his densest window did not — was not in the sentence. Learning it cost a round trip to the person holding the microphone, and it is what falsified the constant. So every refusal now states the comparison the rule made, in the rule's own terms, and **that is also the field diagnostic**: one utterance says whether the input is quiet, the room is loud, or the gate is strict, which is the only calibration available to a rule no tier in this repository can open a device to check.

**Releasing the device is blocking work and is done off the lock.** `AudioStream::drop` joins the device thread, so it is as slow as the platform's teardown. All three paths that release one — `stop`, `cancel` and the length cap's substitution — move the stream out from under the session mutex and drop it after the guard, and all three Tauri commands hand the call to `spawn_blocking`, the treatment the device *open* already had. Holding the mutex across a driver teardown parked every concurrent `status`, `cancel` and `start` behind it, and doing it on a runtime task parked a worker as well. The same rule covers the keychain, which is why `secrets::load_off_runtime` exists: `SecretStore::load` may put an unlock prompt on screen, and both voice backends read it from inside an `async fn`.

**Steps 5 and 6 are the load-bearing pair.** Voice gets no execution path of its own — a `VoiceOutcome::Dispatch` names an `invoke` target in the frontend registry and the webview dispatches it exactly where a click does — and the **app** renders every user-facing sentence, from `report` and `unavailable_hint` in the table plus fixed prose in `outcome.rs`. The model writes no prose a user reads. The one deliberate exception is the **transcript**, which is shown verbatim on a no-match: seeing exactly what was heard is what turns a mis-transcription into a correction, and `outcome.rs` asserts the exception alongside the rule so a later sweep cannot tidy it away. Every *other* foreign string in a sentence — a model-supplied param, a daemon-supplied agent label, a backend's failure detail — goes through `safe_message`.

**One tool with an `action` enum, and the full list every time.** Every command is sent on every request, each annotated `callable: true|false` computed from its `screens` against the current screen, rather than the list being filtered. Filtering produces a bad failure: asking to open an agent from a screen where that is impossible would yield "I don't know how to do that", which is false. The truthful answer names the prerequisite, which is what `unavailable_hint` is for. The trade, because it is easy to forget: **filtering prevents wrong calls; flagging explains them.** Flagging relies entirely on stage 4 to refuse an action the model called anyway — the flag improves the message, not the misfire rate.

### The command table, and its three consumers

`desktop/src-tauri/src/voice/commands.toml`, embedded with `include_str!` so it ships inside the binary and cannot drift from the build that compiled it. It is in the Rust crate because two of its three consumers are Rust-side and the third reaches it through an existing IPC reply rather than through a second copy.

1. **Schema generation** (`voice/schema.rs`, `voice/prompt.rs`, `voice/remote.rs`) — `id`, `description` and `params` become the model's tool definition.
2. **Validation** (`voice/table.rs`, `voice/outcome.rs`) — `screens` and each param `kind` check the model's answer before anything runs.
3. **Dispatch** (`desktop/src/lib/voiceActions.ts`) — `invoke` names an existing registry entry, and `report` is the sentence shown afterwards.

**`description` is a PROMPT, not documentation.** The model picks on it. Write it for a model and review it as an interface — it is the only thing separating one command from another at the moment of choice, and no user ever reads it. Two rows in the shipped table (`open_deck` and `close`) are both asked for by a bare "go back", and **listing the phrase on both was measured to be not enough**: with each description merely naming "go back" among the utterances it serves, a real-model run answered `none` on the overview rather than falling through to the row that was callable. Each row now states what a bare "go back" means **under its own precondition** — "on the agent overview", "while an agent's pane is open" — and the word *bare* is load-bearing, because "go back to settings" has to keep reaching `open_settings`. That still leans on `screens`: no envelope carries a screen name, only the per-row `callable` flag, so the model learns which precondition holds by reading which row is callable. The prose tells it what to do with that signal; the signal is still `screens`, which means **a change to either row's `screens` column is a change to how those two are disambiguated**.

**`report` is the success sentence and is named `report` on purpose.** Without it, "the app renders every sentence" would be lost — refusals have `unavailable_hint` and fixed prose to draw on and success had nowhere, so the frontend would have had to invent success wording. A `{param}` placeholder is replaced with what that param resolved to, and a placeholder naming no declared param is refused at parse time. Do not rename it to `confirmation`: [D5](../../prds/802-desktop-voice-control.md#milestones) is *destructive commands behind confirmation*, so rows in this same file will later carry a genuine confirm-before-acting flag, and the two are close to opposite — a report describes a completed action, a confirmation blocks one before it happens.

**The shipped slice WAS navigation-only and is not any more**, which is worth saying in the sentence that used to claim it rather than leaving the claim to rot. `voice_off` stops the microphone, `list_commands` opens an overlay, `dictate_to_agent` types what it hears into the open agent's prompt and `submit_prompt` presses Enter in it. What still holds is the narrower property the old sentence was protecting: **no row stops an agent, or answers a permission prompt, BY ITSELF.** Two rows lead to a stop — `stop_agent` and `close_orchestration` (PRD #1223) — and each terminates at a `ConfirmDialog` the user answers by hand, never at a direct dispatch: that is [D5](../../prds/802-desktop-voice-control.md#milestones)'s confirm-before-acting step, and [below](#d5-a-spoken-stop-opens-a-confirmation-a-spoken-start-starts) is how it is wired. One row starts something — `start_new_agent`, the New agent dialog's Start by voice — and since 2026-09-23 it starts at once: D5's start half was revisited, for reasons given below. No row answers a permission prompt at all. `close` closes a **view** and not a terminal pane — `closeAgent` in `App.tsx`, which is `setView({ kind: from })` and nothing else — and its `description` says so in as many words, because the destructive operation is one word away from it in speech.

#### D5: a spoken stop opens a confirmation; a spoken start starts

PRD #802's D5 requires consequential commands to be confirmed regardless of model confidence, because a confident wrong answer is indistinguishable from a right one. PRD #1223 first satisfied it for all three of its start and stop rows. **On 2026-09-23 the start half was revisited**: the user found the start confirmation "only introduced friction by me having to give the same instruction twice", and three things made it unnecessary — action grounding requires a start word in the transcript, so a hostile label cannot steer a start the user did not ask for; the confirmation re-stated a form that is on screen; and a mistaken agent is one "close" away. What remains: **every voice row that STOPS anything terminates at a `ConfirmDialog` (`desktop/src/components/ConfirmDialog.tsx`), never at a direct dispatch.** Each stop row's registry entry (`confirmStopAgent`, `confirmCloseOrchestration` in `voiceActions.ts`) calls a member that can only *open* a confirmation; the deck action runs from that confirmation's own button, pressed by hand. Each stop row's `report` says nothing has happened yet, because nothing has, and each `description` tells the model the row only asks — so an insistent "kill it now, don't ask me" is routed to the row, which is the same as routing it to a confirmation (the adversarial phrase fixtures measure exactly that). The stops keep it because their target may be off-screen — there is no form to read — and an orchestration close takes several roles at once. It is also, since 2026-09-24, what makes a stop's target safe to resolve from a partial reference: references are no longer held against the transcript ("stop the orchestration 1" reaches `dot-agent-deck-orchestrator-1`), so the confirmation naming the target is where a wrong one is caught. [`voice-first-design.md`](voice-first-design.md) section 5 has the full record, and the companion principle: an ambiguous reading resolves to the non-destructive action, so "close the agent" closes the view, and `close_orchestration` needs the orchestration or run named beside its verb (`heard_as_also`).

- **Start** (`start_new_agent`, "start it", "go ahead") fills nothing and acts on the form as it is: its registry entry is `startNewAgent`, which calls the function the Start button calls. It `requires = ["new_agent_dialog"]` — the dialog open, whatever its form — so an incomplete form reaches the dialog and is refused in words (`START_NEEDS_DECK`, `START_NEEDS_DIRECTORY`, a live run title, a start already under way). An utterance judged against a live form starts only that form: a deck or directory that moved during the round trip refuses with `FORM_MOVED_ON`, which is what replaced the old confirmation's signature check. With the dialog **closed** the row is not callable, and a start-shaped utterance ("start the new agent") is `open_new_agent`'s, which opens the dialog — steered by the two rows' descriptions, and made deterministic by `start_new_agent`'s `unavailable_redirects = "open_new_agent"`, which redirects a pick of the start row that cannot run to the dialog's opener when that row can run and the same words ground it. The reverse holds with the dialog **open**: `open_new_agent` requires `new_agent_dialog_closed`, and its `unavailable_redirects = "start_new_agent"` hands a pick of it whose words ground the start — "Start agent", the button's own label — to the start, unless the pick names a deck, which starting the form would drop. The Start button's labels, "Start agent" and "Start orchestration", are both in the start row's description: a control's visible label is part of its voice vocabulary ([`voice-first-design.md`](voice-first-design.md), section 5).
- **Stop** (`stop_agent`, an `agent_ref`) and **close** (`close_orchestration`, an `orchestration_ref`) open the **existing** U4 confirmations — the ones a row's Stop and a card's Close open, `stopControls` in `AgentOverview.tsx` — and the orchestration one names every role it will stop. An `orchestration_ref` resolves against the orchestrations among the selected deck's live agents, grouped exactly as the overview groups cards (by orchestration id, an id-less agent as its own card) and named by the card's title or config name; its `value` is one member's agent id, which finds the card even when the daemon reported no orchestration id. A spoken stop is refused when its target has left the overview, when another confirmation is already open (a second would replace the first under the user's pointer), and while the New agent dialog is open (it is modal).

**Where the gate lives, and where it belongs.** In the frontend, today. The command table anticipates a `confirm` column (`table.rs`, the `report` column's doc) that would make confirm-before-acting a property of a ROW rather than of its registry entry; it is deliberately not built here, because it is #802's feature-wide mechanism and coupling it to one feature's PR would put that mechanism on this feature's schedule. When it lands, these two rows are the first to carry it. `voice_table_d5_rows_only_ask` pins the set until then.

**Three rows carry no `screens` column, which means callable everywhere, and that is a requirement rather than laziness.** Stopping must never be unavailable; neither must the phrase that lists what can be said; and aiming the microphone at an agent must work from the screen where the user is already watching one. Such a row's `unavailable_hint` is the one column nothing renders — `Unavailable` is produced only by a row the current screen cannot run — and `voice_table_rows_callable_everywhere_are_the_deliberate_set` pins which rows are in that position so a fourth is a decision somebody made.

### What adding a row costs, measured rather than asserted

PRD #802's acceptance criterion is that **adding a command changes no implementation** — one row and one classification flip — **plus the test assertions that pin the shipped row set by value**.

It was originally written as "a ONE-FILE change". That count was wrong. PRD #802's M8 added `open_settings` and measured **seven** files:

| # | file | what changed | implementation? |
| --- | --- | --- | --- |
| 1 | `desktop/src-tauri/src/voice/commands.toml` | the row | the row itself |
| 2 | `desktop/src/lib/voiceActions.ts` | `no_voice` → `voice: true` | the classification flip |
| 3 | `desktop/src-tauri/src/voice/table.rs` | 2 pinned-row test assertions | no |
| 4 | `desktop/src-tauri/src/voice/schema.rs` | 3 pinned-row test assertions | no |
| 5 | `desktop/src-tauri/src/voice/prompt.rs` | 3 pinned-row test assertions | no |
| 6 | `desktop/src-tauri/src/voice/remote.rs` | 1 pinned-row test assertion | no |
| 7 | `xtask/linkage-check/src/voice_command_registry.rs` | 1 guard-test plant re-pointed | no |

**Five of the seven are test-only, and every Rust hunk is inside a `mod tests`.** Nine unit tests pin the shipped row set **by value** — the action enum in the generated schema, the CLI prompt, the remote request body, the per-screen `callable` flags, and the table's own row list. That is a deliberate choice `table.rs` documents: the shape of the shipped table is pinned rather than described, because a test that merely described the shape would pass while the table said something else. So the real cost is **one edit per place the row set is written down**, and that is what those tests are *for*.

Two practical consequences when you add your own row:

- **Expect `cargo test-fast` to go red in five files with value mismatches, and read that as the pinning working.** Each failure names the expected and actual row set; add your row to each. None of it is plumbing left undone and none of it surfaces at runtime.
- **The seventh file is conditional and usually will not apply to you.** Rule 13's `overtaken` planted-bad-input test names a specific registry entry that has *no* row; give that entry a row and the plant has to move to another unspoken entry. It only bites when the entry you are voice-enabling happens to be the one the plant names.

**Scope the promise correctly.** It covers *commands*, which is the case that grows per feature. Activation modes, the dictation countdown and the destructive-confirmation flow are plumbing — built once, not extended per feature — so "one more costs a row and no implementation" is true for commands and not for new *interaction modes*, which should be rare.

#### Four more rows, measured the same way, and what they cost

M8's `open_settings` was a *navigation* row whose behaviour already existed behind a rail button. The four added after it are the cases that measure the claim where it is under strain, and the answer is not one number.

| row | files beyond the seven M8 measured | why |
| --- | --- | --- |
| `voice_off` | **+2** — `App.tsx`, `VoiceControlPanel.tsx` | the behaviour belongs to the **voice surface**, which is not a screen, so there was nowhere for it to be published from |
| `list_commands` | **+6** — a Tauri command, `bridge.ts`, `types.ts`, `useDeckRuntime.ts`, the panel, `styles.css` | the overlay needs the annotated table in the **webview**, and nothing sent it there |
| `dictate_to_agent` | **+3 beyond `voice_off`'s** — the panel's whole dictation mode, plus a `speech` flag through `voice/capture.rs` and the status DTO | it was an interaction **mode**, not a command. **The mode is gone** (see below) and the row is now ordinary; the `speech` flag stayed, because the countdown it cancels did |
| the empty report row | not a row at all | static text in the panel |

**Read the first column as the honest measure, and read the pattern rather than the totals.** A row exposing behaviour a click already reaches still costs the row, the flip and the pinned assertions — M8's number held. A row exposing behaviour that belongs to the *voice surface itself* costs a publishing seam, once: `VoicePanelChannel` is `DeckSurface`'s `voiceChannel` mechanism pointed the other way round the screen switch, and the second and third such rows reused it for one line each. A row that needs data the webview does not have costs an IPC verb. And a row that changes what an utterance *means* is not a command at all.

**So the sentence to carry forward is narrower than M8's and truer:** adding a *command* changes no implementation; adding a *capability the voice surface owns* costs a seam the first time; adding an *interaction mode* is a feature. PRD #802 said the third of these in advance and it was right to.

#### Dictation, which stopped being a mode

**`dictate_to_agent` was an interaction MODE and is now a row like any other.** What shipped first aimed the microphone at one agent — *"type to the tester"* — after which every utterance was typed into that agent's prompt until an exit phrase was heard. That design carried the two failures [D6](#milestones) itself warned about: a missed exit types the exit phrase into the agent, and a false positive truncates somebody mid-sentence. The product owner used it and asked for a different shape, which is the one described here: **open the agent's pane, then say what you want typed, one utterance at a time.** There is no mode, so there is no exit to miss and nothing that can be truncated. The words go into that agent's prompt through `sendTerminalInput`, the same path a keystroke takes, rather than `submit_text`, which types *and* submits in one call and would make them invisible until they were already gone.

**Targeting is the SCREEN**, which is finally what the screen model is for. The row declares no `agent` param and carries `screens = ["agent"]`: the agent being typed into is the one whose pane is open, and with no pane open Rust renders the table's own not-here hint instead. That is a stronger guarantee than the old row's, which opened the pane itself — the words cannot reach a terminal the user is not looking at, because there is no dispatch at all until one is.

##### The model marks a boundary; the app supplies the text

This is the property the whole design is arranged around, and it is the reason an obvious alternative was rejected. A dictation command with a free-text **content** parameter would mean the model retyping the user's words on their way into an agent's prompt — unacceptable in the one place fidelity matters most. But that only rules out the model *supplying* the content. *Locating* where the content begins is a different job, and unlike the first it can be checked:

```
transcript:  "let's write a prompt run the login tests"
model:       action = dictate_to_agent, prefix = "let's write a prompt"
app:         verify OUR transcript starts with those words (token-wise) →
             strip them → type "run the login tests" VERBATIM
```

`voice::dictation::strip_opening` is the whole of that check and it **fails closed**: a prefix that is not genuinely the front of the transcript returns nothing, and nothing is typed. There is deliberately no arm anywhere that falls back to the model's own string — the refusal is `param_unresolved`, rendering *"…" is not how that started, so nothing was typed*, and `voice_outcome_a_prefix_nobody_said_types_nothing_and_reports` pins it. The comparison is over normalised **tokens** rather than bytes, because the model is quoting from prose and its casing and punctuation are its own; token-wise matching also makes a partial-word match impossible, so *"type"* does not open *"typescript is confusing"*.

**The model can still mark the boundary one word too late, and the cost is a word of the user's, not a word of the model's.** Measured on the same manifest: *"ask it to summarise what it just did"* came back with `prefix = "ask it to summarise"`, which types *"what it just did"* — a shorter instruction than the user gave, never a different one. The row's description now carries that exact case as a worked example (*"the prefix is 'ask it to' and NOT 'ask it to summarise', because the thing the agent is being asked to do is part of the text"*) and the fixture passes, but the failure mode is structural rather than fixed: the guard is on **fidelity**, not on the boundary being the best one. A user watching the words land in the prompt sees a short one and can finish it, which is what the countdown is for.

The param's `kind` is `spoken_prefix`, one of the entries in `ParamKind`'s closed set (`agent_ref`; `deck_ref`, `dir_ref`, `mode_ref`, `agent_type_ref` and `orchestration_ref`, all PRD #1223; and this one — [voice-first-design.md](voice-first-design.md#3-the-resolver-kinds-that-exist-and-what-adding-one-costs) tabulates them). **It is a new shape of param**: `agent_ref` resolves against *live state* — is there one agent called "the tester"? — while this one resolves against *the transcript itself*, and what it resolves **to** is the rest of what the user said. On the wire that reads exactly like the other kind — `spoken` is what the model supplied, `value` is what the app resolved it to — which is why `ResolvedParam` needed no new field and the frontend needed no new branch. Linkage-check rule 14 reads `ParamKind` out of `table.rs`, so the closed set and the table's `kind` column cannot drift.

##### Two paths, and the fast one is an optimisation rather than the vocabulary

An utterance opening with `type`, `write`, `say` or `dictate` (`voice::dictation::DICTATION_OPENERS`) is answered **before the resolver is called at all**, in `handle_utterance`'s `local_intercept`. Everything else goes to the model and comes back as the same row with the boundary marked.

**Read the opener list as a cost decision and not as a set of magic words.** A closed prefix list is the guess-the-magic-word problem PRD #802 opens by rejecting: *"let's write a prompt …"*, *"tell it to …"* and *"ask it to …"* are all things people say and none of them could be in any list worth maintaining. They work, through the model. What the list decides is only who pays for a round trip — and the round trip is **not an extra one**: the resolver runs on every utterance anyway to decide whether it is a command, so a dictated utterance costs the same call either way. The four openers are verbs of producing text and nothing else in the table is about producing text, which is the whole test for membership; `voice_dictation_no_opener_starts_another_row_s_own_trigger_phrase` is the mechanical half of that claim, checking that no other row's description offers a quoted trigger phrase beginning with one of them.

*"submit"* was named by the product owner as a plausible opener and is deliberately **not** one: on its own it means press Enter, and a word that means two things is exactly what a fast path may not resolve by guessing. *"tell"*, *"ask"* and *"let's"* are absent for the opposite reason — they are real openers, but where the content begins after them varies (*"tell it to run the tests"* against *"tell the reviewer that the build is green"*), and a fixed rule would cut in the wrong place. The model cuts those, and the app checks the cut.

##### Sending, which is the irreversible half

Three ways, none of which can fire on words meant to be typed:

- **a whole-utterance submit phrase** — `end`, `send`, `send it`, `submit`, `enter`, `press enter` (`voice::dictation::SUBMIT_PHRASES`), matched by **equality against the entire normalised transcript**, never as a prefix and never as a suffix. That is the defence against the failure that makes this dangerous: a trailing rule would submit *"the meeting is at the"* when somebody said *"type the meeting is at the end"*, and no candidate phrase escapes it — *"tell him to send it"* defeats *"send it"* identically. Sending is the last thing that happens to a prompt, so a half-finished instruction delivered to an agent is unrecoverable. **Trailing matching was considered and explicitly rejected; do not implement it.** The cost is stated rather than hidden: those six phrases are the words a user cannot dictate *alone*. *"type end"* types `end`; a bare *"end"* sends.
- **the countdown.** After `VOICE_DICTATION_SEND_MS` with nobody speaking the surface presses Enter — behind a countdown on screen for every one of those seconds, cancelled by any speech.
- **the user's own Enter**, in the prompt they are looking at.

*"stop"* is deliberately not a submit phrase, though it was suggested: a bare *"stop"* is the most likely thing somebody says when they want everything to stop, and `voice_off`'s own description claims *"stop listening"*, *"stop voice"* and *"stop voice control"*. *"go ahead"* is out of the **list** and is still a send phrase, which is worth stating because the two halves read like a contradiction: `submit_prompt`'s description names it, so a bare *"go ahead"* is answered by the model rather than by the fast path — and the model is told it means send. That is the one-way check working as designed: the list decides who answers an utterance, never what happens to it. The worry that kept it out — an agent that has just asked *"shall I proceed?"* makes *"go ahead"* an answer the user wants **typed** — is real, and keeping it out does not serve it: `strip_opening` always strips at least one introducing token, so no utterance is ever typed whole. *"type go ahead"* is how those words get typed.

**What cancels the countdown is `CaptureStatus::speech`**, a flag added for it: the capture session's latched answer to *has anybody spoken since this recording opened*, computed on the device thread by the same detector that finds utterance boundaries. Nothing else on that status could answer it — `captured_ms` counts audio and grows in a silent room, and `Done` arrives only after `SILENCE_HOLD` past the **end** of a sentence, which for a long one is well after a five-second countdown would have fired.

##### The two phrase lists are the only spoken words the model is never asked about

They live in `voice/dictation.rs`, ahead of the resolver, and linkage-check rule 14 is what keeps that second place from drifting: every phrase in `DICTATION_OPENERS` must appear in the `dictate_to_agent` row's `description`, and every phrase in `SUBMIT_PHRASES` in `submit_prompt`'s. A phrase the fast path answers and the model was never told about would work one way through one path and be a no-match through the other, and nothing at run time would say so. The two lists must also stay **disjoint**, because the submit list is checked first and a shared phrase would make that order silently load-bearing. The check runs one way only, and here that is not a residual but the design: a phrasing in a description that no list matches is precisely the fallback's job.

**They used to live in `VoiceControlPanel.tsx`** — the mode's exit phrases and a local copy of `voice off` — and linkage-check rule 14 read the panel for them. The assertion moved with the lists rather than being deleted, which is the whole point of having it.

**What the rebuild deleted.** The mode state machine (`dictateOne`, the aimed-or-resolve fork in `takeUtterance`, the `Dictation` state and its ref), `VOICE_DICTATION_EXIT_PHRASES`, `VOICE_OFF_PHRASES`, `VOICE_DICTATION_ENDED`, `spokenPhrase`, the `startDictation` context member and registry plumbing, the `agent` param on the dictation row, and the `aimed` flag in `App.tsx` that existed only to suppress an Undo beside an aiming dispatch. What is left in the panel is a `Pending` — whose prompt has unsent words in it — which changes nothing about how the next utterance is judged.

**A known residual, stated rather than discovered.** Nothing checks *before* dispatching that the open agent's pane accepts input. An agent holding a read lease, with no live target, or already finished is unwritable (`terminalInput.ts`), and dictating at one fails on the utterance itself: the write rejects, the report shows the rejection and no countdown is armed. Loud rather than silent, but later than it could be — the check would need the agent snapshot, which the panel does not take. It is narrower than it was: the old mode aimed at an agent and then failed on the *next* utterance, while this fails on the one that asked.

#### `close`, which means the thing on top

The discovery overlay was opened by voice and could only be dismissed by a click or Escape, which breaks the premise of a hands-free surface. `close` is the row that answers, and it dismisses **the topmost dismissible thing**: the list of spoken commands if it is showing, otherwise the open agent's pane, otherwise nothing — and when it is nothing the surface says so rather than reporting a close that did not happen.

**The precedence is decided at DISPATCH and cannot be decided in the table.** `screens` draws on `DeckView`, and the voice surface's overlay is a `useState` boolean that is not in it — M1 recorded that limitation and it is unchanged. So the row is callable everywhere and `closeTopmost` (`voiceActions.ts`) does the ordering, reading `dismissVoiceOverlay` — a context member the panel publishes **only while the overlay is open**, which is how *"an overlay is open"* reaches a dispatch at all — then `closeNewAgent`, then `target.agentViewOpen`, then `reportNothingToClose`. The order is the only one that cannot surprise: the overlay is literally on top of everything, so closing what is underneath it would leave the thing the user was looking at still on screen. `closeNewAgent` is PRD #1223 U5: the overview publishes it (through the shell's `overviewVoiceContext` channel) **only while the New agent dialog is open**, the same presence mechanism as the overlay, so `close` with the dialog open closes it instead of answering *Nothing to close*. It is the dialog's own `requestClose`, so while a start is in flight it is refused exactly as the X, Esc and the backdrop are (audit F5), and the voice report shows the dialog's own sentence — *Waiting for the deck to answer the start…* — through the panel's `reportCloseRefused`, rather than doing nothing silently. This is not how the dialog is opened by voice — that is the separate `open_new_agent` row — and nothing spoken fills the dialog's fields.

**`close_agent_view` folded into this row rather than sitting beside it.** Two rows both claiming *"close this"* is exactly the tie `schema::TOOL_INSTRUCTIONS` documents at length, and on the agent screen the callability tie-break could not have broken it because both would be callable. The `closeAgentView` **registry entry** stays — the pane's own X dispatches through it, and `closeTopmost` calls the context member it names — so it now carries a `no_voice` reason saying exactly that.

**The fold cost the bare *"go back"* claim its old protection, and the sentence that pays for it was measured rather than reasoned.** `close_agent_view` was `screens = ["agent"]`, so on the overview it was `callable: false` and the tie-break handed a bare *"go back"* to `open_deck` for free. `close` is callable everywhere, so both rows are callable there and the tie-break decides nothing. Measured on the 37-fixture manifest against `gpt-5-mini`: with `close` claiming *"go back"* unconditionally, `open-deck-back` failed, answering `close` on the overview. Two interventions were tried. Cross-referencing from **both** ends — adding *"whenever this action is callable a bare 'go back' is THIS one and not `close`"* to `open_deck` as well — fixed that fixture and **moved the failure** to `close-back` on the agent screen, which answered the not-callable `open_deck`; that is the third time on this branch a prompt edit has moved a failure rather than removed one. What shipped is the **one-sided** version: `close` says a bare *"go back"* means it **only when `open_deck` is not callable**, and `open_deck`'s own description is untouched. That reads as a fact the model can check rather than a preference, and it leaves the callability tie-break working in the same direction on the agent screen instead of overriding it. 37/37 twice consecutively. **Do not add the symmetric sentence to `open_deck`** — it is the variant that was measured and rejected.


**Why the `invoke` column is stringly typed rather than a Rust enum the compiler checks.** A typed enum would mean editing the table *and* a variant *and* a match arm, and the last two are implementation — exactly what the criterion forbids. Reference-plus-guard buys the same safety at commit time, which is the trade `tests/CATALOG.md` already makes. Linkage-check rule 14 is what makes it a trade rather than a hope.

### The two intent backends, the one transcription backend, and the settings that choose them

The `[voice]` section has **three keys, two of which are tables**. `activation` is still one closed token; `transcription` and `intent` each became `{ backend, endpoint, model }` in PRD #802's provider work, because a user cannot know which key to paste when the endpoint is a secret of the build's. The backend token is still a closed enum with a folding deserializer in the `AppearanceMode` idiom — an unknown token folds to the default rather than failing the document, because a file written by a newer build may name a backend this one has never heard of, while an **over-length** token is a malformed document and is an error. A document written before the tables existed supplies a bare string where a table is expected, and `StageSpec` reads both shapes so the whole document is not lost over a stale voice token.

| field | backend tokens | default | what it is |
| --- | --- | --- | --- |
| `transcription` | `local`, `remote` | **`local`** | `local` is a speech container on loopback at `127.0.0.1:18000` (`Systran/faster-whisper-tiny.en`), which sends no credential at all; `remote` is hosted `whisper-1` at `api.openai.com`, and needs an OpenAI key |
| `intent` | `anthropic`, `openai_compatible` | **`openai_compatible`** | which **wire protocol** the one HTTPS call speaks. `openai_compatible` is `/v1/chat/completions` with a nested `json_schema`, preset to `api.openai.com` on `gpt-5-mini` with `reasoning_effort: minimal`; `anthropic` is Messages with tool-use at `api.anthropic.com` on `claude-haiku-4-5` |
| `intent.max_tokens` | an integer, 64–32768 | **`4096`** | how much answer one command may cost, **reasoning included**. `max_tokens` on the Anthropic wire and `max_completion_tokens` on the OpenAI one — one knob, two spellings. It is a ceiling and not a reservation: it costs nothing unless it is used |
| `activation` | `toggle` | `toggle` | press once to start, press once to stop. The only mode today |

**Speech needs no key and Commands does, and the asymmetry is a measurement rather than an oversight.** A keyless speech container answers in a **0.653 s median** warm, so the honest default for that stage is one that works on the day the app is installed with the audio never leaving the machine. Local *intent* was measured twice against the same phrase fixtures and is **not** viable today: a 1.5B chat model scored 20/24 — turning *"what time is it?"* into `open_agent`, which is the refusal that is most of voice's safety — and a 34 MB embedding classifier held two false positives no threshold repairs (*"start the tester"* at 0.668, above several valid commands). So Commands asks for a key, deliberately, and what the provider work buys is that the user chooses whose.

**ONE key runs the whole feature, and that is why Commands defaults to OpenAI.** Speech's hosted option is OpenAI's `whisper-1`, so while Commands defaulted to Anthropic a user who wanted voice hosted end to end needed two vendors, two accounts and two keys — for a feature whose pitch is that it works the moment you turn it on. The three shapes now available are: **one OpenAI key** for both stages; **an Anthropic key** for Commands if that is the provider they have, with Speech either keyless or on an OpenAI key; or **no key for Speech at all**, running the local container, which is still the default for that stage. Both hosted presets being one vendor is pinned by a test rather than left to the two constants drifting apart.

**The switch cost nothing measurable, which is the only reason it was available.** `gpt-5-mini` through the shipped builder and parser: 24/24 on the 24-fixture table at an **818 ms** median against `claude-haiku-4-5`'s 780 ms, at $0.0073 for the sweep. `reasoning_effort: minimal` is what buys that — provider-default reasoning also scored 24/24 but took 1,778 ms and $0.0161, spending 4,416 reasoning tokens for no score. Re-measured afterwards against the current **34**-fixture table, in the shipping configuration — which includes the tie-break sentence below, without which the OpenAI preset does **not** reach these numbers. Six runs on the OpenAI default: **34, 34, 32, 34, 34, 34**. Three on the Anthropic preset for comparison: **33, 34, 33**, both failures being `open-agent-by-state`, its own long-recorded wobble. Read the two as level rather than as one beating the other; a single fixture's coin-flip is what separates them, and it is a different fixture for each.

**An earlier sweep scored 18/24 and that was OUR defect, not the model's.** Six fixtures had spent exactly 256 completion tokens — the whole of the hardwired ceiling, all of it reasoning — and returned `finish_reason: "length"` with nothing written. That is what made `intent.max_tokens` a field; see the row above, and note the defect was never specific to this model or this provider. It made the generic `openai_compatible` path unusable with **any** reasoning model whose reasoning is not suppressed.

**`reasoning_effort` is sent with the preset and with nothing else.** It is an OpenAI-family parameter, not part of the `/v1/chat/completions` shape every server implementing that path accepts — a different one may refuse an unknown field outright (the `llama.cpp` server probed for PRD #802 wanted `chat_template_kwargs` instead), and even `api.openai.com` refuses it for models that do not reason. `IntentSettings::reasoning_effort` is the gate and answers `Some` only when the endpoint **and** the model are both still this build's preset, so every edit a user can make — another provider, a gateway in front of OpenAI, a server on loopback, a different model at the same endpoint — takes it off and gets the provider's own default reasoning instead. That is the safe direction to be wrong in *because* the ceiling is 4096; at 256 it was not.

**One sentence in `TOOL_INSTRUCTIONS` is load-bearing for the switch and was added for it.** A bare *"go back"* fits `close` on the agent screen and `open_deck` on the overview, and the prompt conveys which screen you are on only through each row's `callable` flag — while also saying that picking a `callable: false` action is right when that is what was asked for. `claude-haiku-4-5` read the descriptions' screen qualifiers; `gpt-5-mini` did not, and answered `open_deck` on the agent screen in **seven of eight** measured runs — one fixture, failing systematically, which is not what the 24-fixture table above could have shown. The fix is a tie-break rather than a rewrite: *when the words fit more than one action and only one is `callable: true`, pick that one*. Editing the two rows' descriptions to point at each other was tried first and merely moved the failure to the other fixture, which is the same outcome the 2026-09-20 prompt-variant sweep recorded.

**The agent-CLI intent backend is GONE, and both of `intent`'s tokens name a protocol because of it.** It shipped as the default — `claude -p`, spawning the pre-authenticated CLI already on the box, so the app needed no key and nothing downloaded — and PRD #802's provider work removed it. Two reasons. First, the product one: once Commands needs a credential at all, the app cannot assume everyone uses the provider it picked, and a *subprocess* has no endpoint, model or key to choose. Second, it was the most security-expensive code in the feature: the prompt is built partly from untrusted input, so containing a general-purpose coding agent took seven CLI flags, absolute-path resolution, an app-owned working directory, an allowlisted environment, process-group teardown and a `Drop` guard — all of it tracking another program's flag set. `opencode` had already been withdrawn on that ground, for having no equivalent flags. A user's stale `intent = "claude"` costs one re-pick: it folds to the default and the rest of the document survives, as does `"opencode"`. **The pre-provider-work `"remote"` is mapped explicitly to `anthropic` rather than folded**, and that stopped being a nicety when the default moved: it named the Anthropic API, so its user's stored `SecretId::VoiceIntent` is an Anthropic key, and folding it to today's default would put that key in an `Authorization` header addressed to `api.openai.com`.

**The command token names a PROTOCOL, not a transport and not a vendor.** Both go over the same client to whatever `ServiceUrl` the user named; what differs is the request body and where the answer is read from. `voice/remote.rs` owns the transport and the Anthropic dialect, `voice/openai.rs` the OpenAI-compatible one, and `Protocol` selects between them. The labels name the provider because that is the word on the key a user has to paste.

**One thing about the OpenAI-compatible envelope is a trap and is written into the code.** `llama.cpp` also accepts `response_format: { type: "json_schema", schema: {…} }` — the schema hoisted a level, no wrapper and no `strict` — and that form returns **HTTP 200 and is silently not enforced**. Only the nested `{ type: "json_schema", json_schema: { name, strict, schema } }` is. `voice_openai_request_uses_the_nested_json_schema_envelope` asserts the schema is *not* at the hoisted key, rather than merely asserting one is present.

**That protocol's schema differs from the tool-use one in a way that reaches `IntentAnswer`.** OpenAI's strict structured outputs refuse a declared property that is not in `required`, so `params` cannot be left optional: it enumerates every param the table declares, requires all of them, and types each as `["string", "null"]`. The model is therefore obliged to name a param the chosen action does not take. `IntentAnswer`'s field deserializer drops those nulls, so it arrives downstream as absent and becomes `ParamMissing` exactly as the other protocol's answer does.

**A local model is reachable and is not recommended.** With Commands on HTTP, pointing `endpoint` at a server on this machine is the same code path, and a **loopback endpoint asks for no key at all** — `RemoteResolver::run` never consults the keychain for one, which is the same rule Speech has reached from the other side. There is no preset for it and no label offering it, for the measurements above.

**The latency is reported rather than hidden.** Every `VoiceResult` carries the milliseconds it cost beside the backend's name, because the mitigation for *slow enough to feel broken* is to show the number; the keyed backend's median is ~0.9 s against the deleted agent CLI's 3.1–4.7 s. Both protocols constrain the answer to the table's enum — Anthropic through tool-use with `strict: true`, OpenAI-compatible through a nested `json_schema` with `strict: true` — so an action outside the set is unrepresentable rather than merely unlikely. A **new** strict schema pays a one-time ~0.9 s compilation cached for 24 hours, so the user-visible shape is one slower utterance after you edit `commands.toml`.

### The credential, and the `SecretStore` seam

**No credential is in `desktop.toml` and none is in `localStorage`.** PRD #803 named the `SecretStore` seam and deliberately did not build it; #802 M4 built it, in `desktop/src-tauri/src/secrets.rs` — store, load, delete, keyed by a `SecretId`, over the OS keychain through `keyring`. Two ids exist: `voice-intent` and `voice-transcription`. Those tokens are **stable**; changing one strands a key a user already stored under the old name, where nothing reads it and nothing offers to delete it.

**The guard is what forced it, and going red is the mechanism working.** `xtask/linkage-check/src/desktop_settings_secrets.rs` pins every settings field's type to `ALLOWED_FIELD_TYPES`, and `String` is deliberately not on that list. Adding the `[voice]` section turned that check red at four fields; the resolution was closed enums for the three choices and the keychain for the key, which is why the document holds no field for a credential at all. It holds no *boolean* either, although the rule would allow one — the panel asks the store, so the answer cannot go stale against the keychain.

**The value is never read back.** There is no `loadSecret` on the settings bridge and there must not be: a stored key arriving in the webview is one `JSON.stringify` from the `localStorage` half of the rule. The input starts empty every time and the panel says *A key is stored* rather than showing a masked preview, because a preview is an information channel out of the keychain whose only consumer is reassurance. A failed save **rejects** rather than resolving with `stored: false`, and the panel renders that sentence and keeps what was typed — the outcome being designed against is a user who thinks their key is stored and finds voice broken tomorrow.

**A Linux box with no Secret Service is a documented, non-silent failure.** `keyring` has nowhere to put the key there, and the panel says so rather than reporting a save that did not happen.

### What is NOT verified, on any platform

Green checks cover none of the following, and a reader should not infer otherwise. There is no `tauri-driver` and no WebDriver session in this repository ([#953](https://github.com/vfarcic/dot-agent-deck/issues/953)), so **nothing automated drives the real Tauri window**, and the Rust unit tests run capture against a stubbed device.

1. **That the OS actually grants the microphone, on any platform.** The permission dialogs are reached by nothing automated. The macOS `NSMicrophoneUsageDescription` entry lives in the bundle overlay (`desktop/src-tauri/tauri.bundle.conf.json`) and `Info.plist`, and `build-macos` is the only job that will ever touch either. Whether a `tauri dev` window needs it to record, or only a packaged `.app`, is [PRD #802's Open Question 2](../../prds/802-desktop-voice-control.md#open-questions) and needs a macOS host.
2. **That a real utterance transcribes end to end.** Every transcription test is against a stub or a recorded fixture.
2a. **That the speech rule's one remaining picked number, `SPEECH_MARGIN`, is right for a population of microphones.** The relative rule that replaced `SPEECH_FLOOR` was reasoned out and tested synthetically across gains of 1 to 100 and noise levels of 180 to 9 000, from **one** real-world measurement — and that measurement was a refusal rather than a success. Four things behind it are argued rather than measured: that 3x the room (9.5 dB) is the right operating point on a real device, that a 1 s `NOISE_WINDOW` is longer than any sustained sound a speaker produces, that a noise *onset* mid-segment costs little (every adaptive estimator reads one as a speech onset until it catches up), and that a real device never delivers its first callback mid-syllable with no room in front of it. Nothing here can open a microphone, so the refusal's own three numbers are the whole diagnostic and the owner's re-test is the only falsifier — which is exactly the loop that produced the rule.
2b. **That a machine actually declines to sleep — on any of the three.** Narrower than it first read here, and the correction is worth keeping. This item was written as *"the macOS and Windows arms are run by nobody"*, reasoning from the true fact that no tier runs the GUI app on either platform. That does not follow: `build-macos` and `build-windows` each run `cargo nextest run --workspace`, so both link this crate and run its unit tests on a real Mac and a real Windows runner — `the_real_platform_is_asked_for_real_and_lets_go_again` is a `PASS` in both job logs. `IOPMAssertionCreateWithName` and `SetThreadExecutionState` are therefore called for real on every PR, and that test now **asserts the grant** on those two platforms, where the call depends on no daemon, bus or session and a refusal would be a defect rather than an environment. What is genuinely unverified is the half after the call: **nothing anywhere watches a machine fail to sleep.** The Linux arm was additionally watched going into and out of `systemd-inhibit --list`, including after a `kill -9` on the holder, which is the mechanism's own contract rather than the outcome. And one Linux-specific gap remains: a desktop whose power manager decides idle by itself and never consults logind will suspend regardless — the acquisition still succeeds, so a refusal is not the signal for it. The manual walk's step 7a is the whole of the outcome check on all three platforms.
3. ~~**That the `openai_compatible` command protocol works against OpenAI itself.**~~ **VERIFIED 2026-09-20**, and it had to be: that protocol on `api.openai.com` with `gpt-5-mini` is now the shipping default, so the phrase fixtures run against it on every credentialed run. Six runs in the shipping configuration on the day of the switch, 32/34 to 34/34, plus a dozen more while the tie-break sentence was being settled. The shape was already verified against two other servers — a local `llama.cpp` in PRD #802's reconnaissance, and Anthropic's own OpenAI-compatible endpoint, where a probe confirmed the nested `json_schema` envelope is genuinely grammar-enforced: an `action` enum holding one bogus token came back holding that token, over the answer the model plainly wanted to give. What stays unverified about it is what stays unverified about every entry in this list: no CI job runs it, so it is verified exactly as often as a developer runs it.

   (Two different third items have stood here before — *"that the agent-CLI intent backend resolves from an app launched with a Finder-shaped PATH"*, whose backend is gone, and the unverified-preset note this one replaces.)

The phrase fixtures (`desktop/src-tauri/src/voice/phrase_fixtures.toml`, driven by `desktop/src-tauri/tests/voice_phrase_fixtures.rs`) map utterances to expected actions against a real backend. They are opt-in, skipped when no backend is configured, and **run in no CI job** — CLAUDE.md rule 5's lane 2 in the desktop crate's own idiom. Run them yourself when you change the table, the prompt or a backend, because nothing else will.

### Adding a voice command: the checklist

1. **Implement the capability as normal**, reachable by click, and make sure it dispatches through `VOICE_ACTIONS` rather than a bare `onClick` — [the next section](#the-voice-action-registry-and-what-its-guard-does-and-does-not-see) is why, and what the guard cannot see.
2. **Add the row** to `commands.toml`: `id`, a `description` written for a model, `invoke` naming the registry entry, `screens` listing only the screens where **a click can already reach it**, an `unavailable_hint` naming the prerequisite, a `report` sentence, and a `[[commands.params]]` block per param.
3. **Flip the registry entry** from `no_voice: "<reason>"` to `voice: true`, and declare the context members its `run` needs in `needs`.
4. **Run `cargo test-fast`** and add your row to each pinned-row assertion that fails. Expect about five.
5. **Run `cargo xtask linkage-check`** — rule 14 is what catches a typo in `invoke`, an unknown screen, an unknown param `kind`, or an entry nobody classified.
6. **Add a phrase fixture** and run it against a real backend yourself.

**If the behaviour belongs to the voice surface rather than to a screen** — the microphone, the report row, anything the panel owns — publish it through `VoicePanelChannel` instead of `deckVoiceContext`. `VoiceScreenContext` is `Omit<VoiceActionContext, keyof VoicePanelContext>`, so the two halves are complements by construction: a member cannot end up served twice or not at all, and putting one on the wrong side is a type error rather than a runtime refusal.

**Do not widen `screens` beyond what a click can reach.** M8's row is the worked example: `open_settings` shipped as `["deck"]` and not `["deck", "overview"]` because the Settings overlay was then reachable only from the deck rail, and listing `overview` would have let voice open a surface no click could reach from that screen. Issue #1197 widened it the honest way round — by making the rail one component, rendered once by `DeckShell` with the same entries beside every screen, so Settings gained a button on the overview first — and `open_overview` and `open_deck` became `["deck", "overview"]` for the same reason. None of the three lists `agent`: the rail is still mounted there, but inert behind the modal pane. Issue #1198 then hid the deck by default, and `open_deck` follows the rail button rather than the `screens` column: while `show_desktop_deck()` is false the crate offers the row `callable: false` with its own hint (`voice::schema::hidden_by_flag`) and refuses a pick of it, `DeckShell` refuses the dispatch as well, and the list of what can be said leaves it out. That is voice becoming a second control surface with capabilities the first lacks, which is the one thing this design forbids — **a row does not define behaviour, it exposes behaviour that already exists**.

## The voice action registry, and what its guard does and does not see

PRD #802 is building voice control for this app, and the piece to understand first is **`desktop/src/lib/voiceActions.ts`**, because it is what the command table's `invoke` column points at. The surface that drives it — the panel, the microphone toggle, the report — is the next section. The credentialed phrase fixtures belong to that PRD's M10 and are not documented here yet.

**`VOICE_ACTIONS` is the app's dispatch seam for the rail and the command palette, not a list beside it.** Every rail button, every palette entry, the agent tile's open/close pair and the overview's row open all run through `VOICE_ACTIONS[id].run(context, params)`. That is deliberate and it is the whole point: the voice command table's `invoke` column names an entry here rather than a `#[tauri::command]`, because none of the registered Tauri commands performs a navigation — navigation in this app is React state. If a control dispatched its own `setState` beside the registry, `invoke` would be pointing at a list rather than at the app.

**Voice dispatches through `dispatchVoiceAction`, in the same file, and there is deliberately no `switch` over action ids anywhere.** It looks the outcome's `invoke` up in `VOICE_ACTIONS` and calls the same `run`, with one target object every entry reads the members it declared from. A `switch` would type-check and would be a second list of the ids — and it would be *implementation* that every new command has to edit, which is what PRD #802's acceptance criterion forbids. [What adding a row actually costs](#what-adding-a-row-costs-measured-rather-than-asserted) is measured below and it is not one file. It returns `false` for an `invoke` naming no entry — unreachable, since linkage-check rule 14 fails the build on one — so that the surface can say the command did not run instead of reporting a navigation nothing performed.

**Each entry is classified.** Either `voice: true`, which means a row in `desktop/src-tauri/src/voice/commands.toml` names it, or a written `no_voice` reason saying what about that capability makes it a poor fit for a spoken command. A reason is read by a person, so "not yet" is not one — write what a later reader would need in order to disagree with you.

**Rule 14 in `cargo xtask linkage-check` is what keeps the two files honest.** It asserts that every `invoke` names a registry key, that every `screens` entry is a `kind` of `DeckView` in `desktop/src/types.ts`, that every param `kind` is a `ParamKind` from `voice/table.rs`, that every entry is classified, and that the registry literal stays statically readable — a spread or a computed key among its entries is refused, since the keys are read as text. `xtask/linkage-check/src/voice_command_registry.rs` holds the rule and its planted-bad-input tests, one per assertion, so "no findings" means something.

**What it does NOT see, said here as well as at the failure itself.** The check proves every registry **entry** is classified. It does **not** prove the registry is **complete**. A control wired with a bare `onClick` that never reaches `VOICE_ACTIONS` is invisible to it, and there were 80 `onClick=` sites in non-test `.tsx` under `desktop/src` when PRD #802 was written, against 7 static palette entries and 7 rail buttons. Most of the ones sampled are chrome — dismiss a toast, close a sheet — and the check could not tell a capability from a close button anyway, so the residue is not claimed to be harmless. Read a green rule 14 as "the table and the registry agree", never as "nobody forgot a capability". The one mechanical approximation to the second claim is pinning a count of interactive controls, which that PRD's Open Question 3 recommends against for now with its churn cost named.


### The voice surface, and the four decisions in it worth knowing before you edit it

PRD #802 M6 added `desktop/src/components/VoiceControlPanel.tsx`: a fixed **Voice** button, and the transient report beside it. There is no dialog and no text box. Pressing the button turns continuous voice control **on**; pressing it again turns it off; in between, each utterance is segmented by the voice-activity detection in `voice/capture.rs`, transcribed, resolved, run and reported, and the microphone opens again for the next one without another press.

**The typed path was withdrawn at the product owner's direction, and the withdrawal is the reason the rest of this section reads as it does.** The shipped M6 opened a dialog with a `Command` text box and a *Run command* button, and offered the microphone only once transcription was configured — a build-time decision rather than anything issue #802 asked for. The requirement it was replaced with is: *"Only voice. When I click the Voice button, it should activate voice control and it should keep being controlled by voice until the button is clicked again. The button should indicate whether voice is currently on or off. If voice control does not work, it should show instructions how to enable it instead."* That is closer to what the PRD's own default activation mode already said, and it is why the VAD deferred to D4 came forward into M6.

**The button carries its state twice, and both are required.** `Voice on` / `Voice off` as visible text, and a matching `aria-pressed`, because the two audiences read different things and a toggle that showed one without the other would be on for only one of them. `desktop/e2e/voice-control.spec.ts` pins both against the real app in two browsers.

**The button is the ONLY indication the microphone is open, which is what makes a wrong one a privacy defect rather than a cosmetic one** — and PR #1163's security audit found a state in which it lied. A fresh panel initialises its toggle to *off*, and a mount is not always the first one: a hard webview reload, a crashed web-content process, or a webview the platform destroyed and recreated all produce a panel with no memory in front of a `CaptureSession` that may still be recording. So the panel had nothing to reconcile against, and rendered `Voice off` over a live device — with no way out, because the press that looks like it turns voice on calls `desktop_voice_start`, which `CaptureState::accepts_start` refuses while a recording is running. Past the 30-second cap it is worse: `cap_reached` releases the device on purpose but keeps the audio, so up to 960 KB of captured speech sat in the session with nothing able to reach it. Two halves fix it, and the first is the one that matters.

**The RELEASE is Rust's, at four teardown triggers, because a React passive effect making an asynchronous IPC call is not a lifetime.** A webview that is going away is not guaranteed to run the cleanup, let alone to let the call finish. `release_microphone` in `desktop/src-tauri/src/lib.rs` is called from: the main window's `CloseRequested` and `Destroyed` (`window_ends_capture` — both, because a vetoed close never reaches the second and a real one reaches it after the window is unusable); `on_page_load` with `PageLoadEvent::Started`, which is the document being replaced without the window going anywhere; `on_web_content_process_terminate`, **on macOS and iOS only** — tauri exposes no WebKitGTK equivalent, so a Linux web-process crash is released by whichever other trigger arrives first; and `release_on_exit`, where it runs **before** the terminal detach and the tunnel close, since those are bounded at 250 ms *per session* and a microphone should not stay open through a teardown that has nothing to do with it. `cancel` is idempotent and never refused, so every one of them is unconditional. It is also synchronous on the calling thread — `CpalStream::drop` joins the device thread — and that is deliberate: a release that is *scheduled* rather than done is a release that may not happen before the process does.

**Voice also holds the machine awake, and it is released by the same call — which the compiler enforces rather than review.** Voice control generates no input events: every idle timer the three platforms ship counts mouse movement and keystrokes and none of them counts a microphone, so a user actively driving the app by speaking looks *idle* to the operating system and the machine suspends underneath them. The product owner's framing is the requirement — *"if it's controlled by voice, it would be silly that one needs to move a mouse every once in a while to keep it from going asleep"* — and the trigger is the Voice button rather than window focus or a running agent, because *while voice is on* is exactly the state that produces no events, it is already visible (the button **is** the indicator), and it is under the user's deliberate control. There is no setting for it.

`desktop/src-tauri/src/voice/wake.rs` owns the platform calls and `desktop/src-tauri/src/voice/hold.rs` owns the pairing. **`VoiceHold` keeps the `CaptureSession` PRIVATE**, so nothing in `lib.rs` can spell `cancel` at all — the only release it can reach is `VoiceHold::release`, which lets go of both. A teardown trigger that forgot the inhibit therefore does not fail review; it fails to compile. That is deliberately not a convention: an inhibit is invisible in a way an open microphone is not — the OS shows the microphone and shows nothing for this — so a stuck one is a laptop that never sleeps again with nothing anywhere to explain why, and the user's remedy is a reboot.

| platform | call | released on process death by |
| --- | --- | --- |
| Linux | `org.freedesktop.login1.Manager.Inhibit("idle", …, "block")` over D-Bus | the **kernel**: the inhibit is a file descriptor, and logind drops the inhibitor when its end sees EOF. Measured — a `SIGKILL` on the holding process took the entry out of `systemd-inhibit --list` |
| macOS | `IOPMAssertionCreateWithName(kIOPMAssertPreventUserIdleSystemSleep, …)` | **powerd**, which tracks assertions per process |
| Windows | `SetThreadExecutionState(ES_CONTINUOUS \| ES_SYSTEM_REQUIRED)` on a **dedicated thread** | the **kernel**: the request is per-thread state, cleared when the thread exits |

**No new dependency.** `zbus` is already in the graph through `keyring`'s Linux backend and `windows-sys` through the root package; macOS is four hand-written `extern` declarations against frameworks the target already links, since `objc2-io-kit` is not in the lockfile and adding it for four lines is a worse trade. The lockfile change is two edges on the desktop package and no new crate. **Windows needs its own thread** and it is not ceremony: `SetThreadExecutionState` sets state on the *calling* thread and the system clears it when that thread exits, so making the call from the blocking pool would lapse silently the next time the pool retired a worker — a machine that sleeps anyway with everything here still reporting *held*.

**Sleep, not the display.** The user is talking, not watching, so all three are the system-idle request and each leaves the screen free to blank. On Linux that is also why `what=idle` rather than `what=sleep`: `sleep` blocks the user's own explicit suspend and needs a privileged polkit action, while `idle` denies the false premise the automatic suspend was drawn from. What it does **not** cover, stated rather than implied: a desktop environment whose power manager makes its own idle decisions without consulting logind will still suspend, and covering those means a second desktop-specific call that is worth adding against a real report rather than guessing at.

**The two device releases that deliberately KEEP it.** `VoiceHold::stop` and `VoiceHold::cap_reached` close the device without touching the inhibit, because voice control is a *cycle* — start → speak → stop → transcribe → resolve → start — and the microphone is shut for the whole transcribe-and-resolve leg, which PRD #802 measured at over a second per utterance. An inhibit that tracked the device would lapse in the gap between every pair of sentences, and an idle timer coming due in one of those gaps is the exact defect this was built to fix. So the inhibit spans the session and the device does not; `VoiceHold::start` is still the only thing that takes one, so it cannot be held by a process that never opened a microphone.

**The platform is asked with the lock NOT held, and that is a fix rather than a style.** The first version of `WakeLock::hold` asked for the inhibit with its mutex held, on the reasoning that it made two concurrent holds impossible to interleave. It also made `release` wait on the platform — and the two run on different threads by construction, `hold` from `desktop_voice_start`'s `spawn_blocking` and `release` from `desktop_voice_cancel`'s. A system bus that accepted the connection and then never answered would have wedged the *release*, and a release that cannot run is **a microphone that cannot be closed**: the audit blocker above, reintroduced by the thing meant to sit beside it. The acquisition now happens outside the guard, ordered against a release by a **reservation** — the same device `CaptureSession`'s `SessionInner::opening` uses, for the same finding, so read that doc comment for the microphone's version of this. A release bumps the generation, so a hold that arrives afterwards finds its reservation stale and gives the inhibit straight back rather than installing it against a session that is over. `a_release_during_an_acquisition_leaves_the_machine_free` drives exactly that instant deterministically, by having the stub inhibitor perform the release from inside the platform call; against the old implementation it does not fail, it **deadlocks**, which is the defect stated precisely. The D-Bus call is additionally bounded at 2 s (zbus's own default is no timeout at all), which is ~800x the measured median and therefore cannot be tripped by a loaded machine.

**Measured on the dev box**, warm: **2.3–4.0 ms** to acquire — the first call carries the system-bus connection — and **4–42 µs** to release, against a call path that is already opening an audio device. Once per voice-on, not once per utterance: `hold` returns immediately when something is already held.

**Failure is silent and non-fatal, and that is the whole policy.** A headless box, a container with no D-Bus, a policy that says no: `WakeLock::hold` swallows the refusal into one log line — latched to once per process, since `hold` runs at the top of every utterance — and voice control works exactly as it did. There is no sentence for the user because there is no action they could take from inside the app, and `VoiceHold::awake_held` then reports `false`, which is deliberately the answer to *is the machine being held* rather than to *did we ask*.

**The panel's half is a mount RECONCILE, and a third button state to go with it.** On mount it asks `voiceStatus` once and cancels anything Rust is still holding (`recording` or `transcribing` — the two states `accepts_start` refuses) before it claims the device is closed. Until that answer arrives the button reads `Voice…` with `aria-pressed="mixed"`, ARIA's own value for a toggle whose state is not settled, because `false` there is the same claim the reconcile exists to stop making. A press during the round trip supersedes the reconcile and does the same reconciliation itself, so the user is never waiting on it. A `pagehide` listener cancels too, and is explicitly defence in depth rather than the mechanism — it is the same asynchronous IPC on a document that is going away.

**A reconcile whose RELEASE is refused does not go on to claim the device is closed**, which is the same audit's finding on the same surface and was the one place the paragraph below did not hold. The reconcile swallowed the rejection and settled the button to `Voice off` with a crossed-out microphone over a session Rust had just declined to let go of — on the surface with the least information of any of them, a fresh panel with no press to report against. It now takes the same route `turnOff` takes: re-query, and on anything other than *the device is genuinely gone*, render the not-released presentation and put `VOICE_RELEASE_REFUSED` in the report. `turnOn`'s own reconcile did the same thing one step less obviously — it fell through to `desktop_voice_start`, which `accepts_start` then refused, so the press ended at `off` with an error sentence beside it — and takes the same route now. **The press is the retry on both**: the click handler routes `unreleased` to the release rather than to the start, whether or not the toggle was ever on.

**Turning voice off AWAITS the release, which is the same audit's should-fix and the same defect at a shorter timescale.** `turnOff` used to call `setOn(false)` and then fire `voiceCancel` without awaiting it, swallowing any rejection — so the button read `Voice off` before Rust had acknowledged anything, and permanently if the call failed. That is not a scheduling instant: `desktop_voice_cancel` runs the teardown on a blocking thread and `CpalStream::drop` joins the device thread, which is exactly why the three concurrency regression tests in `voice/capture.rs` drive a stub stream whose `Drop` blocks. So there is now a fourth button state: while the release is in flight it reads `Voice stopping…` with `aria-busy`, and a press meanwhile is **serialised** — ignored rather than racing the answer or starting a recording over a device Rust has not let go of. A press in any other state is live, `unreleased` included, because that press is the retry.

**A rejected release never reads as `off`, at any of the three places a release is attempted** — `turnOff`, `turnOn`'s reconcile and the mount reconcile, which all go through `releasedAfterRefusal`. The panel re-queries `voiceStatus`: if Rust reports the session idle, the call failed but the device is genuinely gone, so `off` is truthful and the rejection is still reported. Anything else — a held session, a status read that also failed, a runtime with no `voiceStatus` at all — keeps the button saying the microphone may be open and puts `VOICE_RELEASE_REFUSED` in the report beside the rejection's own sentence, naming the retry. **Every unknown about a RELEASE resolves to *not released***, because this is the one place where guessing in the direction that suits the button would be the whole defect.

That sentence used to be written one quantifier wider — *every unknown resolves to not released* — and PR #1163's review was right that it was false of two paths (both now closed, above). The narrower version is the one that is actually upheld, and the residual it leaves out is worth naming rather than re-absorbing: a **mount reconcile whose `voiceStatus` read itself fails** attempted no release and has no evidence a session exists, and it settles the button to `off` rather than leaving it at `Voice…` for ever. That is a guess, in the direction this surface is otherwise careful about — and the alternative is a permanent `aria-busy` `Voice…` claiming a round trip that is not in flight, which is its own false statement to a screen reader. `turnOn` reconciles again at the next press, which is what bounds it. If that trade is ever revisited, the thing to change is the *indicator*, not the reconcile: a fourth word for *nobody has been able to ask* would let the panel stop guessing without claiming to be busy.

**A press with no usable transcription backend shows instructions rather than failing quietly**, and the button is neither hidden nor disabled — a greyed-out control shows nothing and reads as a fault. The press reports `VOICE_UNAVAILABLE`, which names *Settings → Voice* rather than advising the user to "configure a backend". (This paragraph used to open with *"`off` is the default and a product statement"*, which the keyless-speech work made false twice over: the default is `local`, and the `off` token is gone. What a user can still meet is a `local` endpoint with no container behind it, which `voice::transcribe`'s unreachable sentence answers by naming the `docker run` rather than a setting.) Availability is read **at the press**, not cached from mount, so choosing a backend and pressing again works without a restart.

**It renders sentences; it never composes one.** Every situation an utterance can end in — a dispatch, a command the screen cannot run, a no-match, a param that resolved to nothing or to more than one thing, a backend that failed — arrives from Rust carrying its own `sentence`, rendered from the command table. The panel reads `outcome.kind` for exactly one decision, whether to dispatch, and prints `outcome.sentence` for everything else. It never builds wording out of `action`, `param`, `matches` or `detail`. **Six** sentences are written frontend-side (it said *two*, and was already three short before this list grew). `NOTHING_DISPATCHED` and `SCREEN_MOVED_ON` are for situations Rust structurally cannot see; `VOICE_UNAVAILABLE`, `VOICE_CAP_DISCARDED`, `VOICE_RELEASE_REFUSED` and `VOICE_EMPTY_STATE` are about the surface's own state — a microphone it has nothing to open, an utterance it threw away, a release it cannot vouch for, a row with nothing in it yet — rather than about an utterance. The dictation countdown is a seventh and the only one built around a value from elsewhere: nothing Rust-side knows a send is pending, so no table column could carry it, and a countdown that did not name whose prompt it was about to submit to would leave the one question a user needs answered before it reaches zero. `NOTHING_DISPATCHED` is for a dispatch whose `invoke` names no registry entry — the entry lives in TypeScript, and linkage-check rule 14 already fails the build on one that resolves to nothing. `SCREEN_MOVED_ON` is for an outcome that arrives after the user has walked to another screen: the classification was made against the screen declared with the utterance, so running it on the screen they are standing on now would act with the wrong screen's permission (see *The screen is declared* below). Every string does cross `displayText` on the way to the DOM, at the `message` budget: a sentence embeds a transcript, and a transcript is model- or microphone-supplied text with control and bidi codepoints still in it. That sanitises and bounds; it does not reword.

**It is mounted ABOVE the deck/overview screen switch, and that is architectural.** `DeckShell` returns one fragment on both paths — the rail at index 0, the screen at index 1, the panel at index 2 — so React keeps the panel's fiber while the screen below it is replaced. The report describes a navigation that has just happened and the Undo beside it reverses one, so both have to outlive the screen change they are about; mounted inside either screen, a successful `open_overview` would unmount the panel that was explaining it. If you restructure that return, keep the panel at a stable position: a fragment on one path and a bare `DeckSurface` on the other is a different root type and remounts everything under it.

**The browser fixture is what replaced the typed path's coverage argument.** The typed box was defended as the path every user has with no credential, and therefore as the only one anybody could test. That does not survive its own premise: a path nobody speaks through exercises the resolver and never the surface. `FixtureDeckBridge` now simulates a microphone the same way it simulates a daemon — with a transcription backend chosen in its settings, a start is accepted, the next status poll reports the utterance over, and a stop returns one canned transcript from `FIXTURE_VOICE_COMMANDS`. So the whole segment → transcribe → resolve → execute → listen-again cycle is driven in Playwright with no device and no key anywhere near it. It speaks **once per activation** and is then quiet, latched by `voiceCancel`: a stand-in that spoke every quarter second would overwrite its own report before anyone could read it, and a person who says one command and stops is the behaviour being previewed anyway.

**The screen is declared, not passed.** `resolveVoice(utterance)` takes the utterance and nothing else; the mounted screen reaches the bridge through `declareVoiceScreen(screen)`, called synchronously immediately before each resolve. The reason is that an utterance is resolved against live state read where that state already lives — the agent list Rust-side from the selected deck's own snapshot, the command table from the binary it is embedded in — and the mounted screen is the one piece that exists *only* in the webview, as a `useState<DeckView>`. Declaring it from an effect instead would let it lag a navigation and validate the next utterance against the screen the user just left.

**And the screen is re-checked when the answer comes back, which is a second thing and not the same one.** Declaring it at submit time fixes what the utterance is judged against; it does nothing about the user moving while the answer is being worked out — PRD #802 measured 0.653 s for speech and 0.62–1.03 s for commands, and 4.3–6.3 s for the whole pipeline before the agent-CLI intent backend was withdrawn. An outcome resolved against the overview and dispatched on the deck runs a command the overview authorised on a screen that never did, and PR #1163 found it doing exactly that: *"open settings"* spoken on the overview opened the Settings sheet on the deck. The surface compares the screen at submit with the screen at answer and renders `SCREEN_MOVED_ON` rather than dispatching. **The same reservation covers voice being turned off mid-flight**, which is the sharper half of one finding: abandoning used to invalidate nothing, so the continuation still called `onDispatch` and the app navigated seconds after the user stopped Voice. A `request` counter is claimed by each in-flight cycle and bumped by `turnOff` and by unmount, and every continuation asks whether it is still current before it writes state or dispatches. The claim is taken in `takeUtterance` rather than in `resolveOne` — transcription is the *first* await, so a cycle abandoned during it would otherwise reach the resolve through a step that claimed the surface afresh, which is an abandoned request laundering itself into a live one.

**The 30-second cap DISCARDS the segment, and does not transcribe it.** With `voice::Vad` ending an utterance after `voice::SILENCE_HOLD` of quiet, reaching `voice::MAX_UTTERANCE` means no boundary was ever found — continuous speech, a conversation in the room, a call, steady noise. In a navigation vocabulary where every command is one to four words, that is *definitionally* not a command, so transcribing it would spend a paid transcription call and then a paid intent call to arrive at a no-match, once every thirty seconds for as long as the microphone is open. The surface calls `voiceCancel`, reports `VOICE_CAP_DISCARDED`, and keeps listening. The earlier design offered **Send the recording** or **Discard it**; those buttons lived on a panel that no longer exists, and auto-transcribing was the wrong thing to replace them with.

**The webview learns an utterance ended by polling, and the interval was chosen with the latency in hand.** `voice::Vad` decides the speaking has stopped on the device thread, and `CaptureSession::status` reports it as `state: "done"` while the audio is still there to be taken; a poll that comes back `done` is the boundary the stop → transcribe → resolve → start cycle hangs off, and `capped` is checked first because a capped segment is both and the two answers differ. A Tauri event emitted from the capture side would carry the boundary with no wait at all — it would also be a new IPC verb in each of the three bridges plus a listener seam, for a saving bounded by the interval, so `VOICE_STATUS_POLL_MS` was cut from one second to **250 ms** instead. That puts the polling tax at 125 ms of mean added latency. **The pipeline it was weighed against has since got much faster** — that 4.3–6.3 s was the agent-CLI intent backend, which PRD #802's provider work withdrew, and the measurements left are 0.653 s for speech and 0.62–1.03 s for commands — so the tax is nearer a tenth of the wait than the under-3% it was, and about a sixth of `SILENCE_HOLD`'s 800 ms, which remains the term that dominates the end of an utterance. The cost is four state reads a second while the microphone is open, each an in-memory read of the capture session plus the small settings document, touching no device. The event path stays available if the hold ever gets short enough for this to matter.

**The cycle is serial, and that is a stated trade rather than an oversight.** The microphone is shut while the backends are working rather than recording over them. Overlapping would let a second command resolve against a screen the first is still changing, and the `SCREEN_MOVED_ON` guard would then be refusing the user's own sentences. The cost is that speech during those seconds is not captured, which is why the report says what it is doing — *Turning that into text…*, then *Working out what that means…* — instead of continuing to claim it is listening.

**A dispatch the host cannot serve is refused, not attempted.** `DeckShell` is the only component mounted for every screen, so the voice dispatch is built there; but five registry entries reach `useState` booleans that live inside `DeckSurface`. `DeckSurface` publishes the context it can serve upward through a `VoiceContextChannel` — a mutable slot the shell holds, republished on every commit because the context closes over that render's setters, and cleared on unmount — and the shell merges it in, putting its own `navigate` and `closeAgentView` last so those two mean the same thing whether or not a deck is mounted. `dispatchVoiceAction` then checks each entry's declared `needs` **before** calling it: an entry the host cannot serve answers `false`, which the surface reports as `NOTHING_DISPATCHED`. Checking rather than catching is the point — M6 did neither, and an overlay dispatch from the overview threw `TypeError: context.openOverlay is not a function`, which the report then rendered at the user. A caught message would break the "the app renders every sentence" property exactly as the thrown one did. `needs` is read at runtime and the `Pick<VoiceActionContext, …>` on `run` is not, so a `NeedsCoversRun` mapped type proves at compile time that the list covers what `run` requires; it deliberately tolerates an over-declared list, which only makes dispatch stricter.

**The Undo window is ten seconds**, `VOICE_UNDO_WINDOW_MS`. Long enough to react to a navigation you did not mean, short enough that it never becomes a second permanent navigation control in the report — the deck's own controls are what "go back in general" means, and this affordance is only about undoing *that* command. The destination is read off the view at dispatch time rather than popped from a stack, which is the same choice `closeAgent` makes and for the same reason: there is no history to be wrong about. It is **offered** only where a view actually moved: `open_settings` puts an overlay over the deck and leaves the view where it was, so the shell wraps the two view-moving context members and offers the Undo only if one was called. An object with no `undo` is the existing contract for *it ran and there is nothing to reverse*, which `onDispatch` already distinguishes from `undefined`.

**The voice surface is a PEER of the agent pane, not background, and it took an exemption in the modal fence to make that true.** M6 shipped with the opposite: while a pane is open `useInertBackground` marked every element that is not one of its ancestors `inert`, and the voice surface is the screen's sibling, so it was marked with the rest of the background. `inert` removes an element from hit testing as well as from the tab order, so the trigger was not merely untabbable but unclickable — measured in the browser tier, where the click was intercepted by the pane's own footer — and `close_agent_view` — the row that then owned "close this" on the agent screen, and the only one whose `screens` column was `["agent"]` — could not be spoken in a shipped build. (That row has since folded into `close`, which is callable everywhere; what the exemption now keeps reachable on that screen is the whole dictation pair as well.) The second of the two fixes named here was taken: the hook now skips elements carrying `VOICE_PEER_PROPS`, which the trigger and its report spread, and nothing else. It is an allow-list of one marker rather than a rule about roles or dialogs, so a second peer has to be exempted there deliberately. **The exempt elements are no longer a dialog and the exemption did not widen when they stopped being one** — nor when the surface became a reserved row and the count went from two marked elements to **one**. That is the exemption narrowing rather than moving: the row is one marked sibling holding the trigger and the report, so the walk skips the same subtree it skipped before, and the Undo inside the report is still exempt because it is a control. What the fence still guarantees is unchanged for everything else, the `DeckSelector` above all, and `AgentPaneDeckIdentity.test.tsx` states it as an equality against that one trigger rather than as an emptiness — so any *other* control escaping an open pane is still a failure.

**The surface is ONE RESERVED ROW at the bottom of the window, and that is a layout decision the product owner made after driving the real app.** Until PR #1163's review round the trigger and the report were two independently fixed boxes at z-index 90, the report stacked above the button and growing upward as it filled — so it was drawn over whatever was beneath it, including over the enlarged agent pane, which is the one place the user is actually working. Moving the boxes buys a different collision rather than none: a floating box's height is its content's, and no other element can set room aside for something whose size it cannot know. A row can. `--voice-row-height` (40px) is the contract, and every surface that would otherwise reach the window's bottom edge subtracts it — `.deck-main` on both screens, `.agent-pane-overlay`, `.reader-overlay`, `.evidence-drawer` and `.toast`, each marked *voice row* at its own rule. The row is z-index **95**: above the agent pane (90) and the reader overlay (80), below `.dialog-backdrop` (100). Two exclusions are deliberate rather than missed. A **modal dialog or sheet still covers the row**, which is correct — the voice surface is not exempted from `useInertBackground` for those the way it is for the agent pane, and shrinking their backdrops would leave a control clickable underneath a window-covering modal. And the **rail** is not on the list because it never overlaps: at desktop widths the row starts at the rail's right edge, and at `max-width: 680px`, where the rail becomes a 54px bottom bar, `--bottom-rail-height` lifts the row onto it. The agent pane keeps `inset: 0` — a modal scrim should cover the window — and it is its *content* box that stops above the row, via `padding-bottom`. The row's last report **persists** until the next utterance replaces it, which is now a decision rather than something inherited from the floating card: the space is reserved whether or not it holds anything, the pipeline goes straight back to listening, and a sentence that faded on a timer would be gone before a user who glanced away came back. `voice-control.spec.ts` asserts the geometry in both engines, including `elementFromPoint` at the row's own centre — a box comparison proves the pane's content stops above the row, and only a hit test proves a click there reaches the row rather than the scrim.

**The `aria-modal` question went away with the dialog rather than being answered.** The exemption had made the voice dialog's own `aria-modal="true"` a false claim — a dialog that deliberately coexists with another interactive dialog, and that inerts nothing itself, is not modal in the ARIA sense — and the correction at the time was to state `aria-modal="false"` and trap no focus. There is now no dialog at all: a button and a `role="status"` live region, which claim no modality, take no focus and trap none, so nothing has to be stated. The live region is mounted before any sentence arrives, which is what makes the announcement reliable, and the card inside it is what appears and disappears. The agent pane's own `aria-modal="true"` is untouched: it genuinely inerts everything but one named peer, which is the ordinary shape of a modal with a status region beside it.

### What the voice feature does and does not persist

PRD #802's Open Question 5 asked whether any part of an utterance is stored. The answer is in **three parts**, and the unqualified one-part version was written down and was false, which is why all three are spelled out here. Quote all three or none: the first on its own is the claim that was false, and the first two without the third read as covering a hosted endpoint that this app cannot speak for.

**This app persists nothing.** No transcript, utterance or audio buffer is written to a log or to disk by any code in `desktop/src-tauri/src/voice/`. `Transcript` and `Pcm16` both carry hand-written `Debug` impls that print a length and no content, so a derived `{:?}` on a container that holds one prints neither — which closes the careless route; the deliberate route is a rule this crate keeps. The IPC seam carries it one step earlier: `desktop_voice_stop` transcribes Rust-side and returns text, so no audio buffer ever crosses into the webview.

**A child process the app hands the prompt to was a different question, and this is where the first answer was wrong.** The `claude` intent backend passed the whole prompt — the utterance, and the agent labels from the daemon with it — to another application, and that application has storage of its own. It was writing a resumable session containing all of it; a redacted `Debug` impl in Rust does nothing about that. The backend was given `--no-session-persistence` along with the rest of its containment flags, and the `opencode` backend was withdrawn in the same change for having no equivalent one. **PRD #802's provider work then removed the agent-CLI backend outright**, so today no stage of this feature hands a prompt to a child process at all and the unqualified claim is true again. The two-part wording is kept as the rule a future backend inherits rather than has to rediscover — the last one cost an audit round to notice.

**A hosted backend's own retention is neither of those halves and is not something this app can assert at all.** The keyed remote intent and transcription backends upload to an endpoint; what that endpoint keeps is its policy.

**What each command SENDS is a separate question from what is kept, and the Voice panel answers it beside the Commands endpoint** (PRD #1223, audit finding A1): the words heard and the command table always, and — with the panel's **Names** row on *Shared*, the default — the names the app observed: agents with their live status, deck labels (`user@host[:port]` for a remote one), up to 200 directory names on screen, the New agent form's chips and picker entries, and orchestration titles and roles. *Withheld* sends only the first half, and the commands that need a name report themselves unavailable. Those names travel in a data turn marked untrusted, never in the system message. Every ACTION the model returns is held against the transcript before dispatch; a REFERENCE is not — since 2026-09-24 it only has to resolve against what is on screen, and the two rows that stop anything stop at a confirmation naming the target, which is the control that a destructive action reaches the right thing. [voice-first-design.md](voice-first-design.md#6-observed-names-are-untrusted) has the field-by-field list, the three layers, and why reference grounding was removed.

## Current milestone limits

- The Tauri crate directly reuses the root library; the standalone protocol-crate extraction remains pending.
- **A remote deck is now the app's own feature, it HAS run against a second machine, and the half still missing is what the client rendered.** PRD #741 replaced the hand-rolled forwarded socket with a configured deck: an `Endpoint` whose kind is in the type, stored in `desktop.toml`, reached over an app-managed `ssh -N -L` child, chosen from the **Deck selector**, with a **Test connection** button that classifies twelve outcomes — [Decks](#decks-which-ones-the-app-talks-to-and-how-it-reaches-another-machine) is the whole of it. The thing that made remote *observation-only* is gone too: project resolution moved daemon-side in #819 M6, so the desktop sends back only a path the daemon supplied or the user typed. **The run on a second machine has now happened**, recorded in `prds/741-desktop-connect-any-daemon.md`'s Work Log for 2026-09-13: a desktop on a Mac drove a workflow launch against a deck on the Linux dev box over `ssh -L`, that daemon resolved a project there, published `orchestrator-context.md` there, and five roles started under it with real agents rather than stand-ins (`ClaudeCode` on two panes, `Codex` on two more). **What stayed unticked is the display half** — nobody looked at the connected window to confirm that the project header and the agent panes name `/home/vfarcic/…` and never `/Users/viktorfarcic/…`, which is the exact symptom the 2026-08-29 measurement reported. A daemon log proves what the daemon did and says nothing about what the client drew. So the mechanism is verified and the rendering is not; PRD #742's own two-deck run is what discharges the remainder. A loopback `ssh` is not a substitute for the **rendering** half — nothing local can say what the connected window drew — but it is no longer disqualified from the other two: PRD #742 M10 added `fleet/observe/002` for the transport and `project/resolve/002` for "whichever side resolved it", both in lane 1 and both explained under [Configuring a remote deck by hand](#configuring-a-remote-deck-by-hand).
- **The fleet view has never been seen with two real decks**, and four things about it are unverified in named ways rather than merely untested. **No automated tier reaches a real second daemon** — there is no `tauri-driver` ([#953](https://github.com/vfarcic/dot-agent-deck/issues/953)), so every fleet assertion in the repository is against fixtures. **That each watcher stamps its own endpoint on the emit is unguarded**: the payload *shape* is pinned by a key-set difference proving the stamp adds exactly `deck` and moves nothing, but attribution needs an `AppHandle`, which needs a running Tauri app — making the emit chain generic over `R: Runtime` is the lever that would close it. **`EndpointTunnels::acquire`'s lock has no test** and was fixed from the code, because the risk test injects its stall at `hello()`, which sits after `acquire`, and a local endpoint returns from `acquire` promptly. And **a watcher-teardown ordering residual**: `retain_watchers` aborts, so a watcher parked in its `select!` stops before its next emit while one already inside `snapshot_with` may have an emit in flight. Until the two-deck run exists and is recorded, read the fleet view as designed-for-N and measured-at-one.
- The deterministic loop, transition evidence, diffs, checks, artifacts, spend, and profile/workflow editors are preview data or local-only UI where the daemon does not expose structured data yet.
- **Voice control ships navigation-only, in one activation mode, and three of its properties are verified by nothing.** The five shipped rows open an agent, the overview, the deck, the settings sheet and close an agent view; nothing that starts, stops or approves is a row, and that is [D5](../../prds/802-desktop-voice-control.md#milestones) behind a confirmation rather than an oversight. (That was the slice as first shipped. PRD #1223 has since added two rows that lead to a stop, each ending at a confirmation the user answers by hand, and one that starts the New agent dialog's form — see [D5: a spoken stop opens a confirmation; a spoken start starts](#d5-a-spoken-stop-opens-a-confirmation-a-spoken-start-starts) — and still nothing approves.) Activation is press-on/press-off only, with voice-activity detection segmenting the utterances in between; hold-to-talk is [D4](../../prds/802-desktop-voice-control.md#milestones). Transcription needs **no key**, and its default is a speech container on loopback (`ghcr.io/speaches-ai/speaches`, `Systran/faster-whisper-tiny.en`, measured at a 0.653 s median warm) rather than the `off` this shipped with — **and since M6 was rewritten to voice only there is no typed fallback behind it**, so the prerequisite a user can still miss is the container, not a credential, and an endpoint nobody answers at says which `docker run` to type. Commands, by contrast, needs a key on both its presets — the agent-CLI backend that made it keyless by default is gone with PRD #802's provider work, because a stage that spends a credential has to let the user choose whose, and local intent was measured twice and is not viable; the only keyless Commands configuration left is an endpoint on loopback, which `RemoteResolver::run` never consults the keychain for. **That key is an OpenAI one by default**, which is Speech's hosted provider too, so going hosted end to end is one account rather than two. What no tier here proves is that the OS grants the microphone on any platform, or that a real utterance transcribes: see [what is NOT verified](#what-is-not-verified-on-any-platform), and read a green `desktop-web` or `desktop-browser` run as covering neither. (A third item stood here — that the `openai_compatible` protocol works against OpenAI itself — and the credentialed fixtures now run against exactly that, because it is the default.)
- Live orchestration graph events (`delegate`, `work-done`, and `dispatch`) and the graph view remain pending.
- OS-native notifications, signing, notarization, auto-update, remote/web hosting, and mobile layouts are out of the current spike. Packaging is no longer — see **Release bundles** above — but the bundles it produces are unsigned, so every OS still warns on first launch.
- Native Windows workflow-command construction is pending; the webview and Rust bridge both block desktop workflow launch on Windows rather than forwarding POSIX-quoted commands to `cmd.exe`.
- Pi coordinator launch is blocked on the Runs screen pending an acknowledged native seed-delivery protocol; Pi may still be used for non-start worker roles. The New agent dialog's orchestration launch (PRD #1223 M6) is not blocked: the deck starts that role with its configured command and seeds it natively, as the TUI does, and the delivery there has no acknowledgement either.
- Daemon startup is an explicit user action, and visual rename is not exposed by the current frontend. A single agent is started through [the New agent flow](#the-new-agent-flow) (PRD #1223).
- Multi-pane throughput still needs the explicit M1.3 stress qualification, and a real-agent terminal session still needs the PRD's pre-release end-to-end gate.
- **The agent overview is the landing screen, and has one drill-in rather than none.** Issue #1196 made it what the app opens on and put it first in the rail. PRD #1105 built #745's iteration-3 **single-agent view** — [the agent pane overlay](#the-agent-pane-overlay), reachable from an overview row and from a deck tile — so the overview's exits are one agent's pane and, with the `experimental` flag on, the deck. With the flag off, the shipped default since issue #1198, the pane is the only one: the deck is hidden, and the pane is how a terminal is seen. The **group view** (the deck filtered to one tab bucket) is untaken, and #745's iteration 3 is therefore still open.
- **The real Tauri window is still driven by nothing.** There is no `tauri-driver` and no WebDriver session anywhere in this repository, so real IPC, real xterm over a real PTY, and the distribution's own WebKitGTK or WKWebView build are exercised by the manual smoke check below and by nothing else. PRD #1105's PTY resize is the newest thing that lands on the wrong side of this line: the automated tiers observe the client's *proposed* geometry and stop there — see [What the automated tiers prove about the resize](#what-the-automated-tiers-prove-about-the-resize-and-what-they-do-not). What DOES run automatically, since issues [#823](https://github.com/vfarcic/dot-agent-deck/issues/823) and [#836](https://github.com/vfarcic/dot-agent-deck/issues/836), is a browser tier one rung below that: Playwright drives the production `pnpm build` bundle — served by `vite preview`, loaded through the PRD #745 fixture query strings, so no daemon and no credential are involved — in two engines, Chromium and Playwright's own WebKit build. See [The browser tier](#the-browser-tier) for what it asserts and what it does not.

## The browser tier

Issues [#823](https://github.com/vfarcic/dot-agent-deck/issues/823) and [#836](https://github.com/vfarcic/dot-agent-deck/issues/836) closed the same absence from two directions: nothing in this repository ran a layout engine over the desktop app. The vitest suite runs under `environment: "jsdom"`, and jsdom is a DOM implementation rather than a browser — it computes no geometry, performs no layout and paints nothing — so those tests can assert what React renders into a tree and nothing about what a reader sees. The tier under `desktop/e2e` is what closes that.

**What it drives.** Playwright's `webServer` runs `vite build` and then serves the resulting `desktop/dist` with `vite preview`, so every spec loads the same production bundle `pnpm build` produces: minified, built for the `safari15` target, with the real stylesheet. Not the dev server, and not the hand-built runtime the vitest tests render into. Each spec reaches its screen through the PRD #745 fixture query strings (`?fixture=1&state=crowded` and friends), so there is no daemon, no credential and no real agent anywhere in it.

**Which engines.** Two Playwright projects, `chromium` and `webkit`, both run on every invocation. The second is the point. Before this tier, every automated check of this app ran in Chromium or in jsdom while the app ships on WebKit — WebKitGTK under Tauri on Linux, WKWebView on macOS. Playwright's `webkit` is its own build from the WebKit source tree; the binary it launches on Linux is `minibrowser-gtk`, WebKit's GTK port. So it answers WebCore and JavaScriptCore questions the way WebKit answers them, which is a different thing from answering them the way *this machine's* WebKitGTK or *this Mac's* WKWebView would.

**What it asserts today.** **Sixteen** files. Every one of them makes at least one assertion jsdom structurally cannot, and the bullets say which parts of each are geometry, real event dispatch or real storage and which are ordinary DOM-shape checks that happen to live here because the screen they are about is only reachable in a browser. (This said *eight* and listed eight until PRD #742 M7 recounted it; `deck-selector.spec.ts` and `decks.spec.ts` arrived with PRD #741 and are the two it was missing. It then said *ten* while twelve existed. A count in a sentence goes stale every time a spec lands, so check it with `ls desktop/e2e/*.spec.ts | wc -l` rather than trusting this number; the enumeration below is the part that has to stay complete.)

- `overview-scroll.spec.ts` — with every column ticked at a 900px viewport, the overview's table really overflows; every group card resolves the shared grid template to the identical used track widths; each cell sits inside its own track; and scrolling `.overview-table-region` moves every card *and* the legend by the same amount. A card scrolling on its own is the failure this layout exists to prevent, and it only becomes reachable once the chosen column set is wide enough to overflow.
- `overview-rows.spec.ts` — every row is the same height whether its prompt is long or blank, and injecting the longest string the app can ever put in a prompt cell (`DISPLAY_LIMITS.prompt` plus the elision marker) changes neither the row's height nor its width. This is the assertion #836 records as unmakeable: an audit found a vitest test whose comment claimed to prove it, which jsdom cannot, and the comment was corrected rather than the test strengthened.
- `page-overflow.spec.ts` — at 1280x800 and at 900x800, on the deck and on the overview, nothing extends past the page body. `body { overflow-x: hidden }` is what makes getting this wrong invisible to the eye: content past the right edge is clipped rather than reachable, so a reader sees a truncated screen and no scrollbar saying why. `scrollWidth` still reports it.
- `appearance.spec.ts` — the FIRST of the two behaviours #823 names as the ones Chromium cannot answer for WebKit, and only the first. `<meta name="color-scheme" content="light dark">` actually puts the document's used colour scheme into dark under a dark preference, checked through the `Canvas` system colour with `only light` / `only dark` probes as controls, and the canvas repaints between the two appearances — both engine-side measurements. The `theme-color` half is **not** a measurement of engine behaviour, and the file says so at length: what an engine resolved for `theme-color` is not observable from inside the page, so that test asserts only that `index.html` carries one correctly `media`-scoped meta per appearance, that the engine's own `matchMedia` separates the two `media` strings in both directions, and that the dark meta's value still equals the dark `--canvas` it was copied from (a drift guard, read as a composited colour). Whether an engine honours `media` on `theme-color` is answered by nothing automated, and by no named step of the manual walk either — see [Both appearances, and the settings surface](#both-appearances-and-the-settings-surface) below for exactly what that leaves, and [#953](https://github.com/vfarcic/dot-agent-deck/issues/953) for the rung that could close it.
- `column-picker.spec.ts` — the picker driven the way a person drives it. Three of its six tests are here because no other tier can ask the question. The menu is measured: it lays out below its trigger, right-aligned to it, wholly inside the viewport, and extending past the top bar's own bottom edge — which is only possible because it is out of flow — and a hit test at its own centre lands inside it rather than on the fleet underneath, which is what `z-index: 30` buys. A real pointer press on an element elsewhere on the screen dismisses it, and the test first measures that its target is genuinely clear of the menu's box, because an assertion that turned out to be clicking *inside* the menu would fail for a reason that has nothing to do with the behaviour. And a second press on the trigger closes the menu rather than closing and reopening it: the dismiss listener runs on `pointerdown` and the trigger on `click`, so one press fires both in the engine's own order, and a trigger outside the picker's root would look dead. The other three are end-state checks — Escape dismisses; the name column's checkbox is checked and `disabled`, and unticking everything else leaves a one-column screen that still paints every one of the fifteen names at a non-zero width; Restore defaults returns both the legend and the menu's own ticks to the four the screen opens on.
- `column-persistence.spec.ts` — the item jsdom cannot reach at all, because there is no navigation to survive: the vitest suite mounts a component into a tree it already owns and unmounts it again. Here a chosen five-column set is written to real per-origin storage, a real reload tears the document down, and the fresh mount comes back on the same five with the engine resolving five used grid tracks. Then the scoping, asserted against the two literal key names: a fixture visit writes only `…overview-columns.v1.fixture`; a live visit — which in a plain browser has no Tauri bridge, so no fleet ever arrives, but the screen still mounts and still writes — wakes up on the defaults under `…v1.live` instead of inheriting the fixture layout, and leaves the fixture key untouched. Last, a stored value that cannot be parsed leaves the screen on its opening four rather than white: `readStoredColumns` parses on mount, so an unguarded throw there takes the whole app down before anything renders, and only a real reload of a real stored value can observe that.
- `connection-states.spec.ts` — the single-deck states put a different screen each in front of the reader, and `empty` is the one that used to be got wrong (it fell through to the disconnected branch, so a healthy fresh install was told its daemon was missing). Which note renders, which do not, what the daemon message says and what the AGENTS instrument prints are DOM-shape checks. Two things are not. Each screen is measured — a non-empty box, below the sticky top bar, inside the viewport — because a screen clipped to nothing is indistinguishable in the DOM from the right one. And the daemon lamp is read as a **composited colour** across the four single-deck states: it resolves through `--status-ok`, `--shell-faint` and `--status-error` under the dark appearance, so the comparison is the engine's answer rather than a class name. Connected and empty must paint the *same* colour — an idle daemon is not a fault — while disconnected and error must differ from it and from each other. PRD #742 M4 added partial connectivity on top: at `?fixture=1&state=fleet`, one deck down among two up must read as **one deck's** problem rather than the screen's — the healthy decks keep their rows and their pips while the silent one carries the degraded chrome — and the header must count the decks that answered and never the fleet the silent one was last seen running. M14 added a fourth deck to that scenario, one that has not reported yet, and the pair of assertions that matter are the ones the DOM tier cannot make: its note must not be the disconnected one, and its lamp must paint as neither a healthy deck's nor a downed deck's. The header reads `2/4`, and a build that dropped an unreported deck would read `2/3` and look exactly as correct.
- `overview-grouping.spec.ts` — the fifteen-agent fleet at `?fixture=1&state=crowded`, which PRD #745 built to make this hard: its agents are declared out of role order and orchestration `dot-ai`'s start role is deliberately its third, so grouping, ordering and coordinator identification are three separate things a screen has to get right rather than one accident of declaration order. Which agent lands in which of the four cards, in what order, and which row carries the COORDINATOR badge are DOM-shape. What only an engine answers is whether the reading order is the *seen* order: every card and every row is measured, and the assertions are that the boxes descend the page in the sequence the DOM lists them and that no two overlap. A `flex-direction: column-reverse` leaves every DOM assertion in that file green and puts the fleet on screen upside down — which is exactly how it was mutation-tested. The badge is measured too: it has a box, that box sits within its own row, and it follows the agent name in the cell rather than sitting on top of it. PRD #742 M4 added the fleet to the same file: at `?fixture=1&state=fleet` it renders one section per deck, in order, each holding only its own deck's rows — which the fixture makes a real question rather than a formality by giving the remote decks' agents the **same agent ids** as the local deck's, since ids are per-daemon monotonic values and a fixture that avoided the collision would pass every `(daemonId, agentId)` assertion for the wrong reason. It also states how many decks answered and counts only those, and stacks the whole fleet down a phone-width screen without sliding the page sideways.
- `deck-selector.spec.ts` — the Deck selector driven on **both shells**, the deck and the overview, since it sits in the top bar of each and a regression on one would be invisible from the other. DOM-shape: every configured deck is listed, All Decks leads, the local deck is the default, and switching names the chosen deck and survives a reload. Engine-only: the menu opens under its trigger, floats over the screen and toggles rather than closing-and-reopening on a second press — the same `pointerdown`/`click` ordering the column picker depends on — it stays wholly on screen at 900px with the page not sliding sideways open or closed, and it groups its options with a `<span>` rather than a `<legend>`, which is [the WebKit layout divergence](#adding-a-setting) that made the fieldset form unusable (issue #1032).
- `agent-pane-overlay.spec.ts` — PRD #1105's agent pane, from both origins, in both engines. Geometry: the pane's box really occupies the viewport, measured against `window.innerWidth`/`innerHeight` rather than merely being bigger than a tile. Engine-only beyond that: the screen underneath is still the **same DOM node** and still `isConnected` while the pane is up — a `.agent-grid` that was torn down and rebuilt would look identical to every DOM-shape query and would have cost five scrollback replays — and `Escape` returns to the screen the pane was opened from rather than to the app's default. The dialog's `role`, `aria-modal` and accessible name are ordinary DOM shape.
- `agent-pane-modal.spec.ts` — the pane's `aria-modal="true"` made true, and the only **automated** tier that can tell (the manual walk's step 8 is the other). The base screen stays mounted under the pane by design, so until PRD #1105's security audit every control on it — the rail, the tiles, and the `DeckSelector` that retargets the whole app — kept its place in the tab order behind a full-window dialog. The fix marks everything that is not an ancestor of the pane `inert`; the vitest tier asserts the attribute, and only a real engine's tab order can assert what the attribute DOES — measured, not assumed: in this repo's jsdom, `"inert" in HTMLElement.prototype` is `false` and `focus()` on a button under an inert ancestor still makes it `document.activeElement`. So this presses Tab twenty times and requires focus never to leave the pane **except onto the one peer the fence exempts, the Voice trigger** — matched by identity, so the deck selector, the rail and the other tiles all still fail it — checks the trigger is outside every `inert` while the selector is under one, and checks the selector is clickable again after `Escape`. PRD #802 narrowed that claim by exactly that one control: an exempt control is in the tab order as well as clickable, which is the accessible half of "reachable" rather than something to take back with a `tabindex`. Both engines, because `inert` is a comparatively recent platform feature and WebKit is the half of this pair that ships in the packaged app. It was mutation-tested by removing the hook: both engines go red.
- `agent-pane-narrow.spec.ts` — the phone-width trap, and the only spec below 900px. At 400x780 the pane keeps a visible terminal and a visible tab strip, both of which the tile's own `@media (max-width: 680px)` rules hide. jsdom evaluates no media query, so this claim is not assertable in the vitest tier at any effort. It drives the overview origin because the deck's `.agent-tile:not(.is-selected) { display: none }` leaves no Open control on screen at that width.
- `overview-row-open.spec.ts` — a click on an overview row's text, away from its maximise control, opens that agent's pane, and a drag across the row's text does not. Engine-only: a real mouse drag across a prompt leaves a non-empty selection **and** still delivers a `click` to the row — counted on the row itself, so a pass cannot come from an engine that never sent one — and no pane opens; a plain click on the same row then does. The hover is the other half jsdom cannot see, since it never matches `:hover`: the row and its text show a pointer cursor, and hovering the row's text gives its open control the same computed colour, background and border as hovering the control itself, measured on a **failed** row, whose red background already hides the ordinary row hover. The row's own rules (the control opens once rather than twice, other controls keep their clicks, a selection elsewhere does not block a row) are vitest's, against selections it builds itself.
- `decks.spec.ts` — the **Settings → Decks** panel, and the only browser spec of the settings sheet. Geometry: a deck's fields lay out as the house two-column settings row. Engine-only behaviour beyond that: a bidi override planted in a settings-supplied host cannot reverse the deck label on screen, which is a question about what the engine draws and not about what the DOM holds; **Test connection** in the browser preview says what it cannot do here rather than inventing a verdict; and, since PRD #742 M6, a stored `all` selection paints as a choice rather than as nothing — the panel's chooser is built from `deckChoices` now, so the fleet token is checked, the missing detail form is replaced by a sentence saying why, and the button is disabled instead of live for a token no probe can answer.

**What it does not cover.** Enumerated rather than summarised, because this paragraph will be read later as a safety property:

- **The real Tauri window.** No `tauri-driver`, no WebDriver session, no native window. Real IPC, the `desktop://` event stream, xterm over a real PTY, window chrome, zoom and the sidecar are outside it. Closing that rung — `tauri-driver` with `WebKitWebDriver` against a native window under xvfb — is tracked as [#953](https://github.com/vfarcic/dot-agent-deck/issues/953).
- **The engines that actually ship.** Playwright's WebKit is not the WebKitGTK your distribution installs and not WKWebView, and their versions move independently. A WebKit-family bug this tier catches is very likely real for the app; a WebKit-family bug it *misses* is not evidence of absence on either shipping engine.
- **Everything live.** Fixture data only: no daemon handshake, no snapshot stream, no attach protocol, no real agent.
- **The deck.** Most of these files are about the overview. The split is deliberately not kept here as a fraction: this bullet read "six of the ten files" while eleven existed, so count them with `ls desktop/e2e/*.spec.ts` rather than trusting a number in a sentence. The deck screen is reached by six files. Three reach it for a narrow question — `page-overflow.spec.ts` checks that it does not push the page sideways, `appearance.spec.ts` reads its canvas colour, and `deck-selector.spec.ts` drives the selector in its top bar. Three are about what is on it: `agent-terminal-input.spec.ts` (issue #1042) pins that xterm's own textarea is each tile's ONLY text input — there is no second composer beside it — and that a pane the daemon reports as history-only or as having no live target renders its terminal disabled, marked with its verdict, and carrying a readable reason; `agent-pane-overlay.spec.ts` (PRD #1105) opens a tile's agent as a full-window pane and checks the grid beneath survives it; `agent-pane-modal.spec.ts` (the same PRD's security audit) tabs around that pane and checks the deck behind it is genuinely out of reach. The tile's remaining surfaces, the Reader overlay, the handoff rail, the zoom panel and the workflow panels have no browser spec at all. The settings sheet has two, not the one this bullet used to claim: `decks.spec.ts` covers its **Decks** panel and nothing else in it, and `settings-rows.spec.ts` measures the two-column row in every panel the registry offers and asserts nothing else about any of them.
- **Keyboard reach, and the narrow breakpoint almost everywhere.** One file tabs through a screen, and it is not about that screen: `agent-pane-modal.spec.ts` presses Tab to prove the agent pane CONTAINS focus, so what it enumerates is the pane's own tab order and not the deck's. No spec walks a screen's controls in order, checks that every one is reachable, or checks that any of them carries a visible focus ring. Every spec but one stays at or above 900px — deliberately, since below 680px the padding and the status pips change. The exception is `agent-pane-narrow.spec.ts` at 400x780, and it asserts two things about the agent pane at that width and nothing about any other surface there.
- **Non-Linux runners.** The CI job runs on `ubuntu-latest` only, so nothing here has ever run against a macOS or Windows browser build.

**Running it locally.** From `desktop/`:

```sh
pnpm exec playwright install --with-deps chromium webkit   # one-time; `--with-deps` needs sudo and is for WebKit
pnpm test:browser                                          # both engines
pnpm test:browser --project=chromium                       # one engine
pnpm test:browser overview-scroll                          # one file
pnpm test:browser:types                                    # type-check the specs; Playwright itself does not
```

The install downloads 399 MiB and unpacks to about 927 MB under `~/.cache/ms-playwright`. `--with-deps` runs `apt-get` for WebKit's GTK4 and GStreamer runtime — ~130 packages, measured locally on Ubuntu 26.04 rather than on the `ubuntu-latest` image CI uses, so the exact set depends on what the image already carries — and Chromium needs almost none of it. `@playwright/test` is pinned to an exact version in `package.json` rather than carrying a caret, because a Playwright version selects the browser revisions it downloads: the pin is what makes "which engine ran" a fact you can read off the lockfile. Renovate still bumps it; a bump moves the engines with it, which is the point.

There is no `pnpm build` to run first: the Playwright `webServer` builds and serves in one command, so the bundle under test is always current. A failing run writes a trace and a screenshot under `desktop/test-results/`; open one with `pnpm exec playwright show-trace <path>`.

**One devbox gotcha.** Inside a `devbox shell` or `devbox run`, nix's `glibc-bin` puts its own `ldconfig` ahead of the system one on `PATH`, and that `ldconfig` reads a cache file that does not exist. Playwright's host-requirement check then reports every shared library as missing — `libglib-2.0.so.0` included, on a machine that plainly has it — and refuses to launch WebKit. The libraries are fine; the lookup is not. Put the system one first for the run:

```sh
env PATH="/usr/bin:/bin:$PATH" pnpm test:browser
```

`PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1` also works, but it suppresses the check rather than fixing it, so a genuinely missing library then surfaces as a browser crash instead of a clear message.

**Where it runs.** The `desktop-browser` job in `.github/workflows/ci.yml`, on every PR, under the same `changes` gate as `desktop-web`. It is **advisory**: the ruleset requires exactly `build`, `build-macos`, `build-windows` and `security` (`CLAUDE.md` rule 8), and this job is new enough that its stability on a GitHub runner is unmeasured — the same reason `e2e-deterministic` was added advisory. Making it required needs a run history plus a deliberate `scripts/apply-branch-protection.sh` change, which is a repository-administration act rather than something a PR does.

**Its relationship to the palette guards.** They are not redundant and neither replaces the other. `xtask/linkage-check/src/desktop_palette.rs` reads the stylesheet as text — no hard-coded colour outside the palette, the three `:root` blocks agreeing — and runs in the **required** `build` job. This tier resolves colours in an engine and runs in an advisory one. PRD #743's 399-declaration comparison in `chrome-headless-shell` was a one-off measurement rather than a checked-in gate, and nothing here reimplements it.

## Verification commands

Run frontend tests and the production web build from `desktop/`:

```sh
pnpm test
pnpm build
```

Run the browser tier from `desktop/` — the jsdom suite above answers no question about layout, scroll or painted colour, so this is a separate obligation rather than a nicer way to run the same tests. It needs a one-time browser install; [The browser tier](#the-browser-tier) has that, the devbox `ldconfig` gotcha, and what it does and does not cover:

```sh
pnpm test:browser         # Chromium and WebKit, against the production bundle
pnpm test:browser:types   # type-check the specs, which Playwright does not
```

Run the desktop Rust bridge tests and lints from the repository root:

```sh
cargo test --locked -p dot-agent-deck-desktop
cargo fmt --check
cargo clippy -p dot-agent-deck-desktop --all-targets -- -D warnings
```

Run the repository-wide required fast gates from the repository root. Both flags on the clippy line and `--workspace` on the test alias are load-bearing — `CLAUDE.md` rules 2 and 5 explain each of them:

```sh
cargo fmt --check
cargo clippy --workspace --all-targets --features e2e,e2e-live -- -D warnings
cargo test-fast
```

There is no full-tier obligation before a PR — CI runs lane 1 on every PR (CLAUDE.md rule 5, [`e2e-lanes.md`](e2e-lanes.md)). What you do owe is the tests covering what you changed, so where a desktop change touches a PTY or real-agent path, run those by filter:

```sh
cargo test-e2e <filter>        # lane 1, no credentials needed
cargo test-e2e-live <filter>   # lane 2, needs your own agent credentials — and runs in no CI job
```

### A real agent, end to end

The live manual smoke check is: build the matching CLI, launch `pnpm tauri dev`, use **Start deck** if needed, launch the configured live loop against a disposable project/worktree, confirm every role hydrates under one orchestration, interact with a real agent in an embedded terminal, resize the window and terminal, reconnect without duplicated output, and stop only a disposable agent through the confirmation dialog. Do not treat the fixture as proof that a real agent or daemon path works.

Then, with that same live daemon still owning those agents, extend it over the overview. This half is the only check that exercises the attach model against real sockets, since no automated harness drives the real window:

1. **Land on the overview.** Click **Overview** in the rail. Confirm the fleet renders grouped — standalone panes first, then the orchestration you launched as one card with its roles in role order and its coordinator badged — and that the counts in the header are real numbers rather than `—`. The deck card's header should read `Local deck` with no socket filename and no `Deck responding` beside it; hover the name and the full socket path is there. If your desktop and daemon builds differ, the two stamps are on that same hover.
2. **Confirm zero terminals stay attached while it is up.** The app has no readout for this, so observe it from outside: the desktop process's open connections to the attach socket (`DOT_AGENT_DECK_ATTACH_SOCKET`, via `lsof -p <desktop pid>`) should fall to zero. Arriving here from a deck full of tiles is the case that matters — the warm set flushes to zero rather than down to `MAX_WARM_TERMINALS`, so a lingering three would be a regression rather than by design. Give the fire-and-forget declaration a moment to land before concluding anything.
3. **Read the honest columns.** Tick every column in the **Columns** picker first — the screen opens on four, and this step is about the other five. Confirm the **CLI** column names a binary you could type — `claude`, not `claude_code` — for every agent, and check the picker dismisses when you press the pointer down outside it and closes rather than reopening when you click its own button. Press **Escape** with it open too, and on macOS that one is the point of the step: dismissal is a React `onKeyDown` on the picker root rather than a document listener, so it depends on the click having moved focus inside the picker — measured to hold in Chromium and in Playwright's WebKit, while Safari is documented not to focus a `<button>` on click and WKWebView is an engine no tier runs. [#957](https://github.com/vfarcic/dot-agent-deck/issues/957) records that as **unverified on WKWebView** rather than broken, and this walk is what would settle it. **Restore defaults** should put the four back. Active tool and tool count should move as a real agent works. **Last activity** should read `just now` for the agent you are driving and a real age for one you left alone — never `just now` for all of them at once. A blank cell means the daemon reported no live session for that agent at all (`AgentRecord.live` absent), which is expected for one that has not yet emitted a hook event and is *not* the same thing as an agent that has been quiet. **Uptime** should be populated for *every* agent the daemon spawned, including one whose Last activity is blank — that pairing is the whole reason it comes off the registry record rather than off the session. Drive a `clear = true` delegate and watch that worker's uptime reset while the orchestrator's keeps climbing.
4. **Choose columns and leave.** Untick a column and confirm it disappears from the legend, every group card's header row and every row at once — one template, one scroll region. (Narrowing until the table overflows and checking that it scrolls **as a whole**, with the cards and the legend in step, used to be here too. `overview-scroll.spec.ts` in [the browser tier](#the-browser-tier) now asserts it in both engines on every PR, so it has been removed rather than duplicated — a checklist that repeats what a test already does is a checklist people stop running.) Then quit the app, reopen it and return to the overview: the choice is still there. Check the fixture and live modes do not share it — `?fixture=1`'s columns are stored under a different key. (Both halves are now asserted in both engines by `column-persistence.spec.ts`, but this step **stays**, unlike the scroll one above. That spec survives a page reload inside one browser process; what you are checking here is that the choice survives the packaged app being quit and relaunched, in the webview the app actually ships with. Those are different questions, and the tier answers only the first.)
5. **Drill back to the deck with terminals still working.** Click **Open deck**, confirm every tile re-attaches and streams, and type into one to see it reach the agent. Coming back from the overview costs a replay **by design** — the empty declaration flushed the warm set to zero — so do not read that as a regression. The warm set is exercised without leaving the deck: switch one tile off its terminal tab and back, and it must return with no scrollback replay; do that for more tiles than `MAX_WARM_TERMINALS` and the least-recently-left one pays a replay when it is evicted.
6. **Stop the daemon under a live app** (`dot-agent-deck daemon stop`, from a shell — the app exposes no such action). The overview must switch to the disconnected note with the list blank rather than stale, and the header instruments must read `—` rather than `0` — the app keeps the previous snapshot's agents, so a `0` there is a regression. Reconnect and confirm the fleet comes back. A daemon's agents are its own children and it persists no session state, so a *restarted* daemon owns nothing and correctly lands on the zero-agent first-run screen rather than on a fleet with blank activity cells.

### Voice control, which no automated tier reaches at all

The three tiers between them drive the surface in jsdom, the surface in two real browser engines over the fixture bridge, and every Rust stage over stubs. **None of them opens a device, reaches a model, or runs inside the real Tauri window.** So this walk is the only thing that exercises the feature as a user meets it, and every step below the first is one no test anywhere can replace. **Since M6 was rewritten to voice only there is no typed path to fall back on**, so step 1 is now the gate on all the rest: if the microphone does not open, nothing further in this walk is reachable.

1. **The unavailable path, with nothing usable configured.** `transcription = "off"` is gone and the default is `local`, so reach this state by leaving the speech container **not running**. Press **Voice** and confirm the report names the `docker run` to type rather than reporting an outage. This is what a fresh install meets first if it has not started the container, so a regression here is the whole feature reading as broken. Then start the container and press Voice again **without restarting the app** — availability is read at the press, and the point of the step is that it takes effect immediately.
2. **The microphone, on whatever platform you have.** With a working speech backend — the local container, or `remote` with a key stored in Settings → Voice — press **Voice**. **Confirm the OS permission prompt appears the first time** — this is [unverified on every platform](#what-is-not-verified-on-any-platform) and is the reason this step exists. On macOS do it in a **packaged** build as well as under `tauri dev` if you can: whether a dev window needs `NSMicrophoneUsageDescription` too is PRD #802's Open Question 2, a denial there is silent, and `build-macos` is the only job that will ever touch that file.
3. **Continuous operation, which is the thing the rewrite is about.** With Voice on, say `show me the overview`, wait, and confirm the app navigates and renders `Opening the agent overview.` Then — **without pressing anything** — say `go back` and land on the deck. Confirm the button still reads *Voice on* throughout. Check the reported latency is a real number rather than a spinner that never resolves; around a second is either command preset behaving normally — measured medians are 818 ms on the OpenAI default and 780 ms on the Anthropic one. **This is where a VAD misjudgement shows up**, and it is the one thing the stubbed unit tests cannot tell you: if commands are being cut off mid-word, `SPEECH_MARGIN` is too large for the signal-to-noise ratio your microphone gives you; if nothing ever resolves and every utterance ends at the cap, your room is loud enough that a 1 s `NOISE_WINDOW` is taking part of it for speech. Step 3a is how you tell which, with numbers rather than by feel.
3a. **Read the refusal's measurement, which is the only calibration of `SPEECH_MARGIN` this repository has.** Speak one very short syllable — *"hm"* — and let the utterance close. The row must refuse it, and the refusal names which of the two causes it was **and prints all three numbers either way**. If it says **too quiet** — *the loudest moment reached 410 against a room at 180, where speech has to reach 540* — nothing you say at that level will ever be transcribed at any length; the two numbers beside each other are the evidence, and which one is wrong is readable off them. A room far under the peak and a peak still short of the bar means raise the input (move closer, turn the device's level up). A room close to the peak means the room is the problem, not the input, and no input level fixes it. If it says **I did not hear enough** — *60 ms of speech inside the loudest 200 ms, where 120 ms is needed; the loudest moment reached 820 against a room at 95, where speech has to reach 285* — the level is fine and only the length was short, which is the expected answer for one syllable. Now say a real command and confirm it is **not** refused. **Report all of the numbers if you file anything.** PRD #802's first report was *"it says Nothing was said when it does not understand"* with no measurement attached; its second carried a duration but no level, which is what cost the round trip that falsified `SPEECH_FLOOR` — the density branch printed 60 ms and did not say that the peak had cleared the floor. Both branches print all three now, which is what that round trip bought. The margin is deliberately not calibrated by any test here — [no tier in this repository opens a device](#voice-control-which-no-automated-tier-reaches-at-all) — so this step is the whole of it, and a refusal you can read is the whole of the evidence.

4. **The refusal paths, which are the point of the design.** From the **deck**, ask for something that exists but is not callable there — `close this agent` — and confirm you get the table's own `unavailable_hint` naming the prerequisite, not "I don't know how to do that". Then ask for something outside the closed set — `what time is it` — and confirm the no-match shows **your transcript verbatim**. Both sentences are rendered by the app from the table; if either ever reads like a model wrote it, something has crossed the stage-6 line.
5. **The 30-second cap, which discards.** With Voice on, talk continuously — read a paragraph aloud with no pause in it. At 30 s the report must say the segment was thrown away and Voice must stay **on**. Confirm no transcription happened: nothing appears in the report about what you said, because nothing was sent. If you see a transcript or a no-match sentence here, the cap is transcribing and the paid-call-per-accident problem is back.
6. **Turning it off releases the device.** Press **Voice** while it is listening and confirm the label returns to *Voice off*, `aria-pressed` is false, and your OS stops showing the microphone as in use. Nothing you were part-way through saying should be transcribed — the toggle cancels, it does not stop-and-send. Watch the order rather than only the end state: the label must pass through *Voice stopping…* and reach *Voice off* **after** the OS indicator goes out, never before. On a fast machine that transition is a few milliseconds and easy to miss; what you are checking is that it is never the other way round.
7. **Losing the webview under a live microphone**, which is what the audit blocker was about and what nothing automated can reach: no tier here runs inside the real Tauri window. With Voice **on** and your OS showing the microphone in use, force a hard reload of the webview (in `tauri dev`, the devtools' reload). Confirm the OS indicator goes out — that is `on_page_load(Started)` calling `release_microphone` — and that the panel that comes back reads `Voice off` rather than `Voice on` over nothing. Then press **Voice**: it must open a microphone rather than report *a recording is already running*, which is what a session nobody cancelled leaves behind. Do the same for the window: with Voice on, quit the app and confirm the indicator goes out immediately rather than after the terminals have finished detaching.
7a. **The machine stays awake, and — the half that costs more if it regresses — stops staying awake.** No tier anywhere can check the OUTCOME. The *call* is checked on all three platforms — `voice::wake::tests::the_real_platform_is_asked_for_real_and_lets_go_again` runs on Linux, on `build-macos` and on `build-windows`, and asserts the grant on the latter two — but nothing automated can watch a machine then decline to sleep. So this step is the whole of that half. On **Linux**, press **Voice** and run `systemd-inhibit --list`: a row must appear reading `dot-agent-deck … idle Voice control is on block`. Press **Voice** again and confirm it is gone — that is the direction that matters, since a stuck inhibit is a laptop that never sleeps again with nothing on screen to explain why and a reboot as the user's only remedy. Then do the same for the other three teardowns: quit the app with Voice on, hard-reload the webview with Voice on, and `kill -9` the process with Voice on. **All four must leave the list clean** (the `kill -9` case is the kernel's doing rather than ours — the inhibit is a file descriptor — and it is worth confirming rather than believing). On **macOS** the equivalent is `pmset -g assertions`, which must show `PreventUserIdleSystemSleep` named for this app and, crucially, **must not** show `PreventUserIdleDisplaySleep`: the display is meant to be free to blank while you talk. On **Windows** it is `powercfg /requests`, which must list a `SYSTEM` request for the executable and no `DISPLAY` one. **On macOS and Windows this is the only check of the OUTCOME that has ever been run** — the calls themselves are made and asserted on every PR by `build-macos` and `build-windows`, but no tier on either platform observes the machine's sleep behaviour afterwards.

8. **Undo.** Speak a navigation, then press **Undo** inside ten seconds and confirm it puts the screen back. Then say `open settings` — which opens an overlay and moves no view — and confirm **no Undo is offered**, because there is nothing to reverse.
9. **The peer exemption, which is the one that has regressed once already.** Open an agent's pane so it is modal, then click the **Voice** button: it must be clickable *and* tabbable while every other background control is neither, and an Undo in the report beside it must be clickable too. Say `close this` and confirm the view closes. `dictate_to_agent` and `submit_prompt` are the rows whose `screens` is `["agent"]`, so if the modal fence ever marks the trigger `inert` again, the whole dictation pair is unreachable in a shipped build and nothing automated will say so.
10. **Stopping by voice, and the row that tells you how.** Press **Voice** and, before saying anything, read the report row: it must name the phrase that lists everything **and** both ways to stop. Then say `voice off` and confirm the microphone closes exactly as the button closes it — your OS indicator goes out and the label reaches *Voice off*. Do it once from the **agent** screen as well, which is the screen that serves the fewest context members: `voice_off` is published by the voice surface rather than by a screen, and this is the step that would catch that publishing seam going missing.
11. **Discovery.** Say `what can I say?` and confirm the overlay lists commands in two groups — the ones this screen can run and the ones it cannot — and that the list matches what `commands.toml` actually holds, since it is generated from it. Close it with its own button and again with **Escape**. Open it once with an agent's pane enlarged: it must be clickable there, which is step 9's exemption covering a second element. Note that what you are reading is each row's `description`, written for a model; that is deliberate, and if it reads badly the decision to fix is about that column.
12. **Dictation, which is the one thing here that types into somebody's agent.** Pick an agent whose pane **accepts input** — not one that has finished, and not one the app shows as read-only — and say `open the <agent>`. With its pane on screen say `type run the login tests` and watch four things: the words appear **in that agent's prompt where you can see them**, `type` is **not** among them, nothing is submitted, and the countdown appears and runs down a second at a time. Say something else and confirm **speaking again starts it over**. Let one countdown reach zero and confirm the prompt is submitted then and not before.

    Then the three that are about the design rather than the plumbing. Say `let's write a prompt run the tests` — an opener no list contains, so this is the **model** finding the boundary — and confirm `run the tests` arrives and the introducing words do not; if the whole sentence lands in the prompt, the boundary was not stripped, and if *nothing* lands and the row says *is not how that started*, the model marked words you did not say and the app refused them, which is the fidelity guard working. Say `type the meeting is at the end` and confirm it is **typed in full and not sent** — that is the trailing-phrase false positive the whole submit rule is arranged against. Then dictate something and say `send it`, and confirm it goes at once rather than after five seconds.

    Finally the escape that matters most: dictate a sentence and press the **Voice button** during the countdown — the send must be called off with the text left in the prompt. That is the one step of this walk that checks the feature is recoverable rather than merely working. And say `type run the tests` once from the **deck**, with no pane open, to confirm you get *typing to an agent needs that agent's pane open* rather than words going somewhere you cannot see.
12a. **`close`, which is the row you can only check by hand.** Say `what can I say?`, then say `close this`, and confirm the list goes — that is the defect the row exists for, and before it the overlay could only be dismissed by a click or Escape. Then with an agent's pane open and **no** overlay up, say `close this` and confirm the pane closes. Then say it on the bare deck and confirm the row says *Nothing to close* rather than claiming a close. On the overview, open **New agent** and say `close this`: the dialog closes. Open it again, start an agent against a deck slow enough to watch, and say `close this` while it reads **Starting…**: the dialog stays and the report says it is waiting for the deck to answer. Finally, with the pane open, say a bare `go back` and confirm it closes the pane rather than trying to reach the deck — that claim is scoped by the word *bare* in two descriptions and the model tells the two rows apart only by which is `callable`.
13. **Then run the phrase fixtures yourself**, because no CI job does and an ordinary `cargo test-fast` never reaches a model either:

   ```sh
   DOT_AGENT_DECK_REQUIRE_REAL_E2E=1 \
     cargo test --locked -p dot-agent-deck-desktop --test voice_phrase_fixtures
   ```

   They are authoritative only for the shipping default backend on this build's own preset — `test_support::api_preset()`, currently OpenAI chat-completions on `gpt-5-mini` — and deliberately offer no backend switch, because a green run against another model would prove less than it appears to. `api_resolver` builds through `resolver_for(&IntentSettings::default())`, the same call production makes, so it follows the default wherever it goes rather than naming a protocol of its own. They need `OPENAI_API_KEY` in the environment: a developer's own key on a developer's own machine, CLAUDE.md rule 5's lane 2, and nothing registered on the repository. **Set that variable** — and keep it matched to the default if that ever moves again, because the preflight only knows whether the variable it was told to read is set. Without it a missing credential is a green runtime skip, which is the failure mode that lets a broken table sit unnoticed for weeks.

### The agent pane, and the PTY resize nobody else checks

**This is the compensating control for the biggest gap in PRD #1105's coverage, so run it as a procedure rather than as a glance.** Nothing drives the real Tauri window ([#953](https://github.com/vfarcic/dot-agent-deck/issues/953)), and what the automated tiers do assert about the resize is narrower than it sounds: they observe the **client's proposed geometry** handed to the runtime — `runtime.resizeTerminal(agentId, cols, rows)` — and nothing past it. They do not show that a real PTY moved, that a native WebKitGTK or WKWebView window measured the same cells, that the daemon's focus-driven arbitration produced the size that was asked for, or that another client attached to the same agent reflowed. [What the automated tiers prove about the resize](#what-the-automated-tiers-prove-about-the-resize-and-what-they-do-not) enumerates that boundary; the steps below are the only place any of it is checked.

Run it with `pnpm tauri dev` or a bundled build, against the same live daemon and the same real agents as the walk above — a **real agent** in the pane is load-bearing for steps 2, 3 and 6, because a pane with no live process reflows nothing. `task run-all` sets up step 6's two clients — a TUI and the desktop on one daemon — against an isolated sandbox rather than your real deck ([local-run.md](local-run.md#task-run-all-the-tui-and-the-desktop-against-one-sandbox-daemon)); read its rule 16 caution before running it from a worktree anyone is editing.

1. **Open from the deck, and confirm the grid did not go away.** Press the maximise control in a tile's header — a real `<button>` carrying the accessible name `Open <role> agent`, rather than a click handler on the tile body, so that a deck of nine reads as nine distinct controls. Nothing automated tabs through a screen in this repository, so this walk is also the place to check that the keyboard reaches it and that the name is the one announced. The pane must cover the window. Then confirm the cost of it: the other tiles' terminals must still be streaming when you come back, with **no scrollback replay** on any of them, and no burst of reconnect activity while the pane is up (`lsof -p <desktop pid>` against the attach socket should show the same count of connections before, during and after). A replay on close means something re-declared the shown set — the deck path is supposed to declare nothing at all.
2. **Watch the agent's own TUI reflow, in the pane.** The pane is the whole window rather than a ninth of it, so the agent's output should **re-lay-out to the new column count**, not stay at the old one and sit in the corner. Box drawing, status lines and wrapped paragraphs are what make this visible; a pane whose content is the right *size* but the wrong *shape* means the daemon was never told, which reads as the agent misbehaving rather than as a resize bug. Type into it and confirm the keystrokes reach the agent — the pane is a working surface, not a viewer.
3. **Resize the window while the pane is open.** Drag the window wider and narrower, and step the zoom with the keys. Each time, the agent's grid should follow within a moment. Take the window down under 680px wide too: the pane must keep its terminal and its tab strip, since the tile's own phone rules hide both and the pane overrides them. (That last one has a browser spec, `agent-pane-narrow.spec.ts`, so you are confirming it in the shipping engine rather than discovering it.)
4. **Close back to the deck.** `Esc`, and again with the close control in the pane's header. Both must land on the deck with the tile you came from in its original cell, on the **same panel tab** it was on, its scroll position and the deck's selection intact, and — again — no replay. Confirm the agent's TUI reflows *back* to tile width.
5. **Open from the overview, and close back to the overview.** Click **Overview** in the rail, then the maximise control on an agent's row. Open it a second time by clicking the row itself, away from the control, and then drag across that row's prompt or working directory: the drag must leave the text selected and open nothing. Two things to confirm that the deck path cannot tell you: the overview is still mounted underneath (`Esc` returns to it, not to the deck, and the scroll position and column choice are as you left them), and this open costs **exactly one attach** where the deck path costs none — one new connection to the attach socket while the pane is up, gone again on close. Use an agent on the deck you are on; step 7 below is the one that covers an agent on another deck, which costs no attach at all. Arriving here from a full deck is the case worth using, because the warm set was flushed to zero on the way to the overview.
6. **The reflow another client sees, which needs two clients on one agent.** This is the step the automated tiers cannot approach at all.

   - In a terminal, attach a TUI to the **same daemon** — `dot-agent-deck` with the same `DOT_AGENT_DECK_ATTACH_SOCKET`, which is the socket a TUI attaches through (`task run-all` does this for you) — and bring up the same agent's embedded pane so it is live and has reported its geometry. A current TUI declares its dimensions at attach, so this needs no coaxing; what it does need is for the pane to actually be on screen.
   - Make that terminal window **narrow** — narrower than the desktop's maximised pane, though wide enough to read. The daemon sizes the agent by whichever client claimed focus last, so the size you see depends on which window you touched last, not on which pane is smaller.
   - **Click into the desktop window, then open the pane.** The agent should take the pane's size, and **the TUI's pane should reflow, live, with nothing on its own screen explaining why** — clipped at its right edge, because it is handed the desktop's wider grid. That is the feature working and a genuine cross-client side effect, chosen knowingly. Close the pane and the TUI should reflow again to the tile's size. Two reflows per open/close cycle is the expected cost to the other viewer.
   - **Switch focus to the TUI without resizing anything:** open the pane again, then click into the terminal window or press a key there. The agent should reflow to the TUI pane's size, the TUI should now fit, and the still-open desktop pane should render at the TUI's grid with the remainder of its box unused. That is `applyAppliedGrid` working as designed — the client proposes and the daemon disposes — and it is not a bug to file. **Click back into the desktop window** and the agent should return to the pane's size. Each switch is one reflow.
   - **Switch to a third application**, such as a browser, from either client. Nothing should reflow: the rule is the *last*-focused deck client, not the currently focused one.
   - If the terminal does not report focus changes — tmux without `set -g focus-events on` — switching to it claims nothing, and the TUI takes over only on the first key or click. Check that the switch still happens then, rather than never.
   - Repeat the switch at 300% zoom in the desktop, since zoom changes the desktop's proposal without changing the window.

7. **A second deck's agents, if you have one configured.** From the overview, find an agent belonging to a deck that is *not* the selected one. Its row must carry the **same maximise control** as every other row, and pressing it must open a pane with a **live terminal**: the header, status, assignment, footer, all five tabs, and output actually arriving from the other machine. Type into it and confirm the keystrokes land on **that** agent and not on the selected deck's same-id namesake — the cheapest check is two agents that share an id, with `echo` into one and nothing appearing in the other. Check what the pane does *not* do as carefully as what it does: the Deck selector must still read what it read before, and nothing may touch `desktop.toml` (`cp` it first and `diff` after, and expect the two files to be **identical bytes**). An earlier build of this feature switched the deck for you and wrote that file twice per open; [the section above](#an-agent-on-any-reachable-deck-opens-with-a-live-terminal) says why the switch was withdrawn, and a non-empty diff here means it has come back. Then move the selection to that deck and back again with the pane open: the terminal must stay live and the pane must not flash or reopen. Finally, take that deck away — stop its daemon, or drop the ssh tunnel — and the pane must replace its terminal with a sentence naming the deck and repeating its own failure, rather than leaving a black rectangle.

8. **The pane is a real modal, and a deck-origin pane is fenced on identity.** Open a pane from the deck and press Tab a dozen times: focus must stay inside it, and the deck selector behind it must not be clickable either. Then reach the selector *without* the pane — close it, switch the selected deck to one running an agent with the same id, and check the deck-origin pane you had is gone rather than wearing the new deck's namesake. There is no supported gesture that opens a deck-origin pane and then moves the selection, which is the point: the browser tier holds the focus half (`desktop/e2e/agent-pane-modal.spec.ts`) and the vitest tier holds the fence, and what this walk adds is the real engine in the packaged app.

### Zoom, at both ends of the ladder

**This is the check that reaches the visual half of PRD #744** — see *What none of this proves* above. It is no longer quite the only one: [the agent pane walk](#the-agent-pane-and-the-pty-resize-nobody-else-checks) above steps the zoom too, and its second bullet here is the overlapping half. Everything else on this list still runs nowhere but here. Run it with `pnpm tauri dev` or a bundled build, with a **real agent** running in a pane, because two of the five things it covers are about the PTY.

- **Step to 300% and back to 100%.** The chrome must stay laid out — nothing clipped at the right edge, no surface overlapping another, the rail and the evidence drawer still usable. The app's own breakpoints should fire on the way up, so it should look like a narrowed window rather than a broken one. Then step down to 75% and check the smallest type is still readable, since several declarations are 6px at 100%.
- **Confirm the agent's terminal re-fitted rather than just re-scaled.** At each level the running agent's own TUI must repaint at the new width — its box drawing and status line should reflow to fit the pane, not stay at the old column count and wrap or clip. A pane whose content is the right *size* but the wrong *shape* means the daemon was never told, which is the failure mode this feature is most likely to ship with, and it reads as the agent misbehaving rather than as a zoom bug.
- **Confirm nothing was typed into the PTY.** Focus a terminal pane, press each zoom key several times, and check the agent's prompt is still empty — no stray `-`, `=` or `0` characters, and no undo. This is the capture-phase guarantee, and on macOS it passes for the wrong reason (`metaKey` misses xterm's branch), so **run it on Linux or Windows** for it to mean anything.
- **Confirm the level survives a restart with no flash.** Set 150%, quit, relaunch, and watch the first paint: it must come up already at 150%. A brief 100% frame before it jumps means the Rust `setup` apply is not happening and the frontend is covering for it.
- **Confirm the Zoom row and the keys agree.** Change the level with the keys, open the settings sheet, and the Zoom row should read the level you are at; change it in the row and the window should follow. They are two writers on one value, so a disagreement here is the echo guard in `useZoom` failing.
- **Confirm the browser preview says it cannot.** Open `?fixture=1` in a browser: the Zoom row must be disabled and point at the browser's own zoom, and the keys must do nothing of ours.

### Both appearances, and the settings surface

**This is the only check that runs on the WebKit builds the app actually ships with** — WebKitGTK as your distribution packages it on Linux, WKWebView on macOS. Nothing automated launches either, because nothing drives the real Tauri window ([#823](https://github.com/vfarcic/dot-agent-deck/issues/823) closed the browser rung of that ladder, not this one). Two behaviours were the most likely to differ from Chromium and are the reason this paragraph was written: `color-scheme: light dark` on `<meta>`, which is what makes the webview's own scrollbars, form controls and default background follow the appearance, and `theme-color` with `media` attributes, where support for the `media` selector is the part that varies. **The first of those two is now asserted on every PR in a WebKit engine** by `appearance.spec.ts` in [the browser tier](#the-browser-tier) — Playwright's own WebKit build, which is neither of the two above, so it narrows the risk there without retiring the walk. **The second is not, and cannot be from that tier**: which `theme-color` an engine resolved is not observable from inside the page (measured in both engines — no `/theme/i` property on `window`, `document`, `navigator` or `screen`, not a CSS property, nothing on `HTMLMetaElement`, and Chromium's CDP `themeColor` is the web app manifest's, not the meta's). So of those two, `media`-on-`theme-color` is the one **nothing automated answers**: the browser tier checks the metas are well-formed, `media`-scoped and undrifted, and stops there. Be precise about what that leaves, because "the manual walk covers it" would be the easy thing to write and is not quite true — no bullet below names `theme-color`, and its effect is a tint on browser UI chrome that a Tauri window may not paint at all, since that window's chrome is native rather than the webview's. The honest position is that the first bullet's "walk every surface in both appearances" would notice a webview whose own chrome stayed light, which is the `color-scheme` failure; a `theme-color` `media` attribute the engine ignored would most likely be invisible here. Closing it properly needs the real-window rung, [#953](https://github.com/vfarcic/dot-agent-deck/issues/953). Everything else on this list still runs nowhere but here.

Do the whole list twice — once with the OS set to Light and once to Dark — with `pnpm tauri dev` or a bundled build:

- **Walk every surface in both appearances.** The deck itself (agent tiles, the rail, the tab strip, the agent footer, the evidence drawer, the output reader), all four config sheets (Projects, Workflows, Profiles, Prompts), the settings sheet, the command palette (⌘K), the shortcut dialog (`?`), a confirmation dialog, the connection banner, and the failure and empty states — the fixture preview's `?state=disconnected`, `?state=error` and `?state=empty` are the cheapest way to reach the last of those, and they are exactly the surfaces least likely to be opened during development and most likely to be wrong. Nothing should be light-on-dark or dark-on-light, and no surface should stop separating from its parent.
- **Confirm the override wins in both directions.** On a dark machine choose **Light** and get a light app; on a light machine choose **Dark** and get a dark app. Both directions matter and they exercise different CSS: the media query under its `:root:not([data-theme="light"])` guard, and the `:root[data-theme="dark"]` block. Then choose **System** and confirm it follows the OS **live** — change the OS appearance with the app open and the window should follow without a restart or a reload.
- **Confirm the terminals are unchanged.** The panes stay dark in both appearances by decision. With a real agent running, its output should look exactly as it does today in light mode; the chrome around the pane themes, the pane does not.
- **Confirm the choice is on the FIRST frame, not applied a moment later.** Set an override that disagrees with the OS — Dark on a light machine, or Light on a dark one — then quit and relaunch, watching the window as it opens. It must come up in the chosen palette with no flash of the other one and no blank frame in front of it. No automated check reaches this ([#845](https://github.com/vfarcic/dot-agent-deck/issues/845)) — the automated half stops at the seam where the ordering is decided, and a paint is past it. Relaunch a few times — a flash is a race, so one clean launch is weaker evidence than it looks.
- **Confirm the choice persists and the footer tells the truth.** Set an override, quit, relaunch, and it should still be applied — before the sheet is ever opened. The sheet's footer should name a path that exists on disk (`~/.config/dot-agent-deck/desktop.toml`) and the file should be readable and hand-editable; in the browser fixture preview the same footer must say there is **no** file rather than printing a plausible-looking path.
- **Confirm the store fails softly.** Delete `desktop.toml`, then corrupt it, then make it unreadable, launching the app each time: all three should start cleanly on defaults, with no crash and no error dialog.

### The New agent flow, on a local deck and a remote one

There is no `tauri-driver` tier ([#953](https://github.com/vfarcic/dot-agent-deck/issues/953)), so no automated test takes [the New agent flow](#the-new-agent-flow) through the real window, real IPC and a real deck together: vitest and Playwright run it over the fixture bridge, and the backend tests run it against real daemons without the window. This walk is the compensating control PRD #1223 names. Do it twice — against a local deck and against a remote one ([Configuring a remote deck by hand](#configuring-a-remote-deck-by-hand)) — with **All Decks** selected so the deck targeting is what is exercised, and in disposable directories.

1. **Open it every way.** On the overview press `Ctrl+N` (`Cmd+N` on macOS) and confirm the dialog opens with every field on screen at once and focus on the deck field. Close it, then open it from the top bar's **New agent**, then from the remote deck group's header, and confirm that deck is preselected and already chosen, its home listed without a key. A disconnected deck is listed, disabled, with its reason.
2. **Browse on the deck, not on this machine.** On the remote deck the listing opens at the remote user's home directory, not yours. Move with `j`/`k`, enter with `l`, go up with `h`, filter with `/`; hidden and symlinked directories are not listed, and a directory holding a `.dot-agent-deck.toml` is tagged **project**. There is no path field.
3. **Start a plain agent in an ordinary directory.** With `No mode`, pick an agent, type a Name with spaces around it, and press Start. The pane opens on the new agent, whose name is trimmed, and the overview lists it under the deck you chose — on the remote deck when you chose it, never on the local deck. Reopen the flow on the same deck: Command is prefilled with what you just started unless that deck's config sets a `default_command`, which wins. The other deck's prefill is its own.
4. **Start an authoring agent.** Pick `schedule`, leave Command blank, and Start. Once the agent is ready it receives the same seed the TUI types, naming the directory you chose. Repeat with `dispatcher`. `schedule: issues` is offered only on a deck whose own experimental flag is on.
5. **Launch an orchestration in a project directory.** Choose the `Orch:` chip; the Name suggests `<basename>-orchestrator-N`. Start. Every role appears under one orchestration with that title, each running the command its config gives it, and the start role's pane opens with its coordinator prompt delivered and no `## Your task` section. With it still running, confirm the same Name is refused. If a role in your project is Pi, it is the case the Runs screen refuses and this flow allows.
6. **Close what you started.** With **All Decks** selected, stop the plain agent you started on the remote deck from its row on the overview, and confirm it leaves that deck's group while the local deck's agents are untouched. Then press **Close** on the orchestration's card: the confirmation names every role, and after it every role is gone from the deck (check with the TUI or `dot-agent-deck` on that host too).
7. **Lose the deck mid-flow.** Stop the remote deck's tunnel or daemon with the form open, and press Start. The refusal is inline — the connection error while the deck is still observed, or once it has left the fleet, the directory panel and the form cleared with the refusal shown at the deck field — and nothing starts on the local deck.
8. **An older deck.** Point a remote row at a deck from a release before PRD #1223 and confirm its group header offers no **New agent**, and that the deck field lists it disabled with the reason that it does not advertise `list-directories` — clicking it does nothing. On a non-Unix deck, confirm the `Orch:` chips are withheld with their reason and a plain start still works.

Record which decks you ran it against, and their versions, in the PR.
