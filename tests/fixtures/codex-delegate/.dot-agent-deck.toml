# Placeholder project config for the real-Codex `clear = true` delegate L2
# scenario (`orchestration/delegate/009`). The REAL `[[orchestrations]]` block
# is generated by the test into the per-test workdir before the new-pane form
# picks that directory, because the worker role's command has to interpolate
# the pinned cheap model (`common::CODEX_TEST_MODEL`) rather than duplicate it
# here where it would silently drift. `load_project_config` runs at
# directory-pick time, so the generated file is the one the form — and the
# daemon's later `handle_delegate` role lookup — actually reads.
