# Chain-smoke fixture for the real-OpenCode L2 test
# (PRD #77 catalog `chain-smoke/opencode/001`). No project config — the
# harness drives the agent via a generated `session.toml` so the deck
# auto-opens one pane running `opencode run` on launch.
