# PRD #742: Connect the desktop GUI to several decks at once — the fleet view

**Status**: Delivered and awaiting the user's merge decision. [PR #1076](https://github.com/vfarcic/dot-agent-deck/pull/1076) is green on all 13 required checks, approved, with every review thread resolved. One thing is deliberately still open: nobody has yet looked at two decks in one window, which is the only surface no automated tier can reach (#953).

**Original status**: Plan drafted 2026-09-13, awaiting the step-1 gate. Four decisions already taken by the user and recorded in Technical Approach; the reconnaissance corrected the issue's own framing of the central retrofit, which is the first thing to read.
**Priority**: High — [#741](https://github.com/vfarcic/dot-agent-deck/issues/741) built the seam for it and named it as the thing that inherits it, and #742's own body argues this is the strongest case for the desktop app existing at all.
**Created**: 2026-09-13
**Issue**: [#742](https://github.com/vfarcic/dot-agent-deck/issues/742) — a deliberate **placeholder**: intent and constraints only, with the plan to be written when work starts.
**Related**: [#741](https://github.com/vfarcic/dot-agent-deck/issues/741) (connect to any daemon — prerequisite, closed, `8f5e8e72` / PR [#1035](https://github.com/vfarcic/dot-agent-deck/pull/1035)), [#819](https://github.com/vfarcic/dot-agent-deck/issues/819) (daemon-side project resolution — the long pole, closed `14e2f034`), [#803](https://github.com/vfarcic/dot-agent-deck/issues/803) (the settings surface endpoints are stored in, closed `2957b70f`), [#801](https://github.com/vfarcic/dot-agent-deck/issues/801) (compatibility from the contract rather than a git tag — open, and a fleet legitimately spans builds), [#953](https://github.com/vfarcic/dot-agent-deck/issues/953) (no driver-level desktop tier — the reason this PRD's headline claim is manual), [#1046](https://github.com/vfarcic/dot-agent-deck/issues/1046) (the toast cannot clear a daemon error — open, and it gets worse with N decks), [#1048](https://github.com/vfarcic/dot-agent-deck/issues/1048) (projects are deck-scoped and nothing remembers them), [#1049](https://github.com/vfarcic/dot-agent-deck/issues/1049) (no wire verb stops a deck — why per-deck lifecycle is out of scope), [#745](https://github.com/vfarcic/dot-agent-deck/issues/745) (the agent overview as landing screen — the screen this PRD merges into), [#802](https://github.com/vfarcic/dot-agent-deck/issues/802) (voice control — its capability registry needs the deck dimension in its shape from the start).

## Problem Statement

The desktop app talks to exactly one deck at a time. PRD #741 turned the deck from a constant into a **choice** — an `Endpoint` whose kind is in the type, stored in `desktop.toml`, reached over an app-managed `ssh -N -L` child, picked from a **Deck selector** — but a choice is still one at a time. A user running agents on a laptop and two build boxes has three windows' worth of fleet and one window that shows a third of it.

That is a gap with a measurement behind it rather than a hypothesis. On 2026-08-29 a single remote host was observed running three distinct long-lived daemons — an installed release plus two from separate dispatch worktrees — alongside roughly 166 short-lived test-scoped ones from a `cargo test-e2e` run. Nothing could see that whole, and nothing can today.

**Why this is the desktop app's job and not the TUI's.** A TUI is one terminal attached to one daemon; it cannot show a second machine's fleet by construction. PRD #176 justifies the desktop app with a *single* deck's agent-communication graph, which is a weaker answer to "why a GUI" than this one. The division the fleet view establishes is that the TUI is the **working** surface — one repo, dense, keyboard-driven, `ssh`-able, you are *in* it with an agent — and the desktop is the **supervisory** surface, where you are deciding *where to look*.

## What the reconnaissance changed, and read this before the rest

Two read-only recon passes ran before this document was written (a reviewer on client architecture, an auditor on trust, lifecycle and resources; neither modified a file). The reviewer's pass **corrected the central claim this issue and #741 have both been carrying for months**, and the correction changes what the work is.

**The retrofit everyone named is already done. The one nobody named is the actual work.** Both this issue's body and #741's hand-off say the central change is `DesktopState` becoming a keyed map of bridges. It is not, because the two maps that matter are **already keyed**: `DaemonLinks` is a `HashMap<EndpointIdentity, Arc<TrustedDaemon>>` (`desktop/src-tauri/src/daemon_bridge.rs:236`) and `EndpointTunnels` is a `HashMap<EndpointIdentity, Arc<TunnelLease>>` (`desktop/src-tauri/src/endpoint_tunnels.rs:181`), both with a working `retain(&HashSet<EndpointIdentity>)`. What is actually hardwired to one deck is a **process-global**: `static SELECTED_DECK: RwLock<Option<SelectedDeck>>` at `desktop/src-tauri/src/dto.rs:846`, read through `selected_deck()` (`:892`) and `selected_endpoint()` (`:905`), written only by `apply_settings_selection()` (`:875`). Every call site that asks "which deck do I mean" and has no endpoint argument resolves it through that global — including `trusted_daemon()` (`daemon_bridge.rs:930`) and `get_snapshot()` (`daemon_bridge.rs:936`), whose already-parameterised siblings `links.trusted(endpoint)` and `snapshot_with(endpoint, …)` exist and are simply not what the callers reach for.

This is good news twice over. The plumbing for N decks is present rather than absent, and the single-deck assumption is **concentrated in one global rather than scattered across a struct's call sites** — so the work is to add a parallel merged read path that iterates an observed set, not to rewrite every consumer.

**The risk nobody had written down is a lock, and it contradicts this PRD's headline requirement.** `DaemonLinks` holds **one async mutex over the whole map, held across connection establishment** (`daemon_bridge.rs:232`, whose own doc at `:227` says "A deck that is not answering queues the other callers behind one connect timeout"). `EndpointTunnels::acquire` has the same whole-map lock (`endpoint_tunnels.rs:200`). With one selected deck that serialisation is a **feature** — it collapses concurrent first-uses of the same deck into one handshake. With N decks it means **an unreachable deck's connect attempt blocks every other deck's handshake for the duration of the timeout**, which is precisely the "one endpoint being unreachable must not degrade the others" property this PRD exists to deliver. It is invisible at every call site. Addressing it is not optional polish; it is the requirement.

## Solution Overview

The **agent overview** — the read-mostly supervisory screen — gains an **All Decks** option on the Deck selector that #741 already built, and renders every configured deck as a sibling group with its own connection state, merging their agents into one screen. The **deck screen** — tiles with live terminals — stays one deck at a time.

That split is the user's decision and it is also what the costs argue for. Attach streams are deliberately **not multiplexed**: the frame header is five bytes, one kind byte plus a four-byte big-endian length, with **no stream id** (`src/daemon_protocol.rs:649-666`), so muxing would be a wire change and a `PROTOCOL_VERSION` bump rather than a refactor. The cost of a live terminal is therefore one connection and one daemon-side task **per visible tile**, linear in tiles. Putting N decks of terminals on one screen multiplies the one cost in the system that is already linear; putting N decks of *rows* on one screen does not.

The frontend is already built for this and says so in its own comments. Agents are keyed by a composite `(daemonId, agentId)` from day one — `AgentOverview.tsx`'s `agentKey` doc says "any bare-id key is wrong the moment #742 connects a second daemon" — and the `daemon-group` `<section>` is already the outer unit, with a comment reading "#742's second daemon becomes a sibling here and changes no inner component." The overview's grouping (`groupAgents`) buckets *within* a deck by tab membership and needs no change at all.

## Scope

### In Scope

- **`Selection::All`** — one variant on the Rust enum (`desktop/src-tauri/src/settings.rs:773`) and the TS union (`desktop/src/lib/endpoints.ts:381`), the `all` token, and the **All Decks** option on the Deck selector.
- **An observed-endpoint set**, and keeping N decks' tunnels and handshakes alive concurrently — `EndpointTunnels::retain` over the whole observed set rather than over a one-element live set.
- **Per-deck connection state.** `ConnectionStatus` (`dto.rs:141`) and the four fields #741 added beside it (`deckKind`, `localOnlyReason`, `selectionFallback`, `projectActionsReason`) stop describing "the selected deck" and describe *a* deck, one per observed endpoint.
- **Per-deck watchers with isolated folds**, so one deck's broadcasts can never be folded into another's view or emitted under another's identity, and so **one unreachable deck does not degrade the others** — including the whole-map-mutex contention above.
- **The merged read view**: the overview renders one group per observed deck with that deck's agents and that deck's connection state, plus a fleet-wide aggregate in the header that is honest about partial connectivity.
- **A multi-deck fixture**, without which neither the vitest tier nor the Playwright browser tier can assert any of this.
- **A real two-deck run**, local plus the remote Linux box, in one window — see Verification.

### Out of Scope

- **Cross-deck attach and terminals.** The deck screen stays single-deck; a tile never attaches to a deck other than the selected one. This is the largest deliberate exclusion and it is the follow-up PRD's subject.
- **Cross-deck workflow launch**, and anything that mutates a non-selected deck's project state.
- **Per-deck lifecycle buttons.** Partly a scoping choice and partly impossible today: [#1049](https://github.com/vfarcic/dot-agent-deck/issues/1049) records that **no wire verb stops a deck** — lifecycle is `peer_pid`-based and therefore local-only by type — so "Stop deck" for a remote deck cannot ship whatever this PRD decides.
- **Attach-stream multiplexing**, and therefore any `PROTOCOL_VERSION` change. See Cross-version safety.
- **The `experimental` flag.** See DECISION 4.
- **A group view or a single-agent drill-in**, and promoting the overview to the landing screen — that is [#745](https://github.com/vfarcic/dot-agent-deck/issues/745).

## Technical Approach

### DECISION 1 — the fleet shape: the overview merges, the deck screen stays single-deck

**DECIDED by the user.** The issue names this as the thing to decide early — "worth deciding early whether agents from different daemons ever appear in one view or stay segregated" — and the answer is *both, on different screens*, which neither of the two options in the issue's framing captures.

The overview merges: one list, every deck's agents, each row carrying its deck. The deck screen does not: it keeps the Deck selector's single choice, and a tile's terminal is always the selected deck's. The reasoning is the per-tile linear cost above, and the fact that the two screens answer different questions — the overview answers "where should I look", the deck screen is where you then look.

Within the merged view, **deck is a grouping rather than only a column**, because the `daemon-group` outer unit already exists and lifting it from one hardcoded section to a `.map` over observed decks changes no inner component. A `daemonId`-backed column is additive on top and type-legal (`daemonId` is already an `OverviewAgent` field), so it can be offered through the existing column picker rather than designed separately.

### DECISION 2 — scope: observe only, and the control half is a named follow-up

**DECIDED by the user**, and it matches the issue's own suggested phasing: "Observe first, control second. The read-mostly subset (list, attach, watch events, stop) is exactly what already works remotely."

One deviation from that sentence, deliberately: the issue's read-mostly subset includes **attach** and **stop**, and this PRD excludes both. Attach is excluded because of the per-tile linear cost and because DECISION 1 keeps terminals on the single-deck screen; stop is excluded because #1049 makes it impossible remotely. What ships is list, watch events, and per-deck health.

A follow-up issue is filed at the end of this PRD for the control half, carrying the seams this document names rather than leaving them to be rediscovered — which is the service #741 performed for this PRD and the reason it was cheap to plan.

### DECISION 3 — the verification is a real two-deck run, and it is in scope

**DECIDED by the user.** The reasoning is in Verification below. In short: a fleet view whose entire premise is "several machines at once" has never had two decks running at once, and no automated tier in this repository can produce one.

### DECISION 4 — no `experimental` flag (CLAUDE.md rule 9)

**DECIDED by the user: no.** This is not a fresh judgement — it is the third time the same mechanical reason has decided it. #803 Open Question 1 and #741 DECISION 2 both said no because **the flag does not reach the desktop app by any route**: nothing under `desktop/` mentions it, it is not on the daemon protocol, and the desktop crate never calls `features::init_and_watch`, so `experimental_enabled()` would read its `false` default forever whatever the TOML or the environment said. Gating this would mean building the flag's Tauri delivery mechanism as part of this PRD, and would raise "against which project directory?" for a packaged build — a question the desktop app has no good answer to. `prds/176-desktop-gui.md` decision 6 holds the prior: a separate GUI binary has no such seam, because the act of building and running it is the opt-in, with maturity handled by packaging (the bundles are unsigned alphas).

### The `all` token, and the one collision worth knowing about

The stored selection is a single string: the reserved word `local`, or an endpoint id. `Selection::All` adds `all`, and the schema was built anticipating exactly this — `EndpointId::mint()`'s doc comment (`settings.rs:698-706`) says sixteen hex characters "cannot collide with `LOCAL_SELECTION_TOKEN` or with any word a future `Selection` variant would reserve."

**The forward-compatibility half already works and is a genuine asset.** Verified end to end by the recon: an older build meeting a stored `all` parses it through `EndpointId::parse` into `Selection::One(EndpointId("all"))` (`settings.rs:815`), finds no matching row, resolves to the local deck with `SelectionFallback::UnknownDeck` reported on screen rather than swallowed (`settings.rs:415-421`), and **writes the token back unchanged**. `desktop/src/lib/endpoints.test.ts:90-93` already asserts that round trip. So shipping `All` does not break an older build's document, and an older build does not destroy a newer build's selection.

**Corrected at M1: the variant costs two production arms, not four.** Both this document and the `Selection` doc comment it was written from listed four sites — `as_token`, `Serialize`, `SelectionVisitor::visit_str` and `EndpointSettings::resolve`. The first three were right and the fourth was one too many. `resolve()` opens with `let Selection::One(id) = &self.selection else { return local() };`, so `Selection::All` falls through that `let … else` the moment the variant exists, with **zero** code change, landing on the local deck with `fallback: None`. That is not merely the incumbent behaviour but the correct one: nothing failed to be honoured, so reporting a `SelectionFallback` there would make the selector print a substitution notice about a selection that is actually in force. The doc comment that was the source of the wrong count has been corrected in the tree, so the next reader gets the true map. What the fleet needs instead of an arm is a **separate** `EndpointSettings::observed_endpoints()`, which is what Open Question 3 recommended and what M1 built.

**The residual collision is narrow and needs a decision.** `EndpointId::parse` (`settings.rs:672-695`) reserves `local` case-insensitively and nothing else, so `all` is currently a **legal endpoint id**. A minted id can never be `all` (sixteen hex characters), so this is reachable only by hand-editing `desktop.toml` — but `Deserialize` runs the same check as the constructor, so reserving `all` would make a hand-written `id = "all"` refuse to load. The recommendation is to **reserve `all` exactly as `local` is reserved**, accepting that refusal, because the alternative — a selection token that is ambiguous between "the fleet" and "that row" — is worse than a named parse error. The milestone that does it carries the test.

### Per-deck watchers, not a merged watcher — and the fold isolation that depends on it

#741 hit a **stale fold**: the watcher's event subscription is a connection to exactly one daemon, nothing about a selection change ended it, and the previous deck's broadcasts went on being folded into the `AgentView` answering the new deck's snapshots and re-emitted under the new deck's name — one machine's fleet shown as another machine's. The fix was a `tokio::sync::watch` generation on `DesktopState` (`terminal.rs:121`), with `watch_one_subscription` returning on that arm **before** its refresh (`lib.rs:961-976`) so the stale fold is discarded along with the subscription that filled it, and a distinct return reason (`SubscriptionEnd::{Ended, SelectionChanged}`, `lib.rs:903`) so the two do not share a retry policy.

With N decks that hazard generalises rather than going away, and it gets worse: the subscriptions are now **concurrent** rather than sequential. `BroadcastMsg` carries no deck id and `desktop://daemon-event` is emitted raw (`lib.rs:927`, `:947`), so "which subscription is this event from" is today answerable **only** by "there is exactly one."

**The design is one watcher task per observed deck, each owning its own `AgentView`, each stamping its own `EndpointIdentity` on what it emits.** That makes the isolation structural rather than a rule someone has to remember: a fold cannot reach another deck's view because it is in another task's `AgentView`, and it cannot be emitted under another deck's name because the identity comes from the task's own endpoint rather than from `selected_endpoint()`. A single merged watcher would reintroduce the #741 hazard at N-way scale, with the mislabel now possible **between two live decks** rather than only across a selection change.

This also settles coalescing. `SNAPSHOT_COALESCE_INTERVAL` (150ms) is applied per watcher loop (`lib.rs:970-981`) and is per-connection today only because there is one connection. Per-deck watchers coalesce independently, producing up to N emits per window, and the frontend merges them — which it is already built to do, since it keys by `(daemonId, agentId)`. The alternative, a global coalescer over N inbound streams, shares the window across decks and reintroduces the cross-deck stall.

**The one thing the frontend cannot fix.** `daemonId` is derived from `connection.socketPath` (`desktop/src/lib/bridge.ts:759`), and the DTO carries exactly **one** `connection`. Any Rust-side merge that emits N decks' agents under `selected_endpoint()` would collapse every agent under one `daemonId` and silently merge two fleets into one — the #741 mislabel, arriving through a different door. The deck identity has to be attached at the **Rust emit seam**.

### The whole-map mutex, and why it is a milestone rather than a note

Restated from the recon because it is the requirement rather than a nicety: `DaemonLinks.trusted` (`daemon_bridge.rs:232`) and `EndpointTunnels::acquire` (`endpoint_tunnels.rs:200`) each hold **one async mutex over the entire map, across establishment**. An unreachable deck therefore queues every other deck's handshake behind its connect timeout. Per-deck watcher tasks alone do **not** fix this — the tasks are separate but they contend on the same lock — so this needs a deliberate change (finer-grained locking, or establishment moved out of the lock), and it needs a test that asserts the property rather than the shape.

This is the one place #741's design actively works against this PRD's headline requirement, and it was invisible at every call site, which is why it is called out here rather than left to be found during implementation.

### What a live deck costs, and the fact that nothing bounds how many there are

Measured from the code by the audit pass, per **live remote** deck in the observe-only scope:

| resource | per deck | evidence |
| --- | --- | --- |
| `ssh -N -L` child process | 1 | `src/remote_tunnel.rs:1117` — one `command.spawn()` per `EndpointConnection::open` |
| detached OS thread draining its stderr | 1 | `src/remote_tunnel.rs:1144` — a `std::thread::spawn` that is **never joined**, one thread blocked in `read` per tunnel |
| forwarded Unix socket, plus client fds | 1 + n | the `-L` socket created per tunnel |
| held daemon connections | 1 long-lived event stream + #741's 2 per refresh ⇒ **2N** | `daemon_bridge.rs` establish/watcher |
| tokio task, client side | 1 watcher per observed deck | per-deck watchers, above |
| tokio task, daemon side | 1 per held connection | the daemon serves each connection independently |

A **local** deck costs the same minus the `ssh` child, the stderr thread and the forwarded socket.

**Nothing bounds the number of configured or live endpoints.** `MAX_ENDPOINT_ID_BYTES` (64) bounds an id and `MAX_SETTINGS_BYTES` (256 KiB, `settings.rs:1081`) bounds the file, so the only ceiling is accidental and sits around a thousand rows — not a bound anyone designed. Twenty configured decks is twenty `ssh` children, twenty never-joined threads, roughly forty held connections and twenty watcher tasks, every one of them established through the whole-map mutex above, which is what turns "twenty decks" into a serial connect storm rather than merely a lot of decks.

**The first thing to hurt is not the one the #741 baseline would suggest.** Of that baseline's three numbers, bandwidth (~644 B/agent) scales with total fleet agents and is mild, and 2-connections-per-refresh becomes 2N as gradual fd pressure — but the **≤6.667/s coalesce ceiling is per watcher**, so with per-deck watchers the aggregate emit rate into the webview becomes **N × 6.667/s**, and the frontend merges every one of them. That is client-side IPC and CPU pressure that scales with **deck count regardless of how few agents each deck has**, which is the opposite of the intuition that a small fleet is cheap. Separately, the first hard OS ceiling is the `ssh` child and fd count, which is unbounded and reaped only by `retain`/`close_all`.

Whether a soft cap or a warning is wanted is an open question below rather than a decision taken here.

### A fourth local-only gate, which #741's list of three did not name

#741 names three operations that are local-only because `Endpoint::as_local()` returns `None` for the remote arm — `peer_pid` termination, the stale-inode `remove_file`, and lazy-spawn — and its own Risks section warns that "`peer_pid` is not the only locality assumption, only the one that was found." The audit enumerated every production call site of `as_local()` and `require_local()` and **the warning was right: there is a fourth.**

It is the **filesystem trust check itself**, at `desktop/src-tauri/src/daemon_bridge.rs:881`, gated by `if let Some(local) = endpoint.as_local()`. For a remote deck `verify_endpoint_trusted` — the uid plus exactly-`0o600` inode predicate — **never runs at all**, and trust rests instead on ssh host-key and user authentication.

**This is by design and is documented** (`daemon_bridge.rs:869-884`, `daemon_client.rs:88-97`, and the **Trust is a property of the deck's kind** section of `docs/develop/desktop-gui.md`): a uid-and-mode test on a socket the local `ssh` client created proves only that the local `ssh` process is ours, and says nothing about who is on the far end. Running it anyway would be exactly the disguise #741 exists to remove.

**The reason it belongs in this document is that it is a different *shape* from the other three, and the difference is the dangerous part.** The three that were named **refuse** for a remote deck — a disabled button with a reason. This one **silently substitutes a different trust basis**. Nothing in the flow says "this deck was trusted differently"; it simply was. For the observe-only scope that is not a leak — each remote deck's ssh trust is per-connection and keyed by `EndpointIdentity` — but with N decks in one view it becomes newly easy for a reader to assume every group in the fleet cleared the same gate, and none of the local ones and all of the remote ones did not. M6 records this in `docs/develop/desktop-gui.md` beside the existing trust section.

### Cross-version safety (CLAUDE.md rule 12)

**Confirmed by the audit pass rather than assumed: this PRD does not touch the TUI↔daemon contract. `PROTOCOL_VERSION` stays 9 (`src/daemon_protocol.rs:346`) and no `changelog.d/742.breaking.md` fragment is needed.** The wire shape does not move — N decks is N connections each speaking the existing wire, with no new frame kind, no new verb and no added field, and the one change that *would* have touched the wire, attach-stream multiplexing, is explicitly out of scope. The daemon is queried N times over the same `Hello`, snapshot and event verbs it already serves, and **cannot tell a fleet client from a single-deck client**.

**The distinction that makes that answer correct is worth stating, because there genuinely is a semantic change and it would be easy to misfile.** `ConnectionStatus` and the four fields beside it going from "the selected deck" to "*a* deck, one per endpoint" **is** a same-shape/different-meaning change — but it lives entirely on the **desktop's own Tauri IPC and DTO surface** (`desktop://daemon-event`, `DesktopSnapshot`, `dto.rs:44` and `:141`), which is the client's internal contract between its Rust half and its webview. Rule 12 governs the TUI↔daemon protocol, and that surface is not it. The rule's manual cross-version test still applies and folds into the two-deck run, where the two decks are deliberately different builds — a verification, not a version bump.

One caveat carried forward: [#801](https://github.com/vfarcic/dot-agent-deck/issues/801) is the adjacent open item, and if a fleet spanning builds trips a **tag-based** refusal path that should have been capability-based, that is a bug to fix and still not a `PROTOCOL_VERSION` move.

**A fleet legitimately spans builds**, which is the part that is genuinely new. #741 settled that a remote deck's build stamp is an **informational badge and never a refusal**, because the remedy a refusal offers — replace the daemon — is not available on a host you do not own; `PROTOCOL_VERSION` stays the hard floor for every deck kind, and what a screen acts on is the capability set that deck advertised in its own `Hello` reply. With N decks that policy is exercised for the first time in its intended form: N stamps and N capability sets in one view, and any code that assumes one capability set becomes wrong. [#801](https://github.com/vfarcic/dot-agent-deck/issues/801) (compatibility classified from a git tag rather than the contract) is adjacent and open; whether it becomes a blocker here is an open question below.

The rule-12 manual cross-version test still applies and is folded into the two-deck run, which is the natural place for it: the two decks can deliberately be different builds.

## Verification, and what cannot be verified here

**No automated tier in this repository can reach a real second daemon.** There is no `tauri-driver` and no WebDriver session anywhere in the tree ([#953](https://github.com/vfarcic/dot-agent-deck/issues/953)), so real Tauri IPC, real PTYs and a genuinely-connected remote deck are exercised by manual smoke and by nothing else. The three tiers that do run prove the merge and fold *logic* against fixtures:

| tier | what it is | what it can assert here |
| --- | --- | --- |
| Rust unit (`desktop/src-tauri`, in `cargo test-fast`) | `#[cfg(test)]` in `settings.rs`, `daemon_bridge.rs`, `endpoint_tunnels.rs`, `agent_view.rs`, `dto.rs`, `lib.rs` | `Selection::All` round-trip and the `all` reservation; `retain` over an N-deck observed set; fold isolation and deck-stamped emit; the lock-contention property |
| vitest (jsdom, `pnpm test`) | components and libs | per-deck `ConnectionView`, the deck grouping, the aggregate header instruments, the selector option |
| Playwright browser tier (`pnpm test:browser`, `desktop/e2e/*.spec.ts`) | the production `pnpm build` bundle under Chromium and WebKit, through `?fixture=1&state=…` | the same, as a reader sees it — `overview-grouping`, `overview-rows`, `connection-states` and `decks` specs already exist to extend |

**The multi-deck fixture is a prerequisite for two of those three, and it is the biggest test-infrastructure lift in the PRD.** `FIXTURE_DAEMON_ID` is a single hardcoded identity (`desktop/src/data/fixture.ts:9`), `createFixtureSnapshot` returns one `DeckSnapshot` with one `connection`, and the `DeckBridge` contract itself has **one** `getSnapshot` — so "one deck" is baked into the contract, not just into the data. A new `FixtureState` (e.g. `"fleet"`) carrying at least two distinct `daemonId`s requires widening that contract.

### The two-deck run, which is this PRD's real done-condition

**Nothing has ever run two decks at once**, and a fleet view that has not been seen with two decks has not been seen at all. This run is therefore in scope by the user's decision rather than deferred, and it discharges a second debt at the same time.

That debt: `docs/develop/desktop-gui.md`'s **Current milestone limits** says a remote deck is unverified end to end and that a loopback `ssh` is not a substitute, because locally the client's filesystem *is* the daemon's and every path assertion passes whichever side resolved it. **That bullet is now partly stale and this PRD should correct it.** #741's own Work Log entry of 2026-09-13 records that M13 *did* run: a desktop on a Mac drove a workflow launch against a deck on the Linux dev box over `ssh -L`, the daemon resolved a project here, published `orchestrator-context.md` here, and five real agents ran (`ClaudeCode` on two panes, `Codex` on two more), with the daemon's own log preserved at `/var/tmp/dad-branch-m13-evidence.log`. What stayed unticked is the **display** half: nobody looked at the connected window to confirm that the project header and the agent panes name `/home/vfarcic/…` and never `/Users/viktorfarcic/…`, which is the exact symptom the 2026-08-29 measurement reported. A log proves what the daemon did; it says nothing about what the client rendered.

The run this PRD owes, then, is one session covering both:

1. A local deck and the remote Linux box both configured, **All Decks** selected, both groups populated in one window.
2. The remote deck's rows and header naming the **daemon's** paths — `/home/vfarcic/…`, never `/Users/viktorfarcic/…`. (Closes #741 M13's display half.)
3. One deck killed while the other keeps working: the killed deck's group degrades on its own, the surviving deck's agents keep updating, and the header aggregate says which of N are up. **Timed** — an unreachable deck must not stall the other's handshake, which is the whole-map-mutex property above and the one most likely to fail.
4. The two decks deliberately on **different builds**, which is both the fleet-spans-builds case and rule 12's cross-version manual test.

Recorded in this PRD's Work Log and in `docs/develop/desktop-gui.md`. Per CLAUDE.md rule 5 the tests covering what was touched are named in the report rather than claimed generically.

## Success Criteria

1. **All Decks** is selectable, and selecting it shows every configured deck's agents in one overview, each row attributable to its deck.
2. **One unreachable deck does not degrade the others** — measurably: the other decks' agents keep updating and their handshakes are not queued behind the unreachable one's connect timeout.
3. **No agent is ever shown under the wrong deck.** Asserted structurally (per-deck `AgentView`, deck-stamped emit) and by test, not by inspection.
4. The header is honest about partial connectivity: agent counts are computed over **connected** decks, a disconnected deck is visible as a degraded group rather than as zero agents, and the number of decks up out of the total is stated.
5. An older build meeting a stored `all` still degrades to the local deck, says so, and preserves the token.
6. `PROTOCOL_VERSION` is unchanged at 9 with no compatibility fragment — confirmed by the audit pass at planning time, and to be re-confirmed against the finished diff rather than assumed to have held.
7. The two-deck run above has happened and is recorded.

## Milestones

Ordering follows the recon's recommendation, which puts the risky correctness work under the user-visible payoff rather than beside it. The **first milestone that changes what a user sees is M4**; M1 is visible only as an inert selector option, and M2 and M3 change no user-visible behaviour at all. That matters for the PR-splitting question in Open Questions.

**This table was rewritten on 2026-09-14, after M5, because it had gone stale in a way worth recording rather than quietly fixing.** As planned, M5 was "multi-deck fixture + `DeckBridge` contract widening, test-only" — but M4 absorbed both (a fleet contract is not something a merged view can be built without, and its tests need the fixture), so that row described work that had already shipped. Meanwhile the *actual* fifth milestone — a real deck identity on the wire — was not in the table at all, because the defect it fixes was not known until M4 went looking. The milestone that was projected as test-only turned out to change the DTO, the crate and the frontend. The coder implementing it caught the discrepancy and declined to edit the PRD from inside a code commit, which is the right instinct and why this correction is here and dated.

| # | milestone | state | user-visible | why here |
| --- | --- | --- | --- | --- |
| M1 | `Selection::All` + the `all` reservation + the selector option (Rust enum, TS union, `DeckSelector`) | **done** `dc065999` | selector option only, inert until M4 | smallest, highest-confidence change; the degradation path already protects older builds |
| M2 | The observed-endpoint set; `retain` over it so N decks' tunnels stay alive concurrently | **done** `054ef879` | no | per-deck lifetime must be correct before N streams run, or one deck's teardown corrupts another's |
| M3 | Per-deck watchers, isolated folds, deck-stamped emit — **and both whole-map mutexes** | **done** `81676c45` (RED) + `7d95efd4` (GREEN) | no | the correctness core and the riskiest piece; proven by Rust unit tests before any UI depends on it |
| M4 | Frontend merge, `DeckBridge` → `DeckFleet`, overview deck grouping, per-deck `ConnectionView`, honest aggregate header, **and the multi-deck fixture** | **done** `7d981dcf` | **yes — the fleet appears** | the payoff; needs M1–M3 underneath. Absorbed the fixture and the contract widening originally projected as M5 |
| M5 | **A real deck identity on the wire** (`EndpointIdentity::wire_id`), and exact fleet membership (`DesktopSnapshot.fleet`) | **done** `1d8c574f` | no (fixes a silent mis-render) | not foreseen when this table was written; M4 established that `describe()` collides and that membership has no wire signal |
| M6 | The three defects this PRD made reachable (#1046's toast, the `chooseDeck` data-loss path, the settings panel under a fleet) plus the `release_if_not_selected` rename | **done** `778cb126` | yes (three fixes) | held out of M1–M5 so each of those stayed about one thing |
| M7 | Docs: the **Decks** section and **Current milestone limits** in `docs/develop/desktop-gui.md`, including the now-stale remote-verification bullet and the fourth trust gate | **done** `07b860d1` | no | corrections this PRD owes regardless of the rest |
| M7a | Resolve the review and audit findings: one regression the lock split introduced, a torn read of the applied selection, a watcher claim that could not tell itself from a re-claim, a bypassed write gate, a cross-deck evidence ring, and four false claims | **done** | yes (two fixes) | not in the plan, because a branch-wide review and audit were not either; the findings are recorded in the Work Log entry below |
| M8 | **The fleet against real daemons** — five in-process tests binding the production attach server twice, plus one lane-1 test driving two real `daemon serve` **processes** | **done** `63c04624`, `64de02cb` | no | every fleet test until here talked to a scripted socket responder; the daemon side was a fake |
| M9 | **A real loopback ssh tunnel** in lane 1, **daemon-side resolution made falsifiable on one machine**, and the linkage-check hex fix | **done** `215de3d3`, `9f22ce50`, `0d166a49` | no | retires most of what this document said needed a second machine |
| M10 | File the follow-up issues | **done** — [#1072](https://github.com/vfarcic/dot-agent-deck/issues/1072), [#1073](https://github.com/vfarcic/dot-agent-deck/issues/1073), [#1074](https://github.com/vfarcic/dot-agent-deck/issues/1074) | no | the service #741 performed for this PRD |
| M11 | Review the new test work, and resolve its five findings | **done** `1b5460cd` | no | a clean bill on ssh isolation and test falsifiability; five low findings, all fixed |
| M12 | Merge `main`, resolve the toast collision, close the socketless-deck gap | **done** `0e9070b4`, `142f22c3` | yes (an unconfigured deck becomes visible) | `main` fixed #1046 independently while this branch was in flight; and an automated reviewer found the gap Open Question 3 had left open |
| M13 | Open the PR, settle CI, answer and resolve every finding | **done** — [PR #1076](https://github.com/vfarcic/dot-agent-deck/pull/1076) | no | 13/13 required checks green, three threads resolved, approved |
| M14 | **A human looks at two decks in one window** | | no | the one surface no test can reach (#953), and the half of #741's M13 that is still open |

## Risks

- **The whole-map mutex is the one that can make this PRD fail its own headline.** `DaemonLinks.trusted` and `EndpointTunnels::acquire` each hold one lock across establishment, so an unreachable deck queues every other deck's handshake. It is invisible at the call sites and it will not fail a compile. Mitigation: M3 owns it, and success criterion 2 is a measurement rather than a claim.
- **A merged emit under `selected_endpoint()` would silently merge two fleets into one.** The frontend's composite key protects against a *bare-id* collision but not against N decks arriving tagged with one `socketPath`. This fails silently and plausibly — the screen looks right. Mitigation: deck identity attached at the Rust emit seam, and a test that two decks' agents never share a `daemonId`.
- **The `DeckBridge` contract is single-deck**, so the two tiers that could assert the user-visible half cannot assert anything until the fixture and the contract are widened. Risk of the UI landing with only Rust-side coverage. Mitigation: M5, pulled earlier if the plan prefers.
- **The headline claim is manual-only and always will be**, given #953. A regression in the fleet view after this PRD ships surfaces when somebody next runs two decks, which is not on a schedule. This is the same accepted trade CLAUDE.md rule 5 names for lane 2, restated for the desktop.
- **N decks change an existing open defect's severity class.** [#1046](https://github.com/vfarcic/dot-agent-deck/issues/1046), confirmed at `desktop/src/App.tsx:791`: the toast renders on `notice || runtime.error` but its dismiss button is wired to `setNotice(undefined)` only, so `runtime.error` — a single App-level value from one `useDeckRuntime` — can never be cleared. With one deck that is an annoyance. With N it is a **masking bug**: one dead deck's stale error sits undismissable in the shared toast over a healthy deck's later message. The audit rates it a strong should-fix rather than a hard blocker, because per-deck `ConnectionView` state covers the *health display* independently and the fix itself is a few lines. Open question below.
- **Resource cost is linear and genuinely unbounded**, and the shape of it is counter-intuitive: nothing caps the endpoint count, each live deck costs an `ssh` child plus a **never-joined** stderr thread plus ~2 held connections plus tasks on both sides, and the first thing to saturate is the **N × 6.667/s aggregate emit rate into the webview**, which scales with deck count even when every deck is nearly idle. The risk is that "it works with two" is read as "it works."

## Open Questions

1. **Does the whole-map mutex get split, or does establishment move out of the lock?** Both fix the contention; they differ in blast radius on #741's code. To be settled at M3 with the implementer.
2. **Merged single emit, or N per-deck emits?** The recon recommends N per-deck emits with the frontend merging by `(daemonId, agentId)`, since that is already the frontend's key and it avoids both a shared coalescer and the cross-deck stall. Recorded as a recommendation rather than a decision.
3. ~~**Does `resolve()` get an `All` arm, or does a new `observed_endpoints()` sit beside it?**~~ **ANSWERED at M1: the latter, and `resolve()` needed no change at all** — its `let … else` already catches the variant and returns the right answer. `EndpointSettings::observed_endpoints()` now sits beside it. One sub-decision was taken there that this document had not anticipated: a stored row with **no socket path** is omitted from the observed set, because it has no address to connect to and a watcher for it would have nothing to watch. The cost is that a half-configured deck is *absent* from the fleet rather than present-and-unconfigured, and whether the merged view should instead render it as a group carrying a "press Test connection" state was left as an **M4 screen question**. **M4 did not answer it and M12 did** — as a group, present and counted. Greptile raised it as a P1 on PR #1076 against Success Criterion 4, which is the same finding this paragraph had already written down: three configured decks with one socket path still to be discovered read as `2/2`, the third in neither the numerator nor the denominator and with no group on screen. The sub-decision itself stands, and was only ever about connectability: a row with no address still gets no watcher, no tunnel and no handshake. What changed is that `observed_endpoints` was answering a second question it had never been asked — what the screen shows — so it is now two methods, `connectable_endpoints` and `unconfigured_decks`, and each caller names the one it means.
4. **Does [#801](https://github.com/vfarcic/dot-agent-deck/issues/801) become a blocker?** A fleet spanning builds is the first real exercise of classifying compatibility from a git tag rather than from the contract. Adjacent today; the two-deck run on deliberately different builds is what would tell us.
5. **Does [#1046](https://github.com/vfarcic/dot-agent-deck/issues/1046) get pulled in?** The audit's recommendation is **yes** — it is the one item it most wanted a decision on before merge, because N decks move it from annoyance to masking bug, and the fix is a few lines. Recommended for inclusion; the user's call.
6. **One PR or two?** M1–M3 and M5–M6 change no user-visible behaviour; M4 is the whole payoff. #741 DECISION 4 chose one PR on the reasoning that **a PR whose behaviour cannot be validated is a PR approved on trust**, with the milestones landing as separate commits to keep a usable bisect. The same reasoning applies here and the same answer is recommended, but it is the user's call.
7. **Should there be a bound on configured decks?** The factual half is now answered: **nothing bounds it today** beyond an accidental ~1000-row ceiling from the 256 KiB settings-file cap. What is open is whether this PRD adds a soft cap, a warning past some N, or neither — and the honest input to that decision is a measurement at N=10 (fd and process count) rather than a guess.
8. **Is `#[spec]`-style catalog coverage wanted for any of this?** Today the desktop's three test tiers are catalogued nowhere — `tests/CATALOG.md` covers only the Rust e2e tier. This PRD does not change that, but it is the largest desktop feature yet to ship with no catalog entry, and if that should change, this is the moment to say so rather than after.

## Work Log

### 2026-09-13 — Written from the placeholder issue plus two read-only reconnaissance passes

Written by the orchestrator from #742's body and all three comments (the third, dated 2026-09-11, is a deliberate hand-off from #741), `prds/741-desktop-connect-any-daemon.md`, and the **Decks** section of `docs/develop/desktop-gui.md` — plus two read-only recon passes, one on client architecture, state shape, the overview's data path and test tiers, and one on trust, lifecycle, resources and cross-version safety. Neither modified a file. Full reports at `.dot-agent-deck/prd742-recon-reviewer-report.md` and `.dot-agent-deck/prd742-audit-half2-report.md`.

### 2026-09-13 — M1 landed: the selection value, the reservation, and the selector option

`62693ed6` (this document) and `dc065999` (the change). Green on every gate: `cargo test-fast` 2983/2983, vitest 457, Playwright 88 across Chromium and WebKit, plus `fmt`, the four-flag clippy and `pnpm build`.

**It corrected this document's own arm count**, recorded in full in *The `all` token* above: two production arms, not four.

**Two tests were deliberately inverted, and the way it was done is the part worth keeping.** One Rust and one TypeScript test each pinned the forward-compatibility path using `all` as their worked example — a build *without* the variant degrading an unknown token gracefully and writing it back unchanged. This is the build *with* it, so those assertions about `all` legitimately flip. Rather than flipping them green, the stand-in token was moved to a word this build still does not know (`group`) and the property was kept intact, with the inverted assertions added as their own named test. The degradation path is an asset — it is why shipping `All` cannot damage an older build's document — so it now protects this build against whatever variant comes after `All`.

**Two things were found and routed rather than fixed, both landing on M4.** First, the Settings → Decks panel has its own chooser that is *not* built from `deckChoices` and has no All Decks entry, so with `all` stored no radio is checked and no detail form renders; **Test connection** is still enabled there and returns an already-tested `UnknownDeck` report whose *message* is the wrong sentence for the case. Second, and the one with teeth: choosing All Decks on a document with **no** `[endpoints]` section now writes one, because All Decks is a second choice needing no configuration and the component's no-op guard no longer catches every click. That is ordinarily harmless, but it adds a click to a **pre-existing data-loss hazard** — if the document failed to parse, `load_from` falls back to defaults, the webview sees no section while rows are still on disk, and an empty `remote` merges over them. That is a hole in the `Option<EndpointSettings>` merge protection `docs/develop/desktop-gui.md` documents precisely to stop "every remote deck deleted by somebody changing the theme", and `EndpointsPanel::chooseDeck` has written that shape on every click since #741 with no same-selection guard at all.

### 2026-09-13 — M2: the observed set, and a defect declined rather than fixed

`054ef879`. `retain` now takes the observed set instead of a one-element live set. Not one line of `EndpointTunnels`' production code changed — the map was already keyed and `retain` already took a set, so the widening is entirely in the caller.

**The design decision was to *not* make "the deck actually moved" a set-level concept.** Everything `selection_moved` gates is about the one deck the deck screen talks to — the detach, the re-subscribe, the emit that costs a connect timeout. Widening it to "the observed set changed" would detach a user's live panes because they added a row in the settings sheet, which is the exact defect that gate exists to prevent, reached from the fleet side. `retain` needs no gate and never had one: the live set is derived from the document, so a save that changed no deck produces an identical set. One invariant carries it — **`resolve()`'s answer is always in the observed set** — and it is pinned by test rather than left to inspection.

**The milestone's most valuable output was a defect it declined to fix.** `release_if_not_selected` compares the probed endpoint against `selected_endpoint()`; under `Selection::All` that resolves to the **local** deck, so **Test connection on any remote observed deck releases that deck's transport**. Inert at M2 because nothing held a lease on a non-selected deck — and a live defect the moment M3's watchers did, leaving the child alive under the watcher while the next `establish()` opened a **second `ssh` child** beside it. The correct predicate needs the *applied* observed set rather than the optimistic document the webview passes in, and M3 had to choose that data structure, so fixing it here would have been picking M3's shape for it.

### 2026-09-13 — M3: the correctness core, and the PRD's model of the bug was wrong

`81676c45` (RED, tester) then `7d95efd4` (GREEN, coder). 2997/2997. The mutex test went from eliding its full **5.018 s** bound to **0.017 s**.

**Red-first earned its place here by falsifying this document.** The PRD said the deck identity had to be attached at the watcher's emit seam. It does not: tests 1 and 2 failed at `snapshot_with` **with no watcher involved at all**. `snapshot_with(endpoint, …)` already takes the deck as a parameter and then drops it — `TrustedDaemon.connection` is stamped at establishment by `connection_from_handshake`, which reads `socket_path_text()` and `selection_fields()`, both resolving the process-global. **Even a perfectly isolated per-deck watcher would still have emitted every deck under the selected deck's name.** The fix was smaller and two layers lower than planned, and no amount of reading had produced it across three review passes.

**A second correction: the fold half cannot fail.** All three fold-isolation assertions passed before any change — two `AgentView`s are two objects, so isolation is structural. Items 8 and 9 are one defect measured twice, which is worth knowing before reading a green test as evidence of something it does not show.

**Both mutexes needed splitting, for asymmetric reasons.** Fixing only `DaemonLinks` would have left every `acquire` serialised behind one deck's ssh authentication — Success Criterion 2 unmet for exactly the remote decks it is about — because `establish()` holds that lock *across* `acquire()`. Fixing only `EndpointTunnels` would have left the risk test red. `EndpointTunnels::acquire` has **no test** and was fixed from the code deliberately: stalling it needs a production test seam, and the tester's instruction was to treat it as unguarded rather than trust a green run that could not have failed.

**A pre-existing test could never have failed.** `a_held_link_is_reused_and_costs_one_handshake` drives its two calls *sequentially*, so it measures the held-link cache and would pass with no serialisation at all. Its replacement races two calls against a script offering two replies while asserting one connection, and was verified to fail with the new gate lines removed.

**Three comments were corrected because this commit falsified them.** Two carried #741's reasoning that an early return is what stops the watcher pairing a new deck's identity with an old deck's fold. That is no longer what prevents it — each watcher is pinned to one endpoint for its whole life, so fold and label come from the same deck by construction. The mechanism was kept (it still buys re-establishment without a second of dead air) and the prose now says what it actually buys. Leaving them would have told the next reader that a structural property is enforced by a return statement.

**Two residuals, both stated rather than resolved:** nothing can reach an `AppHandle`, so *that each watcher stamps its own endpoint on the emit* is unguarded and is a hand-check at M8; and `retain_watchers` aborts, so a watcher already inside `snapshot_with` may have an emit in flight. Making the emit chain generic over `R: Runtime` is the lever that would close both, and it is deliberately deferred to the follow-up.

### 2026-09-14 — M4 and M5: the fleet appears, and the key it rests on turns out to collide

`7d981dcf` then `1d8c574f`. Rust 3001, vitest 472, Playwright 98.

M4 made `DeckBridge` return a `DeckFleet` (selected deck first, never empty — the crate's invariant, since `observed_endpoints()` returns one element for every selection but `All` and leads with local for `All`), lifted `daemon-group` from one hardcoded section to a map over observed decks, and added a `DECKS 2/3` instrument whose hover carries the sentence a ratio cannot. `groupAgents` was untouched: it groups *within* a deck.

**M4 then found that the key the whole merged view rests on can collide.** `Endpoint::describe()`'s own doc comment says two decks differing only in remote socket path, identity file or jump host describe identically — and the frontend derived `daemonId` from `socketPath`, which *is* `describe()`. So two rows pointing at the same `user@host` with different `socket` values — **two daemons on one host, precisely the case the `socket` field exists for** — folded into one entry and shared a `daemonId`. The composite `(daemonId, agentId)` key does not save it, because the key component is the collision; the two decks alternate in one group and it looks like a healthy single deck. `EndpointIdentity` was introduced on PR #1035 *specifically* for the N-deck case and stopped at the Rust boundary.

**M5 put it on the wire.** `EndpointIdentity::wire_id()` — FNV-1a over the newtype's *derived* `Hash`, rendered `deck-<16 hex>` — so a new `RemoteEndpoint` field joins the token the moment it is declared, with nothing to update. `DefaultHasher` was rejected because `std` documents its algorithm as unspecified, so the token would not name the same deck across a restart. (M8 corrected the second half of that sentence, which said the webview keys `localStorage` state on this token: it does not — every `localStorage` key it writes is scoped by runtime mode, and the reason to pick a stable hash is that this is the identity a per-deck preference *would* be keyed on.) The event stamp had to move with the snapshot key: `isSelectedDeckEvent` compares them, so a stamp left on `describe()` would have gone on answering "yes, this is your deck" for a second daemon on the same host after the snapshot stopped doing so.

**M4 also found that fleet membership has no wire signal at all** — a deck leaving the observed set produces no event, and under `All` → `local` the resolved deck does not move, so `apply_selection` emits nothing while the departed deck's watcher has just been ended. M4 solved it frontend-only (reset at `connect()`, reconnect on an endpoints-fingerprint change) and M5 replaced it with `DesktopSnapshot.fleet`, the observed deck ids in observed order. One subtlety worth keeping: **prune after the upsert, not before** — a watcher being torn down can land one last snapshot whose `fleet` no longer names its own deck, and pruning first deletes the entry only for the upsert to reinstate it. The fingerprint reconnect was kept, with its justification rewritten from correctness to promptness: without it the self-correction is bounded by `RECONCILE_INTERVAL` at five seconds, and five seconds of a just-removed deck still on screen reads as the app ignoring the user.

### 2026-09-14 — M8 and M9: the fleet against real daemons, and two claims this document had wrong

`63c04624`, `64de02cb`, then `215de3d3`, `9f22ce50`, `0d166a49`. Rust **3017**, vitest 487, Playwright 100, the scoped e2e set 36 passed.

**Every fleet test before these talked to a fake.** `scripted_daemon` is a hand-written socket responder, so the whole merged view was pinned against something *shaped like* a daemon. M8 bound the **production** attach server twice in process — real `Hello`, real capability negotiation, real `ListAgents`, real `cat` PTY children, and the umask-before-`bind(2)` dance creating an inode `verify_endpoint_trusted` actually runs against — and M8's second commit drove two real `dot-agent-deck daemon serve` **processes** from a client that created neither inode (`fleet/observe/001`).

**The fold test is the one that matters, and the reason is worth keeping.** M3's fold assertions are structurally incapable of failing: two `AgentView`s are two objects, and the scripted test hand-applies the event. The real-daemon sibling asserts on the **wire** — a broadcast through the real daemon-wide channel and the real `handle_subscribe_events`, read back off a real socket — with both decks sharing the same agent id *and* the same pane id, so nothing but per-deck isolation separates them. That one can fail.

**A ceiling nobody had written down:** a fleet of two **local** decks is not expressible as a settings document at all (`observed_endpoints` is `[Endpoint::local()]` extended with remote rows only), so `observed_fleet()` and `DesktopSnapshot.fleet` cannot be driven from two local daemons. That bounds what any real-daemon test of the fleet can assert, and is why the membership half is asserted at the map level instead.

**M9 then retired most of "this needs a second machine", and corrected two things this document asserted.**

**First: a real loopback ssh tunnel is testable, and now tested** (`fleet/observe/002`) — a sandboxed `sshd` with its own host key, its own `authorized_keys` and a keypair minted in the test's temp dir, on an ephemeral port, reading and writing nothing under the operator's `~/.ssh`. Nothing in this repository had ever exercised `Endpoint::Remote` end to end: not the `ssh -N -L` child, not the forwarded socket, not the teardown. It also asserts the **fourth local-only gate** from both sides — that `verify_endpoint_trusted` is correctly *not* run against a forwarded socket, **and** that the same predicate run by hand on that inode passes, because every fact it checks is a fact about our own `ssh` client and none is a fact about the far end. A gate that cannot fail is not a gate.

One measurement from building it, kept because it is the kind of thing that costs an afternoon twice: **`$HOME` does not move OpenSSH's `known_hosts` anchor.** OpenSSH expands `~` against `getpwuid()->pw_dir`, so under `env -i HOME=<sandbox>` a forced `StrictHostKeyChecking=yes` still consults the operator's real file (measured, OpenSSH 10.2p1). The test therefore points the tunnel at a wrapper via `SshProgram::at` — the production escape hatch whose own doc names exactly this use — which execs the resolved binary with four `-o` flags prepended and **weakens no check**: strict checking stays on and was verified able to refuse, with a decoy key.

**Second: `EndpointTunnels::acquire` does not hold a whole-map lock, and this document said it did.** It scopes the map lock to a block and releases it *before* `spawn_blocking`; what is held across the ssh spawn is the **per-deck gate**. The whole-map hold was M3's *previous* shape. The audit finding, this document and several task briefs all carried the stale framing, which made a standing concern sound larger than it is: the gate is already the correct shape, and what is missing is a test of it, not a fix. It stays untested for the reason its own doc section gives — reaching it needs an injectable `SshProgram`, a production seam deliberately left out of scope, and without one the test would pass **vacuously** on any host where `ssh` is not at one of seven hardcoded paths.

**Third: `HOME` was the wrong lever for the path-resolution test, and the reasoning against it is better than the test.** No project-resolution path reads it — `ResolveProject` refuses anything non-absolute, so there is no `~` to expand and nothing resolves against an environment. The tell: the e2e harness has given every daemon its own `HOME` since it was written, so had `HOME` been an input, this property would already have been "provable" by every test in the tree — which is the shape of a test that proves nothing. The lever is **cwd** (`project/resolve/002`), falsified by inversion: start the daemon where the client stands and the listing carries the client's project and the test goes red.

So `docs/develop/desktop-gui.md`'s claim that a loopback `ssh` cannot substitute for a second machine is now **narrowed rather than retired**: the **transport** and the "whichever side resolved it" half are both covered on one machine. What a loopback genuinely cannot reach is the **display** half — what a connected window draws — which is what #819 M10 was unticked for all along, and which needs `tauri-driver` (#953) rather than more hardware.

### 2026-09-14 — M11: the second review, and a flake that was not one

`1b5460cd`. Rust **3019**, full lane 1 **3225**, vitest 487, Playwright 100.

A second review pass over the M8–M9 test work returned **a clean bill on the two things that mattered** — ssh isolation and whether each new test can actually fail — and independently verified both corrections the implementing agent had made, reproducing the OpenSSH `known_hosts` measurement on the same box. It added one nuance that three documents had missed in the other direction: **"a whole-map lock across establishment" is true of the `#[cfg(not(unix))]` arm** (`endpoint_tunnels.rs:522`, whose own comment says so), and false of the Unix arm that actually ships. So the concern is platform-specific rather than wrong, and `acquire`'s doc now states the split instead of describing what M3 changed in the past tense.

Its five findings were all low or informational and all fixed. Two are worth keeping for the reasoning rather than the change.

**The xtask cue list was narrowed against its own brief, and the brief was wrong.** The instruction was to exempt a bare leading `#NNN`; only the parenthesised form `(#742)` was implemented, because every looser spelling has a real colour behind it — line-start admits the second line of a wrapped `linear-gradient(\n  #333,`; string-start admits `"#333"`, which is line 2 of that very test's own `bad` fixture; an opening paren alone admits `linear-gradient(#333, #000)`. Requiring both brackets leaves none of them. `fixed`, `closed` and `resolved` were left out of the cue list for the same class of reason: **`background: fixed #333` is valid CSS**, an any-order shorthand, so that cue would launder a genuine all-decimal colour. What the list states is now restatable — base and `-s` forms, never the past participle — rather than being an arbitrary set. And the residual was *named* rather than claimed away, per rule 17: `fn(#NNN)` still exempts, whose two spellings are `url(#742)` (where exempting is the right answer) and CSS Color 5's Safari-only `contrast-color(#333)`.

**A red met while working turned out to be a real defect rather than contention, and rule 6 made it in scope.** `scheduler/dispatch/005` failed **4 of 5** full-tier runs. The test waits for both worktree **directories** and then immediately snapshots `count_orchestrators` — but the worktree and the orchestrator agent are two steps of one dispatch, and a loaded box reads the registry between them. Every sibling in that file already pairs `wait_for_path` with `wait_for_agent_where`; this one was the exception. **Zero failures in six runs since.** That it predated this branch was checked rather than assumed: the tree was restored to `eaceff37` and the tier run three times green there.

Worth recording beside it, because it is the backdrop for every timing claim in this PRD: **the box ran at load average 76–91 on 16 cores for the whole session**, roughly 5x oversubscribed. Under that, different tests redden on each run and pass in isolation — the contention rule 5 documents and #415 measured. Those were not chased and not quarantined. `dispatch_005` was treated differently because it failed *reproducibly* and had a real unwaited condition behind it, which is the distinction rule 6 asks for.

One smaller fix generalised further than asked: the panic-safe cwd restore was mirrored onto `platform::fsperm::with_socket_umask`, which had the same restore-on-success-only shape. Its exposure was narrower — one `bind(2)` call — but the failure mode was process-wide and silent, and the file's own comment already records four sibling tests dying with `EACCES` when that mask stuck. The agent also caught a sentence it nearly shipped: "the only test in the tree that moves the process cwd" is false, and `CwdGuard` already existed in three places.

### 2026-09-14 — M12 and M13: `main` fixed the same bug underneath us, and a reviewer found what Open Question 3 left open

`0e9070b4`, `142f22c3`, then [PR #1076](https://github.com/vfarcic/dot-agent-deck/pull/1076). Rust **3031**, vitest **495**, Playwright **100**, all 13 required checks green.

**`main` fixed #1046 independently while this branch was fixing it.** `1d4c58b4` landed on 2026-09-14 taking the opposite approach — a `clearError` on `useDeckRuntime` — where M6 had minted an error id and suppressed per failure. They collided in `App.tsx`, `useDeckRuntime.ts` and `types.ts`, and the collision also explains why **no CI had run on the PR at all**: GitHub executes `pull_request`-triggered workflows against `refs/pull/<n>/merge`, which it cannot compute for a conflicting PR, so only the `pull_request_target` labeler had fired.

**The resolution took `main`'s side, and the deciding argument was not the obvious one.** Simplicity and F5 both favoured `clearError`, but the finding that settled it is that **this branch's model was worse on a case `main` had already fixed**: one press cleared the notice *and* suppressed whatever error sat behind it, discarding an error that said something different from the notice, unread. That is the defect Greptile filed against `main`'s own #1046 PR. M6's original objection to a clearing setter — that a re-assertion would put the toast back — was re-checked against the merged tree and still does not apply: `reportFailure` has three call sites and every `reconnect()` caller is a click handler, so nothing re-asserts on its own.

**Greptile's P1 found the gap this document had written down and never closed.** `observed_endpoints` built the observed set with `.filter_map(|deck| deck.endpoint())`, dropping any row with no socket, and the overview's denominator is that same set — so three configured decks with one socketless row rendered **`2/2`**, the third absent entirely. That contradicts Success Criterion 4, and Open Question 3 had recorded the exact cost ("a half-configured deck is *absent* from the fleet rather than present-and-unconfigured") as "an M4 screen question rather than a settled one". M4 never settled it. **The code was already confessing to the defect in its own doc comment**, which is worth remembering next time a known-cost note is left unresolved: a reviewer read the confession before a user hit the symptom.

The fix separates two questions one list was answering: `connectable_endpoints` (unchanged behaviour, honest name) drives tunnels, watchers and handshakes, while `unconfigured_decks` rides beside it for display only. Two constraints shaped it that the brief had not anticipated — a socketless row **cannot be an `Endpoint` at all**, so it has no `EndpointIdentity` and is keyed by its own `EndpointId` under an `unconfigured-` prefix; and it **cannot reach the screen by being listed in `fleet` alone**, because with no watcher it emits no snapshot and `pruneFleet` refuses to invent an entry for an id it has heard nothing about, so the fields ride on `DesktopSnapshotDto.unconfigured` instead. One of the five new tests guards the **over-reach** rather than the defect: having made the deck visible, the tempting next move is to make it observable, and `a_deck_with_no_socket_gets_no_watcher_and_no_tunnel` refuses it.

**The approval carries a disclosed coverage caveat, and it is recorded here rather than treated as total.** `dot-agent-deck-reviewer` approved at `142f22c3`, and said it did **not** deep-read the frontend TS/TSX files, `tests/CATALOG.md`, or `xtask/linkage-check/src/desktop_palette.rs` for time and budget reasons. A large share of this PRD lives in exactly those TS/TSX files, so the approval is narrower than its headline. The second reviewer pass (M11) did read the frontend, and the browser tier covers it at 100 tests in two engines — but nobody should read "approved" as "every file was read".

### Two operational failures worth keeping, because neither was about the code

**A role backed by an out-of-credit provider fails silently and looks identical to a slow agent.** The auditor and tester ran on OpenAI models with no credit: each accepted a delegation, spawned a fresh agent, and produced nothing — no report, no `work-done`, no error, just an `Idle` pane. Two delegations and about an hour were lost before the pattern was recognised, and what identified it was that the reviewer role (Anthropic-backed) succeeded on a brief the auditor had failed twice. Swapping both to Claude fixed it immediately, and [#1079](https://github.com/vfarcic/dot-agent-deck/issues/1079) tracks restoring multi-provider coverage so the workaround does not become permanent by inattention.

**A role with `clear = false` can lose a task pointer in a way a respawning role cannot.** The release role's delegation is delivered as a message into a live session rather than spawning a fresh agent — its agent id stayed `22` for the whole run while every other role's advanced. Its second task pointer reached the pane and sat unsubmitted at the input; the daemon's idle-worker detector caught it after 30 seconds, and re-delegating the identical file started it immediately. A respawn cannot fail to start; a message into a live session can.

*(Process note, recorded because it cost an hour and will recur: the second pass was briefed to the **auditor** role twice and produced nothing both times — the delegation was accepted, a fresh agent spawned, and the pane went `Idle` with no report file and no `work-done` signal. It was rerouted to the reviewer and completed normally. Nothing about the task was auditor-specific; the same brief succeeded verbatim elsewhere.)*

**Four details in the inherited hand-off had drifted and were re-verified at `4a7ae532` before planning:** `DesktopState` is at `desktop/src-tauri/src/terminal.rs:74` rather than in `lib.rs` (one `.manage()` at `lib.rs:1725`); the two maps are keyed by `EndpointIdentity` (`src/daemon_client.rs:200`) rather than by `Endpoint::describe()`, which is the safer key and was changed under review on PR #1035 for exactly this PRD's case; `PROTOCOL_VERSION` is 9 rather than 7; and "one variant plus one arm in exactly three places" is the TypeScript half only, the Rust `Selection` having roughly a dozen occurrences across three files — of which, the recon then established, only two are production matches needing a decision.

**The reconnaissance corrected the central framing**, which is recorded at the top of this document rather than buried here: the `DesktopState`-becomes-a-keyed-map retrofit that both #742 and #741 name is already done, and the actual single-deck hardwiring is a process-global at `dto.rs:846`. It also found the whole-map mutex that contradicts this PRD's headline requirement and that no prior document mentions.

**The second pass found a fourth local-only gate** — the filesystem trust check at `daemon_bridge.rs:881`, which for a remote deck does not refuse but silently substitutes ssh authentication for the inode predicate. #741's Risks section had explicitly warned that its list of three was "the one that was found" rather than the complete set, and that warning is now discharged. It also confirmed the rule-12 answer with evidence (`PROTOCOL_VERSION` stays 9), established that **nothing bounds the endpoint count**, and established that the first resource to saturate is the aggregate emit rate into the webview at N × 6.667/s rather than bandwidth or connection count — which is the opposite of what the #741 baseline's headline numbers suggest.

**Four decisions taken by the user at planning time**, before any implementation was delegated: the overview merges while the deck screen stays single-deck; the scope is observe-only with the control half as a named follow-up; the two-deck verification run is in scope; and no `experimental` flag.

### 2026-09-14 — M7a: the review and audit findings, and three of them wanting one mechanism

Rust 3011 (from 3002), vitest 487 (from 483), Playwright 100 (unchanged). A reviewer and an auditor went over the whole branch independently and between them found one regression this branch introduced, two invariant gaps, one inert gate bypass, one pre-existing cross-deck attribution, and four claims that were false as written. **The numbering is a collision worth naming rather than tidying:** the orchestration called this milestone M8, while this document's M8 is the two-deck run and is referred to by that number in the M3 entry below and in Verification. The table row is `M7a` so both references stay correct.

**Three of the findings were one defect, which is the interesting part.** The auditor said so explicitly — it reported the torn selection "because it is a real invariant gap in the value [the lock finding's] remedy wants to key on" — and the third arrived independently from the reviewer. All three are the same shape: **a claim staked before an await and honoured after it, with no way to tell that the world moved in between.** `EndpointTunnels::acquire` publishes a lease after a 30-second `ssh` spawn; `DaemonLinks::trusted` publishes a link after a handshake; `register_watcher` stores a handle into a slot claimed before a spawn. So the mechanism was designed once — `desktop/src-tauri/src/generation.rs`, a monotonic counter — and the three fixes use it rather than each growing a counter of its own. The counter answers two questions with one property: *did anything move* (compare it) and *is this still mine* (every bump mints a value no other bump mints).

**The regression: splitting the whole-map mutex at M3 gave up a property nobody replaced.** That lock did two jobs — collapsing concurrent first-uses of one deck, *and*, being held across establishment, excluding teardown. M3 replaced the first. `retain` and `invalidate_all` take only the map lock, and a deck mid-establishment has no entry in that map, so a teardown that lands in the window removes nothing for it and the establishment republishes it behind them. The concrete failure the auditor traced: a refresh is inside `acquire(D)` while the user picks a different deck, `retarget_selection` invalidates and retains, and `D`'s `ssh -N -L` child, stderr thread, forwarded socket and held daemon connection survive to a host the user believes they have disconnected from — until the next settings save. Bounded and eventually reaped, so a leak rather than a takeover; worth closing because *"I removed that deck" stops meaning "that ssh session is gone"*, and because the branch that broke it is the one that should fix it.

**Refusing to publish is the right fallback, not an error.** The lease or link is still returned; the caller's own `Arc` is then the last one, so the child dies with the request instead of outliving it. **One counter serves both maps**, since they describe one live set and `retarget_selection` tears both down in consecutive statements. What that buys was stated narrowly rather than generally: `trusted`'s compare is *exact* against `invalidate_all` (both under the links lock) and conservative against the tunnel map's own teardowns — which it need not be exact against, because a link outliving a tunnel `release` is the designed behaviour that `TrustedDaemon`'s held lease exists for.

**The cheaper-looking alternative was refused and the cost of refusing it is stated.** Testing `dto::deck_is_observed` at the publish would be precise, and would make the check depend on the applied selection having been written before the teardown ran — an ordering invariant *between two modules*, which nothing would fail if it broke. The counter is conservative instead: a settings save that keeps the deck being established still costs that establishment its cache entry, and the next call re-establishes. One re-establishment on a rare overlap, against a cross-module invariant nobody checks.

**The torn selection reproduces, which neither of the two people who found it believed it would.** `SELECTED_DECK` and `OBSERVED_DECKS` were two `RwLock`s written in sequence under a comment saying they "can never describe different saves" — true of saves, false of reads. Both reporters rated the window unreachable in practice (two adjacent lock acquisitions, no `await` between) and it was reported only because the lock fix keys on the value. Run against a faithful reconstruction of that shape, the regression test failed **6 times out of 6**, each time on exactly the torn pair described: the one-deck document's selected deck at the head of the three-deck document's observed list, a fleet naming a save that never existed. A user-paced settings save is far rarer than a writer loop, so that bounds nothing about production frequency — what it settles is that "these two statements never interleave" was an assumption rather than a property. They are now one `AppliedSelection` under one lock, with `selected_deck`, `observed_decks`, `deck_is_observed` and `observed_fleet` all deriving from a single read.

**Both reviewers found the write-gate bypass independently**, which is the signal that made it worth fixing rather than documenting. `EndpointsPanel`'s Test-connection write-back called `onSave` directly — because it writes against `latest.current` rather than the document its handler closed over, and `saveSection` closed over that document — while three separate comments said every write in the panel goes through `endpointSectionToSave`. It is inert, and **there is no input that distinguishes the two implementations**, which is precisely how three comments came to assert a coverage nothing checked. `saveSection` now takes the document to write into, so both kinds of write use one gate; the test asserts the call rather than the output, and says so.

**One finding was pre-existing and in scope anyway.** The evidence ring and the handoff rail were process-global, handed to whichever deck was currently selected and never cleared on a switch, so `build-box`'s first snapshot arrived carrying every hook event recorded while local was selected — one machine's agent ids, roles and pane ids under another machine's name. Verified pre-existing at `4a7ae532`. It is in scope because `isSelectedDeckEvent` — the filter M3/M5 added to stop exactly this — stops *live* events crossing and does nothing about a ring that survives the switch, so a reader who sees the filter reasonably concludes the drawer is deck-clean. **Shipping a partial guarantee that reads as a total one is worse than shipping neither.** Keyed per deck rather than cleared, and without a parallel map: each `DeckSnapshot` already carries its own `evidence`/`handoffs`, so the bridge's field is a working copy of the selected deck's and `adoptEvidenceDeck` swaps it on a selection change. Switching away and back therefore restores that deck's drawer rather than emptying it.

**The four false claims, under rule 17.** The `wire_id` stability argument justified a hand-rolled FNV with *"the webview keys `localStorage`-backed UI state on it"* — in `src/daemon_client.rs`, in `dto.rs`, in `docs/develop/desktop-gui.md` and in this document. Nothing does: every `localStorage` key the webview writes is scoped by runtime *mode*, and none is per deck (re-enumerated rather than taken on the auditor's word). The stable hash is still right, for the forward-looking reason. `EndpointIdentity`'s doc listed three things a caller may do with one and `wire_id()` had been a fourth since M5. And `endpoint_tunnels.rs` said the map is "keyed by `Endpoint::describe`", stale since PR #1035 and directly misleading about the one property per-deck isolation rests on, four lines above the gate map.

**The toast residual was cheap enough to take, and M12 then replaced the mechanism with `main`'s.** Dismissal was keyed on the message text, so two distinct failures sanitising to the same sentence were both suppressed — likelier with a fleet, since `safe_message`'s output for a transport failure is largely deck-independent. M8's answer was an id minted per reported failure, which `App` held instead of the sentence. That is **no longer what the code does**: [#1046](https://github.com/vfarcic/dot-agent-deck/issues/1046) was fixed on `main` in `1d4c58b4` while this branch was fixing it here, and M12's merge kept `main`'s `clearError` and dropped the id. Clearing answers the same question with less — an emptied error is per-occurrence by construction, so identity never has to be decided — and it carries a property the id model got wrong: an error saying something *different* from the notice over it survives the click rather than being discarded unread. The F5 property itself is unchanged and still pinned by a test.

**What the tests do and do not prove, since three of these are concurrency fixes.** The `DaemonLinks` half of the lock finding is reached **end to end** — a scripted daemon takes the request frame, the test invalidates while the handshake is genuinely out, the daemon then answers, and the establishment must hand its caller a `Connected` link without holding it; both edges are `oneshot` channels, so there are no sleeps and no timing assumptions. `EndpointTunnels::acquire` is pinned at its publish seam instead, for the same reason M3 recorded: stalling it deterministically means stalling an `ssh` spawn. The watcher claim is pinned at the registry, and abort is *observed* rather than assumed — each parked task owns a `Sender` it never sends on, so the receiver resolves exactly when the task's future is dropped. Every fix was verified RED by reverting it in place. The three that are genuinely unreachable-by-inspection say so in their own doc comments rather than in this entry alone.
