# PRD #802: Voice control for the desktop app

**Status**: M1–M8 and M10 shipped; M9 (the credentialed phrase fixtures) is the last milestone in flight, and the PR follows it. This document was written first and is what implementation was delegated against — where a measurement contradicted it, the document was corrected rather than the measurement explained away, and the [one-file claim](#success-criteria) is the worked example.
**Priority**: Medium (its hard dependency, [PRD #803](803-desktop-settings-surface.md), has shipped; nothing is blocked on this)
**Created**: 2026-09-18

## Problem Statement

The desktop app ([#176](https://github.com/vfarcic/dot-agent-deck/issues/176)) currently mirrors the TUI, and for a terminal user that is strictly worse than the TUI: it costs a window and returns nothing. The test a second front-end has to pass is whether it does something the first **structurally cannot**, and two things do.

[#742](https://github.com/vfarcic/dot-agent-deck/issues/742) (fleet view) is one: a TUI is one terminal attached to one daemon — `Endpoint` (`src/daemon_client.rs:68`) is a single `Local`/`Remote` choice and not a map — while the desktop already holds many at once, one `TrustedDaemon` per deck in `links: AsyncMutex<HashMap<EndpointIdentity, Arc<TrustedDaemon>>>` (`desktop/src-tauri/src/daemon_bridge.rs:284`). #742's own body calls this "the strongest case for a GUI at all".

**Voice is the other.** It needs a microphone, OS integration and a persistent local process — awkward through `ssh -t`, natural in a native app.

Together they give the desktop app a job of its own, and it is not the TUI's job:

| | job |
| --- | --- |
| **TUI** | the **working** surface — one repo, dense, keyboard-driven, `ssh`-able; you are *in* it with an agent |
| **Desktop** | the **supervisory** surface — many daemons, attention routing, cost, voice; you are deciding *where to look* |

Voice is the input method that fits supervision, because a supervisor is not typing.

The second problem this solves is smaller and more immediate: **the user should never have to guess the phrasing.** "Get rid of everything except the tester", "show me the one that's stuck" and "zoom the tester" have to resolve the same way, which a keyword grammar cannot do and a model mapping an utterance against live state can.

## Solution Overview

Six steps, and the fifth is the one that keeps this from becoming a second control surface.

1. **Capture** — a microphone, or typed text into the same box.
2. **Transcribe** — speech to text, producing a plain string.
3. **Resolve intent** — one model call carrying the command table, the live state the app already has, and the transcript.
4. **Validate** — the app checks the returned action exists, is callable on the current screen, and that its parameters resolve against live state.
5. **Execute** — hand the resolved action to the **existing** handler. Voice gets no execution path of its own; it produces an action and dispatches it where a click dispatches one.
6. **Report** — say what happened, showing what was heard when nothing matched.

**The command table is the whole design**, and its acceptance criterion is stated as a test rather than an aspiration: **after the plumbing exists, adding a command changes no implementation — one row and one classification flip — plus the test assertions that pin the shipped row set by value.** Implement the feature as normal, then add a row. A row **does not define behaviour — it exposes behaviour that already exists**, so the table is a reference and not a second implementation.

**That sentence read “a ONE-FILE change” until M8 measured it, and M8 took seven files.** The claim was wrong in its count and right in its substance, which is why it now states the substance and carries no number. [M8](#milestones) has the file-by-file accounting; the short version is that five of the seven are test-only and every Rust hunk is inside a `mod tests`, because nine unit tests pin the shipped row set **by value** — a deliberate choice `table.rs` documents, whose cost is one edit per place the row set is written down. The sixth is the registry classification flip the row exists to make, and the seventh is a guard test that named the entry which had just started speaking. What did **not** happen is the thing the criterion is actually about: no production logic changed, and nothing had to be plumbed.

Three consumers read that one file, which is what keeps the table one file rather than three copies of it: **schema generation** (the model's tool definition), **validation** (`screens` and each param `kind`), and **dispatch** (`invoke` names an existing action). A guard in `cargo test-fast` keeps the stringly-typed references honest, because a typo in `invoke` would otherwise surface at runtime as *nothing happening*.

**What this PR ships is the spine and one activation mode, with both model stages behind seams and no bundled native engine.** The reasoning is in [Staging](#staging-what-this-pr-ships-and-why-the-issues-m1-was-split) and it is a deliberate departure from the issue's M1, recorded rather than implied.

## Scope

### In Scope

- **The command table**, as one TOML file embedded with `include_str!`, plus its three consumers: generated schema, validation, dispatch resolution.
- **A single frontend action registry** that the rail, the command palette and the voice dispatcher all dispatch through, so step 5 above is literally true and the guard has something enumerable to check `invoke` against.
- **The pipeline end to end** — capture, transcribe, resolve, validate, execute, report — with `Transcriber` and `IntentResolver` as seams chosen in the #803 settings.
- **An intent backend that works with no API key and no download**, using the pre-authenticated agent CLI the user already has on the box, plus a keyed remote backend as the quality upgrade.
- **The `[voice]` settings section** and the `SecretStore` that #803 M5 named and deliberately did not build, because this PRD is the first consumer that needs one.
- **One activation mode** — press once to start, press once to stop.
- **Navigation-only commands**, so the first slice cannot misfire expensively, with at least three rows plus a deliberate no-match so disambiguation and the "none of these" path are both exercised.
- **The structural guard**, as a `linkage-check` rule: `invoke` names a registered action, every `screens` entry is a known screen, every param `kind` is in the closed set, and every capability in the registry has either a table row or an explicit `no_voice` marker.
- **The phrase fixtures**, in the credentialed lane, opt-in and skipped when no backend is configured.
- **The cost-of-a-row proof** — a milestone whose whole content is adding the next command and **measuring what it costs**, rather than asserting the cost in advance. It ran, and the measurement is in [Milestones](#milestones): seven files, no implementation among them.

### Out of Scope

- **Any bundled native inference engine.** No `whisper-rs`, no `llama-cpp-2`, no model files. See [Dependency cost](#dependency-cost-and-the-one-native-dependency-this-pr-does-take) for why this is a staging decision about `desktop/src-tauri` being a workspace member rather than a change of direction.
- **Model download management** — progress, retry, disk-usage visibility, removal. With no bundled engine in this PR, nothing downloads; this arrives with the local engines that need it.
- **The remaining two activation modes**, hold-to-talk and always-on with voice-activity detection.
- **Destructive commands.** Approve/deny a permission prompt, close an agent's **terminal pane**, stop an agent — none of them gets a row here; they are D5, behind a confirmation. **"Close a pane" is one word away in speech from something that IS in scope, so name both operations rather than leaving the next reader to infer the split.** Closing an agent's *terminal pane* destroys a PTY and stops work: lifecycle, destructive, deferred. Dismissing the agent *view* — the screen overlaying the deck — is `closeAgent` in `desktop/src/App.tsx`, which is `setView({ kind: from })` and nothing else: it destroys nothing and stops nothing, so it is navigation in exactly the sense the other three rows are, and it ships here as `close_agent_view`. The hazard is real because [#1105](https://github.com/vfarcic/dot-agent-deck/issues/1105) calls the overlay "the agent pane", so the same word points at both sides of this line. Two things follow from keeping the row beyond the classification: without it the `agent` screen has **no callable command at all**, which is a functional hole rather than a coverage one, and the `callable: true` path on that screen would be exercised by nothing. Its `description` says in as many words that it does not stop the agent and does not close its terminal pane, and that sentence is doing real work on the model.
- **Modal dictation** ("type to the tester" until "finished with that agent").
- **Discovery** — contextual examples rendered from the table.
- **Chaining.** The table knows a command's prerequisite, so the app can *offer* the next step; performing that chain automatically is deliberately absent, because a single utterance silently performing two operations is where a misfire gets expensive.
- **An audio-native single-hop backend** doing transcription and intent together. Cloud-only and streams the microphone continuously; an option later, never the default.
- **Siri and any OS-native speech API.** The issue's reasoning is unchanged and is restated in [Transcription](#transcription-and-the-one-place-there-is-no-no-key-trick).
- **The TUI↔desktop parity matrix.** Floated in the issue under "Explicitly rejected" as the enforceable alternative to an unenforceable rule; a real piece of work of its own, deferred with a reason rather than dropped.
- **Any daemon protocol change.** See [Cross-version safety](#cross-version-safety).
- **A driver-level test tier for the desktop app.** None exists ([#953](https://github.com/vfarcic/dot-agent-deck/issues/953)) and this PRD does not build one.

## Technical Approach

### What was measured, and where

Everything in this section was read or run against this branch at **`d7bbbd39`** on **2026-09-18**, on Linux. Where a claim could not be verified it says so, which is the point of saying where the rest came from. Two claims were measured by running something rather than by reading it — the webview capture matrix and the agent-CLI intent backend — and both name their method inline.

### Staging: what this PR ships, and why the issue's M1 was split

The issue's M1 is one milestone containing capture, whisper STT, the table, the three consumers, validation, dispatch, the guard, the fixtures and one activation mode. Two things in it carry a cost this repo feels immediately, and the split is about those two and nothing else.

**`desktop/src-tauri` is a workspace member** (`Cargo.toml`'s `[workspace] members`), so anything it depends on is built by `cargo test-fast --workspace` — the per-task gate every contributor runs — and by all three required build jobs (`build`, `build-windows`, `build-macos`). Bundling whisper.cpp via `whisper-rs` or llama.cpp via `llama-cpp-2` therefore lands a cross-platform C/C++ build toolchain in that gate, on a matrix that includes Windows. That is a repo-wide change to what it costs to run the tests, and it is not a change this feature has to make in order to exist.

So: **the two engines sit behind seams — a `Transcriber` and an `IntentResolver`, chosen in the #803 settings — and no bundled native engine ships in this PR.** Local whisper and a local grammar-constrained intent model become their own deferred milestones, and the seams exist precisely so they drop in without touching the spine. The issue's direction is not being overturned; its *first* milestone is being made deliverable.

**For intent resolution there is a default that needs no key and no download, and it is this product's own asset rather than a trick.** The user already has a pre-authenticated agent CLI on the box — `claude`, `opencode` — which the daemon spawns routinely. A non-interactive print-mode call to that CLI is a candidate default intent backend. [It was measured](#intent-resolution-the-no-key-default-measured) and it works, with a latency and a cost that have to be stated rather than discovered.

**Transcription has no equivalent trick, and this PRD says so plainly.** Every no-download transcription option needs a credential. So **when no transcription backend is configured the surface still works, from typed input, through the identical resolve → validate → execute → report path**, and the settings panel says what to add. The spine is exercisable, nothing is presented as broken, and local whisper is the milestone that removes the requirement rather than a gap being hidden.

### The command table

One file, `desktop/src-tauri/src/voice/commands.toml`, embedded with `include_str!` so it ships inside the binary and cannot drift from the build that compiled it. It lives in the Rust crate because two of its three consumers are Rust-side and the third reaches it through an existing IPC reply rather than through a second copy.

```toml
[[commands]]
id          = "open_agent"
description = """Open one agent's pane over the current screen. Use when the user asks
to focus on, enlarge, zoom, or see only one agent."""
invoke      = "openAgent"               # an entry in VOICE_ACTIONS
screens     = ["deck", "overview"]      # absent or empty = available everywhere
unavailable_hint = "opening an agent works from the deck or the overview"
report      = "Opening {agent}."        # the sentence shown on a SUCCESSFUL dispatch

  [[commands.params]]
  name = "agent"
  kind = "agent_ref"                    # one of a small closed set of resolver kinds
```

**The `description` column is a prompt, not documentation.** The model picks on it, so it is written for a model and reviewed as an interface.

**The `report` column is the success sentence, and without it the "the app renders every sentence" property is lost.** Every *refusal* has somewhere to get its wording — `unavailable_hint` for the not-here case, fixed prose in `outcome.rs` for the rest — and success had nowhere, so the surface would have had to invent success wording in the frontend. A `{param}` placeholder is replaced with what that param resolved to, and a placeholder naming no declared param is refused at parse time: that is the same stringly-reference discipline `invoke` gets from the guard, applied to the one column carrying a reference inside itself.

**It is called `report`, not `confirmation`, and the name is load-bearing.** [D5](#milestones) is *destructive commands behind confirmation* — an actual confirm-before-acting dialog — so rows carrying a genuine confirmation flag will live in this same file. The two are close to opposite: a report describes a completed action, a confirmation blocks one before it happens. The name also matches the pipeline's own stage 6, **Report**. `commands.toml` carries a one-line comment at the column saying so, because the rename is otherwise easy to undo.

The three consumers:

1. **Schema generation** — `id`, `description` and `params` become the model's tool definition.
2. **Validation** — `screens` and each param `kind` check the model's answer before anything runs.
3. **Dispatch** — `invoke` names the action to run, and the app calls it with the resolved params; `report` is the sentence it shows afterwards.

### Dispatch: the `invoke` column names a FRONTEND action, and the issue's wording is wrong about this

The issue says `invoke` names "an existing `#[tauri::command]`". **The survey falsifies that for exactly the commands the issue's own example uses, and this is the single most consequential correction in this document.**

The Tauri command registry at HEAD is one `tauri::generate_handler!` block, `desktop/src-tauri/src/lib.rs:2174-2188`, holding **thirteen** entries: `desktop_get_snapshot`, `desktop_list_projects`, `desktop_resolve_project`, `desktop_bootstrap`, `desktop_terminal_attach`, `desktop_terminal_write`, `desktop_terminal_resize`, `desktop_terminal_detach`, `desktop_get_settings`, `desktop_set_settings`, `desktop_test_endpoint`, `desktop_set_zoom`, `desktop_run_action`. There are exactly thirteen `#[tauri::command]` attributes under `desktop/src-tauri/src/`, all in `lib.rs`, so the registered set and the defined set agree today — but that is an observation about today, not a property, and the guard must read the registration block rather than the attributes for that reason.

**None of those thirteen performs a navigation**, and that was checked variant by variant rather than by reading their names: the widest of them, `desktop_run_action`, takes a `DesktopAction` (`desktop/src-tauri/src/dto.rs:500-565`, twelve variants) whose variants are `Refresh`, `Bootstrap`, `StartAgent`, `StartWorkflow`, `StopAgent`, `StopDaemon`, `RestartDaemon`, `AllowBuildMismatch`, `RenameAgent`, `AttachTerminal`, `DetachTerminal` and `SubmitText` — every one a daemon, agent-lifecycle or terminal operation, and not one a screen change. Navigating to the overview is `onNavigate?.({ kind: "overview" })` (`desktop/src/App.tsx:1063`); opening an agent's pane is a `DeckView` variant set in React state (`desktop/src/types.ts:321`, switched at `desktop/src/App.tsx:357-375`); opening a settings or prompts sheet is a `useState` boolean. `desktop_set_zoom` is the *webview's* zoom level, not "zoom an agent's pane". So a table whose `invoke` column named a Tauri command could expose the daemon-facing verbs and could not expose a single one of the navigation commands this PR is scoped to.

**The resolution, and it is the issue's own step 5 taken literally.** Voice "produces an action and dispatches it where a click dispatches one" — and a click is dispatched in the frontend. So `invoke` names an entry in **one frontend action registry**, `desktop/src/lib/voiceActions.ts`, exporting `VOICE_ACTIONS` as an object literal keyed by action id. A registry entry whose behaviour is Rust-side is a one-line `invoke("desktop_…")` call inside it, so the registry stays the single dispatch seam rather than becoming a second one beside the Tauri registry.

**How a guard enumerates each side.** Both are text scans, which is the established idiom here rather than a compromise: `xtask/linkage-check/src/desktop_settings_secrets.rs` already scans `desktop/src/lib/bridge.ts` for a declared interface's fields and for `localStorage` key expressions, from Rust, in a required gate.

- **The Tauri registry** is enumerable by a text scan of the `generate_handler!` block: find `.invoke_handler(tauri::generate_handler![`, read to the matching `])`, split on commas, trim. One identifier per line today. The `#[tauri::command]` macro does generate a `__cmd__<name>` item, but nothing in the source spells that out, so it is not what a scan reads — and reading the attributes instead would accept a command that is defined and *not registered*, which is precisely the silent failure the guard exists to catch. A scan of the registration site is sufficient **and** is the right one.
- **The frontend registry** is enumerable the same way: the keys of the `VOICE_ACTIONS` object literal. This is only sound while the registry is a literal with statically-readable keys, so the guard also refuses a computed key or a spread inside it — the same refusal `desktop_settings_secrets.rs` applies to a spread in `normalizeDesktopSettings`, and for the same reason.

### One tool with an `action` enum, and the full list every time

With a tool-per-command schema, *listing* a tool means the model may call it, so a per-entry availability flag is advisory at best. **A single tool taking `action` as an enum** keeps the output constrained — the model cannot invent an action — while letting every entry carry an availability flag.

Every command is sent on every request, each annotated `callable: true|false` computed from `screens` against the current screen, rather than the list being filtered. Filtering produces a bad failure: asking to open an agent from a screen where that is impossible yields "I don't know how to do that", which is false. The truthful answer names the prerequisite, and while people are learning the app that case is more common than a genuinely unsupported request. One round trip, including on the failure path, which is where a second one hurts most.

**The trade, stated because it is easy to forget:** filtering *prevents* wrong calls; flagging *explains* them. Flagging relies entirely on validation to refuse an action the model called anyway. That is acceptable because validation exists regardless — but the flag improves the message, not the misfire rate.

### The model returns a situation; the app renders the sentence

The model returns structure — `{"action": "open_agent", "callable": false}` — and the app renders the user-facing sentence from the table. Three reasons: wording stays consistent instead of varying per utterance; a fixture test can assert an action id, whereas asserting on free-form prose is miserable; and the model cannot invent a plausible-sounding but wrong reason for why something is unavailable, because `screens` already knows.

**The model must be able to answer "none of these".** Forcing a pick from a closed set turns "what time is it?" into a command. That one affordance is most of the safety, and [it was measured to work](#intent-resolution-the-no-key-default-measured).

**A failure must say what it heard** — "Heard: *'go beck'* — no matching action." Most failures are transcription rather than intent, and showing the transcript turns a dead end into a correction.

Concretely, the Rust side returns one closed-set outcome per utterance, each carrying its own rendered sentence. The frontend dispatches on the first and renders the sentence the outcome carries. Rust renders the sentences because Rust holds the table; the frontend renders no voice prose of its own.

**There are nine of them, not four, and the four this document first listed cannot express the other five.** M1 found that out by building them: a missing param, a param that resolves to nothing, a param that resolves to more than one thing, a backend that could not answer at all, and a backend that named an action outside the table are five genuinely distinct situations, and each of the first four needs a different sentence from the user's point of view. Collapsing them into *no matching action* would tell a user "I don't know how to do that" when the truth is "I could not tell which agent you meant" or "nothing configured yet".

| outcome | what happened | carries |
| --- | --- | --- |
| **Dispatch** | run this action with these params — the one outcome that asks for anything to run | transcript, action, `invoke`, resolved params, the row's `report` |
| **Unavailable** | the action exists and this screen cannot run it | transcript, action, the row's `unavailable_hint` |
| **NoMatch** | the model answered "none of these" | transcript |
| **UnknownAction** | the model named an action that is not in the table | transcript, the action it named |
| **ParamMissing** | the action declares a param and the model supplied none | transcript, action, param |
| **ParamUnresolved** | a param was supplied and nothing in live state matches it | transcript, action, param, what was spoken |
| **ParamAmbiguous** | a param was supplied and more than one thing matches it | transcript, action, param, what was spoken, the matches |
| **ResolutionFailed** | the intent backend could not answer — unconfigured, timed out, unparseable | transcript, detail |
| **TranscriptionFailed** | speech could not be turned into text; upstream of everything else, so the one outcome with no transcript | detail |

**`UnknownAction` and `NoMatch` deliberately render the SAME sentence while staying separate variants, and that is not a redundancy to simplify away.** The *cause* differs and the *effect* does not: a model that invents an action id is a backend not honouring the enum — impossible under grammar-constrained decoding, merely unlikely under the print-mode agent CLI — while a no-match is the escape working exactly as designed. From where the user is standing both mean the app did not know how to do what they asked, so both say so. Keeping them apart is what lets a fixture, a log or a future metric tell a misbehaving backend from a genuine no-match; merging them would throw that away to save one variant.

### Screens: the smallest honest closed set is three

"The current screen" has a precise meaning in this app and it is narrower than it sounds. `DeckView` (`desktop/src/types.ts:297-321`) is a discriminated union with exactly three variants — `{ kind: "deck" }`, `{ kind: "overview" }` and `{ kind: "agent"; deckId; agentId; from }` — and `DeckShell` switches on it at `desktop/src/App.tsx:357-375`, over a `base` derived at `:182`. There is no router library and none is wanted (`types.ts:291-296` says so).

**So the closed set for the `screens` column is `deck`, `overview`, `agent`, and the guard checks an entry against the `kind` literals of that union in `types.ts`.** A text scan again, and it keeps the table and the app's own type in step without a second list.

**What that set deliberately cannot express, and this is worth knowing before writing a row.** Five of the rail's seven buttons toggle overlay booleans inside `ControlDeck` (`desktop/src/App.tsx:1060-1067`) rather than changing the view, and #803's Settings sheet is one of them. Those booleans are not in `DeckView`, so *"an overlay is open"* is not part of the closed set and a command's availability cannot depend on it without first moving that state somewhere a screen identifier can see. No row in this PR needs that. A row that does need it is a change to where the state lives, and it should be recognised as that rather than worked around in the table.

### Capabilities: the mechanical definition, and exactly what it costs

The guard is required to assert that every user-facing desktop capability has **either** a table row **or** an explicit `no_voice` marker. That assertion is only implementable if "user-facing desktop capability" has a mechanical definition, and the honest answer has two halves.

**The definition this PRD adopts: a capability is an entry in `VOICE_ACTIONS`** — the same single registry `invoke` dispatches through. Each entry carries `id`, a human label, and either a table row somewhere or `no_voice: "<reason>"`. The guard then asserts a total function from registry to classification, which is a real assertion: it cannot be satisfied by forgetting, only by writing a reason.

**What makes it more than an honour system is that the registry is load-bearing at runtime.** It is not a list beside the app; it is what the rail buttons and the command palette dispatch through, so an entry cannot be deleted without breaking a control, and a control reachable through the palette cannot exist without an entry. That is the property that earns the definition, and it is why **M2 moves the palette's existing `commandItems` array (`desktop/src/App.tsx:1043-1053`, seven static entries plus per-agent and fixture-mode ones) into the registry.** Without that move the registry is a parallel list with no consumer, which is the checked-in-list failure under a better name.

**And here is the cost, stated plainly rather than implied: the guard cannot discover a capability on its own.** A control wired with a bare `onClick` that does not route through the registry is invisible to it. Measured at `d7bbbd39`: **80 `onClick=` sites in non-test `.tsx` under `desktop/src`**, against 7 static palette entries and 7 rail buttons. Most of the ones sampled while writing this are chrome — dismiss a toast, close a sheet, tick a column — but all 80 were not classified, and the guard could not tell a capability from a close button anyway, so this document does not claim the residue is harmless. So the claim the guard supports is narrower than the issue's wording and is the one to write into the code: **every entry in the action registry is classified, and the registry is the app's own dispatch path for rail and palette controls.** Completeness of the registry against the whole rendered surface is not mechanically discoverable, and the one mechanical approximation — pinning the `onClick` count so a new interactive control forces a classification — is [Open Question 3](#open-questions) with its churn cost named, not something this PRD adopts quietly.

### The guard: where it goes, and what number it takes

`xtask/linkage-check` has two shapes and the difference matters for where this lands.

Most desktop-adjacent rules there are `#[cfg(test)]` modules whose whole content is `#[test]` functions — `desktop_palette`, `desktop_settings_secrets`, `contract_breaks`, `gh_aw_lock_consistency` and a dozen others (`xtask/linkage-check/src/main.rs:100-200`). They run under `cargo test-fast --workspace` (CLAUDE.md rule 5's `--workspace` is what reaches them) and therefore in the CI `build` job, one of the four required checks. The other shape is a **numbered live rule** in the binary's own run; there are twelve, and `desktop_project_boundary` is check 12 — the one desktop module carrying a live rule as well as its own tests. `cargo xtask linkage-check` runs in the same `build` job (`.github/workflows/ci.yml:512`).

**This guard takes both shapes, deliberately, and they are not redundant.**

- The **table-integrity half** — `invoke` names a registry entry, `screens` entries are known screens, param `kind` is in the closed set, every registry entry is classified — becomes **numbered rule 13**, because it is a cross-file consistency rule of exactly the kind the numbered rules are for, its failure output belongs in the per-finding summary, and rule 7's precedent is that a rule CI runs is a rule that gets fixed. The numbers are stable identifiers in the failure output, so this takes 13 rather than renumbering anything.
- A `#[cfg(test)]` module beside it holds the **scanner's own tests**, in the idiom `desktop_settings_secrets.rs` establishes: a test proving the scanner catches a planted bad `invoke`, a planted unknown screen and a planted unclassified registry entry, so "no findings" is a meaningful result rather than a vacuous one. Skipping this is how a guard ends up scanning nothing and passing.

Same budget as its neighbours: reads files, no network, no git, no sleep, no subprocess, and a filesystem error fails the test rather than silently shrinking the scan.

**Why not a typed Rust enum**, which the compiler would check: adding a command would then mean editing the table *and* a variant *and* a match arm — and the last two are **implementation**, which is exactly what the criterion says a new command must not require. M8's measurement sharpens this rather than softening it: a row already costs an edit in five test files, and a typed enum would add production edits on top of them. Reference-plus-guard buys the same safety at commit time, which is the trade `tests/CATALOG.md` already makes.

### The settings section, and the credential rule that binds this PRD hardest

#803 shipped the store and the contract, and it left this PRD one obligation that is easy to miss and impossible to work around.

**The contract itself is three steps** (`docs/develop/desktop-gui.md:1002-1009`): a field or a section struct on `DesktopSettings` in `desktop/src-tauri/src/settings.rs`; one row — `id`, `label`, `icon`, `component` — on `SETTINGS_SECTIONS` in `desktop/src/lib/settingsRegistry.ts` plus your own panel implementing `SettingsPanelProps` from `desktop/src/lib/settingsContract.ts`; and never a secret in the document. The registry already names this PRD as an expected tenant in its own doc comment. Three sections exist today: `appearance`, `decks`, `zoom`. The document is `platform::paths::config_dir().join("desktop.toml")`, overridable by `DOT_AGENT_DECK_DESKTOP_CONFIG`.

Two constraints from that contract apply directly. Field names stay `snake_case` **and single-word where that is natural**, because the same struct is serialised to TOML for the user and to JSON for the webview and every name today is one word; the first genuinely multi-word field is the point at which a separate webview DTO has to be introduced. And a section whose frontend half has not landed is declared `Option<…>`, because `#[serde(default)]` turns "the webview said nothing" into "the webview said empty" and the merge then writes that empty section over the user's data — `endpoints` is the worked example. A `[voice]` section that ships with its panel in the same PR does not need the `Option`, but it does need the frontend to round-trip it from the moment the panel can render it.

**The part that binds hardest: `xtask/linkage-check/src/desktop_settings_secrets.rs` will go RED when this PRD adds a `String`, and it says so in its own module docs** — *"the one that will go red when PRD #802 adds a `String`, which is exactly the moment somebody has to route it through the `SecretStore` seam instead."* That is not a guess about the guard; it is the guard's stated purpose.

Its four checks, read rather than assumed:

1. **Every field in the Rust settings schema has a type from a pinned allowlist**, `ALLOWED_FIELD_TYPES`, currently **fifteen** entries: the scalars `u32`, `SshPort`, `AppearanceMode`, `ZoomLevel`, `Selection`, the five ssh-argument newtypes `Hostname`, `SshUser`, `KeyPath`, `HostAlias`, `RemoteSocketPath`, and `EndpointId`; plus four section structs whose own fields the scan then walks. A field is resolved through `allowlisted_as`, which strips one `Option<…>` or `Vec<…>` layer — so `Option<String>` and `Vec<String>` resolve to `String` and have no way in either. **`String` is not on the list. Neither is `bool`, `PathBuf`, or any map.** A serde attribute that hides a field from the serialised document (`skip`, `flatten`) is refused for the same reason: the sentinel sweep in `settings.rs` walks that document, so a field missing from it is a field nothing follows a value through.
2. **The TypeScript DTO's declared shape is pinned field by field with its types** — `PINNED_TS_FIELDS`, sixteen rows today, over `DesktopSettingsDto` and friends in `desktop/src/lib/bridge.ts`. The Rust check cannot see `bridge.ts` at all, and a frontend-only `apiKey` once got declared there where nothing read it.
3. **`normalizeDesktopSettings` constructs its result and never copies its input** — a spread anywhere in that function is refused, so an extra field arriving over IPC is dropped rather than carried.
4. **The `localStorage` key set is pinned** — `PINNED_STORAGE_KEYS`, five entries, every access must name one of those constants, and `localStorage` may only be spelled `localStorage.<op>(...)` so an alias cannot name a key nothing can read. Its stated scope is `localStorage` in `.ts`/`.tsx` under `desktop/src`; `sessionStorage`, IndexedDB, `document.cookie` and `.js` files are outside it, none of the four appears under `desktop/src` today, and the moment one does it needs its own row.

**So the settings work this PRD owes is concrete:**

- A `[voice]` section whose every field is an allowlisted type. The backend choices, the activation mode and any model identifier are **closed enums** with folding deserializers in the `AppearanceMode` idiom — not free strings — which satisfies the guard by being the right shape rather than by an exemption. The app only supports the backends it ships an adapter for, so a closed enum is also the truthful type.
- Any genuinely new type — including `bool`, if a plain on/off field is wanted — is a **deliberate edit to `ALLOWED_FIELD_TYPES` with a written reason in the entry**, which is exactly the friction that list exists to apply. Do not reach for an exemption; state what values the type can be.
- **Building the `SecretStore`.** #803 M5 named the seam — store, load, delete, keyed by a stable identifier, OS keychain as the intended implementation via `keyring` — and deliberately did not build it, because an unused trait is dead code and #802 would design its shape against a real backend. This is that moment. The document may hold a non-secret reference (which backend holds a key, or whether one is stored) and nothing more. Two things #803 recorded so this PRD would not rediscover them: a Linux box with no Secret Service needs a documented, non-silent failure path, and an owner-only file in the config directory is the viable fallback if one is wanted. "Read it from the environment" is not available to an app launched from Finder.
- **There is no credential storage anywhere in this repo to copy** — no crate here *declares* `keyring`, `secret-service`, `security-framework` or `stronghold`, and what exists is credential *detection* in the e2e harness, which reads and never writes. So `keyring` is a genuinely new dependency in a workspace member, and it belongs in the [dependency accounting](#dependency-cost-and-the-one-native-dependency-this-pr-does-take) with the rest. (**M4 narrowed that sentence**, which said the four were at "zero matches workspace-wide". True of declarations and not of the lockfile: `security-framework 3.7.0` is already in it, reaching the graph on macOS through `reqwest` → `rustls-platform-verifier`. The dependency is still new; the Apple backend is a second consumer of a crate already there.)
- Extending `PINNED_TS_FIELDS` and `normalizeDesktopSettings` in the same commit as the Rust section, because check 2 fails otherwise and that is the check doing its job.

### The CSP, and why every network hop is Rust-side

Verified at `d7bbbd39`. `desktop/src-tauri/tauri.conf.json:26` carries:

```
default-src 'self'; connect-src ipc: http://ipc.localhost; img-src 'self' asset: http://asset.localhost data:; font-src 'self' data:; style-src 'self' 'unsafe-inline'; script-src 'self'
```

`connect-src` names `ipc:` and `http://ipc.localhost` and nothing else, so **the frontend has no way to make a request to a network origin and read the reply** — `fetch`, `XMLHttpRequest`, `WebSocket` and `EventSource` are all governed by `connect-src` and all refused. The consequence for this feature is simplifying rather than awkward: **every network hop — transcription, intent resolution, any future model download — is Rust-side.** #803 already reached the same conclusion for downloads and put it in its Out of Scope for this reason.

The narrower claim is deliberate: *"the frontend cannot make a network call at all"* is how this is usually stated and it is one quantifier too wide. `form-action` has no `default-src` fallback and is not declared here, so a form submission or a top-level navigation is not what `connect-src` refuses. That is not a route to reading a transcription reply, which is why the conclusion above is unaffected — but it is the kind of sentence CLAUDE.md rule 17 exists to catch, and writing the wide version here would put it in a document read as a specification.

`script-src 'self'` has a second consequence worth writing down, because it decides a detail in [capture](#capture-what-each-webview-can-actually-do-measured): a `blob:` URL is not `'self'`, so an `AudioWorklet` module loaded from a blob would be refused in the packaged app. A worklet, if one is used, ships as a real file in the bundle. The `devCsp` beside it adds only `http://localhost:1420` and `ws://localhost:1420` for the dev server, so it changes nothing here.

**Current Tauri surface, because a microphone and any plugin are a capability change rather than a toggle.** `tauri = { version = "2", features = [] }` in `desktop/src-tauri/Cargo.toml` — no features — and the capability set is `"permissions": ["core:default"]` for the `main` window, the only entry in `desktop/src-tauri/capabilities/` (`default.json`). So there is no Tauri plugin enabled at all, and #803 already rejected `tauri-plugin-store` partly on that ground. Resolved crate versions: `tauri 2.11.5`, `wry 0.55.1`, `tao 0.35.3`. `bundle.active` is `false` in the base config and `true` only in the `desktop/src-tauri/tauri.bundle.conf.json` overlay the release job passes — which is also where a macOS `Info.plist` addition would have to live, and where `release.yml` already merges the real version with `jq`.

### Capture: what each webview can actually do, measured

This is the question the issue does not answer and the one most likely to be assumed wrong, so it was measured three ways.

**In Playwright's two engines**, launched from `~/.cache/ms-playwright` on 2026-09-18, against a secure `https` origin fulfilled by a route handler:

| engine | `grantPermissions(["microphone"])` | `getUserMedia({audio:true})` | `MediaRecorder` |
| --- | --- | --- | --- |
| Chromium 153.0.8010.12, no flags | accepted | **threw `NotFoundError: Requested device not found`** | present; `audio/webm`, `audio/webm;codecs=opus`, `audio/mp4` supported, `audio/ogg;codecs=opus` and `audio/wav` not |
| Chromium, `--use-fake-device-for-media-stream --use-fake-ui-for-media-stream` | accepted | **1 track, label `"Fake Default Audio Input"`** | recorded one 2340-byte chunk in 600 ms |
| WebKit 26.6, no flags | accepted | **1 track, label `"Mock audio device 1"`** — a mock device with no flags at all | **absent — `MediaRecorder` is `undefined`** |

Two things follow. **A Playwright spec can fake a microphone** — in Chromium with two launch flags, and in Playwright's WebKit with none. And **a `MediaRecorder`-based capture path cannot be exercised in the WebKit project at all**, which matters far more than the test tier, because WebKit is the engine this app *ships* on.

**The Web Audio route is available in both**, measured the same way: `AudioContext({sampleRate: 16000})` honoured the rate in both engines, and `createMediaStreamSource`, `AudioWorkletNode`, `ctx.audioWorklet` and `createScriptProcessor` are all present in both. Chromium loaded a worklet module from a `blob:` URL and delivered 128-frame buffers; WebKit refused the blob module (`'text/html' is not a valid JavaScript MIME type for module script`), which is consistent with the CSP conclusion above that a worklet must ship as a bundled file rather than a blob. Raw PCM is also what whisper wants, so it is the better wire format anyway — no container, no codec, no decode step in Rust.

**Caveat, because it changes how much these numbers are worth.** `desktop/`'s own `@playwright/test` is not installed in this worktree, so the probe was driven by a `playwright-core` from an unrelated project against the browsers in the shared cache. The engine versions are therefore not guaranteed to be the ones `desktop/playwright.config.ts` launches in CI, and the config's own comment already states the wider limit: Playwright's WebKit is its own build from the WebKit source tree (`minibrowser-gtk` on Linux), **not** the distribution's WebKitGTK, not WKWebView, and not a Tauri window.

**In the webviews the app actually ships on, the answer is per-platform and one platform is blocked.** Read from `wry 0.55.1` in the cargo registry, since that is what the build resolves:

| platform | what `wry` does | consequence |
| --- | --- | --- |
| **macOS, WKWebView** | implements `webView:requestMediaCapturePermissionForOrigin:initiatedByFrame:type:decisionHandler:` and calls it with `WKPermissionDecision::Grant` unconditionally (`src/wkwebview/class/wry_web_view_ui_delegate.rs:126-136`) | the webview layer will not block it; the OS layer still gates it, and an `.app` with no `NSMicrophoneUsageDescription` does not get the microphone |
| **Windows, WebView2** | adds a `PermissionRequested` handler **only when `attributes.clipboard` is set**, and allows only `COREWEBVIEW2_PERMISSION_KIND_CLIPBOARD_READ` (`src/webview2/mod.rs:498-515`) | nothing in `wry` decides the microphone kind, so it falls to WebView2's own default handling. Not verified — there is no Windows host here — and Windows is not a desktop bundle target today (`release.yml`'s matrix comment says so) |
| **Linux, WebKitGTK** | **nothing.** A case-insensitive grep for `permission` across the whole crate matches only the two files above; the WebKitGTK backend sets `enable_webgl`, `enable_webaudio`, page cache and developer extras (`src/webkitgtk/mod.rs:427-449`) and never `enable-media-stream`, and it connects no `permission-request` signal | with `WebKitSettings:enable-media-stream` left at its default and no permission-request handler, webview-side `getUserMedia` on Linux has no path to being granted without an upstream change. This is read from the source rather than observed in a running window — stated as evidence, not as a run |

**So webview-side capture would ship a macOS-only feature**, which is the same shape of mistake the issue already rejects when it argues for whisper over `SFSpeechRecognizer`: build it once for three platforms rather than twice for one. **The recommendation is therefore to capture Rust-side**, in the desktop crate, and hand PCM to the transcription seam. That also removes the worklet, the CSP question and the three-webview matrix in one move, and it is where the audio has to end up anyway because the transcription hop is Rust-side by CSP. Its cost is a native audio dependency, accounted for below.

The residual macOS obligation does not go away with Rust-side capture: a bundled `.app` recording audio needs `NSMicrophoneUsageDescription`, and the place for it is the `bundle` section of `desktop/src-tauri/tauri.bundle.conf.json` (Tauri v2 carries Info.plist additions under the macOS bundle config). Whether the dev-time `tauri dev` window on macOS needs the same entry to avoid a silent denial is **not verified here** and is [Open Question 2](#open-questions).

### Intent resolution: the no-key default, measured

Measured on 2026-09-18 on this box with `claude 2.1.277`, three runs of `claude -p --model claude-haiku-4-5 --output-format json` against a closed action set and a short utterance.

**It works.** "show me just the tester" against `["zoom_agent","go_overview","none"]` returned `{"action":"zoom_agent","pane_ref":"tester"}`. "what time is it" against a four-entry set returned `{"action":"none"}` twice — so the **"none of these" affordance, which the issue calls most of the safety, holds on this backend** rather than being assumed.

**Four costs, all of which have to be written down rather than discovered.**

- **Latency: 4.49–4.68 s wall** across three runs, with `duration_api_ms` 3137–3202 and time-to-first-token ~3.1 s. That is a poor interactive experience for a supervisor pressing a button, and it is the strongest argument for the seam: a direct keyed API call or a local grammar-constrained model both replace this backend without touching the spine.
- **Cost: $0.0036–$0.0126 per utterance**, and the variance is the tell — it is driven by `cache_creation_input_tokens: 7695` plus `cache_read_input_tokens: 13790` for a **ten-token** user input. Print mode loads the CLI's own session context on every call, so the price is the harness rather than the task. The cold run paid $0.0126, a warm one $0.0036.
- **The output is not constrained.** Every one of the three runs came back wrapped in a ```` ```json ```` fence despite the prompt forbidding one, so the caller strips a fence and then parses, and a malformed reply is an ordinary outcome to handle rather than an impossibility. This is the concrete difference from the issue's GBNF direction: grammar-constrained decoding makes an invalid action unrepresentable, and this backend makes it merely unlikely. Validation is what refuses it either way, which is why this is tolerable as a default.
- **Stdout carries banner noise.** The first run printed a warning about `ANTHROPIC_API_KEY` taking precedence over a claude.ai login *before* the JSON. So the reader must locate the JSON rather than trusting that stdout is JSON — the same problem `src/login_shell.rs` solves for its PATH probe with marker tokens, and the same lesson applies.

**"No key" needs narrowing, and the narrowed claim is still the interesting one.** The CLI is pre-authenticated with whatever the *user* gave it — on this box that is an environment variable — so it is not that no credential exists anywhere. What is true, and what makes this worth being the default, is that **the app needs no credential of its own and asks the user for nothing**: the thing the issue is emphatic about, that a feature needing credentials before it does anything is a feature most people never try.

**Two mechanisms this needs already exist in the tree.** `src/login_shell.rs` captures the user's interactive-login-shell PATH and applies it to the process environment (`capture_login_shell_path`, `apply_login_shell_path`), which is exactly the problem an app launched from Finder has when it wants to find `claude` in `~/.local/bin`; it is `pub`, it is in the root crate the desktop crate links, and its only caller today is `src/main.rs:1459` on the daemon path. And `src/agent_registry.rs:361-362` already carries the CLI's identity — `detect_basenames: &["claude"]`, `default_command: Some("claude")`. So this backend reuses PATH resolution rather than inventing it.

**And there is a closer precedent than expected, which came out of checking an absolute rather than trusting it.** The sentence that wanted to go here was *"nothing in `src/` invokes an agent CLI non-interactively — every agent goes through a PTY"*, and it is **false**: `src/codex_hooks_manage.rs:640` spawns `codex app-server` with piped stdio to list installed hooks. It is not inference, so the spirit of the claim survives — no agent is *asked anything* outside a PTY — but the mechanical precedent is real and is exactly the right shape to copy: `CODEX_HOME` pinned on the child, stderr discarded, the wait bounded by a named timeout constant, the child killed before returning, and **every** failure mode (binary absent, non-zero exit, protocol drift, timeout) returned as `Err` so the caller degrades quietly rather than blocking. An intent backend that spawns an agent CLI needs each of those five, and it should read that function before writing them again. For print mode specifically, `docs/develop/config-gen-regeneration.md:49` already documents a `claude -p --model claude-haiku-4-5` invocation.

~~`opencode` offers `opencode run [message..]` and `opencode serve`, so the same shape exists for the other CLI. Not measured; the adapter is written against whichever CLI the settings name, and the second one is best-effort until someone runs it.~~ **WITHDRAWN 2026-09-19.** *The same shape* turned out to be the wrong test. What an agent-CLI intent backend needs is not a non-interactive entry point but a **containable** one — no tools, no hooks, no MCP, no project configuration, no session written to disk — because the prompt it is handed is built partly from untrusted input. `claude` has a flag for each of those; `opencode run` has no no-tools equivalent and no no-persistence option, and its sessions were confirmed locally to be resumable and to hold the utterance. The variant is gone rather than documented as best-effort; the 2026-09-19 work-log entry has the reasoning and the migration.

**The honest verdict: workable as a zero-configuration default that proves the spine, needs no key and no download, and is slow.** It is what makes the feature try-able on day one; it is not the end state, and the PRD should not pretend 4.5 seconds is fine. A keyed remote backend ships beside it in the same milestone for anyone who wants the latency to be tolerable.

### Transcription, and the one place there is no no-key trick

**Siri is the wrong tool**, and the issue's reasoning stands: it does not hand an app a transcript, it matches speech against *declared* App Intents — a structured vocabulary committed to in advance, which is precisely the guess-the-phrasing problem this PRD exists to avoid — it needs native Swift in the bundle with no first-class Tauri path, and it hands matching to Apple's matcher rather than to a model that can be given live daemon state.

**`SFSpeechRecognizer` is the right Apple piece and the wrong foundation**, also unchanged: on-device, no key, no cost, and available on exactly one of three platforms. Windows has its own APIs and Linux has nothing comparable, so a local model is required for Linux regardless, and building whisper covers all three in one implementation. OS-native STT is a later per-platform optimisation.

**And neither is in this PR.** What ships is the `Transcriber` seam plus one remote backend, chosen in settings, with a credential in the `SecretStore`. **When no transcription backend is configured the panel works from typed input** through the identical resolve → validate → execute → report path, and the settings panel says what to add. That is a deliberate product statement and not a degraded mode being dressed up: the whole pipeline downstream of the transducer is exercisable, the failure message is a configuration instruction rather than an error, and local whisper is the deferred milestone that removes the requirement.

**The consequence the issue names for rejecting an exact-match fast path applies here too, and it is worth repeating because it lands on a later milestone rather than this one:** with no zero-download tier, **first run of the local engines must handle model fetching well** — explain what is downloading and why, show progress, and never present voice as broken while it is fetching.

### Phrase fixtures: two halves, in two lanes

The issue puts the guard in `cargo test-fast` and the fixtures beside the table, and those two cannot be the same thing, because a fixture needs a model and `cargo test-fast` must not reach one.

- **The structural guard is deterministic and belongs in `cargo test-fast`** — every `invoke` names a registered action, every `screens` entry is a known screen, every param `kind` is in the closed set, every registry entry has a row or a `no_voice` marker. That is [numbered rule 13](#the-guard-where-it-goes-and-what-number-it-takes) and it reads files only.
- **The phrase fixtures need a real model, so they belong in lane 2** — CLAUDE.md rule 5's `e2e-live` idiom: opt-in, skipped when no backend is configured, run on a developer's machine and nowhere in CI. They live in the desktop crate rather than in `tests/e2e_*.rs`, because what they exercise is the desktop crate's resolver and not the TUI's spawn path; the lane discipline they inherit is the reasoning, not the file location. Set `DOT_AGENT_DECK_REQUIRE_REAL_E2E=1` when a skip should read as UNVERIFIED rather than green.

**The fixtures are not optional, and the reason is the issue's own:** adding a row can silently steal utterances from an existing one. A new "close the panel" quietly takes utterances from "close the pane"; nothing errors, the model just starts picking differently, and it surfaces weeks later as a misfire. They must also cover availability — a phrase valid on the deck resolves there *and* produces the unavailable message on the overview — because otherwise a `screens` typo makes a command quietly unreachable on the screen it was written for.

**And they are authoritative only against the reference backend.** A fixture that passes with a remote model may fail with a local one, so "adding a row is safe" is only true for whoever's backend matches the reference. The reference is whichever backend the settings default to — in this PR, the agent-CLI one — and other backends get a smaller smoke set that is best-effort. Leaving this implicit would reproduce the hole class [#494](https://github.com/vfarcic/dot-agent-deck/issues/494) removed: a knob whose behaviour nothing verifies.

### Dependency cost, and the one native dependency this PR does take

Stated as an accounting, because the staging decision above is entirely about this.

**Already in the graph, costing nothing new.** `reqwest = { version = "0.13", default-features = false, features = ["rustls", "json"] }` is a root-crate dependency (`Cargo.toml:47-50`), used today only by `src/version.rs`, and the desktop crate path-depends on the root crate — so **an HTTPS client with JSON is already available to `desktop/src-tauri` with no new dependency at all.** `tokio` is there. `toml_edit` with `serde` is there, which is what parses the command table. `serde`/`serde_json` are there. There is **no** audio or inference crate in the lockfile — `cpal`, `rodio`, `whisper-rs`, `llama-cpp-2`, `hound` and `symphonia` all match nothing.

**New, and each one is a decision rather than an incidental.**

- **`keyring`** for the `SecretStore`. Required by #803's rule, no precedent in the repo to reuse, and the Linux Secret Service failure path needs designing rather than assuming. **Resolved at M4 as `keyring` 4.2.0 on default features, and it needs no system dev package on any of the three platforms** — the `v1` feature takes the Security framework on macOS, `windows-sys` on Windows, and a *pure-Rust* zbus Secret Service client on Linux, so the three-file edit below is the audio crate's alone. 59 lockfile entries; 13.9 s wall to build them all from cold on 16 cores.
- **A Rust-side audio capture crate** (`cpal` is the obvious candidate) for [capture](#capture-what-each-webview-can-actually-do-measured). This is the one native dependency this PR takes, and the cost is precise: on Linux it wants ALSA development headers, and several places would go red without an edit — `tauri-deps/flake.nix`'s `deps` list, which had `glib`, `gtk3`, `webkitgtk_4_1`, `libsoup_3`, `libayatana-appindicator`, `librsvg`, `xdotool` and `dbus` and no `alsa-lib`; and the two `apt-get install` blocks in `.github/workflows/ci.yml` (`:407-410` for the `build` job and `:629-632` for `e2e-deterministic`), which named the same set and no `libasound2-dev`. macOS needs no dev package for CoreAudio and Windows none for WASAPI, so the edit is Linux-shaped. **This paragraph originally said "three places" and named exactly those three; M7 found it was wrong by one and left a fifth file that has to move with them** — `release.yml`'s `desktop-bundle` job installs its own compile set, and `scripts/devbox-check-gtk.sh`'s `MODULES` list is an assertion rather than an install but reopens issue #815's silent failure if it drifts. The count is corrected here rather than only in the work log, because whoever reads this section next is deciding what the dependency costs. It is still a bounded, statable change, and it is still why this dependency is acceptable where a C/C++ inference engine is not: no CMake, no bundled C++ tree, no model, and no new build toolchain — a Rust binding to a system library the platform already has.
- **Nothing else.** In particular no Tauri plugin, so the capability set stays `["core:default"]`.

**The alternative that was rejected, and why**, because it is the tempting one: put the audio crate behind an off-by-default cargo feature so the workspace gate does not build it. That reproduces the exact hole class this repo has closed three times — [#407](https://github.com/vfarcic/dot-agent-deck/issues/407), [#436](https://github.com/vfarcic/dot-agent-deck/issues/436), [#502](https://github.com/vfarcic/dot-agent-deck/issues/502) — where code behind a feature nobody's gate enables is type-checked by nothing and lints clean by compiling nothing. Rule 2's clippy command names `e2e,e2e-live` for precisely this reason. A feature is not how this gets cheaper; deferring the *engines* is.

### Cross-version safety

CLAUDE.md rule 12, answered from the rule's own definition rather than by shape, and CLAUDE.md rule 18's instruction taken seriously: if the desktop needs something the daemon does not expose, the answer is to **add it to the daemon**, not to contort the client.

**Did this change the TUI↔daemon contract? No.** `PROTOCOL_VERSION` stays **10** (`src/daemon_protocol.rs:421`). No `.breaking.md` fragment, no `CONTRACT_BREAKS` entry, and no cross-version manual test is owed.

**The reasoning, because "no" has to be argued here rather than asserted.** The live state the resolver needs is role names, statuses and which pane is focused, plus the current screen — and every piece of it is something the desktop already reads:

- **Role names and statuses** arrive over `AttachRequest::ListAgents` (handled at `src/daemon_protocol.rs:2876-2877`, returning `registry.agent_records()`) and are projected into `DesktopAgent` (`desktop/src-tauri/src/dto.rs:340`), which already carries `status`, `active_tool`, `tool_count`, `cli_name`, `last_user_prompt`, `spawned_at_ms` and `last_activity_ms`. The agent overview renders them today, so the resolver reads a snapshot that already exists rather than requesting anything.
- **The focused pane and the current screen are frontend state.** The selected agent is a `useState` in `ControlDeck`; the screen is `DeckView` in `DeckShell`. Neither is on the wire and neither needs to be.
- **The Tauri commands this PRD adds are webview↔desktop-crate IPC, which is not the daemon wire.** #1105's section of this name settled the wider question and the reasoning is its own: rule 12 defines a break as *"an older and a newer build can no longer safely interoperate"*, and the webview bundle and the Rust binary are built, versioned and shipped as one artifact, so there is no configuration in which an old bundle meets a new binary.
- **Rule 12's trigger is a change to "the daemon, the TUI↔daemon protocol, orchestration, or hooks."** This is a change to a daemon **client** and touches none of the four. The manual test was **not run**, and that is stated rather than implied, because what it exercises — a branch TUI against a previous-release daemon — has no path through this code.

**Where a future vocabulary item would land, so the graded rung is named rather than left to be re-derived.** #745 already recorded that this screen's data ceiling is voice's vocabulary ceiling: "show me the one that's stuck" resolves against a status the daemon reports, and "the one burning the most money" cannot, because cost is not on the wire ([#633](https://github.com/vfarcic/dot-agent-deck/issues/633)). Adding it would be an **additive optional field** on an existing message — `#[serde(default, skip_serializing_if = "Option::is_none")]` — which is the protocol's own written **do-not-bump** case (`src/daemon_protocol.rs:12-18`), and PRD #745 landed two such fields without moving the constant. If voice ever needed a new *verb* from the daemon, the next rung is a **capability-gated new request variant**, also no bump, with the gate in the client library rather than at each call site (#1105's `CAP_FOCUS_GAINED` is the worked example). Only a wire-shape change bumps, and only a same-wire/different-meaning change owes a fragment plus a `CONTRACT_BREAKS` entry. **Reaching for one of those rungs is normal and is not scope creep**; what this PRD asserts is the narrower thing, that the commands it actually ships need none of them.

Patch bump.

### Feature flag

CLAUDE.md rule 9 asks whether a new user-visible surface ships behind `experimental`. **The answer is no.** This was asked of the user while planning this PRD and answered **no**, so: no `src/features.rs` wrapper, no `docs/develop/experimental-flag.md` entry, and no `graduate-voice-control` follow-up issue.

**The issue body's `## Rule 9` section says the opposite and is SUPERSEDED.** It reads "it ships behind the `experimental` flag with a `graduate-voice-control` follow-up at ship time", and that is no longer the decision. It is recorded here explicitly so the next reader does not re-derive the wrong answer from the issue.

The decision also has a precedent rather than being a fresh judgement, which is worth knowing because it is the same answer #745 and #1105 both recorded. `prds/176-desktop-gui.md` decision 6 settled it for the entire desktop binary: the flag is a presentation switch gating render and input seams inside the *TUI* binary, a separate GUI binary has no such seam because building and running it is itself the opt-in, and maturity is handled by packaging. #803's Open Question 1 added the confirming mechanics: the desktop crate links the root library so `features::experimental_enabled()` is callable, but the desktop never calls `features::init_and_watch` — whose only callers are `src/main.rs:1764` and `:2368` — so the flag would read its `false` default forever regardless of TOML or env, and gating this surface would mean building the flag's Tauri delivery mechanism as part of this PRD.

### Testing: what rule 4 means here

Rule 4's vocabulary is the Rust TUI's — L1 is `insta` + `TestBackend`, L2 is PTY + vt100 in `tests/e2e_*.rs` — and this feature lands in the Tauri app, so the mapping is stated rather than assumed. There are **three** desktop tiers and none of them is rule 4's L2. Counted at `d7bbbd39`:

- **Rust unit tests inside `desktop/src-tauri`** — **302** `#[test]`/`#[tokio::test]` functions. They run under `cargo test-fast --workspace` and are linted by rule 2's clippy command, and the `build` job that runs both is one of the four **required** checks. **This is the only desktop tier that blocks a merge, so it is where this feature's substance has to live**: table parsing and its rejections, schema generation, the `callable` computation against each screen, every validation refusal, param resolution against a snapshot, the outcome-to-sentence rendering, and the settings section's round-trip. It also holds the `SecretStore`'s behaviour.
- **vitest + jsdom + Testing Library** — **25 files, 514 `it`/`test` cases**, run by `pnpm test` in the `desktop-web` CI job. jsdom computes no geometry. This covers the panel rendering, the typed-input path, the dispatch through `VOICE_ACTIONS`, and that the palette and rail still dispatch through the registry after M2 moves them. **`desktop-web` is ADVISORY** — the required set is exactly `build`, `build-macos`, `build-windows`, `security` — so a red vitest run does not block a merge, which is why the guard lives in `linkage-check` and not here.
- **Playwright over the built bundle** — **16 spec files, 56 `test()` calls**, in two projects (Chromium and WebKit), run by `pnpm test:browser` in the `desktop-browser` CI job, also **advisory**. Its `webServer` runs `vite build` then `vite preview` and serves `desktop/dist`, and specs drive the app through the fixture query strings (`?fixture=1&state=<scenario>`, scenarios `connected | crowded | empty | disconnected | error | fleet`) with no daemon involved.

**What each tier cannot reach, which is the half that gets assumed wrong.**

- **A Playwright spec cannot exercise a Rust-side Tauri command.** There is no Tauri runtime in that tier at all — it is `vite preview` serving static files — so the IPC an `invoke` would travel over does not exist, and the app selects its `FixtureDeckBridge` rather than `TauriDeckBridge`. Anything Rust-side is out of its reach by construction. **The bridge seam is what makes the browser tier useful here anyway**: a fixture-mode resolver returning a canned outcome lets a spec drive the whole panel, the situation-to-sentence rendering and the dispatch, in two real engines, with no model and no credential.
- **A Playwright spec CAN fake a microphone** — [measured above](#capture-what-each-webview-can-actually-do-measured): Chromium with two launch flags, and Playwright's WebKit with none. So a webview-capture path would have been testable there. With Rust-side capture the relevant tier moves: the capture code is Rust and its tests are Rust unit tests over a stubbed device, and what nothing automated can prove is that a real microphone on a real OS produced audio.
- **Nothing drives the real Tauri window.** There is no `tauri-driver` and no WebDriver session in this repository ([#953](https://github.com/vfarcic/dot-agent-deck/issues/953)), so real IPC, the distribution's own WebKitGTK, WKWebView, and the OS microphone permission dialogs are exercised by nothing automated. **This is not rule-4 parity and is not claimed as such.** The compensating control is the manual smoke check in `docs/develop/desktop-gui.md`, which a milestone extends — and which, like every manual check, decays.

**The three things no automated tier here can prove**, listed so green checks are not read as covering them: that the OS granted the microphone on any platform; that a real utterance transcribes; and that the agent-CLI backend is reachable from an app launched from Finder with a Finder-shaped PATH. All three are manual, and the third is the one most likely to be discovered late — it is [Open Question 1](#open-questions).

## Success Criteria

- **Adding a command changes no implementation** — one row and one classification flip — **plus the test assertions that pin the shipped row set by value.** **Met, and measured rather than asserted**: M8 added `open_settings` in seven files, five of them test-only, every Rust hunk inside a `mod tests`, and no production logic in any of them. The criterion this document shipped with — *“a one-file change … the table and its fixtures and nothing else”* — was **not** met and is not what is claimed here now; [M8](#milestones) accounts for all seven files and for why five of them are the pinning tests doing their job rather than plumbing that was left undone.
- A spoken or typed request that maps to a command **executes through the same handler a click executes**, with no second execution path anywhere in the diff — checkable by the action registry having exactly one dispatch site per entry.
- Asking for something that exists but is not available on the current screen produces **the table's own hint naming the prerequisite**, not "I don't know how to do that".
- Asking for something outside the closed set produces a **no-match that shows the transcript**, and the model is able to answer "none of these" rather than being forced to pick.
- The user-facing sentence for every outcome is **rendered by the app from the table**, so the model writes no user-facing prose at all.
- A typo in `invoke`, an unknown screen in `screens`, an unknown param `kind`, or an unclassified capability **fails `cargo test-fast` and the required `build` job**, rather than surfacing at runtime as nothing happening.
- **No credential is in `desktop.toml` or in `localStorage`**, enforced by `desktop_settings_secrets.rs` continuing to pass with the `[voice]` section present — which for a `String` field means the guard going red until it is routed through the `SecretStore`.
- **With no transcription backend configured the surface still works from typed input**, end to end, and the settings panel says what to add. Nothing about it reads as broken.
- **With no API key and nothing downloaded, intent resolution works** on a machine that has an authenticated agent CLI, and the latency is reported to the user rather than looking like a hang.
- The daemon is unaware that any of this exists: `PROTOCOL_VERSION` unchanged, no verb added, no fragment owed.
- `cargo test-fast --workspace` and all three required build jobs stay green on every platform, including the one that needs a new Linux dev package.

## Milestones

**In this PR.** Each is independently deliverable and testable, and they are ordered so the riskiest platform-specific work is last rather than first.

- [x] **M1 — The table and its three consumers, Rust-side.** `commands.toml` with `include_str!`, the parse and its rejections, generated single-tool enum schema, the `callable` computation from `screens` against a given screen, validation of action and params, and resolution of `invoke` to an action id plus params. No model, no capture, no UI: the resolver seam takes a transcript and returns an outcome, and a stub resolver stands in. Rust tests for every rejection and every outcome variant.
- [x] **M2 — The frontend action registry, and the palette moved into it.** `desktop/src/lib/voiceActions.ts` exporting `VOICE_ACTIONS`, with the rail buttons and the command palette's `commandItems` (`desktop/src/App.tsx:1043-1053`) dispatching through it. **This is what makes the capability definition real rather than a parallel list**, so it lands before the guard. No behaviour change visible to a user; vitest proves each moved control still does what it did.
- [x] **M3 — The guard.** `linkage-check` numbered rule **13** plus a `#[cfg(test)]` module holding the scanner's own tests, including planted-bad-input tests for each assertion so "no findings" is meaningful. Covers `invoke` ↔ registry, `screens` ↔ `DeckView`'s `kind` literals, param `kind` ↔ the closed set, and every registry entry classified by a row or a `no_voice` marker.
- [x] **M4 — The `[voice]` settings section and the `SecretStore`.** The section struct with all-allowlisted types, closed enums for the backend and mode choices, the panel component, one `SETTINGS_SECTIONS` row, the `PINNED_TS_FIELDS` and `normalizeDesktopSettings` extensions, and the `SecretStore` #803 M5 named — store/load/delete over the OS keychain, with a documented non-silent failure path where no Secret Service exists. Any edit to `ALLOWED_FIELD_TYPES` arrives here with its written reason.
- [x] **M5 — Intent backends behind the seam.** The agent-CLI backend (no key, no download, the default) with fence-tolerant parsing, stdout-noise tolerance, a timeout, and PATH resolution through `login_shell`; and a keyed remote backend over the `reqwest` already in the graph. Settings choose between them. The measured latency is surfaced in the UI rather than hidden.
- [x] **M6 — The voice surface.** The panel, typed input, the outcome-to-sentence rendering, the transcript shown on no-match, and the report with an undo window. Navigation-only commands: **at least three rows plus a deliberate no-match**, because a one-command vocabulary makes the model map everything to it and tests neither disambiguation nor the no-match path. vitest for each outcome; Playwright over the fixture bridge for the panel in both engines.
- [x] **M7 — Microphone capture, and one activation mode.** *(Rust half shipped 2026-09-18; the panel and the mic button belong to M6.)* Rust-side capture handing PCM to the `Transcriber` seam, one remote transcription backend, and press-once-to-start / press-once-to-stop. The Linux dev-package edits to `tauri-deps/flake.nix` and both `apt-get` blocks in `ci.yml` land here, as does the macOS `NSMicrophoneUsageDescription` entry in the bundle overlay. **This is the milestone to descope if the platform permission work proves worse than the survey measured** — the spine ships without it, from typed input, and saying so now is cheaper than discovering it under pressure.
- [x] **M8 — Measure what a row costs.** *(Shipped 2026-09-19, `49dbdabc`. Written as “prove the one-file claim”; what it proved is that the claim was wrong by six files, and the corrected wording is in [Solution Overview](#solution-overview) and [Success Criteria](#success-criteria).)* The next command is `open_settings`: one row in `commands.toml`, and the `openSettings` registry entry flipped from a `no_voice` reason to `voice: true`. **Seven files, and no implementation among them.**

    | # | file | what changed | production code? |
    | --- | --- | --- | --- |
    | 1 | `desktop/src-tauri/src/voice/commands.toml` | the row | the row itself |
    | 2 | `desktop/src/lib/voiceActions.ts` | `no_voice` → `voice: true` | the classification flip |
    | 3 | `desktop/src-tauri/src/voice/table.rs` | 2 pinned-row test assertions | no |
    | 4 | `desktop/src-tauri/src/voice/schema.rs` | 3 pinned-row test assertions | no |
    | 5 | `desktop/src-tauri/src/voice/prompt.rs` | 3 pinned-row test assertions | no |
    | 6 | `desktop/src-tauri/src/voice/remote.rs` | 1 pinned-row test assertion | no |
    | 7 | `xtask/linkage-check/src/voice_command_registry.rs` | 1 guard-test plant re-pointed | no |

    **Five of the seven are test-only, and every Rust hunk is inside a `mod tests`.** Nine unit tests pin the shipped row set **by value** — the action enum in the generated schema, the CLI prompt, the remote request body, the per-screen `callable` flags, and the table's own row list. That is deliberate and `table.rs` says so: the shape of the shipped table is pinned rather than described, because a test that merely describes the shape would have passed while the table said something else. So the cost is **one edit per place the row set is written down**, which is what those tests are *for*. Read the number that way — “five test files need a value added” — rather than as six files of work, and note none of it surfaced at runtime.

    **The seventh is the sharpest, and it is the one that does not recur.** Rule 13's `overtaken` planted-bad-input test rewired a row's `invoke` to `openSettings` precisely because that was the registry's only `no_voice` entry an `openOverlay` row would plausibly claim. Giving it a row turned that plant into the `unbacked` case instead, so it is re-pointed at `openProjects`. **A guard test that names a specific unspoken entry has to move when that entry starts speaking** — a cost the next row pays only if it happens to name the entry that row is for.

    **`screens = ["deck"]`, which corrects this milestone's own earlier spec of `["deck", "overview"]`.** The Settings overlay is reachable only from the **deck** rail — the overview's rail holds Deck and Overview and nothing else, verified in `AgentOverview.tsx` — so listing `overview` would let voice open a surface no click can reach from that screen. That is voice becoming a second control surface with capabilities the first lacks, which is the one thing this PRD forbids.
- [ ] **M9 — Phrase fixtures, in the credentialed lane.** Utterance → expected action, including availability cases in both directions, opt-in and skipped when no backend is configured, authoritative against the default backend and best-effort elsewhere. Run them locally and name them in the PR, because nothing in CI will.
- [x] **M10 — Docs, smoke check, changelog.** `docs/develop/desktop-gui.md` gained the pipeline end to end, the command table and its three consumers, [what adding a row actually costs](#milestones), the two intent backends and the `[voice]` settings, the `SecretStore` seam and the credential rule, a section on the three properties **nothing here verifies**, a step-by-step checklist for adding a command, and a seven-step manual smoke walk — the only thing that exercises the feature as a user meets it, since no automated tier opens a device, reaches a model, or runs inside the real Tauri window. Changelog fragment at `changelog.d/802.feature.md`, type `feature`.

    **No user-facing page under `docs/` — and that is PRD [#740](https://github.com/vfarcic/dot-agent-deck/issues/740)'s standing decision, not this milestone skipping its own line.** This milestone's own text said *“user-facing setup goes under `docs/` per CLAUDE.md rule 11”*, which was written without checking what is already there: **the desktop app has no published page of any kind.** It is not in `site/sidebars.js`, nothing under `docs/` tells a user it exists, and #740's Decision 11 says why in as many words — *“an install page is an invitation, and this build is not yet inviting”* while the artifact is unsigned, Windows-less and about to change its icon. A published `docs/voice-control.md` would document a feature of an application the same site never mentions and gives no way to obtain. The obligation is not dropped, it is **scheduled**: [#765](https://github.com/vfarcic/dot-agent-deck/issues/765) (open, `documentation`/`desktop`) writes that page when the GUI leaves alpha, gated on #757, #746 and #164. Voice belongs on it when it is written, which is recorded as [D12](#milestones) below so it is not rediscovered.

    **What carries the user-facing half instead**, since “no page” must not mean “nowhere”: the **release note** — which #740 names as the whole of the public surface for this app — so `802.feature.md` is written as user documentation rather than as a summary, and states the limits a user would otherwise meet as a defect: navigation-only, one activation mode, speech needs a key until D1, and the microphone permission unexercised on a packaged build. And the **panel itself**, at the point of use: with transcription `off` it says *“Voice commands are typed for now; pick a speech backend to talk instead”* rather than leaving an unresponsive microphone button to be read as broken. A user discovering a limit from the docs is fine; discovering it from a dead microphone is not, and that sentence is the one standing between the two.

**Deferred, each with what it is waiting on.** None of these is dropped; a deferred milestone with a reason is the deliverable and an omitted one is a hole.

- [ ] **D1 — Local whisper transcription.** Waiting on a deliberate decision to take a C/C++ build toolchain into `cargo test-fast --workspace` and all three required build jobs, and on D3's download management. This is what removes the credential requirement from transcription, so it is the most valuable of the deferred set.
- [ ] **D2 — A local grammar-constrained intent model.** Same toolchain decision, plus D3. This is what makes an invalid action unrepresentable rather than merely unlikely, and what takes intent resolution off the 4.5-second path measured above.
- [ ] **D3 — Model download management.** Progress, retry, disk-usage visibility and removal, rendered in the #803 settings panel with the download itself Rust-side because the CSP leaves no alternative. Waiting on D1 or D2 — with no bundled engine, nothing downloads, and building this first would be a manager with nothing to manage.
- [ ] **D4 — The remaining two activation modes**, hold-to-talk and always-on with voice-activity detection. Waiting on M7 shipping and one mode being used enough to say what the other two are worth.
- [ ] **D5 — Destructive commands behind confirmation.** Approve or deny a permission prompt, close a pane, stop an agent — confirmed regardless of model confidence, because the cost of a rare misfire on "approve" is an agent doing something nobody sanctioned and a confident wrong answer is indistinguishable from a right one. Waiting on the navigation-only slice being proven in real use and on a confirmation flow; #803 recorded that the settings surface has no destructive action at all today and that `ConfirmDialog` is the intended route.
- [ ] **D6 — Modal dictation.** "Type to the tester" aims input at that agent until an exit phrase. Three things to design for, recorded now because they are the whole difficulty: intent detection keeps running during dictation to catch the exit phrase — a missed exit sends the exit phrase into the agent's prompt and a false positive truncates mid-sentence — so it needs a distinctive phrase **and** a non-voice escape; it streams into the **visible** input rather than an invisible buffer; and it **never auto-submits on exit**. Waiting on D4, since it is an interaction mode rather than a table row.
- [ ] **D7 — Discovery.** Contextual examples rendered from the table, filtered to what is callable right now, with the no-match message doubling as discovery by showing the two or three nearest things it could have done. Waiting on the table having enough rows for a list to be worth reading.
- [ ] **D8 — An audio-native single-hop backend.** Transcription and intent in one call: lowest latency and the best conversational feel, cloud-only, and it streams the microphone continuously. An option, never the default. Waiting on the seams being exercised by two backends each, so a third does not reshape them.
- [ ] **D9 — The TUI↔desktop parity matrix.** A checked-in matrix (TUI capability ↔ desktop: shipped / not-applicable / gap) with a `linkage-check` rule failing on any new TUI action that is unclassified. The issue floats this under "Explicitly rejected" as the enforceable alternative to *"whenever we add to the TUI, add it to the desktop if it makes sense"* — which is unenforceable and taxes every TUI change with a second implementation. Under the working-vs-supervisory split most entries would legitimately be not-applicable, so drift becomes measured rather than assumed. Deferred because it is a repo-wide classification exercise over the TUI's whole surface, not a part of this feature; it is named here so the rejection does not read as "nothing to do".

**Deferred by this run, and not on the list above when it started.**

- [ ] **D10 — Close the five second dispatch paths.** Five `no_voice` capabilities reach their state through an in-panel `setState` as well as through `VOICE_ACTIONS`: the agent tile's own `onSelect` (bypassing `focusAgent`), `toggleEvidenceDrawer`, `openWorkflowOrder`, `openProjects` and `openAgentProfiles`. `voiceActions.ts` lists them, so they are a known residual rather than a finding waiting to be rediscovered. **Nothing is voice-reachable through them**, so the load-bearing property — voice has exactly one execution path and acquires no second one — holds today. Waiting on a reason to touch those eight call sites: re-routing them late in this PR was regression risk for no functional gain. **If a later PRD gives any of the five a row, closing its second path is that PRD's work**, because a second path to a voice-reachable capability is behaviour voice cannot see.
- [ ] **D11 — Group teardown on the SUCCESS path of an agent-CLI resolve.** `ChildGroup` performs the `killpg` on every *drop* path and `terminate_group` runs on the timeout and flood paths, so a cancelled or abandoned resolve takes its descendants with it. A **successful** resolve does not: `ChildGroup::wait` disarms the moment the direct child is reaped, so a tool or shell the CLI started and left behind survives. The disarm is not the bug and must not be removed — a reaped pid can be recycled, and `killpg` on a recycled pgid is the one thing this type exists to prevent. Closing it means teardown **before** the reap, or a pidfd-style handle that cannot be recycled. Waiting on evidence that the default backend actually leaves descendants on a clean exit, which has not been observed; it is recorded because the reasoning that makes it safe today is subtle enough to be worth writing down rather than re-deriving.
- [ ] **D12 — Voice on the published install page.** [#765](https://github.com/vfarcic/dot-agent-deck/issues/765) writes the desktop app's user-facing page under `docs/` when the GUI leaves alpha. Voice needs a section on it: how to turn it on, what it can do, the two backends, and that the zero-configuration path uses the agent CLI the user already has. Waiting on #765, which waits on #757 (signed), #746 (logo settled) and #164 (Windows exists) — see M10 above for why a page written before then would be an invitation to an application the same site does not offer.

**What is deliberately NOT a table row, so the cost-of-a-row claim is not oversold.** The table covers the closed action set, which is the case that grows per feature. Activation modes, modal dictation and the destructive-confirmation flow are plumbing: built once, not extended per feature. "After the plumbing, one more costs a row and no implementation" is true for *commands* and not for new *interaction modes*, which should be rare.

## Risks

- **The no-implementation claim is the whole design, and M2 is where it is actually won or lost.** If the palette move leaves a second dispatch path — a rail button still calling `setState` directly, say — then `invoke` references a registry that is not the app's real dispatch seam, and the guard is checking a list rather than the app. Mitigation: M8 is a milestone whose only content is measuring it, and it runs before the fixtures rather than after. **It ran, and it came out green on the half that matters**: no implementation had to be touched. What it also showed is that the count was never going to be one — five test files pin the row set by value — so the criterion is now written about implementation rather than about a number of files.
- **The capability assertion is weaker than the issue's wording, and reading it as stronger is the risk.** The guard cannot discover a capability; it can only prove that everything in the registry is classified. 80 `onClick` sites in production `.tsx` are outside its sight. Mitigation: say so in the code and in the docs, at the guard's own failure message, rather than letting the next reader infer completeness.
- **The default intent backend is slow enough to feel broken.** 4.5 seconds measured, with ~3.1 s to first token, for a supervisor who just pressed a button. Mitigation: the keyed remote backend ships in the same milestone, the latency is surfaced rather than hidden, and D2 is the real fix. This risk is why the seam is not optional.
- **Microphone permission on Linux may simply not be available in-webview, and the Rust-side capture that avoids it brings a dev-package edit to three files.** Both halves were read from source rather than run, so the first real `tauri dev` window on each platform is where this gets confirmed or falsified. Mitigation: M7 is last and explicitly descopable, and the spine works from typed input without it.
- **The phrase fixtures are authoritative for one backend only**, so a contributor whose settings name a different one gets a green run that proves less than it looks like. Mitigation: it is written into the fixtures' own module docs, not only here, and other backends get a clearly-labelled smoke set.
- **Adding a `String` to the settings schema will go red, and the tempting fix is to widen the allowlist.** `ALLOWED_FIELD_TYPES` exists to force that conversation. Mitigation: the section's fields are closed enums by design, and any allowlist edit arrives with its written reason in the entry — which is the friction working, not an obstacle.
- **A row's `description` is a prompt, and nothing type-checks prose.** A wording change can silently move which utterances land where. Mitigation: the fixtures are the only defence, and they cover availability in both directions so a `screens` typo is caught too.
- **Two advisory CI jobs cover the user-visible half.** `desktop-web` and `desktop-browser` are both advisory, so a red frontend run does not block a merge. This is a standing property of the desktop app rather than something this PRD creates, and it is the reason the guard is in `linkage-check` and the substance is in the Rust tier.

## Open Questions

1. ~~**Does the agent-CLI intent backend work from an app launched from Finder or a desktop launcher, where PATH is minimal?**~~ **ANSWERED in M5 (2026-09-18): capture lazily, at most once per process, and only when the program was not found without it — and use `capture_login_shell_path`, never `apply_login_shell_path`.** Three findings, and the first one changes which function is even available.

    **`apply_login_shell_path()` is the wrong half of that API for this process, and the question presupposed it was the right one.** It calls `std::env::set_var`, and its own contract is explicit: exactly once, at process startup, before the async runtime or any worker thread exists, because on glibc a concurrent `getenv` from another thread is a data race and therefore UB. A desktop app resolves an utterance on the Tauri runtime, long outside that window — so calling it there would be unsound, and its debug-build run-once latch would trip on the second utterance. The **capture** half, `capture_login_shell_path()`, is `pub`, mutates nothing, and returns the string; M5 puts that on the child `Command`'s own environment. Same effect, no process-global mutation, no soundness argument to make.

    **The cost is real and wildly variable, which is what rules out a startup capture.** Measured three runs of this box's `bash -ilc` at **0.03–0.04 s**, against the **~6 s** `zsh -ilc` with plugins that `CAPTURE_TIMEOUT`'s own doc comment records, and a **10 s** ceiling. A startup capture spends an unknown fraction of that on window appearance for **every** user — including everyone who never uses voice, and everyone whose PATH was already fine. Ten seconds is also not a hypothetical worst case: it is what a shell that hangs on a prompt costs, and the app would sit there.

    **And the common case needs none of it.** An app launched from a terminal already inherits a working PATH; measured here, `claude` resolves at `~/.local/bin/claude` and `opencode` at `~/.opencode/bin/opencode` with no capture at all. So the shipping shape is: spawn on the inherited PATH first, and only on `ErrorKind::NotFound` — which is exactly the Finder/launcher case this question asks about — capture, memoize in a `tokio::sync::OnceCell`, and retry once. The common case costs one `execve`; the Finder case costs one capture for the life of the process; a failed capture is remembered as a failure so a machine with no `$SHELL` does not pay `CAPTURE_TIMEOUT` per utterance. The capture runs under `spawn_blocking`, because it is blocking by nature and a runtime thread is not where a 10-second shell belongs.

    **What is still unverified, stated rather than implied: none of this was run from an actual Finder launch.** No macOS host was available. What was verified is the mechanism — that the fallback fires on `NotFound`, that `Inherit` mode never captures (asserted by the clock, since a capture cannot return in milliseconds), and that the resulting error is `NotConfigured` naming the missing binary rather than a hang. The Finder launch itself belongs with Open Question 2's macOS verification.
2. **Does a macOS `tauri dev` window need `NSMicrophoneUsageDescription` to record, or only a bundled `.app`?** The bundle overlay (`desktop/src-tauri/tauri.bundle.conf.json`) is where the packaged answer goes, and a dev-time denial would be silent. Needs a macOS host; unverifiable here.
3. **Should the guard also pin a count of interactive controls, so a new one forces a classification?** It is the only mechanical approximation to "did you forget a capability", and its cost is a red gate on routine UI edits — 80 `onClick` sites today, most of them chrome. Recommendation is no for this PR, and to revisit if a capability is in fact found to have been missed.
4. ~~**Which model does the keyed remote intent backend default to, and is the tool-use API or a plain constrained-JSON prompt the right shape?**~~ **ANSWERED in M5 (2026-09-18): tool-use with `strict: true` and a constrained enum, on `claude-haiku-4-5`.** The latency is comparable, so the rule this question itself set — prefer the constrained one if it is — decides it.

    **Measured**, four utterances × two runs each against the real command table, wall clock on this box, all three shapes answering all four correctly including `{"action":"none"}` for *"what time is it"*:

    | shape | min | median | max | correct | input tokens |
    | --- | --- | --- | --- | --- | --- |
    | tool-use, `strict: true` | 0.81 s | **0.91 s** | 1.39 s | 8/8 | ~1280 |
    | tool-use, no `strict` | 0.66 s | 0.76 s | 0.93 s | 8/8 | ~1280 |
    | plain constrained JSON | 0.63 s | 0.75 s | 1.37 s | 8/8 | ~588 |

    The constrained shape costs **~0.16 s** against the unconstrained one and **~700 input tokens** against the prompt-only one, on a path whose *alternative backend* costs 3.1–4.7 s. What it buys is the thing this question was about: under `strict: true` the API validates the tool arguments against the schema, so an action outside the enum is not representable rather than merely unlikely — which is the distinction this document draws between a hosted backend and D2's grammar-constrained one, and this is as close as a hosted API gets.

    **One cost was measured rather than assumed, and it is worth knowing before anyone edits `commands.toml`: a NEW strict schema pays a one-time compilation.** A novel strict schema took 1.81 s then 1.12 s; the same schema afterwards took 0.91 s and 0.85 s; and a novel **non-strict** schema took 0.84 s — so the ~0.9 s is the strict compile and not a cold connection. It is cached for 24 hours. The user-visible shape is one slower utterance after a table change or after a day away, which M6 renders honestly like any other latency. (A single 10.58 s first call was observed and did **not** reproduce: a cold-process first call measured 0.90 s for the strict shape, 0.73 s for the unconstrained one and 0.89 s for plain JSON. It is recorded as an outlier, not attributed to the schema.)

    **`strict: true` forced one design decision and it is the interesting part.** A strict schema may not carry `additionalProperties` set to anything but `false`, **anywhere** — so `params` cannot be an open map of strings, which is what this app's own tool shape uses. The params object has to *enumerate* its properties. The table is what makes that reachable: it declares every param name statically, so the schema enumerates them from the table and a row that adds a param adds a property with nobody editing the backend. That is the no-implementation promise holding on a path that could easily have broken it — and it is exactly the distinction M8 went on to measure: the schema **generator** needs no edit for a new param, while the tests that pin the generated schema **by value** do. A test walks the whole generated schema asserting `additionalProperties: false` on every object, so a future open map fails at commit time rather than as a 400 at run time.

    **The model is `claude-haiku-4-5`** — the cheapest current tier at **$1.00 / $5.00 per 1M tokens**, and the right one rather than merely the cheap one: short, closed-set, latency-sensitive classification with no reasoning to do is the workload that tier is for, and it was correct on every measured case. At the measured ~1280 input / ~40 output tokens an utterance costs about **$0.0015**, against the agent-CLI backend's $0.0036–$0.0126. It stays a **constant and not a settings field**, which is why M4's `[voice]` section was right to leave `model` out: having answered the question, a field would offer a choice whose only correct value the code knows, and a user who wants a different model is asking for D2 or D8.

    Read `tool_choice: {"type":"tool"}` and the model pin as one decision rather than two: forced tool choice is rejected by some newer models and accepted by this one, so moving the model means re-checking it.
5. ~~**Does the transcript belong in the settings document's reach at all — that is, is any part of an utterance persisted?**~~ **ANSWERED in M7 (2026-09-18), and CORRECTED on 2026-09-19 by the landed-work security audit.** The honest answer is in two halves. *This app* persists nothing: nothing in the capture or transcription path writes to disk or logs at any level, `Transcript` and `Pcm16` both have hand-written `Debug` impls that print a length and no content, and the rule reaches the IPC seam too — `desktop_voice_stop` transcribes Rust-side and returns text, so no audio buffer ever crosses into the webview. *A child process it hands the prompt to* is a different question, and the first answer got it wrong: the agent-CLI intent backend passed the whole prompt — the utterance and the agent labels with it — to `claude` in its ordinary session mode, which writes a resumable session to disk. Redacted `Debug` impls do nothing about another application's storage. It now passes `--no-session-persistence`, so the claim is true again — but it is true **because a flag is passed to another program**, and the answer is written that way wherever it appears rather than as the unqualified version. The `opencode` backend, which has no such flag, was withdrawn (see the 2026-09-19 work-log entry).

## Work Log

### 2026-09-18 — Created

Written from [#802](https://github.com/vfarcic/dot-agent-deck/issues/802) read as the specification rather than as a summary, against a survey of the tree at **`d7bbbd39`**. The intent, the command table, the single-tool enum, the full-list-with-`callable` trade, the situation-not-prose rule, the "none of these" affordance and the say-what-you-heard rule are all the issue's and are carried forward expanded. Six things the survey changed or settled:

**The `invoke` column cannot name a `#[tauri::command]` for the commands this PR ships, which is the one structural correction.** The issue says it does. The registry holds thirteen entries and not one of them performs a navigation — navigation is React state — so a table built that way could expose the daemon verbs and none of the commands this PR is scoped to. Resolved by reading the issue's own step 5 literally: voice dispatches where a click dispatches, a click is dispatched in the frontend, so `invoke` names an entry in one frontend action registry, and a Rust-side capability is a one-line `invoke()` inside that entry. Both registries are enumerable by text scan, which is the idiom `desktop_settings_secrets.rs` already uses on `bridge.ts` from a required gate.

**"User-facing desktop capability" has no mechanical definition the guard can discover, and the honest version is narrower than the issue's.** Adopted: a capability is an entry in the action registry, which earns the definition by being load-bearing at runtime rather than a list beside the app — which is also why M2 moves the palette's `commandItems` into it. Named cost: 80 `onClick` sites in production `.tsx` are outside its sight, so the claim is that the registry is classified, not that the app's surface is.

**The engines were staged out and the reasoning written down rather than left to look like the issue being ignored.** `desktop/src-tauri` is a workspace member, so a bundled C/C++ inference engine lands a cross-platform native toolchain in the per-task gate and in three required build jobs. The seams ship; the engines do not. An off-by-default cargo feature was considered and rejected for reproducing the #407/#436/#502 hole class.

**The no-key intent backend was measured rather than argued.** `claude -p --model claude-haiku-4-5 --output-format json` resolves a closed-set pick correctly, answers `{"action":"none"}` for "what time is it", and costs 4.49–4.68 s wall and $0.0036–$0.0126 per utterance — the price being the CLI's own ~21 k-token session context rather than the task. Output is fenced despite instructions and stdout can carry a banner, so parsing is fence- and noise-tolerant. Verdict: workable as a zero-configuration default that makes the feature try-able, slow enough that saying so is part of shipping it.

**The capture question was measured in two directions and one platform is blocked.** Playwright's WebKit has a mock audio device and **no `MediaRecorder`**; Chromium needs two launch flags and has one. Web Audio at 16 kHz works in both. In the shipping webviews, `wry` 0.55.1 grants media capture unconditionally on WKWebView, decides only clipboard-read on WebView2, and for WebKitGTK sets no `enable-media-stream` and handles no permission request at all. So webview capture would ship a macOS-only feature, and the recommendation became Rust-side capture — which the CSP was pushing toward anyway, since every network hop is Rust-side and whisper wants PCM.

**#803's credential guard will go red when this PRD adds a `String`, and its own module docs say so.** That turned the settings work from "add a section" into "add a section whose every field is a closed enum, and build the `SecretStore` #803 M5 deliberately did not build". Recorded with the guard's four checks read rather than guessed, including that `bool` is not on the fifteen-entry allowlist either, so even a plain on/off field is a deliberate edit with a written reason.

Two decisions were taken before the document and are recorded rather than re-opened: **rule 9's flag question was asked of the user and answered no**, so the issue's `## Rule 9` section is superseded and this surface ships visible by default; and **the PRD is written first and implemented against**, with milestones as the unit of delegation.

### 2026-09-18 — M1 shipped, and three corrections to this document

**M1 landed** as `desktop/src-tauri/src/voice/` — `commands.toml` with `include_str!`, the parse and its twelve named rejections, the single-tool enum schema, the `callable` computation, validation, param resolution against the live snapshot, and the outcome-to-sentence rendering, with a `StubResolver` standing in for a model. Three things it found are corrections to this document rather than to the code, and they are recorded here so M2/M3/M6 build against the corrected shape.

**The outcome set is nine, not four.** The four listed above could not express a missing param, a param that resolves to nothing, a param that resolves to more than one thing, a backend that could not answer, or a backend that named an action outside the table — and each of the first four wants a different sentence. The table above enumerates all nine. `UnknownAction` and `NoMatch` share a sentence and stay separate variants, which is written down so a later reader does not merge them.

**The success-sentence column was added and is called `report`.** The example row in this document had nothing to render on success, so without the column M6 would have invented success wording in the frontend and the "the app renders every sentence" property would have gone with it. `confirmation` was the obvious name and is the wrong one: D5 is *destructive commands behind confirmation*, so a genuine confirm-before-acting flag will live in this same file, and the two are close to opposite. Renamed while M1 was its only dependent.

**`close_agent_view` stays, and the Out of Scope entry now names both operations.** Closing an agent's *terminal pane* is lifecycle and destructive; dismissing the agent *view* is `setView({ kind: from })` and is navigation. #1105's vocabulary calls the overlay "the agent pane", so the distinction is written into the scope entry rather than left to be inferred. Without the row the `agent` screen has no callable command at all.

Also in the same commit: a CLAUDE.md rule 17 sweep over M1's own prose. Four claims were narrowed — the transcript no-logging rule was scoped to a `tracing`/`log` call this crate does not use (it logs with `eprintln!`) and was stated as an established property rather than as the rule it is; `table()`'s "a failure can only be introduced by editing `commands.toml` or the parser" omitted a `toml_edit` bump; and `outcome.rs`'s "every sentence is rendered from the table" was one quantifier too wide, since the table supplies two pieces of wording and fixed prose in that file supplies the rest. The fourth was resolved by changing the code instead of the sentence: the report template now renders in one pass, so a user-chosen agent name containing `{…}` cannot be substituted into by a later param.

### 2026-09-18 — M4: the `[voice]` section and the `SecretStore`

The section is three closed enums and nothing else: `activation` (`toggle`), `intent` (`claude` | `opencode` | `remote`) and `transcription` (`off` | `remote`), each with a folding deserializer in the `AppearanceMode` idiom and a 64-byte bound. `DesktopSettings::voice` is an `Option<VoiceSettings>` for `endpoints`' reason — `None` means *unspecified*, so a client that does not send the section cannot have the merge write defaults over a choice the user made.

**No model identifier and no stored-key boolean**, both deliberately. Open Question 4 is M5's to answer against a measurement, so a `model` field now would be this section inventing that answer. The boolean the credential rule explicitly allows is absent because the panel asks the `SecretStore` itself — an answer that cannot go stale against the keychain — which also keeps `bool` off `ALLOWED_FIELD_TYPES`.

**The guard went red exactly where its own module docs said it would**, at all four new fields, and the resolution was the first of the two honest ones it names: the values that could have been a `String` are closed enums, and the value that genuinely is a credential went behind the seam. `ALLOWED_FIELD_TYPES` gained four entries, each with its written reason.

**The `SecretStore` is built, and two of its properties are decisions rather than details.** There is **no IPC command that reads a credential back** — the webview may ask whether one is stored, replace one and forget one, and `load` is Rust-side only, where M5's and M7's backends make their network call because the CSP forces every hop there anyway. And **a failed store is an `Err`, never a status saying "nothing stored"**: the three situations #803 asked to be handled — no credential store on this machine, a locked one, anything else the platform reports — each carry a sentence naming what did not happen, because the outcome to design against is a user who thinks their key is saved and finds voice broken tomorrow.

**`keyring` 4.2.0 on default features, and the cost is that it adds NO system package on any of the three platforms.** `default = ["v1"]` selects one store per target, each target-gated in `keyring`'s own manifest: the Security framework on macOS, `windows-sys` on Windows, and `zbus-secret-service-keyring-store` on Linux — zbus speaks D-Bus in Rust rather than binding `libdbus`, so `tauri-deps/flake.nix` and both `apt-get` blocks in `ci.yml` are untouched. That is the one shape of cost this PRD's dependency accounting said to avoid, and it was avoidable here; M7's `alsa-lib` still is not. The two alternatives were each worse in a stated way: `dbus-secret-service` wants `libdbus-1-dev`, and `linux-keyutils` wants no package and keeps its keys in the kernel, where they do not survive a reboot — which a user experiences as the app forgetting their key overnight. It costs 59 lockfile entries and, measured on 16 cores, 13.9 s wall / 66 s CPU to build all of them from a clean slate; warm it is a no-op.

**One correction to this document's own accounting**, which said `keyring` was needed "and the Linux Secret Service failure path needs designing rather than assuming". It did, and the design is above — but the sentence beside it implied the dependency would cost a dev package the way the audio crate will. It does not, and saying so matters because the two were being weighed together.

### 2026-09-18 — M5: the intent backends, and two Open Questions answered

Both backends ship behind M1's `dyn` seam, `VoiceSettings.intent` selects between them at call time, and `handle_utterance` now returns a `VoiceResult` carrying the outcome, the latency it cost and which backend produced it. Six things worth recording, four of which are corrections or narrowings of what this document said before them.

**The agent-CLI backend was built against a re-measurement rather than against the survey.** With the prompt the code actually sends — not the survey's — `claude -p --model claude-haiku-4-5` took **3.08 s and 3.51 s** and `opencode run --pure` took **4.43 s**, both answering `{"action":"open_agent","params":{"agent":"tester"}}` correctly. The survey's 4.49–4.68 s was with `--output-format json`, which this does not ask for. **The fence held on every run**: `claude` wrapped its reply in ```` ```json ```` both times despite the prompt forbidding one, which is now an expectation the parser is written for rather than a tolerance. `opencode` returned a bare object with no fence — so neither *stdout is JSON* nor *stdout is a fenced block* is a property, and the reader tries the fence first (which is what stops a structured log line from being read as the answer) and falls back to scanning for the first `{` at which an answer parses.

**`opencode run` was measured and it works, which narrows this document's "not measured; best-effort until someone runs it".** The invocation exists (`opencode run [message..]`), `--pure` runs it without external plugins, and it answered correctly. What stays best-effort is the model: its `-m` flag takes `provider/model`, so a pin would have to name a provider this app cannot know the user configured, and the adapter takes the user's own default instead. The `claude` adapter does pin `claude-haiku-4-5`, which also stops a user's expensive default model deciding what an utterance costs.

**Open Questions 1 and 4 are answered above, in place, with their measurements.** Short forms: the login-shell PATH is captured **lazily and only on `NotFound`**, through `capture_login_shell_path` rather than `apply_login_shell_path` — which is `set_var`-based and unsound anywhere but single-threaded startup, a fact this document's own framing of the question had missed; and the remote backend is **tool-use with `strict: true`** on **`claude-haiku-4-5`**, at a 0.91 s median against the agent CLI's 3.1–4.7 s.

**The latency is produced here rather than left for M6 to invent**, which is this document's own mitigation for *slow enough to feel broken* made concrete. `VoiceResult` carries `resolve_ms` and `backend`, measured around the `resolve` call so it is the wall clock the user actually waited for — including a lazy PATH capture or a one-time schema compile, which are real waits and are not excused out of the number. `resolve_ms` is `None` for silence, which short-circuits before any backend call; rendering `0 ms` there would claim a measurement nobody took. The two travel together because they are only useful together: *4.2 s* is a complaint and *claude, 4.2 s* is a reason to switch backends.

**`ActivationMode` is now STATED rather than offered.** M4 rendered it as a one-option `<select>`, on the sound reasoning that the app has no other surface saying what the activation mode is. The field and that reasoning both stand; the control does not — a select that opens, shows one item and changes nothing on close implies a choice the user does not have. It is a line of text naming the mode until D4 adds the other two, at which point it becomes the third select and `VOICE_ACTIVATION_MODES` is already the list it will render from.

**No secret-reading IPC command was added, and that is a deliberate confirmation rather than an omission.** `desktop_secret_status`/`_store`/`_forget` exist and `load` does not; the remote backend reads the credential Rust-side, at call time, straight into a request header. Reading at call time rather than caching at construction means a key stored in Settings works on the next utterance instead of after a restart, and a keychain that fails is reported as *I could not find out* rather than as *no key stored* — M4's rule one layer up.

**Test shape, because M5's rule was that none of it may need a credential or the network.** The agent-CLI paths — fence, banner, plain JSON, malformed, empty, non-zero exit, missing binary, timeout — are driven by `/bin/sh` stubs written into a `tempfile::tempdir()`, which are real child processes with real pipes and exit codes and cost milliseconds. The timeout test records the child's pid and then asserts via `/proc` that it is no longer **running** (a signal probe would have passed on a zombie, asserting nothing on the one path it exists to check). The remote backend's tests are pure `Value`-in/`Value`-out over the request builder and the response parser, so the suite opens **no socket at all** rather than a loopback one. The transport itself belongs to M9's credentialed lane, and this document should not be read as claiming otherwise.

**Both backends were then driven end to end against the real thing, because a suite of stubs proves the code and not the feature.** Through the whole pipeline with the shipping prompt: the agent CLI took **4.30 s and 6.30 s** — slower than its own 3.1–4.4 s standalone runs, because the shipping prompt carries the annotated command list and the agent labels, and 6.3 s is the honest number to quote at a user; the keyed backend took **0.62–1.03 s**, matching its measurement. All three answered correctly, and the third case is the one worth naming: *"take me back to the deck"* **from the deck** produced `Unavailable` with the table's own hint, which is this document's flag-don't-filter trade working against a real model rather than against a stub — the model picked a command marked `callable: false` because that is what the user asked for, and the app explained the prerequisite instead of saying it did not understand.

One thing to carry into M6: an action outside the table now has an end-to-end proof, not only a stubbed one — the agent-CLI backend driven by a script answering `launch_missiles` produces `UnknownAction` and dispatches nothing. That is the case the original four-outcome list could not express, and it is what the pipeline's safety rests on whenever the backend is a print-mode CLI that constrains nothing.

### 2026-09-18 — M7's Rust half: the microphone and the transcription seam

The device, the bounded utterance, the resampler, the state machine, the `Transcriber` seam, one keyed remote backend and four Tauri commands. **No frontend**: M6 owns the panel and the button, and this stops at the IPC boundary. Nine things worth recording, four of which correct or narrow what this document said before them.

**`cpal` cost less than this document's accounting predicted, and the shape of the prediction was right.** The dependency-cost section said a Rust-side audio crate would add ALSA dev headers on Linux and nothing on macOS or Windows, and that is what happened. `cpal 0.18.2` declares `default = []` — no default feature content at all — so `jack` and `asio`, the two that would drag in a system library or a vendored SDK, stay off without having to be turned off. The whole lockfile delta is **16 entries**, of which only four build on Linux (`cpal`, `alsa`, `alsa-sys`, `dasp_sample`); the rest are the target-gated `objc2-*`/`coreaudio-rs`/`mach2` CoreAudio tree, the `windows`/`windows-*` WASAPI tree and `ndk-context`, none of which a Linux or CI build compiles. Measured on this branch, 16 cores: the four Linux crates plus the desktop crate rebuilt in **23.1 s** from cold, and warm the gates did not move.

**The three-file claim was wrong by one file, and the missing one is a release job rather than a gate.** This document said "three places would go red without an edit" — `tauri-deps/flake.nix` and the two `apt-get` blocks in `ci.yml`. There is a **fourth**: `.github/workflows/release.yml`'s `desktop-bundle` job installs its own compile set, and it is the job that produces the shipped `.deb`, `.AppImage` and `.dmg`. It would have gone red at the first tagged release after this landed rather than on the PR, which is the worst place to find it. A **fifth** file is not an install but is part of the same set and has to move with it: `scripts/devbox-check-gtk.sh`'s `MODULES` list, which asserts each module resolves under `/nix/store` — without `alsa` in it, issue #815's silent failure reopens through a second crate, since a `/usr/lib` answer links the crate against a library the Nix loader cannot find. `xtask/linkage-check/src/devbox_gtk_origin.rs` drives that script in the fast tier and still passes; it pins the assertion and not the module list.

**`devbox.json` itself needed no edit, and the reason is the flake.** The audio library reaches a devbox shell through the existing `path:tauri-deps#tauri-deps` entry, because `alsa-lib` went into that flake's `deps` list and the derivation freezes the transitive pkg-config closure. Verified rather than assumed: the rebuilt output carries `alsa.pc`, and `pkg-config --modversion alsa` against it answers `1.2.16.1` with `libdir` under `/nix/store`. The `nix` and `devbox` CI jobs were both checked and **neither builds the desktop crate** — the root `flake.nix` passes `cargoExtraArgs = "-p dot-agent-deck"` to both `buildDepsOnly` and `buildPackage`, and `scripts/devbox-smoke.sh` runs no `cargo build` at all — so neither needed anything beyond the flake entry they already share.

**Capture is Rust-side, as the survey recommended, and the trait is what makes it testable.** `cpal`'s own types cannot be stood in for and a CI runner has no microphone and no audio server, so `AudioSource` is a trait and every test drives `StubSource`. `CpalSource` is never constructed by a test and never touched at startup: `VoiceState::default()` builds one, and it opens no host and no device until the first `start`, so an app on a machine with no audio server launches normally and finds out at the first press — where the sentence for it already is.

**`cpal::Stream` is `!Send` on some hosts, which decided the threading.** It cannot be held in a `tauri::State` at all, so a dedicated thread owns the stream for its whole life and is told to let go by a channel hang-up. That makes the handle `Send` on every platform rather than on two of them, and its `Drop` joins the thread — so no callback can still be writing when the buffer is read.

**The length cap is enforced twice and both halves are load-bearing.** `MAX_UTTERANCE` is 30 s, which is whisper's own window and caps one utterance at 960 KB. `PcmSink` refuses to grow past it, which bounds the *allocation* on the data path; a timer spawned by `desktop_voice_start` drops the stream when it elapses, which releases the *device*. The first alone leaks a live microphone; the second alone trusts a timer. The timer carries a generation ticket, so a timer that outlived its own utterance closes nothing — record, stop, start again inside 30 s is an ordinary sequence, not a contrived one.

**Voice-activity detection was NOT built, and is explicitly deferred to D4** rather than left ambiguous. M7's brief made it optional; the activation mode that ships is toggle, exactly as `ActivationMode` declares. `Pcm16::is_silent` exists and is **not** VAD and must not be read as one: it looks at a finished buffer to skip a backend call that could not produce words, and decides nothing about when an utterance ended.

**Open Question 5 is answered, and the answer now covers audio.** (**Corrected 2026-09-19** — the unqualified claim below was false for the agent-CLI backend's child process, and the honest two-part version is in Open Question 5 and in the 2026-09-19 work-log entry. What is written here is true of *this app* and was never true of everything the app hands a prompt to.) No part of an utterance is persisted by this app: no buffer is written to disk, no log line carries a transcript or a sample at any level, and nothing in the capture or transcription path calls `eprintln!`. `Pcm16`'s `Debug` is hand-written and prints a duration and no samples, the same closure `Transcript` already applies to the text, so a derived `{:?}` on a container prints neither. The IPC shape carries the same rule one seam earlier: **`desktop_voice_stop` transcribes and returns text**, rather than handing the webview a buffer — the alternative would put a base64 copy of up to 960 KB of the user's voice across the bridge for no reason. M10 owes this to the docs.

**The transcription backend is hosted whisper, and the reason is D1 rather than the price.** `whisper-1` at `https://api.openai.com/v1/audio/transcriptions`. Pinning the hosted backend to the same model family as the deferred local one makes D1 a swap rather than a behaviour change: the phrases that work today go on working when the credential goes away. A cheaper or more accurate hosted model would buy a fraction of a cent an utterance and spend that property. The multipart body is assembled by hand rather than through `reqwest`'s `multipart` feature — that feature would add `mime_guess` and a streaming body to a spec this crate copies verbatim from the root package's, so cargo would feature-unify them and `src/version.rs` would start carrying a multipart encoder it has no use for. What it buys is forty lines for a body with two fields and one file.

**`Off` is a first-class variant, and the tests assert the sentence rather than only the type.** `TranscriptionOutcome` has three variants, not two: `Heard`, `NotConfigured` and `Failed`. `handle_audio` renders *Nothing to listen with — …* for the second and *Could not turn that into text (…)* for the third, and a test asserts the not-configured sentence contains neither "Could not" nor anything else that reads as a break. `transcribe_ms` is `None` on that path, for `VoiceResult::resolve_ms`'s reason: nothing was transcribed, and rendering the microseconds it took to say so would claim a measurement nobody made.

**What M6 calls.** `desktop_voice_status()` to decide whether to offer a microphone at all — it answers `{ state, capturedMs, maxMs, capped, available, backend }`, where `available` is false exactly when `[voice] transcription` is `off`; `desktop_voice_start()` on the first press, answering the same shape or rejecting with the not-configured sentence; `desktop_voice_stop()` on the second, answering `VoiceTranscription { outcome, transcribeMs, backend, audioMs }`; and `desktop_voice_cancel()` when the panel closes, which is idempotent and never refused. Between start and stop, `status` is how the surface learns the cap ended the recording — from the user's side the microphone simply stopped, and a panel that did not know why would go on rendering *listening…* over a closed device.

**What is not verified here: everything macOS.** `desktop/src-tauri/Info.plist` carries `NSMicrophoneUsageDescription` and `tauri.bundle.conf.json` names it through `bundle.macOS.infoPlist` rather than relying on Tauri's implicit lookup for a file of that name. The plist is well-formed and the reference is explicit; that no macOS host was available to run a bundle through it is stated rather than implied, and Open Question 2 — whether a `tauri dev` window needs the same key, where a denial would be silent — is still open.

### 2026-09-19 — M2, M3, and the live-state defect M9 found

**M2 landed** as `desktop/src/lib/voiceActions.ts` with **fourteen** entries, and the rail, the command palette, the agent tile's open/close pair and the overview's row all dispatch through it. Nothing a user can see changed: the overlay setters keep today's non-exclusive behaviour, `onNavigate`/`onCloseAgent` stay optional props so a deck mounted without them renders exactly as before, and the numeric focus keys were routed through the same `focusAgent` entry the palette uses so one capability keeps one dispatch path. Four entries carry `voice: true`; the other ten carry a written `no_voice` reason.

**Two rulings this document did not anticipate, both about the palette's GENERATED entries.** A guard cannot enumerate one registry key per live agent, because those do not exist at compile time — so `focusAgent` **registers the kind and takes the agent as a parameter**, exactly as `openAgent` already does, and the palette renders N items that all dispatch through the one entry. And `Advance fixture` is classified `no_voice` as a **fixture-mode debug affordance**: it exists only while the app is driven by `FixtureDeckBridge`, so it is not a capability of a shipped build and has no user to speak to.

**One correction to this document's own count.** It calls `commandItems` "seven static entries plus per-agent and fixture-mode ones". It is **six** static plus **three** generated groups: `Message coordinator…` is conditional on a live coordinator existing, so it belongs with the generated half rather than the static one. The rail is seven buttons, five of which open an overlay — which is right here, and was stale in `DeckShell`'s own doc comment ("five of the rail's six buttons"), fixed in the same commit.

**M3 landed** as `xtask/linkage-check` check **13**, taking the next number rather than renumbering anything, in both the shapes this document called for: a live numbered rule plus a `#[cfg(test)]` module with a planted-bad-input test for each of the five assertions. The classification test plants all five ways an entry can be misclassified, including the two cross-table ones this document did not name — a `voice: true` no row backs, and a `no_voice` reason a row has **overtaken**, which is what will go red the moment M8 adds `open_settings` to the table without flipping the entry. `toml_edit` joins the xtask crate's dependencies so the rule parses `commands.toml` with the same library `table.rs` parses it with; no new crate enters `Cargo.lock`.

**The residual is stated at the failure itself**, which is this document's own mitigation made concrete: the rule sentence quoted in every finding says that a control wired with a bare `onClick` is invisible to it and that a green check 13 is not evidence that no capability was forgotten. It is also in [`docs/develop/desktop-gui.md`](../docs/develop/desktop-gui.md), ahead of M10, because the misreading is the risk rather than the rule.

**M9's real-model run found a defect in the prompt, and it is not a fixture's problem.** 16 of 17 phrases passed against the real `claude` backend; the failure was `open-agent-by-state` — *"show me the one that's stuck"* resolving to `action=none`. That phrase is one of the three examples issue #802 uses to argue this feature is not a gimmick, and the app could not serve it: `voice::prompt::state` serialised `agents_on_screen` as bare display strings, so nothing in the prompt said which agent was stuck.

**Each agent is now an object**: `label` and `status` always, `role` and `cli` when the daemon reported them and they are not already the label, `tool` when it reported an active tool. Everything is copied through verbatim — `status` in the daemon's own vocabulary rather than restated — and every optional field is **omitted** when the daemon supplied nothing, never placeholdered, which is PRD #745's rule about fabricated fields applied to a prompt. `role` and `cli` earn their place on a second argument worth recording: the app's own `spoken_names` already matches a reference against both, so withholding them left the resolver able to match a name the model was never shown. `TOOL_INSTRUCTIONS` gains the paragraph that makes the block usable — what the status words mean, and that a reference made by **state** is answered with the agent's `label` while a reference made by **name** is still answered with the user's own words.

**The cheap fix was rejected**: adding "stuck" to `open_agent.description` can coerce the action id while leaving the pipeline unable to say *which* agent was meant, turning a clean no-match into a later `ParamUnresolved`. **`last_user_prompt` was rejected too**, and it is the best disambiguator in the DTO — it is unbounded operator-written prose, and `cli_prompt` is careful that the utterance is the one untrusted span, labelled and last. Ids stay out for the reason already recorded. Open Question 5 is untouched: agent state is not an utterance, and nothing here is written or logged.

**A test seam came with it, because an integration test could not construct a rich agent at all.** `mod dto` is private, so `DesktopTab` is not nameable outside the crate and `tests/voice_phrase_fixtures.rs` could not write a `DesktopAgent` literal however `pub` the struct is; the `#[cfg(test)]` fixtures module was invisible to it for the same reason. It is now `#[doc(hidden)] pub mod voice::test_support`, with the in-crate `fixtures` alias kept so the unit tests' imports did not move. **Deliberately not behind a cargo feature** — `Cargo.toml`'s `cpal` entry already states why at length, and it is this repository's #407/#436/#502 hole class: code behind a feature nobody's gate enables is type-checked by nothing.

**What is still owed.** `pnpm test` is green apart from M6's 25 panel cases, which are the tester's failing-by-design set and the next milestone's to satisfy. The M9 fixture itself still has to plant agents through the new seam and be re-run against the real backend — the prompt can now carry the state, and only a credentialed run says whether the model uses it.

### 2026-09-19 — the landed-work security audit, and the six findings it returned

An audit of the landed Rust work returned **two blockers and four should-fixes**, all six accepted without dispute, and one scope decision was taken alongside them. They are fixed before M6, because a panel over an unsafe backend is worth nothing.

**The `opencode` intent backend is WITHDRAWN, and this is the decision rather than a deletion.** The agent-CLI backend hands a general-purpose coding agent a prompt built partly from input this app does not control, so the child has to be containable: no tools, no hooks, no MCP servers, no skills, no project or user settings, no session written to disk. `claude` has a flag for every one of those and they are now all passed. `opencode run` has no no-tools equivalent and no no-persistence option, and the auditor confirmed locally that it writes resumable sessions holding the utterance. Shipping an uncontainable subprocess executor in a feature that is visible by default is not an acceptable trade, so the variant, its adapter, its settings token and its panel choice are gone. The cost to a user who had picked it is **one re-pick**: `IntentBackend`'s folding deserializer loads a stale `intent = "opencode"` as the default and the rest of the document survives, which is asserted by `the_withdrawn_opencode_intent_backend_folds_to_the_default`. `ALLOWED_FIELD_TYPES`' written reason for `IntentBackend` now says `claude | remote` and says why the third went.

**Blocker 1 — the agent-CLI child was neither contained nor pinned.** It ran with tools, project/user settings, hooks, MCP servers, skills and session persistence all enabled, from the app's own working directory, on a PATH that accepts empty and relative entries, with almost the whole environment inherited. The action validator constrains only the JSON printed *after* the CLI exits, so anything the model did while producing that output had already happened — which makes `prompt.rs`'s claim that a successful injection buys one navigation false, and that comment is corrected rather than only the code. The threat is not limited to what the user says: `prompt::state` puts **agent labels from the daemon** in the prompt, and under #741 that daemon can be remote, so the whole prompt is treated as untrusted.

Every flag was read off `claude --help` on this box (`claude 2.1.277`) rather than taken from the audit's list, and the argv is pinned by a test: `--tools ""` (disable all built-in tools), `--safe-mode` (all customisations — CLAUDE.md, skills, plugins, hooks, MCP servers, custom commands and agents — disabled), `--restricted` (removes the code-running tools and ignores user/project/local settings files), `--strict-mcp-config` (only MCP servers from `--mcp-config`, and none is passed), `--setting-sources ""` (the settings sources named as the empty set), `--permission-prompts none` (anything that would prompt is denied automatically — the fail-closed answer), `--no-session-persistence`. Order is load-bearing: `--tools` is variadic, so the prompt goes last and every flag precedes it. **`--bare` is deliberately NOT used** — it restricts Anthropic auth to `ANTHROPIC_API_KEY`/`apiKeyHelper` and never reads OAuth or the keychain, which would break the one property this backend exists for.

Beside the flags: the executable is resolved to an **absolute** path from a PATH with empty and relative components rejected, the child runs in an app-owned empty directory under `state_dir()` rather than inheriting the app's, and the environment is an allowlist rather than an inheritance — what survives is what the CLI needs to find its own credentials and reach the API, `AWS_*` included because that *is* the Bedrock route's authentication.

**Blocker 2 — a cancel could lose a race with the device opening and resurrect recording.** `CaptureSession::start` checked idle, dropped the lock and blocked in `AudioSource::start`; a `cancel` in that window set the session idle, and the returning open then saw a state that still accepts a start and installed the stream anyway. User-visible: press the microphone, close the panel while the OS permission prompt is up, watch the cancel succeed, and the microphone records with the panel closed until the thirty-second cap with the buffer waiting for the next opening. `start` now **reserves a generation under the lock** and installs only if that exact reservation is still held; otherwise it drops the returned stream, which is what closes the device. A reservation rather than an `Opening` state deliberately: `CaptureState` is serialised to the webview, and a token the panel has never heard of would arrive on any status poll made during the open.

**Should-fix 3 — the no-persistence claim was false as written.** Fixed by `--no-session-persistence` and then re-stated honestly in all five places it appeared: `voice/mod.rs`'s `Transcript` doc, `voice/capture.rs`, `voice/transcribe.rs`, Open Question 5 above, and the M7 work-log entry. The claim is now *this app writes none, and where it hands one to a child it instructs that child not to* — which is weaker than the unqualified version, and true. [`docs/develop/desktop-gui.md`](../docs/develop/desktop-gui.md) carries the same two halves.

**Should-fix 4 — `x-api-key` survived a cross-origin redirect.** `reqwest` 0.13.5 strips `Authorization`, `Cookie`, `Cookie2`, `Proxy-Authorization` and `WWW-Authenticate` on an origin change and **not** a custom header, so a redirect could carry the user's Anthropic key to an arbitrary HTTPS host — whose error body the API-error path would then have quoted into the webview. Both keyed backends now build their client through `voice/http.rs` with `redirect::Policy::none()`, and a client that cannot be built is reported in a sentence rather than falling back to a permissive one. The transcription backend gets the same policy for a different reason: it authenticates with `Authorization`, which *is* stripped, but a 307 or 308 re-sends the **body**, and the body there is the user's voice.

**Should-fix 5 — a timeout killed the child and not its descendants.** `kill_on_drop(true)` signals one pid, so a tool, hook or shell below the CLI outlived the twenty-second bound with this app's descriptors still open. Each invocation is now its own Unix process group and a timeout `killpg`s it, then waits for the reap under a two-second bound. The old test overstated itself — it checked the stub's own pid and not its `sleep 120` grandchild — and is renamed and extended to assert both; with the process group removed it fails on the grandchild, which is how the assertion is known to be load-bearing. **Windows is weaker and is written down as such**: it walks the tree with `taskkill /T /F` rather than a Job Object, which misses a descendant that has re-parented itself. The faithful mechanism already exists in `platform::proc::AgentProcessGroup`, but its terminate half is private and shaped around a `portable_pty::Child`, and the alternative was fresh `windows-sys` FFI that `build-windows` would compile and nothing here can execute.

**Should-fix 6 — output was fully buffered before bounded parsing.** `wait_with_output` drained all of stdout before the 64 KiB scan limit applied, and both HTTP backends called `Response::json`, which collects the whole body first; a timeout bounds elapsed time and not bytes. The CLI's stdout is now read through an explicit cap derived from `MAX_SCAN_BYTES` — reading more than will ever be scanned is allocation with nothing on the other end of it — and both HTTP bodies through `voice::http::capped_body`, on the success and non-success paths alike, since the error detail is quoted out of that same body. Exact-limit and over-limit are both covered, and the HTTP tests build a real `reqwest::Response` from an `http::Response` so the suite still **opens no socket**.

**What the audit confirmed sound, so a later reader knows what not to undo:** the secret boundary (no `Serialize`/`Display` on `Secret`, no load command, a closed `SecretId`, `spawn_blocking`, main-webview enforcement); the utterance passed as one argv element, so shell metacharacters cannot become syntax; a non-zero exit rejected even with parseable stdout; the model-output/dispatch boundary for the scoped navigation commands; the multipart envelope's generated boundary and constant fields; the thirty-second audio cap and the redacted `Debug` impls; and that no merge-blocking test opens a socket.

### 2026-09-19 — M6: the voice surface

The panel, the trigger, the microphone controls, the report and its Undo — plus the one Tauri command the surface needed and did not have. `pnpm test` is **667/667** and `pnpm test:browser` is **140/140**; the tester's 25 vitest cases and 4 Playwright specs (×2 engines) were written before the panel existed and none of them was edited.

**`desktop_voice_resolve` was missing, and M6 could not be a frontend-only milestone without it.** M7 shipped the four `desktop_voice_*` capture commands, but nothing resolved an utterance over the IPC boundary — `handle_utterance` had no caller outside the Rust tests. So the panel would have shipped as a control that works in the browser fixture and does nothing in the app it is for. The command is thin by design: it bounds the utterance (2 KiB — thirty seconds of speech is around 700 characters, and the string becomes part of a model prompt and an argv element), reads the intent backend per call so a settings change takes effect on the next utterance rather than after a restart, fetches the **selected deck's** snapshot for param resolution, and returns the outcome. It runs nothing: a `Dispatch` names a registry entry and the webview dispatches it.

**The screen is declared rather than passed, and the tester's contract is what forced the shape — for the better.** `resolveVoice` is pinned to exactly one argument (`expect(resolveVoice).toHaveBeenCalledWith(utterance)` fails on a second), which rules out `resolveVoice(utterance, screen, agents)`. What replaced it is more coherent than what was planned: an utterance is resolved against live state read **where that state already lives** — the agent list Rust-side from the deck's own snapshot, the table from the binary it is embedded in — and the mounted screen is the one piece that exists *only* in the webview, as `DeckShell`'s `useState<DeckView>`. So it is the one piece the webview states, through `declareVoiceScreen(screen)`, called synchronously immediately before each resolve. Both bridges hold it and neither acts on it. Declaring from an effect was rejected: a declaration that lagged a navigation would validate the next utterance against the screen the user just left, which is the `unavailable` outcome misfiring in the direction nobody notices.

**The panel is mounted above the deck/overview switch, and `DeckShell`'s return had to be restructured for it.** It returned the screen directly on the deck path and a fragment on the overview path, so a panel added to both would have been a different root type on each and remounted — taking the report and the Undo with it on the exact navigation they describe. It now returns one fragment on both paths, screen at index 0 and panel at index 1, and React keeps the panel's fiber while the screen below it is replaced.

**The registry got a generic dispatcher rather than a switch**, `dispatchVoiceAction(invoke, context, target)`. A `switch` with an arm per action id would type-check and would be a second list of the ids — which breaks M8 before M8 starts, since the one-file promise is that voice-enabling a capability is a row in `commands.toml`. One target object carries everything any entry needs and each entry reads the members it declared. It returns `false` for an `invoke` naming no entry: unreachable, because rule 13 fails the build on one, and there so the surface can say the command did not run rather than report a navigation nothing performed.

**M8's next row needed one thing M6 did not build, and it is built now — before M8 rather than by watching a command throw.** The dispatch context the shell offered was `navigate` plus `closeAgentView`, which is every `voice: true` entry M6 shipped. `openSettings` takes `openOverlay`, and the overlay booleans live in `DeckSurface`, which is unmounted while the overview is up; the voice dispatch is built one level up in `DeckShell`, the only component mounted for every screen. So giving that entry a row was a one-file change that type-checked, passed rule 13 — the guard asks whether an `invoke` names an entry, never whether the host can serve the context that entry reads — and then **threw at the call**, with `TypeError: context.openOverlay is not a function` rendered by the voice surface above the report sentence. That is a worse failure than "it does not work": the app's central property is that it renders every sentence it shows, and a JavaScript engine's message became user-facing prose.

**Two halves, and both were needed.** `DeckSurface` now publishes the context it can serve **upward**, into a `VoiceContextChannel` the shell holds — a mutable slot rather than a callback, because the context is rebuilt on every render (it closes over that render's setters) so a host keeping a copy would dispatch through stale closures, and because the shell needs `undefined` the moment the deck unmounts. The shell spreads that in and puts its own two members last, so `navigate` and `closeAgentView` are the same navigations whether or not a deck happens to be mounted. And `dispatchVoiceAction` now **refuses before it calls**: each registry entry declares the context members its `run` requires as `needs`, and an entry the host cannot serve answers `false`, which becomes the existing `undefined` dispatch result and `NOTHING_DISPATCHED` in the report. Checking rather than catching is deliberate — a caught exception message rendered at the user breaks the same property a thrown one does.

`needs` is read at runtime and `Pick<VoiceActionContext, …>` is not, so the two could drift; the `NeedsCoversRun` mapped type is the compile-time proof that they do not, mutation-tested by emptying one entry's list, which fails `tsc --noEmit` naming that entry. It refuses only the dangerous direction: an over-declared `needs` makes dispatch stricter than necessary, which is a refusal rather than a throw. An optional member read through `?.` — `openAgent`'s `selectAgent` — is deliberately not listed, so the overview is not refused a command it can serve.

**One consequence the row exposes, closed with it.** Not every command is a navigation any more, so the shell's *Undo* — which means "put the screen back" — would have sat under `Opening settings.` doing visibly nothing. The navigation is now **observed** rather than assumed: the two view-moving members are wrapped at the dispatch, and the Undo is offered only if one was actually called. That uses a distinction `VoiceControlPanel.onDispatch` already drew — an object with no `undo` means *it ran and there is nothing to reverse*, which is not the same answer as `undefined` — and needs no second list beside the registry.

**Undo is ten seconds.** The test pins only an upper bound (gone after a minute); ten seconds is long enough to react to a navigation you did not mean and short enough that it never becomes a second permanent navigation control in the report. The destination is read off the view at dispatch time, the same choice `closeAgent` already makes, because there is no history to be wrong about.

**The cap is observed and NOT acted on.** Between a start and a stop the panel polls `desktop_voice_status` once a second, and `capped` is the only field it reacts to — a status that merely disagrees about the state can be a beat behind a start just made, and tearing down a live recording on that would be worse than the thing being fixed. On `capped` the panel stops presenting itself as listening and offers **Send the recording** or **Discard it**; it deliberately does not call `stop` for the user, because a voice command that ran thirty seconds is almost certainly a microphone somebody left open and transcribing it would spend a paid call and its latency on that accident every time.

**The fixture bridge became the whole backend for the browser tier**, in `data/fixture.ts` beside the snapshots rather than in the panel. That placement is the point: a panel carrying its own vocabulary would be a second resolver, and the property the pipeline rests on — the app renders every sentence once, from the table — would be gone the moment the panel phrased in fixture mode what Rust phrases in live mode. Matching is trim-plus-lowercase equality, which is also what keeps the no-match path reachable: `Go, BACK to "Deck"?!` has to resolve to nothing, and any substring rule would have it matching the deck phrases. Three commands rather than four: `open_agent` is the one left out, since it would need a second param resolver of the fixture's own. `close_agent_view` was the fourth until the voice surface could be reached on the `agent` screen at all — see the residual below, which is now closed.

**One residual, and it was a real hole rather than a rough edge: `close_agent_view` could not be spoken in a shipped build.** While an agent pane is open it is modal, and `useInertBackground` marked every element that is not one of its ancestors `inert` — including the voice surface, because the panel is the screen's sibling. So the row whose `screens` column is `["agent"]`, the one this document argues is needed so that screen has a callable command at all, was unreachable. Nothing was silently broken (the pane's own Close and `Escape` both worked) and no test covered it either way. **It is closed now, by the second of the two fixes named above**, and the cost was worse than "unreachable" suggested: `inert` removes an element from hit testing as well as from the tab order, so the browser tier's click on the trigger was intercepted by the pane's own footer. The hook skips elements carrying `VOICE_PEER_PROPS` — the trigger and the dialog backdrop, an allow-list of one marker rather than a rule about roles — and `AgentPaneDeckIdentity.test.tsx`'s assertion moved from "nothing outside the pane is reachable" to an **equality** against that one trigger, so it stays strict: any other control escaping an open pane still fails it. The fixture vocabulary gained the `close_agent_view` row it had no reason to carry before, and `e2e/voice-control.spec.ts` drives the whole path in both engines.

**One tester-authored leftover, reported and not touched.** `VoiceControlPanel.test.tsx:15` declares `type VoiceBackend = "claude" | "opencode" | "remote" | "stub"`, and `opencode` was withdrawn by the security audit above. It is a harmless superset — the type is only used to build fixtures — and editing a test to tidy an implementation's vocabulary is the wrong direction.

### 2026-09-19 — the two review passes, and the twelve findings they returned

Two reviews — one over M1/M2/M3, one over the six security fixes — returned twelve findings between them. None was a blocker, none was disputed, and the second review confirmed all six security fixes genuinely closed with no P1 or P2. Two wanted code and the rest were corrections to prose that had drifted wider than the thing it described. What follows is what each one cost and what now holds it.

**A placeholder the table accepted did not render, because two scanners over one syntax disagreed.** `table::placeholders` trims a placeholder name before checking it against the row's declared params, so a report reading `Opening { agent }.` parses; `outcome::report` looked the name up byte-exactly, found no param called `" agent "`, and printed the braces at the user on a dispatch that had otherwise succeeded — param supplied, resolved, dispatch fine. The render now trims to match, and the absence of a test using the spaced spelling is precisely how the two were allowed to drift, so `voice_outcome_a_spaced_placeholder_parses_and_renders` drives all four spellings through `CommandTable::parse` and the whole pipeline rather than through a hand-built row: what made the bug possible was that the *table* accepts the spelling, which a hand-built row cannot show. `report`'s doc comment argued from the wrong premise (that the missing-param refusal catches every unreplaced `{…}`) and now argues from the agreement itself.

**Every foreign string in a sentence is scrubbed now, and the transcript is the one exception.** `outcome.rs`'s module doc claimed a model-supplied param and a backend's failure detail "both go through `safe_message`"; only the detail did. Three strings were not: the model's param in `unresolved_phrase`, and in `ambiguous_phrase` both that param and the daemon-supplied agent **labels** it lists. The decision was to scrub rather than to narrow the doc — the doc described the behaviour we want, the asymmetry was arbitrary (identically-sourced strings treated differently by route of arrival), and it is defence in depth for text bound for a DOM node with no frontend seam yet to lean on. The successful `report` sentence interpolates a label by the same route, so it is scrubbed too; leaving it would have meant fixing an over-claiming comment by writing an exception list, which is the shape of defect being cleaned up rather than a fix for it. **The transcript stays verbatim**, which is a deliberate exception rather than an oversight: seeing exactly what was heard is what turns a mis-transcription into a correction, and the new test asserts the exception as well as the rule so a later sweep cannot tidy it away.

**`display_label`'s positional fallback is not a name any agent answers to, and the invariant is now pinned rather than the gap papered over.** `TOOL_INSTRUCTIONS` tells the model to answer a by-state reference with the agent's label, so a label outside `spoken_names` refuses a model that did exactly as instructed. Only `dto::map_agent` flooring `agent_type` at `"none"` keeps `Agent <n>` unreachable. **Teaching `spoken_names` to match positionally was rejected**: "agent 3" is the agent's place in a snapshot whose order the user neither controls nor sees change, so resolving by it is a worse behaviour than the refusal it replaces. A test asserts the invariant over the shapes the app actually produces — including the `"none"` floor — so a change that lets `agent_type` be genuinely empty goes red there rather than in a user's refusal.

**The `params` schema description said "Verbatim from the utterance", which contradicts the instruction beside it.** Both reach the model on the tool_schema path, and for *"the one that's stuck"* the model is told to emit the agent's label — which is not verbatim. Answering with the label is the correct behaviour, so the weaker line was the one to narrow, in both `schema.rs` and `remote.rs`.

**"One capability keeps one dispatch path" was false when M2's commit body said it, and the code was not re-routed to make it true.** The agent tile's own `onSelect` calls `setSelectedAgentId` directly, bypassing `VOICE_ACTIONS.focusAgent`, and four more `no_voice` capabilities have second in-panel `setState` paths: `toggleEvidenceDrawer`, `openWorkflowOrder`, `openProjects`, `openAgentProfiles`. Re-routing eight UI call sites late in this PR is regression risk for no functional gain — none of the five is voice-reachable, so the load-bearing property (voice has exactly one execution path and acquires no second one) holds unchanged. CLAUDE.md rule 17's instruction in exactly this situation is to write the narrower true claim, so `voiceActions.ts` now states the seam as the rail, the palette and the four voice-reachable entries, **and lists the five known second paths** so they are a known residual rather than a finding waiting to be rediscovered. If a later PRD gives any of them a row, closing its second path is that PRD's work, because a second path to a voice-reachable capability is behaviour voice cannot see. The same file's "none of the **thirteen** registered Tauri commands performs a navigation" lost its number for the same reason: `generate_handler!` has grown past thirteen and the substantive claim never depended on the count. (The thirteen enumerated in this document's *Capabilities* section above, with their line numbers, are a dated design-time measurement and are left as one — that section says so itself. The line range it quotes has already moved.)

**`open_deck` and `close_agent_view` do not disambiguate by prose, and that is the design working.** Both descriptions say "go back"; what separates them is `screens` surfacing as the `callable` flag, and a real-model fixture proves the model picks correctly. One tool with the full list, per-entry flags and validation that refuses a non-callable pick is the whole shape this PRD chose over filtering. It is recorded here because **the prose leans on `screens` to do the separating** — a future change to the screen set, or to either row's `screens` column, is a change to how those two rows are told apart. *(Superseded on 2026-09-19 — the live run below turned this observation into a failing fixture. The reading that survives is the last sentence; the first is now false, because both descriptions name the condition explicitly.)*

**The agent-CLI backend's fallback working directory was the shared system temp root.** `app_owned_cwd` fell back to `std::env::temp_dir()` on a `create_dir_all` failure — typically `/tmp`, which any local user can populate with a `CLAUDE.md` or a `.claude/`. Defence in depth rather than a hole (`--safe-mode` plus `--setting-sources ""` plus `--restricted` mean project configuration is not loaded), but the doc argued "a temp root is not a project", which is true of the directory and false of what can be sitting in it. It now falls back to a **named subdirectory** of that root, created by the app — narrower rather than exclusive, and the doc says so: another local user can create that name first, and what the change buys is that a plant has to target this exact path rather than merely exist in `/tmp`. If even that cannot be created the path is returned anyway and the spawn fails with the directory missing, which is the fail-closed end of it. The choosing half is split out as a pure function over its two roots, so a test drives the fallback against two `tempfile::tempdir()`s and touches neither the real state directory nor `/tmp`.

**The `kill_on_drop` backstop signalled one pid, so a dropped resolve leaked descendants — the audit's own finding, on a rarer path.** The module doc said `kill_on_drop(true)` "covers every *other* drop path"; it covers the *direct child* on those paths, never the group, and only the timeout and flood paths called `terminate_group`. So a resolve future dropped mid-spawn — app shutdown, a cancelled utterance — left a tool or shell the CLI had started running with the app's descriptors open. Having just fixed this exact class on the timeout path, documenting the remainder would have been inconsistent, so it is **closed**: a `ChildGroup` guard owns the child, and its `Drop` performs the group kill. That works in a destructor because the group kill is synchronous — `killpg(2)` on Unix, a fire-and-forget `taskkill` spawn on Windows — and the direct-child reap stays with `kill_on_drop`, which runs immediately afterwards as the guard's fields drop. The guard disarms the moment the child is reaped, because `killpg` on a recycled pgid is the one thing it must never do. `voice_agent_cli_tears_the_group_down_when_the_resolve_is_dropped` is the only test that reaches that path — its inner timeout is two minutes, so nothing in it can reach `spawn`'s own teardown — and it was confirmed load-bearing by neutering the `Drop` body, which leaves the grandchild alive and fails on exactly that assertion.

**Three smaller corrections in the same file, each a claim rather than a behaviour.** `resolve_on_path` does not reject `..` inside an absolute component (`/usr/bin/../../tmp` satisfies `is_absolute()`); the ruling was to **document, not normalise** — reaching it means already controlling the user's `PATH`, which is a stronger primitive than anything this recovers, and canonicalising would resolve symlinks and break the version-manager layouts that legitimately put one there. The Windows `CREATE_NEW_PROCESS_GROUP` comment claimed the flag gives teardown "a tree to walk"; `taskkill /T` walks the PPID tree regardless, and what the flag actually governs is Ctrl-C/Break delivery. And the flood decision — an answer inside the first 64 KiB followed by a flood is rejected **as a flood** — is now stated where the cap is, rather than left to be inferred: parsing what arrived first would reward a child for talking past its bound.

**`CaptureSession::start` can return `Ok` with an `Idle` status.** The status is read after the lock is released, so a `cancel` landing in that window returns `Ok((Idle, ticket))` with a ticket already a generation behind. Benign — the cancel released the device and the stale ticket makes `cap_reached` a no-op — but a caller reading `Ok` as *"it is recording"* would be wrong, so it is documented at the method. Not closed by re-reading under the lock, which would return a status contradicting the cancel the user just made.

**The registry guard's masker does not handle regex literals, and that is now written down beside the `< >` generic gap it joins.** `mask` knows `//`, `/* */` and the three quote kinds; a `/[},]/` in a scanned range would have its `}` and `,` read as structure, and a `/` before a `*` could flip it into comment state. No current impact — no regex literal or bare division appears in a code position of either scanned file — so this is a documented constraint rather than a checked property: a regex literal must not appear in a scanned range, and the remedy if one ever must is to teach the function the state rather than to work around it at the call site.

**Two recorded as no-change so they are not re-raised.** The `AWS_`/`GOOGLE_`/`GCLOUD_`/`CLOUDSDK_` env prefixes are broad and stay: they admit endpoint, region and profile variables as well as credentials, but none is injectable from the utterance, the child is `claude` itself and legitimately needs them, and a prefix is narrower in practice than a stale enumerated list that silently breaks Bedrock or Vertex. And `AgentOverview`'s `openAgent` built an inline context where its siblings used the memoised one — harmless, two spellings of one thing, now one.
### 2026-09-19 — M8: the one-file claim measured, and found wrong by six files

**M6's work-log entry above was its own `docs:` commit, so M8 had no entry until now; this is it, written with M10.**

The next command is `open_settings` — one row in `commands.toml`, and the `openSettings` registry entry flipped from a `no_voice` reason to `voice: true`. No production logic changed: the entry's `run` already opened the Settings overlay for the rail and the palette, and the row exposes it rather than defining it. That half of the acceptance criterion held exactly as designed.

**The count did not. It took seven files, not “the table and its fixtures”**, and the accounting is in [M8](#milestones) rather than repeated here. The short version: five of the seven are test-only, every Rust hunk is inside a `mod tests`, and the edits exist because nine unit tests pin the shipped row set **by value** — which `table.rs` chose deliberately, so that a test could not pass while describing a table the build does not ship. **The right correction was to the sentence, not to the tests.** Loosening those nine into shape-assertions would have bought a one-file diff and given up the property they exist for, which is the trade this document would have been making without noticing, had the claim been left standing and the tests quietly relaxed to fit it.

So the claim is now stated as substance rather than as a number, in [Solution Overview](#solution-overview), [Success Criteria](#success-criteria), [In Scope](#in-scope), the [typed-enum argument](#the-guard-where-it-goes-and-what-number-it-takes), [Open Question 4](#open-questions) and the [risk](#risks) — and in `table.rs`, `remote.rs`, `voiceActions.ts`, `App.tsx`, `commands.toml`, `voice_command_registry.rs` and `docs/develop/desktop-gui.md`, because CLAUDE.md rule 17 covers comments in code and an over-claiming comment is read as a property by whoever arrives next. The dated work-log entries above keep the wording they were written with; they are history, and the M6 entry's reasoning about the generic dispatcher was correct on the substance whatever the count.

**`screens = ["deck"]`, correcting this document's earlier spec of `["deck", "overview"]`.** The Settings overlay is reachable only from the deck rail, so listing `overview` would let voice open a surface no click can reach from that screen — voice becoming a second control surface with capabilities the first lacks, which is the one thing this PRD forbids.

**One file was deliberately not touched and is the tester's:** `phrase_fixtures.toml`. Six `pending_action = true` markers stop being true the moment the row exists, and three fixtures expecting dispatch on the *overview* have to flip to `unavailable` under the `["deck"]` ruling. One file, one owner.

### 2026-09-19 — M10: the docs, the fragment, and the three things nothing proves

**`docs/develop/desktop-gui.md`** gained what a contributor needs in order to add a voice command without reading this PRD: the six-stage pipeline and why capture and every network hop are Rust-side, the command table and its three consumers, that `description` is **a prompt the model picks on** rather than documentation, the measured cost of a row, the two intent backends and the `[voice]` settings that pick them, the `SecretStore` seam and why the settings document holds no field for a credential at all — not even the boolean the rule would allow — and a six-step checklist ending in the rule that decides `screens`: never wider than what a click can already reach.

**The guard's section already said what it cannot see and that was left alone**, because it was right: check 13 proves every registry *entry* is classified and does not prove the registry is *complete*, with 80 `onClick` sites outside its sight. What was added beside it is the cost of a row, so the next reader meets both halves — what the guard catches, and what a row will charge them.

**Three properties are now stated as unverified in their own section, rather than left for a green check to imply.** That the OS grants the microphone on any platform; that a real utterance transcribes end to end; and that the agent-CLI backend resolves from a Finder-shaped PATH — Open Question 1 verified the *mechanism* and never the *platform*, which is a distinction that reads as a technicality right up until someone treats the answered question as covering a Mac. There is no `tauri-driver` here ([#953](https://github.com/vfarcic/dot-agent-deck/issues/953)) and no macOS host, and `build-macos` is the only thing that will ever touch `Info.plist` or the bundle overlay. The same three are summarised in that document's **Current milestone limits**, which is where someone scanning for what to distrust actually looks.

**The manual smoke walk is seven steps and leads with the two no test can replace** — the typed path on a machine with no credential at all, and the two refusal paths, because “it names the prerequisite” and “it shows you your transcript” are the design's whole claim to not being a second control surface. It ends by telling the reader to run the phrase fixtures **with `DOT_AGENT_DECK_REQUIRE_REAL_E2E=1`**, since without it a missing CLI or an expired login is a green runtime skip, which is exactly the failure mode that lets a broken table sit for weeks.

**No user-facing page under `docs/`, and the reasoning is at [M10](#milestones)** rather than here, because it is a standing decision someone will want to find later: #740 Decision 11 keeps the desktop app off the published site while it is an unsigned alpha, [#765](https://github.com/vfarcic/dot-agent-deck/issues/765) is the scheduled page, and the user-facing half of this feature rides the **release note** and the **panel's own sentence** meanwhile. [D12](#milestones) records that voice belongs on #765's page when it exists.

**The changelog fragment is `changelog.d/802.feature.md`, type `feature`, and it is written as user documentation** rather than as a list of milestones — what to say, what it does with no configuration at all, what needs a key and what does not, that every sentence comes from the app rather than the model, the three-part persistence answer, and the limits a user would otherwise meet as a defect.

**`breaking` was considered and rejected on two independent grounds, because under-classifying costs a wrong version bump.** `pyproject.toml` defines the type as *“a change to the TUI↔daemon protocol/handler contract”*, explicitly **not** generic user-facing breakage — and this feature touches no `src/` file at all. The withdrawn `opencode` intent backend is the one thing that could look like a compatibility break, and it is not one on either ground: it is not on that axis, and it **never shipped** — `git log origin/main -- desktop/src-tauri/src/voice/` is empty and `v0.41.0` carries no voice module, so it was added and withdrawn inside this unreleased branch. Nobody's configuration can name it.

**Rule 12, answered by verification rather than by inheritance**, and it is the cheapest check in this PRD to get right because the diff settles it outright: **no file under `src/` is in this branch at all** (`git diff --name-only origin/main...HEAD | grep '^src/'` returns nothing). So `PROTOCOL_VERSION` is untouched at **10** (`src/daemon_protocol.rs:421`), `CONTRACT_BREAKS` is untouched, no `.breaking.md` is owed, and **the cross-version manual test is not owed**. The substantive reasons stand behind the mechanical one: the resolver reads role names and statuses from the `ListAgents` → `DesktopAgent` projection the agent overview already renders, the screen and the focused pane are webview state that was never on the wire, and the Tauri commands this PRD adds are webview↔crate IPC — not the daemon wire — which #1105 settled on the reasoning that the bundle and the binary ship as one artifact, so no configuration exists in which an old bundle meets a new binary.

### 2026-09-19 — the one live-model failure, and what a `description` had to be told

**The definitive credentialed run came back 23 of 24, and the miss was the plainest phrase in the table.** `open-deck-back` — a bare *"go back"* on the overview — returned `action=none, outcome=no_match`. The M1 review above had recorded the shape of this as an observation with no failing case behind it; the live run supplied the case, so that paragraph is superseded rather than merely annotated.

**The asymmetry is what identified the cause.** `close-agent-view-back`, the same bare *"go back"* on the `agent` screen, passed throughout. So `close_agent_view` was winning the phrase in general — both rows listed *"go back"* among the utterances they serve, and the one whose description read most like a bare *"close/dismiss what is on screen"* took it. Where that row was not callable the model did **not** fall through to the row that was; it answered `none`, which is the worst of the three available answers because it blames the user's phrasing for a table problem. `open-deck-terminals` and `open-deck-panes` both passed, so the capability was never in doubt — only its plainest phrasing.

**The fix is prose on BOTH rows, and it had to be both.** Narrowing one row only moves the ambiguity: the two descriptions were symmetric, so a fix on one leaves the other still claiming the phrase unqualified. Each row now states what a bare *"go back"* means **under its own precondition** — `open_deck`: "On the agent overview a bare 'go back' or 'back' means THIS one, because the deck is the screen the overview was opened from"; `close_agent_view`: "While an agent's pane is open a bare 'go back' or 'back' means THIS one". The word **bare** is load-bearing and not decoration: `open-settings-deck-collision` sends *"go back to settings"* on the overview and must still reach `open_settings`, so `open_deck`'s claim on the phrase has to be scoped to the unqualified form.

**It still leans on `screens`, and the honest statement of what changed is narrower than "now disambiguated by prose".** The agent-CLI backend's prompt (`cli_prompt`) carries no `screen` key — only the per-row `callable` flags — so the model learns which precondition holds by reading which of the two rows is callable. What the prose buys is that the model is now told **what to do with that signal** instead of being left with two equally plausible rows and an implicit tie-break. The last sentence of the superseded paragraph therefore stands unchanged: a future change to either row's `screens` column is still a change to how these two are told apart.

**The re-run is the point, not the fix.** Changing a `description` can silently steal utterances from another row, which is the regression the fixtures exist to catch. `DOT_AGENT_DECK_REQUIRE_REAL_E2E=1 cargo test-e2e-live --test voice_phrase_fixtures` came back **24 of 24 in 119.89 s** on the first wording and **24 of 24 in 121.98 s** on the shipped one, with every `close_agent_view` and `open_deck` fixture green both times and both settings collisions still landing on `open_settings`. A nine-utterance probe over the rows most at risk ran before either, which is why neither full run had to be a diagnosis — and the probe is also the evidence for *both rows*: it confirmed `"close this"` on the deck still reaches `close_agent_view` as `unavailable` after that row's rewording, which is the fixture a one-sided fix would most plausibly have broken.

**The second run bought no fixture and was still worth its 122 seconds**, which is the part worth recording. The first wording opened `close_agent_view` with *"Close the one agent's **pane** that is open"* and closed it with the catch-all *"any request to close or dismiss what is on screen is this one"*. Both are green today and both are liabilities tomorrow: this file's own comment above that row exists because the destructive operation is **one word** away from it in speech, so spending that word on the non-destructive row is the opposite of what the comment asks — and a catch-all over *everything that closes* is precisely the shape that will steal from [D5](#milestones)'s confirm-gated rows when they arrive, silently, with nothing erroring. The shipped wording says *view* in both places and lists its utterances instead of generalising over them. **A `description` that is green only against the table as it stands today is not finished** — the measurement cannot see the row that has not been written yet, so that part stays a judgement and the re-run is what keeps the judgement from costing anything.

