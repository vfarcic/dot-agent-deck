node_modules/
dist/
.vite/
coverage/
src-tauri/gen/schemas/
src-tauri/target/

# Playwright browser tier (`pnpm test:browser`): the HTML report and the
# per-failure traces/screenshots. Regenerated by every run; CI uploads them as
# artifacts on failure rather than committing them.
playwright-report/
test-results/
blob-report/

# Driver tier (`pnpm test:driver`, issue #953): a failing scenario's screenshot,
# page text, terminal text and logs. CI uploads it on failure.
driver-results/
