{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    ":semanticCommits",
    ":separatePatchReleases"
  ],
  "timezone": "UTC",
  "labels": ["dependencies"],
  "prHourlyLimit": 3,
  "prConcurrentLimit": 5,
  "rebaseWhen": "conflicted",
  "lockFileMaintenance": {
    "enabled": true,
    "schedule": ["before 9am on monday"]
  },
  "vulnerabilityAlerts": {
    "enabled": true,
    "schedule": ["at any time"]
  },
  "customManagers": [
    {
      "customType": "regex",
      "description": "The Rust toolchain pinned in every workflow (`toolchain: 1.97.1`, matching devbox.json). dtolnay/rust-toolchain publishes toolchain versions as BRANCHES with only a `v1` tag, so pinning the ref instead (`@1.97.1`) would be a version Renovate could never bump; the action stays at `@v1` and the toolchain rides an input, which splits the two concerns — the github-actions rule bumps the action, this manager bumps the toolchain. Without it the pin would silently rot, which is the failure mode that made floating `@stable` attractive in the first place. CORRECTION (2026-08-21): this description used to open 'The github-actions manager cannot see this: it reads the `uses:` ref.' That is FALSE now and the falsehood had teeth. Renovate's github-actions manager carries a registry of community actions whose version lives in an INPUT rather than the ref (`lib/modules/manager/github-actions/community.ts`), and `dtolnay/rust-toolchain` is in it: it reads the same `toolchain:` string this manager matches and emits it as depName `rust`, depType `uses-with`, datasource `rust-version`. So the toolchain now arrives down TWO lanes, and until the `uses-with` packageRule below was added the second one landed in the automerging `GitHub Actions` group — PR #623 (`renovate/github-actions`, automerge enabled, no `manual-review` label) and PR #624 (this manager's lane, held) proposed byte-identical 1.97.1 -> 1.98.0 diffs on the same day. Only an unrelated red CI kept #623 from merging itself. The duplicate PR is now deliberate and is NOT suppressed: both lanes are held for a human, and two independent detectors on a pin whose named failure mode is SILENT ROT are worth one redundant PR per Rust release. They self-resolve — the lanes edit the same lines, so merging either makes the other conflict and `rebaseWhen: conflicted` autocloses it.",
      "managerFilePatterns": ["/^\\.github/workflows/[^/]+\\.ya?ml$/"],
      "matchStrings": ["toolchain:\\s*(?<currentValue>\\d+\\.\\d+\\.\\d+)"],
      "datasourceTemplate": "github-releases",
      "depNameTemplate": "rust-lang/rust",
      "extractVersionTemplate": "^(?<version>\\d+\\.\\d+\\.\\d+)$"
    },
    {
      "customType": "regex",
      "description": "The cargo-nextest version pinned in every workflow (`tool: cargo-nextest@0.9.140`, matching devbox.json). Same reasoning as the toolchain manager above: taiki-e/install-action is pinned at `@v2` and the tool version rides an input. `taiki-e/install-action` is NOT in Renovate's community-actions registry today (checked 2026-08-21), so this manager is currently the only lane that sees the pin — but that is exactly what was true of `dtolnay/rust-toolchain` until it was added, so the claim is written as an expiry date rather than a property. The `uses-with` rule below holds this pin the day the registry grows an entry for it, WITHOUT anyone having to notice.",
      "managerFilePatterns": ["/^\\.github/workflows/[^/]+\\.ya?ml$/"],
      "matchStrings": ["tool:\\s*cargo-nextest@(?<currentValue>\\d+\\.\\d+\\.\\d+)"],
      "datasourceTemplate": "crate",
      "depNameTemplate": "cargo-nextest"
    }
  ],
  "packageRules": [
    {
      "matchManagers": ["cargo"],
      "matchUpdateTypes": ["patch"],
      "groupName": "Rust crates (patch)",
      "automerge": true,
      "automergeType": "pr",
      "description": "Patch bumps are in-range for Cargo and gated on CI (build + build-macos + build-windows + fast test tier)."
    },
    {
      "matchManagers": ["cargo"],
      "matchUpdateTypes": ["minor"],
      "matchCurrentVersion": ">=1.0.0",
      "automerge": true,
      "automergeType": "pr",
      "description": "Minor bumps automerge on green CI — but only for crates already at 1.0 or later, where a minor is genuinely backwards-compatible under semver. This is the `matchCurrentVersion` the earlier note here recommended, applied 2026-07-30 after an automerge-eligible portable-pty 0.8 -> 0.9 landed PSUEDOCONSOLE_INHERIT_CURSOR and stalled every Windows pane. 0.x minors are handled by the next rule."
    },
    {
      "matchManagers": ["cargo"],
      "matchUpdateTypes": ["minor"],
      "matchCurrentVersion": "<1.0.0",
      "automerge": false,
      "labels": ["dependencies", "manual-review"],
      "description": "A 0.x minor is a BREAKING change as far as Cargo is concerned (0.8 -> 0.9 is not compatible), so it is a major in everything but Renovate's label. Held for a human, matching the intent 'automerge anything that is not a major'. Stated as its own rule rather than left to the default so the intent is explicit and labelled: portable-pty 0.9.0 reached main through exactly this gap."
    },
    {
      "matchPackageNames": ["portable-pty"],
      "enabled": false,
      "description": "Renovate ignores this crate entirely: no PRs, and no Dependency Dashboard entry to remember to check. portable-pty is held at =0.8.1 deliberately (see the Cargo.toml comment): 0.9.0 sets PSUEDOCONSOLE_INHERIT_CURSOR, so ConPTY withholds a Windows pane's output until a terminal answers ESC[6n, stalling every pane. Renovate re-proposed 0.9.0 within minutes of the v0.35.1 release; a standing PR for a pin held on purpose is pure noise, and the unpin is gated on a PRODUCT decision (native Windows support), not on a version becoming available — so no dependency bot can know when the time is right. Safe to silence because the `security` CI job runs `cargo audit` on every PR and every push to main, so a vulnerability in this crate surfaces there regardless of Renovate. The reminder to revisit lives in the Cargo.toml comment and the tracking issue, i.e. where the Windows decision will actually be made."
    },
    {
      "matchManagers": ["devbox"],
      "matchUpdateTypes": ["digest", "pin", "patch", "minor"],
      "groupName": "Devbox packages",
      "labels": ["dependencies", "devbox"],
      "automerge": true,
      "automergeType": "pr"
    },
    {
      "matchManagers": ["devbox"],
      "matchUpdateTypes": ["lockFileMaintenance"],
      "enabled": false,
      "description": "Lock file maintenance is disabled for devbox.lock, because Renovate resolves the lock inside its own throwaway container and devbox writes an ABSOLUTE path for `path:` flake entries. PR #348 therefore added a `path:gcloud#google-cloud-sdk` entry whose `resolved` field pointed at `path:/tmp/renovate/repos/github/vfarcic/dot-agent-deck/gcloud`, a directory that exists on no real machine; 77b5e87 removed it. The entry is not something devbox recreates — `devbox install` leaves the lock at 17 entries and resolves the local flake from devbox.json — so it is purely an artifact of where the bot ran, and every lock file maintenance run would re-add it. Actual devbox package bumps are unaffected: they still flow through the `Devbox packages` rule above, so only the wholesale lock refresh stops. Consequence to accept: the nixpkgs revisions pinned in devbox.lock now only move when a package bump moves them, so refresh them by hand (`devbox update`) if they drift too far."
    },
    {
      "matchManagers": ["github-actions"],
      "matchDepTypes": ["action"],
      "matchUpdateTypes": ["digest", "pin", "patch", "minor"],
      "groupName": "GitHub Actions",
      "automerge": true,
      "automergeType": "pr",
      "description": "Automerge covers ACTION REFS ONLY — `uses: owner/repo@ref`, which is depType `action`. This is a WHITELIST on purpose, and `matchDepTypes` is the load-bearing line in it. The github-actions manager emits six depTypes (`action`, `docker`, `container`, `service`, `github-runner`, `uses-with`), and this rule used to match the manager with no depType filter at all, so it automerged every one of them — including tool versions read out of an action's INPUTS. That is how the Rust toolchain pin acquired an automerging lane (see the customManager description above and the `uses-with` rule below). Whitelisting rather than excluding the one dep name observed misbehaving is the point: Renovate's input-detection registry is a list that GROWS, so a blacklist is only ever correct until the next release, whereas anything this manager learns to detect tomorrow falls OUTSIDE this rule and is held by default. The cost of being wrong in this direction is a PR someone has to click; in the other direction it is an unreviewed toolchain change on main."
    },
    {
      "matchManagers": ["github-actions"],
      "matchDepTypes": ["uses-with", "docker", "container", "service", "github-runner"],
      "automerge": false,
      "labels": ["dependencies", "manual-review"],
      "description": "Everything the github-actions manager extracts that is NOT an action ref is held for a human and labelled. Stated as its own rule rather than left to Renovate's automerge default, matching the convention of the 0.x-minor rule above: a hold that exists only as an absence is invisible on the PR page and impossible to grep for. `uses-with` is the one that matters here — it is a version string embedded in an action's inputs, which for this repo means the Rust toolchain (`dtolnay/rust-toolchain`'s `toolchain:`) and the docs site's Node (`actions/setup-node`'s `node-version:`), i.e. two TOOLCHAINS wearing a GitHub Actions costume. A toolchain bump is not an action bump: it changes what compiles, it is where new deny-by-default rustc lints and clippy lints arrive, and in this repo it must move in lockstep with devbox.json by hand. Worse, before this rule the toolchain rode the `GitHub Actions` GROUP, so a Rust bump could arrive bundled inside a PR titled 'update GitHub Actions' alongside a handful of ordinary `@v7` bumps and automerge without ever being read on its own. The remaining depTypes (container/service/docker images, `runs-on:` runner versions) are listed for completeness — this repo has none today, and if it grows one, a base image or runner change is also not something to land unread."
    },
    {
      "matchManagers": ["custom.regex"],
      "matchDepNames": ["rust-lang/rust", "cargo-nextest"],
      "automerge": false,
      "labels": ["dependencies", "manual-review"],
      "description": "The workflow toolchain pins are held for a human even on a patch. They must stay in lockstep with devbox.json's rustc/cargo/clippy/rustfmt and cargo-nextest pins — that lockstep is the whole point of pinning them, and nothing enforces it automatically — so each PR needs the matching devbox bump alongside it. A Rust patch release is also exactly where a new clippy lint appears, which is a `-D warnings` failure to read rather than automerge. This rule is scoped to a MANAGER and a set of dep NAMES, so it only ever covered the lane this repo's customManagers create. It is no longer the only hold on these pins and must not be read as one: the same `toolchain:` string is also seen by the github-actions manager under a different dep name, and the depType rules above are what close that lane and any future one. Keep both — this one says WHY the pin is held, those say WHERE ELSE it can be reached from."
    },
    {
      "matchManagers": ["npm"],
      "matchFileNames": ["site/**"],
      "matchUpdateTypes": ["patch", "minor"],
      "groupName": "Docs site (site/)",
      "automerge": true,
      "automergeType": "pr"
    },
    {
      "matchPackageNames": ["crossterm", "futures-util", "insta"],
      "automerge": false,
      "labels": ["dependencies", "manual-review"],
      "description": "Deliberate EXACT pins in Cargo.toml, never automerged. crossterm's pin is load-bearing: `ui::ctrl_c0_byte` documents crossterm 0.28.1's decoder behaviour and `keyevent_ctrl_c0_matches_crossterm_decoder` guards it (PRD #227). See also #242 (crossterm 0.28.1 + 0.29.0 coexist because ratatui 0.30 pulls 0.29 via ratatui-crossterm). portable-pty is pinned at =0.8.1 because 0.9.0 sets PSUEDOCONSOLE_INHERIT_CURSOR, which makes ConPTY withhold a Windows pane's output until a terminal answers ESC[6n — see the Cargo.toml comment. WITHOUT this entry a 0.8 -> 0.9 bump counts as a `minor` for the cargo rule above and would AUTOMERGE, silently reintroducing the stall; that is how 0.9.0 landed the first time."
    },
    {
      "matchUpdateTypes": ["major"],
      "automerge": false,
      "labels": ["dependencies", "manual-review"],
      "description": "Major bumps always stay manual."
    }
  ]
}
