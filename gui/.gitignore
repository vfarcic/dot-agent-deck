# JS toolchain — contained entirely in gui/ (never in the repo root).
node_modules/

# Tauri build output / generated files.
src-tauri/target/
src-tauri/gen/

# `nix build` result symlink from the gui/nix Linux build-deps flake.
nix/result

# The hand-authored frontend lives in dist/ and IS tracked; only generated
# bundler output (if a bundler is added later) would be ignored here.
