## Long reports from dispatched units and workers are no longer cut off

When a dispatched unit finished, or an orchestration worker reported work nobody had delegated to it, a report longer than a few pages reached the receiving agent with its ending missing, and nothing could recover it. Dispatch-completion reports hit this routinely. The receiving agent can now read the whole report, so what your dispatcher or orchestrator relays to you is complete.
