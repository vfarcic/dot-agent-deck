Stop a runner stall from reddening the lane-1 e2e tier. A single ~16-21s stall lands on one arbitrary test per `e2e-deterministic` run: measured across four reports of the same tree, `pane_input_007`, `inline_001` and `manager_016` each failed in exactly one run and were fast (0.25-1.6s) in the other three, with the tier's shape unchanged throughout. It is not slower hardware — the median CI/local per-test ratio is 0.96x across 2653 shared tests. CI now runs the tier with `--retries 1`, which is not rerun-until-green: a genuine regression fails both attempts and nextest reports a recovered run as FLAKY, so the stall stays visible. Local runs keep `retries = 0`. The harness's default wait ceiling also goes 10s to 30s, and two explicit budgets in `prompt/pane-input/007` are raised to the suite's usual values, for the stalls that do surface as wait timeouts.
