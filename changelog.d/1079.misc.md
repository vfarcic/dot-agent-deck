The default `mixed` orchestration is cross-provider again: its **auditor** runs `opencode` and its **tester** runs `codex` on `gpt-5.6-sol`, alongside Claude and Pi in the other four roles.

Both roles had been swapped to Claude during an OpenAI credit outage, which quietly made `mixed` an all-Anthropic team differing from the `anthropic` orchestration in one role's harness and in no provider at all — so the three-way choice offered at dispatch time had only two distinct behaviours. That swap has now happened twice; a note in `.dot-agent-deck.toml` records why a provider outage does not belong in the committed default, and points at `--orchestration 'anthropic'` as the way to run all-Claude without changing anyone else's default.
