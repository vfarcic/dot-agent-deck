Corrected three false absolutes in `docs/develop/governance.md`, all instances of the defect class rule 17 exists to catch — a narrow fact written one quantifier too wide — plus the stale counts of the required check set that went with them.

The matching `CLAUDE.md` rule 8 corrections are **not** part of this change. #901 landed an independent narrowing of the same paragraphs first, so the conflict was resolved to `main`'s version rather than re-applying a second rewrite of the same facts; the evidence below is what this change actually contributes, and it belongs in `governance.md` anyway (rule 11).

`e2e-deterministic` **is** a required status check as of 2026-09-07T15:30:16Z, and rule 8 said the opposite: "the one e2e lane issue #502 added to CI is deliberately advisory, and the required set is UNCHANGED … the ruleset still requires exactly `build`, `build-macos`, `build-windows` and `security`". The live ruleset carries five contexts and the lane genuinely blocks — #924 is `APPROVED`, `MERGEABLE`, has no review threads to resolve and four green checks, yet reports `mergeStateStatus: BLOCKED` on `e2e-deterministic` alone. The rationale that held it back has expired: measured across the last 40 `pull_request` runs of `ci.yml`, 35 completed jobs took 9.0–42.0 minutes, median 12.6, none reaching the `timeout-minutes: 60` kill window.

The change was applied to the **live ruleset directly**, not by merging anything, so the script and the ruleset drifted for about six hours until PR #909 carried the fifth context into `REQUIRED_CHECKS` (`0c35e537`, the first commit to touch `scripts/apply-branch-protection.sh` since `b55d8118`). Both files now record that they are back in step, and both still say to read the contexts back with `status` after any `apply` — the drift window is the thing the read-back exists to catch, and closing one instance of it does not prevent the next.

Two further absolutes fell out of sweeping for the *construction* rather than the topic. "**No direct push to `main` has happened since `required_status_checks` returned**" was false ~5 hours after it was written — eleven releases (v0.36.0 through v0.39.3) have each pushed two check-run-less commits directly to `main`, and all 22 of those commits were checked rather than sampled: every one belongs to no PR and names `github-actions[bot]` as author and committer — so that leg is exercised, not untested. And "`dismiss_stale_reviews_on_push: true` means **any push** after an approval voids it" is too wide: a `gh pr update-branch` merge left all eight of #924/#923/#909/#907/#902/#899/#894/#886 `APPROVED` with zero dismissals eleven hours after approval, while three hand-pushed conflict resolutions (#872, #901, #919) were all dismissed. Recorded as the two measurements, not as a theory of which pushes dismiss, and scoped to `gh pr update-branch` — a rebase was not measured. Greptile's review of this change caught the same absolute surviving in `governance.md`'s sequencing bullet, which the original sweep had missed, plus two paragraph headings still calling the release-bypass leg untested above bodies that now say it is exercised.

Left alone as accurate: the "four rules" counts (the rule *count* is still four — only a context inside one of them moved), #510's 2026-08-11 measurement of four skipped checks, and the #504/#499/#469/#464 `changes`-gate measurement, all of which are time-anchored history rather than present-tense claims.
