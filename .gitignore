/target
tmp/
# Per-clone dev state — orchestrator IPC, regenerated recordings,
# regenerated test docs. Never committed; regenerated by running
# tests + `cargo xtask docs --tests` locally.
.dot-agent-deck/
.claude/scheduled_tasks.lock

# Documentation site
site/node_modules/
site/build/
site/.docusaurus/

# Demo-reel build artifacts (regenerated each build; reel lives on YouTube)
/reel.mp4
/manifest*.json

# dot-ai skills lock (transient, like scheduled_tasks.lock above)
.dot-ai.lock

# Checkout-relative CI cache paths used by `.semaphore/ci.sh` (PRD #376). They
# only appear if that script is run by hand while debugging it, but they are
# multi-GB when they do.
/.cargo-home/
/.semaphore-cache-restore/
